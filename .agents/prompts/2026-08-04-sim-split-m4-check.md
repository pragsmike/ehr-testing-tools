2026-08-04 — M4: sim-check extraction, residual thinning, arc close-out
Session prompt (design channel, 2026-08-04). Plan: `.agents/plans/2026-08-04-sim-split-b-plan.md` (RULED). Final stage; M1–M3 landed and were independently verified (`f522db7..6f45a82`). R30 ceremony throughout. This stage closes the sim split B arc: after it, `components/sim` is pure orchestration ({run, identifiers, version, manifest-build, façade}), the doctrine graph is complete (engine / check / two sibling emitters / shared provenance), and the plan's Done conditions are all discharged.
Context
`check.clj` (571 lines, the invariant catalog) moves to `components/sim-check` (`ehrt.sim-check.check` + `.interface`), depending on sim-engine's acceptance surface — which M2 already built for it. Design-channel evidence (call-position + require-form census at `6f45a82`; the session re-derives fresh):

* Require-form callers of `ehrt.sim.check` (the other eight files grep matched are docstring mentions): src-scope `run.clj:30`, `interface.clj:11` (the façade); test-scope `check_test.clj:19` (moves), `engine_test.clj:26` (sim-engine), four vendored files in `sim-emit-hl7/test` (tjr:27, ear-infections:39, uti:42, sepsis:28).
* src-scope union for the interface: `check-all` only — façade arities 1–4, run.clj's 3-arity call at line 329.
* `check.clj`'s own requires include `ehrt.sim-engine.interface` aliased TWICE (`:as engine`, `:as order-profiles`) — an M2 artifact that minimized body churn. It moves as-is (move-don't-improve).
* Residual deps hygiene: neither `metosin/malli` nor `org.babashka/cli` has any require in residual sim's src (manifest.clj lost malli in M1's thinning; run/identifiers/version never used either). Both are drop candidates pending the session's own test-tree and interface verification.
* `explain-profiles` (`sim-engine/order_profiles.clj:66`): one def, zero callers found.
* Coverage alias (`projects/ehrt-cli/deps.edn:85–98`): thirteen test paths, omits `sim`, `sim-engine`, `sim-emit-fhir` (and will need `sim-check`). `provenance` is present (M1).
* Oracle: `digest.clj` requires neither check nor the façade — no script change expected this stage; escalate if resolution fails.

Read first

* `.agents/plans/2026-08-04-sim-split-b-plan.md` — M4 section and the plan's Done conditions.
* `components/sim/src/ehrt/sim/check.clj` and `interface.clj` — the mover and the façade.
* `components/sim/test/ehrt/sim/{check_test,emitter_order_independence_test}.clj`.
* `components/sim-emit-hl7/test/` — the four vendored repoint files.
* `.agents/plans/2026-08-02-sim-split-plan.md` AR-3 (façade permanence) — the ruling M4 honors, not revisits.
* `notes/ADRs.md` ADR-0043 M1–M3 execution records — format.
* `projects/ehrt-cli/deps.edn` `:coverage` alias.

Author rulings (record verbatim in ADR-0043's M4 execution record)

1. AR-M4-1 (the move). `check.clj` → `components/sim-check`, ns `ehrt.sim-check.check`; `check_test.clj` moves alongside. `ehrt.sim-check.interface` carries `check-all` only (all four arities, thin delegation) — the call-position-verified union; the both-directions delta discipline applies as in M2/M3. The double-alias require moves unchanged. sim-check depends on sim-engine, sim-model, kernel — and is forbidden from depending on the residual sim, either emitter, corpus, or provenance.
2. AR-M4-2 (test-scope repoints). `engine_test.clj` and the four vendored sim-emit-hl7 tests repoint `ehrt.sim.check` → `ehrt.sim-check.check` (internals, test-legal, mechanical).
3. AR-M4-3 (the façade). `interface.clj`'s require repoints to `ehrt.sim-check.interface`. The façade's SURFACE is frozen — var list, names, arities byte-identical; the file diff is the require line plus the docstring change licensed next: the fat-component disclosure (the docstring's account of sim's nine-concern state) RETIRES with a dated note — the split it disclosed is complete. The 08-02 plan's own AR-3 (façade permanence; corpus depends on it, ADR-0012) is honored and explicitly NOT revisited; any future façade thinning is a separate author ruling, cited as such in the ADR.
4. AR-M4-4 (order-independence test). `emitter_order_independence_test.clj` moves to `components/sim-emit-hl7/test` — it is emit-hl7's own determinism guard (its docstring says so; its calls are `emit-hl7/emit` and `engine/run`). Classification rationale recorded; ns renamed to the sim-emit-hl7 test convention.
5. AR-M4-5 (findings disposition batch). Each item disposed with fresh-grep evidence, recorded per-item: (a) `explain-profiles` RETIRES with dated disclosure (zero callers; if the session's fresh grep finds one, KEEP and record the caller instead — evidence over ruling). (b) Coverage alias gains `sim`, `sim-engine`, `sim-emit-fhir`, `sim-check` test paths. (c) The architecture doc's component DIAGRAM (the table row landed in M1 — the diagram is the gap; locate it fresh) gains the `provenance` node, plus `sim-engine`/`sim-emit-fhir`/`sim-check` if it predates them. (d) The M3-disclosed stale demo-README reference: locate with ESCAPED-dot grep (`ehrt\.sim\.emit-hl7` — unescaped dots false-match `ehrt.sim-emit-hl7`; the design channel made exactly this error and it is the recorded lesson), fix forward. (e) Residual deps hygiene: for each dep declared in `components/sim/deps.edn`, find a real require in src, test, or any project-level need; drop what nothing uses (candidates: malli, babashka/cli), with per-dep disclosure. Verify no project relied on the residual's declaration by leakage (full suite + `poly check` after the drop is the proof).
6. AR-M4-6 (stale-path fan-out). Tripwire learns `ehrt.sim.check` and path-form `ehrt/sim/check`; current-tense docstring mentions swept per fresh escaped-dot grep. Frozen archives untouched. Red→green recorded.
7. AR-M4-7 (arc close-out). ADR-0043's M4 execution record ends with an arc-complete statement: the five-brick decomposition of plan AR-1 is landed in full; the component graph now states the doctrine (formats as sibling emitters over the state machine, checker separate from doer, provenance as the shared contract). Roadmap: M1–M4 rows to Done; the 08-04 plan gets a dated close-out annotation (annotate-not-rewrite). Standing deferred items are RE-CITED with their triggers intact, not re-opened: the J2 oracle redesign, carry-across emission, sim-cli retirement, census-tool promotion, the docs coherence pass (which is the cleanup arc's next front, not this session's).

Steps
Step 0 — Characterize (evidence, no edits). Verify tip = `6f45a82` (STOP-AND-ESCALATE on mismatch). Fresh require-form + call-position census for `ehrt.sim.check`; both-direction deltas vs this prompt recorded. Per-dep usage census for residual sim's deps.edn. Fresh grep for AR-M4-5's items (c) and (d). Form-anchored deftest counts per tree.
Step 1 — Move, interface, repoints, façade (one commit). Create `components/sim-check`; move + rename check and its test; write the one-var interface; repoint run.clj, the façade (require line + licensed docstring retirement), engine_test, the four vendored files; delete the old ns same commit. Workspace bookkeeping: root `deps.edn`, project deps.edn files, `workspace.edn`, `:necessary`, structure-currency. `poly check` clean, full suite green, both lanes. Commit: `refactor(sim-check): the invariant catalog gets its own home — residual sim is pure orchestration (M4 step 1, AR-M4-1/2/3)`
Step 2 — Findings disposition (AR-M4-4, AR-M4-5). The order-independence test moves; the five findings dispose per-item with evidence. Full suite green (the deps drop's proof rides here). Commit: `chore: parked findings disposed — coverage paths, dead code, diagram, deps hygiene (M4 step 2, AR-M4-4/5)`
Step 3 — Stale-path sweep + tripwire (AR-M4-6). Fresh escaped-dot grep, sweep, patterns extended, red→green recorded. Commit: `docs: check stale-path sweep — tripwire learns the old name (M4 step 3, AR-M4-6)`
Step 4 — ADR execution record + arc close-out (AR-M4-7). The M4 record, the arc-complete statement, roadmap rows to Done, the plan's dated close-out annotation, deferred items re-cited. Commit: `docs: ADR-0043 M4 execution record — sim split B arc complete (M4 step 4, AR-M4-7)`
Step 5 — Verification + record. Normal-mode bracket (`bin/regression-oracle 6f45a82 <step-4-tip>`): all ELEVEN batches byte-identical, expected-change set NONE, any digest change STOP-AND-ESCALATE. Deftest parity ledger vs Step 0: pure relocation wash (check_test and the order-independence test move; nothing retires, nothing new — explain-profiles is src, no deftest effect). Façade seam byte-identical — this stage's most load-bearing check, since the façade file itself changed: `ehr help` / `ehr sim run` / `ehr sim check` via the worktree method. `clojure -M:poly check` clean; both lanes green. Session record `.agents/session-records/2026-08-04-sim-split-m4-check.md`; prompt self-archives (pairing gate enforces). Final commit: `docs: M4 session record — arc complete, residual sim is orchestration only`
Fences
No façade surface changes — the require line and the licensed docstring retirement are the entire façade diff; `ehr sim check` output byte-identical is the proof. No check logic edits — invariant changes are FINDINGS, never edits. No façade thinning (separate future ruling, 08-02 plan AR-3 stands). No oracle changes (none expected; escalate if the bracket fails to resolve). No re-opening of deferred items — re-cite with triggers, nothing more. Frozen archives untouched.
