2026-08-06 — ux audit: every surface a stranger meets, surveyed — nothing moved
Session prompt (design channel, 2026-08-06). Prior: the tag-law session landed and was design-channel-verified (`1d93e6c`, ADR-0057). This is the UX arc's audit session per the brief (`.agents/plans/2026-08-06-ux-arc-brief.md`, its §6 step 1): survey every user-facing surface — invocations, help text, error messages, the first-run experience — and land a findings register. FINDINGS-ONLY: not one string changes, however trivial the fix looks; the register format, fences, and honest-partial license all follow the alignment audit's proven pattern. This session may RUN the CLI to gather evidence — transcripts are rows. R30 ceremony. Read-first: the brief in full; ADR-0056's probe table (the six demo READMEs + two config.edn supplementary finding are pre-enumerated seeds); `bases/cli/src/ehrt/cli/help.clj` in full; the alignment register (`.agents/plans/2026-08-05-alignment-audit-findings.md`) as the row-format model.
Author rulings (record verbatim in ADR-0058)

1. AR-UA-0 (tag, standing ceremony — the first). Per ADR-0057 AR-T-1 clause (ii), tag the predecessor's verified stable point: annotated `stable-20260806-tag-law` at `1d93e6c`, message `tag law landed, design-channel-verified 2026-08-06 (ADR-0057)`; push; verify. No further license exists or is needed — this is the first tag executed under standing ceremony, and ADR-0058 says so.
2. AR-UA-1 (findings-only, absolute). Every defect found is a register row, never an edit. `help.clj`, error strings, READMEs, config comments: all read-only this session. The alignment audit's law verbatim: acting on a finding is the worst outcome.
3. AR-UA-2 (the register). `.agents/plans/2026-08-06-ux-audit-findings.md` + index entry. Row format identical to the alignment register (id | area | probe | evidence | finding | recommendation | proposed disposition), dispositions from the same vocabulary plus one new value: `design-channel-draft` — for rows whose fix is wording the design channel must draft for author ruling before any session touches the file (the help-spec voice rewrite is the expected bulk of these). Seeds U1–U5 from the brief appear first, updated with this session's evidence; ADR-0056's supplementary config.edn finding folds into U1's row. New rows numbered by area.
4. AR-UA-3 (area A — invocation surfaces). Enumerate EVERY command fence and command-bearing comment in the tree's live docs (`README.md`, `AUTHORS-GUIDE.md`, `docs/**`, `components/*/docs/**` including demo READMEs and config.edn header comments, `components/*/README.md` if any): for each, classify against the CLI's real grammar — correct `bin/ehrt` form / stale alias / missing group / flag-pairing drift / cwd-dependent path. The evidence column carries file:line and the fence text. Gate candidate row: an invocation-lint in the docs-tooling family (forbid `clojure -M:cli` on live doc surfaces; assert command fences open with `bin/ehrt` or a ruled allowlist of alternates) — recommend shape, do not build.
5. AR-UA-4 (area B — help voice). Enumerate every user-visible string in `help.clj`'s spec data (group docs, verb docs, flag docs, positional docs): classify each as user-clean / agent-speak (ADR tokens, milestone tags like "M5b", internal law names, session references) / over-long (would wrap badly at 80 columns — probe how the renderer actually wraps, or doesn't). Counts by class per group go in the row; the full classified inventory lands as an APPENDIX section of the register (it is the design channel's rewrite worklist). Also probe `bin/ehrt help`'s actual rendered output and attach a representative transcript — the register should show what a user sees, not only what the source says.
6. AR-UA-5 (area C — error surfaces). Enumerate every operational-error path (exit 2) reachable from `bases/cli` and `ehrt.sim.run`'s command surface: for each, does the message name the concrete artifact (path/flag/module/key) and offer a next step? The `--config` missing-file path FIRST (the brief's open [audit]) — run it deliberately (`--config does/not/exist.edn`) and transcript the result. Also transcript: missing required `--seed`; an unknown flag; an unknown subcommand; a malformed EDN config. U4's near-miss idea (same stem, different extension) gets its assessment here: recommend land/defer with reasoning.
7. AR-UA-6 (area D — first contact). Transcript, as a user would see them: bare `bin/ehrt`; `bin/ehrt help`; `bin/ehrt sim`; the README quickstart's first fence run verbatim from a clean checkout state (the quickstart gate covers the script — this row covers the EXPERIENCE: output volume, signal-to-noise, what a stranger reads first). Judgment rows are fine here — UX quality is partly judgment — but every judgment cites its transcript.
8. AR-UA-7 (budget). The alignment audit's honest-partial license verbatim: an area's rows may land marked `incomplete` with what remains; the register may not be skipped; the help-string APPENDIX inventory (AR-UA-4) is the one deliverable that must be complete, because the design channel's rewrite draft depends on it.

Steps
Step 0 — Preflight + tag. Cwd ext4; tip `1d93e6c` or later-with-disclosure; full suite green baseline; oracle pre-digest. Execute AR-UA-0.
Step 1 — Areas A–D. The probes and transcripts; register drafted complete (or honestly partial per AR-UA-7) with its appendix; index entry. Commit: `docs: the ux audit register lands — every stranger-facing surface surveyed, nothing moved (ux audit, AR-UA-2..7)`
Step 2 — ADR-0058 + record. ADR-0058: rulings verbatim; the first-standing-ceremony-tag note; row counts by area and disposition; anything incomplete and why. Index line; Done pointer `- 2026-08-06 — ux-audit — ADR-0058`. Oracle bracket (`1d93e6c` → tip): all ELEVEN batches identical — register and records only; any change is STOP-AND-ESCALATE. Session record + prompt self-archive. Final commit: `docs: ux audit record — the worklist exists, the rulings come next (ADR-0058)`
Fences
Findings-only (AR-UA-1) — the only mutations are the tag, the register, and the records. Deliberate error-triggering runs (AR-UA-5) use throwaway paths and never write into `config/` or the tree; any files a probe run generates land under `/tmp` or an ignored scratch dir and are never staged. `config/busy-weekday.md` remains untouched and untracked, as every session since the incident has correctly left it. No gates built (recommend only). Frozen archives untouched (ADR-0058 + index sanctioned). Settled law not re-raised.
After landing: design channel verifies by fresh probe, then drafts (a) the help-spec voice rewrite from the register's appendix and (b) per-cluster ruling recommendations for the author, exactly as the alignment arc's pattern ran. This landing's own tag rides the next session's Step 0 under standing ceremony.
