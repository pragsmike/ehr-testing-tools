2026-08-05 — scaffolding compaction C: the continuity register lands, /mnt/c retires, arc closes
Session prompt (design channel, 2026-08-05). Prior: compaction B landed + verified (`53edcad`). This session is INVERTED doer-checker: the design channel AUTHORED the artifact drafts (pasted below the fence line at the end of this prompt); this session's primary job is to VERIFY every citation and claim in them against the live tree, correct-or-tag failures, and land them. R30 ceremony.
Author rulings (record verbatim in ADR-0047)

1. AR-C-1 (state.md). `.agents/state.md` lands from the draft below AFTER per-claim verification: every [V]-tagged claim re-probed at this session's tip; every citation resolved; failures corrected with a note or tagged [unverified] — NEVER silently kept. The file's regeneration contract (design channel, each arc close) is recorded in ADR-0047 as standing rule.
2. AR-C-2 (rulings register, seeded not completed). `.agents/rulings.md` is CREATED as a seed: header stating its contract (standing [A] rulings only, each citing its ADR via the index; appended at each arc close; NOT a history — the attic is), plus the rulings this arc's ADRs (0043–0047) state as standing, extracted by reading those five attic files only. Back-filling ADR-0001..0042's rulings is a named future with trigger "next design-channel onboarding that misses a rule" — recorded, not attempted (45-file extraction is judgment work exceeding one session's licensed scope).
3. AR-C-3 (/mnt/c retirement). Author-ruled 2026-08-05: `bin/sync-mnt-c` retires (dated pointer in a commit note; file deleted — this IS a licensed deletion, the arc's only one); guarded-mirror doctrine and dual-clone hazard language retire from AGENTS.md and `.agents/skills/` current-tense surfaces (dated notes); the four-incident ledger and the UNC resolution are ADR-0047's closing narrative. The reject hooks on the Windows side and the directory itself are the author's acts, recorded as such. If any live automation still invokes sync-mnt-c (fresh grep: Makefile, CI, hooks), STOP-AND-ESCALATE.
4. AR-C-4 (arc close). ADR-0047 closes the scaffolding compaction arc (A/B/C); roadmap Done pointers per AR-B-4; the Code-side memory note on WSL-routing workarounds is superseded (session updates its own project memory accordingly).

Steps
Step 0 — Verify the drafts. Tip = `53edcad` or later-with- disclosure. Per-claim probe of state.md's [V] tags; citation resolution of every ADR reference through the index; the Deferred count; the gate list vs docs-tooling's actual test files. Record the verification table (claim → probe → held/corrected/tagged).
Step 1 — Land state.md + rulings.md seed (AR-C-1/2). Corrected drafts in; README indexing if `.agents/` has an index convention (check); budget-set membership is a FINDING not an edit. Full suite green. Commit: `docs: the continuity register lands — state with citations, rulings seeded (compaction C, AR-C-1/2)`
Step 2 — /mnt/c retirement (AR-C-3). Evidence-gated per the ruling. Commit: `chore: the mirror retires — one clone, one truth (compaction C, AR-C-3)`
Step 3 — ADR-0047 + arc close + record (AR-C-4). Rulings verbatim, the verification table summarized, the incident ledger, Done pointers. Oracle bracket (`53edcad` → tip): ELEVEN batches identical (docs + one script deletion only; escalate on any change). Session record + prompt archive (pairing gate). Final commit: `docs: compaction C record — the arc closes; continuity is a repo artifact now`
Fences
The drafts below are DRAFTS — the session's verification outranks them; silent retention of a failed claim is the worst outcome. No back-fill of pre-0043 rulings. No deletions beyond sync-mnt-c. No new gates. Frozen archives untouched.
---8<--- DRAFT: .agents/state.md follows (paste from the design channel's delivered state.md file) ---8<---
State of the project — continuity register
DRAFT authored by the design channel 2026-08-05 against tip `53edcad`; every claim below carries a citation the landing session MUST verify (Step 0) before this file lands — any claim that fails verification is corrected or tagged `[unverified]`, never silently kept. Regenerated by the design channel at each arc close (standing rule, this file's own contract). Provenance tags: [A] author-ruled, [C] channel-inferred, [V] probe-verified at the stated tip.
What this repo is
Clojure/Polylith workspace generating deterministic synthetic hospital traffic (Synthea GMF at pinned commit `7e08387c...f902`), injecting defects, judging conformance (HL7v2/FHIR). Formats are emitters of the patient state machine [A] (`notes/adr/` index: ADR-0043 sibling- emitter contract). Book repo `ehr-testing-guide` is separate.
Component graph (post sim-split-B) [V @53edcad]
`sim-engine` (engine/churn/order-profiles) ← `sim-check`, `sim-emit-fhir`, `sim-emit-hl7`, residual `sim` (run/identifiers/ version/manifest-build/façade). `provenance` ← {corpus, sim} (the manifest schemas' single home). `oracle` = the digest component (each checkout carries its own producer). Census lives in `sim-trajectory` (src + 7 tested tests). Façade `ehrt.sim.interface` surface frozen [A] (08-02 plan AR-3; corpus depends in-process, ADR-0012).
Where history lives [V @53edcad]

* `notes/ADRs.md` = INDEX (91 lines); entries at `notes/adr/NNNN-slug.md` (43 files). Citation rule: bare ADR-NNNN resolves via the index; frozen-era origin-qualified (`sim/ADR-0008` etc.) — see index header.
* Roadmap = live map only (205 lines); Done history at `.agents/plans/roadmap-done-2026-08.md` (33 entries verbatim).
* Session narrative hierarchy [A, AR-B-4]: ADR execution record = sole narrative; session record = ceremony log; roadmap Done = one-line pointer (gated: `done_pointer_adr_test`); prompt archive = provenance (gated: prompt/record pairing test, 7-slug pre-cutover allowlist).

Standing gates [V @53edcad]
Structure-currency; index-completeness (both READMEs, both directions); reading-set budgets (re-derived 2026-08-05, actual×1.15 round-to-5, AR-D-3); stale-path tripwire (learns each retired ns); prompt/record pairing; Done-pointer→ADR. Oracle: `bin/regression- oracle`, per-worktree digest via `components/oracle`, cross-side source-equivalence check, `--declared-digest-change` flag, all-eleven- batches identity is the standing expectation for structural sessions [A, ADR-0044].
Live work

* Deferred = LIVE rows only (13; header states the contract) [V @3495181]: all amendments/extensions to existing mechanisms — no new capability classes [C, ruled useful 2026-08-05].
* Next (roadmap): lookup `time` gap, Wave G attachment trigger, pairing-as-data registry, storefront fixture, quickstart→nightly, generator-source split, corpus.display placement, table-helper dedup [V @53edcad, titles abbreviated — read the roadmap].
* Externals (author-only): NIST gist send; IG pinning; Clojars publish (ruled, deferred — do NOT re-raise Clojars-vs-Central); SETUP rewalk (unspoiled human); skill upstreaming; /mnt/c disposition — RULED 2026-08-05: retire (see below).

Environment [A, 2026-08-05]
Claude Code project root = UNC path to the ext4 clone (`\\wsl.localhost\Ubuntu\...`); status line shows "Ubuntu". The /mnt/c mirror is RETIRED by author ruling (four-incident ledger: M2 mis-target, M4 staleness, promotion memory note, compaction-A 345-line stale read; UNC resolution closed the hazard class). `bin/sync-mnt-c` and the guarded-mirror doctrine retire with it (this session's riders). Physical directory deletion is the author's Windows-side act.
Design-channel contract (how sessions run)
Two channels: design (chat) plans/verifies vs public clone; Code executes pasted prompts, self-archives, R30 ceremony. Evidence over ruling — live probes have overturned survey rows, channel claims, and the author's own summaries (nine-plus documented instances; "Wave I" provenance, ADR-0044, is the canonical example). Continuity: new design sessions read THIS file + roadmap + latest ADR execution record — structural claims in any continuity prompt carry citations or an explicit [unverified] tag [A, AR-B-5].

## Deviation record (landing session, 2026-08-05)

The build session executing the above ran the doer/checker inversion
as instructed: it did NOT trust the draft's own `[V]` tags at face
value, re-probed each one against the live tree, and found the draft
held on most structural claims but failed on two (the `sim-emit-hl7`/
`sim-engine` dependency claim; the four-incident `/mnt/c` ledger) and
needed minor correction on several more (the `notes/adr/` file count;
AR-C-3's own `AGENTS.md` premise). Full verification table, the
corrected incident ledger, and every disposition: `notes/ADRs.md`
ADR-0047. Landed files: `.agents/state.md`, `.agents/rulings.md` (both
carrying the corrections inline, not as a separate errata layer).
