2026-08-05 — alignment audit: the tree examined, nothing moved
Session prompt (design channel, 2026-08-05). Prior: alignment riders landed and design-channel-verified (`79b7a55`); the arc's brief is in-tree at `.agents/plans/2026-08-05-alignment-audit-brief.md` and is this session's governing document — read it in full before Step 1; where this prompt summarizes it, the brief outranks the summary. This session is the brief's §6 step 1: run the §4 probes in-workspace, land a findings register, fix NOTHING beyond two pre-ruled Step-0 acts. R30 ceremony. Docs-only landing expected; oracle identity mandatory.
Read-first: the brief (all of it); `.agents/rulings.md`; `notes/adr/0048-alignment-riders.md`; `notes/adr/0014-corpus-player.md`; `notes/adr/0017-*.md` and `notes/adr/0018-*.md` (resolve exact filenames via `notes/ADRs.md`).
Author rulings (record verbatim in ADR-0049)

1. AR-AU-0 (tagging delegated — STANDING amendment to AR-R-2's mechanics). Sessions create and push stable tags when a prompt licenses a specific tag at a specific commit; the license issues only after design-channel verification of that landing, so the verification gate is unchanged — only the hands are. This session creates annotated tag `stable-20260805-alignment-riders` at `79b7a55` (message: `alignment riders landed, design-channel-verified 2026-08-05 (ADR-0048)`), pushed via `git push origin <tag>`; if push auth fails, fall back to `gh api repos/{owner}/{repo}/git/refs -f ref=refs/tags/stable-20260805-alignment-riders -f sha=<full-sha-of-79b7a55>` (note: the api route creates a lightweight ref — disclose which path was used). Going forward, each session tags the PRIOR verified stable point when licensed; the author may still tag directly at any time.
2. AR-AU-1 (S7 fix-forward, pre-ruled). The `:mllp` Deferred row's `ehrt.tools.corpus.source-sink` corrects to the live namespace (verify it first: expected `ehrt.corpus-io.source-sink` per `components/corpus-io/src/`), with a dated note: `(ns corrected 2026-08-05, ADR-0049 — ADR-0014's text predates the tools→corpus rename and corpus-io split; transcribed faithfully by ADR-0048, corrected fix-forward here)`. This is the session's ONLY roadmap edit.
3. AR-AU-2 (findings-only). Every other defect, smell, or tempting fix discovered this session becomes a register row — never an edit. Acting on a finding is the worst outcome; a wrong-but-recorded finding is cheap, an unrecorded fix is not.
4. AR-AU-3 (the register). Findings land at `.agents/plans/2026-08-05-alignment-audit-findings.md` + index entry in `.agents/plans/README.md`. Row format, every row: `id | area | probe (the exact command or file read) | evidence (verbatim excerpt or count) | finding | recommendation with reasoning | proposed disposition` where disposition ∈ {ruling-needed, fix-session-candidate (with suggested cluster), close-as-fine, incomplete}. Seeded rows S1–S7 from the brief (S7 = the tripwire-scope question: should live roadmap/plan surfaces count as current-tense for the `ehrt.tools.` family? — recommendation recorded, NOT implemented) appear first, updated with this session's evidence; new rows follow, numbered A-n through F-n by area.
5. AR-AU-4 (report-only probes). `clojure -M:poly libs :outdated` output is a register row and nothing else — no upgrades, no `deps.edn` edits. Same for reflection warnings, never-called interface vars, and NOTICE-coverage gaps: rows, not fixes.
6. AR-AU-5 (budget). If an area's probes exceed reasonable effort, land its partial rows marked `incomplete` with what remains — an honest partial register outranks a complete-looking one. The register may not be skipped.

Steps
Step 0 — Preflight, tag, S7 fix. Cwd = ext4 clone (a `/mnt/c` default is STOP-AND-REPORT). Tip = `79b7a55` or later-with-disclosure. Full suite green baseline; oracle pre-digest noted. Execute AR-AU-0 (tag + push + verify the ref exists on origin) and AR-AU-1 (verify the live ns first, then edit). Commit: `docs: a stale namespace corrected in the player's row; the first stable tag lands (alignment audit, AR-AU-0/1)`
Step 1 — Probe areas A–C (brief §4.1–4.3). Concretely, at minimum:

* A: `clojure -M:poly check :dev`, `clojure -M:poly deps`, `clojure -M:poly libs` and `libs :outdated`; mechanical 1:1 check of root `:dev` local/root entries and `:test` extra-paths against `ls components bases` (script it — the row's evidence is the diff, expected empty); grep `artifacts.lock.edn` and root `deps.edn` for whether the NIST jar mirroring end-state (file:// repo + engine-jar sha256s) landed or remains hit-nexus-live.
* B: enumerate each component's interface vars (`poly ws get:components:*:interface` or grep `defn\|def `in interface files); for each, grep external callers workspace-wide; rows for never-externally-called vars, noting the alias-heuristic's false-positive risk. Reflection sweep: per src namespace, `(binding [*warn-on-reflection* true] (require ns :reload))` in a `-M:dev:test` eval, stderr captured; row per component with warning counts (report-only, AR-AU-4).
* C: script the resource-nesting rule (every `components/X/resources/` contains exactly one subdir, named `X`); evidence for S1 plus any new violators. Naming-coherence table for the non-`sim-*` components against the brief's §7 retained convention table — drift rows only.

Step 2 — Probe areas D–F (brief §4.4–4.6). Concretely, at minimum:

* D: read ADR-0014's `:mllp` three-namespace shape against live `ehrt.corpus-io.source-sink` — does the seam still fit post-split? Diff `sim-emit-fhir` vs `sim-emit-hl7` src for copied idioms (site-profile access, event-log/replay walking) — the row answers "what would a third sibling copy?". Pairing-as-data landing-spot: for each candidate home (`judge`, `kernel`, a new leaf), state its current dependency position and whether the registry's expected consumers (mutation side, judge side) can reach it acyclically — evidence from `poly deps`, recommendation for the design pass, no code. Re-read ADR-0017/0018 named-futures: per future, still-live / sharpen / close-with-dated-note recommendation.
* E: staleness sweep — every count, path, and ns reference in `AGENTS.md`, `AUTHORS-GUIDE.md`, and each component's `docs/` checked against the tree (script what's scriptable; the evidence column carries the mismatches). Tripwire learned-set completeness: its forbidden list vs every ns retired across ADR-0017/0018 and the sim split (enumerate from those ADRs). S7's scope question gets its recommendation here.
* F: NOTICE/LICENSE coverage per vendored root (the oracle's eleven roots are the enumeration); version-surface enumeration (`version.edn`, `ehrt.sim.version`, anything else claiming a version); the one-artifact-per-workspace question framed as an options row (uberjar project vs library composition — list, don't decide; the registry choice is settled and not re-raised). Register lands complete (or honestly `incomplete`-marked) with its index entry. Commit: `docs: the audit findings register lands — the tree examined, nothing moved (alignment audit, AR-AU-3)`

Step 3 — ADR-0049 + record. Author `notes/adr/0049-alignment-audit.md` (rulings verbatim; the tag act and which mechanism it used; the register's row count by disposition; anything incomplete and why); index line in `notes/ADRs.md`; Done pointer `- 2026-08-05 — alignment-audit — ADR-0049`. Oracle bracket (`79b7a55` → tip): all ELEVEN batches identical — any digest change is STOP-AND-ESCALATE, none is licensed. Full suite green, shape unchanged from Step 0 (the S7 row edit and register are docs). Session record + prompt self-archive (pairing gate). Final commit: `docs: alignment audit record — findings for ruling, fixes for later (ADR-0049)`
Fences
Findings-only beyond Step 0's two pre-ruled acts. No `src/`, no `test/`, no `deps.edn`, no `workspace.edn`, no gate changes, no dependency upgrades (AR-AU-4), no tripwire-scope implementation (S7 is a row). Frozen archives untouched (`notes/adr/0049` + index line sanctioned). Settled rulings not re-raised (Clojars registry; `/mnt/c`; GMF scope; façade freeze — the façade may be DESCRIBED in area-B rows, never proposed for change). Network use limited to git/gh push-pull and Maven dependency resolution already required by the suite; `poly libs :outdated` may consult repositories — that is licensed, acting on it is not. The brief outranks this prompt; evidence outranks both. If a probe contradicts the brief's seeded claims, the register row says so — the brief gets corrected by a later ruled session, not by this one.
After landing: the design channel verifies by fresh probe; on verification the author licenses the next session to tag `stable-YYYYMMDD-alignment-audit` (AR-AU-0's standing mechanic), and the register's rows come back to the author for per-cluster rulings.
