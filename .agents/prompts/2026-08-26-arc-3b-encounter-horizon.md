# Session prompt -- arc 3b-1: the encounter horizon, lifted (ADR-0174 section 2(a), sweep 1 of 3)

**Archived** by the session it drove,
[`.agents/session-records/2026-08-26-arc-3b-encounter-horizon.md`](../session-records/2026-08-26-arc-3b-encounter-horizon.md).
Verbatim, as issued.

---

Context. HEAD b4e3048. ADR-0174 is Proposed; the author ruled 2026-08-26 A1 B1 C1 (+ ADT^A20 for bed status, sweep 2) D1 E1. E1: three sweeps, encounter FIRST and ALONE, each dark-then-on. This session is the encounter sweep only: `:encounter-id`, `PatientState` restructured, `admission-only-when-new` -> `-no-open-encounter`, `evolve :discharge` closes the encounter, PV1-19 renders it, invariants split per section 2(a)'s table. No scheduling kinds, no bed cycle, no A20. Read ADR-0174 whole; section 1 census (i) is your site list (seven single-encounter-assumed fields at :298-304, every invariant in the :64-70 table, `encounter-resource` `emit_fhir.clj:136` -- the one reader that breaks, correctly); section 2(a) is the spec; section 5 the rulings. Re-derive every line. Payload session.

Step 0. ADR-0174 -> Accepted, five rulings quoted where they land; fix `roadmap.md:143` (the bed-board sink landed under ADR-0067, ships as `ehrt play --board`). Own commit.

Opt-in law (channel reading of E1 -- STOP if ADR-0174 contradicts it): a run-config key `:encounters`, ABSENT = today's behaviour byte-for-byte (the wall stands, PV1-19 blank, `:encounter-id` minted but never rendered); PRESENT = the wall is lifted (a repeat arrival with no open encounter opens a second one) and PV1-19 renders. Same shape as `:persons`.

Step 1. RED then GREEN, dark. Unit configs WITH `:encounters`, `pos?` witnesses: (i) B1 -- `:encounter-id` = `(mix64 seed patient-ordinal encounter-ordinal)`, minted at every opener (`:admission`, `:outpatient-visit`, the placeholder registration, delivery, occupational injury), appearing on exactly one opener and at most one closer (section 2(a) :73); (ii) `PatientState` gains `:encounter` (the open record, fields per :16-17) and `:encounters` (closed, accumulating like `:conditions` :317-320); the seven projection fields keep shape and value while open; (iii) the guard: admission legal iff `(nil? (:encounter state))`; a second arrival of an already-discharged patient OPENS a second encounter (arc 3a's `repeat-arrivals-resolve-and-queue-nothing` witness INVERTS: rename it, count the second encounters, `pos?`); `registered-is-every-patients-first-event` UNCHANGED and asserted still to hold (:69 -- a second encounter mints no second `:registered`); (iv) every invariant in the :64-70 table in its per-encounter form, the per-patient forms kept where the table keeps them, each firing on a mutated corpus; (v) PV1-19 renders the id, `pv1-segment`'s blank count 28 -> 27, and `encounter-resource` renders one Encounter per closed record plus the open one (:81) with `"<patient-id>-<encounter-id>"` ids -- say what the FHIR conformance baseline does; (vi) `classify-change` on the schema: report additive-or-not, bump only if owed. PROOF: `bin/regression-oracle b4e3048 HEAD` IDENTICAL, 35+1 roots, no declaration; every pinned fixture and both conformance baselines byte-equal. Commit dark.

Step 2. ON. Opt in exactly the six corpora arc 3a opted in (four `gated-runs`, `ed-tuesday`, `clinic-decade`) plus ONE new oracle pair in `digest.clj` with `:encounters` (existing 36 roots untouched). Predicted movers: every opted-in corpus (the 22/100 and 39/200 arrivals the wall discarded now open encounters; every emitted PV1 gains field 19). Re-pin ONCE: opted-in fixtures + digests, counted witnesses (`pos?` floors stay; emptied witnesses re-derived and disclosed), both conformance baselines (the FHIR one moves: Encounter ids), `make traces`, `event-schema-examples`, scenario READMEs from regenerated runs. Wire witnesses across the opted-in set: second encounters per corpus, PV1-19 non-empty on every PV1, encounters closed = discharges. `bin/regression-oracle <dark sha> HEAD --declared-digest-change`: 36 IDENTICAL, one `+`. `make test` + `make integration`. Commit ON.

Step 3. Push; CI is the gate; no tag. Record one page: both oracle lines, the witness table (second encounters per corpus is the headline -- the horizon's cost, recovered), what re-pinned, ADR premises the tree contradicted. Roadmap: `[multi-encounter-horizon]` CLOSED, `[engine-fold-extensions]` 3b sweep 1 of 3.

Fences. Step 1 moves nothing. Step 2 moves only opted-in corpora and the new pair. No scheduling or bed-status kind; no A20. MRN and the merge family untouched (:56). No re-pin outside step 2's list without naming it. If `:encounters` cannot be made opt-in without a byte moving on the absent path, STOP-AND-REPORT with the diff.
