2026-08-07 — vendoring batch 1: the everyday ambulatory load — six ailments join the mix
Session prompt (design channel, 2026-08-07; conventions and every claim below read at HEAD `cd16fa9` against a fresh public clone). Second session of the vendoring arc (opened ADR-0069). The curation pass ran in the design channel over the substance artifact (`components/sim-trajectory/docs/census/2026-08-07-synthea-7e08387-substance.edn`, tally re-derived from the artifact itself: 33 produces-content / 51 zero-on-every-seed) and the author CONCURRED (design channel, 2026-08-07) with the batched plan: batch 1 (this session) vendors the everyday ambulatory load — asthma (closure 3 files, peak 87 events), bronchitis (1, 22), sleep-apnea (1, 116), injuries (8, 134), fibromyalgia (1, 38), dementia (1, 11) — batches 2–4 held for later ruling; wellness-encounters flagged as a named design item, never routine vendoring; CHF/contraceptives held for after Wave E. Mechanics per the established pattern, all probe-verified at HEAD: modules vendor VERBATIM from the pin into `components/sim/resources/sim/modules/` (submodules at their call-path, tables under `lookup_tables/` — `resolve-modules`' own search paths, run.clj:41–81); every vendored file gets a NOTICE table row (filename | upstream URL at the pin | commit SHA | SHA-256 | retrieved date — the exact format the existing rows use); each module gets a `vendored_<module>_test.clj` engine-layer round-trip in `components/sim-emit-hl7/test/` on WELL-MIXED seeds (sequential small Random seeds are not well-distributed — digest.clj's own repeated lesson) at a run-config witnessed producing real content; each module joins `components/oracle/src/ehrt/oracle/digest.clj` as a NEW root — a FIRST BASELINE, declared, never a regression check (the ADR-0033 AR-4b precedent), while the EXISTING eleven batches stay byte-identical. R30 ceremony. Read-first: `components/sim/resources/sim/modules/NOTICE` in full (the provenance discipline and row format); `components/sim/src/ehrt/sim/run.clj` `resolve-modules` (~52) and the resource search paths; `components/sim-emit-hl7/test/ehrt/sim_emit_hl7/vendored_tjr_test.clj` and `vendored_ear_infections_test.clj` (the round-trip pattern to follow, including closure/initial-attribute handling); `components/oracle/src/ehrt/oracle/digest.clj` header + its ADR-0033 dated note (root-addition mechanics); the substance artifact's rows for the six modules (their event-counts name seeds that produced content under census parameters — engine-layer seeds are found fresh, but the artifact is the existence proof); roadmap Deferred "Vital-sign channel" row (the rider's subject). Author rulings (record verbatim in ADR-0070; `[A]` author-ruled, `[C]` channel-inferred)

1. AR-VB1-0 `[A — tag law, case (ii); debt recorded in ADR-0069]`. Annotated `stable-20260807-census-substance` at `cd16fa9`, message `census substance landed, design-channel-verified 2026-08-07 (ADR-0069)`; push; verify peeled ref.
2. AR-VB1-1 `[A — the author's concurrence with the curation plan, design channel 2026-08-07; the batch composition is the channel's, concurred]`. ADR-0070 records the curation plan and the concurrence: four batches named, batch 1 executed here, batches 2–4 awaiting their own rulings, wellness-encounters a NAMED DESIGN ITEM (it is upstream's wellness machinery and collides with the engine's own wellness-cadence design — never vendored routinely), CHF/contraceptives held for after Wave E, homelessness unbatched. The plan's text lands in ADR-0070 itself — the design channel's chat is not the record.
3. AR-VB1-2 `[C — vendoring mechanics per NOTICE/ADR-0013 discipline]` (the vendoring). For each of the six: enumerate the FULL closure from the author's pin-verified Synthea checkout (`/home/mg/synthea-checkout` at `7e08387c...f902` — verify the pin first, the census tool's own `verify-pin` discipline; the closure = root JSON + every submodule at its call-path + every lookup table, the same resolution the census's own resolve-fns perform); copy each file VERBATIM (byte-identical, no edits ever — a file that seems to need an edit is a STOP-AND-REPORT); SHA-256 each; append one NOTICE row per file in the established format with a dated section header naming this session and ADR-0070. Expected total ≈ 15 files (3+1+1+8+1+1) — a materially different count is disclosed, not silently absorbed.
4. AR-VB1-3 `[C — the round-trip gate per ADR-0033 pattern]` (the tests). One `vendored_<module>_test.clj` per module: engine-layer round trip (`engine/run` + `emit-hl7/emit`) through the vendored closure at a run-config and well-mixed seed WITNESSED producing nonzero trajectory events and nonzero emitted messages — the seed hunt is part of the work; a module for which no reasonable seed/config produces content is a STOP-AND-REPORT (the census says all six produce under census parameters; engine-layer silence would be a real discrepancy). Red-first where red is meaningful: the test lands BEFORE its module's resources in commit order within the step, witnessed failing on the missing resource, then green after the copy.
5. AR-VB1-4 `[C — oracle mechanics per ADR-0033 AR-4b precedent]` (the roots). Six new engine-layer roots join `digest.clj`, one per module, each a FIRST BASELINE (declared in the manifest header per the script's own `--declared-digest-change` contract if the script requires it for additions — fresh-read the script's own addition mechanics and follow them, disclosed). The EXISTING eleven batches must be byte-identical across this session's entire span — any movement in a pre-existing batch is STOP-AND-ESCALATE.
6. AR-VB1-5 `[C — the rider, from the curation pass's own artifact re-derivation]` (vital-sign row correction). The Deferred "Vital-sign channel" row gains a dated note: the substance census (ADR-0069's artifact) shows `congestive-heart-failure` `[0,117,0]` and `contraceptives` `[0,89,0]` — both now produce on some seeds (post-Wave-VS), `covid19` alone remains zero-on-every-seed; the row's census-confirmed-blocked citation updates to name the current evidence, trigger unchanged, nothing else moves.
7. AR-VB1-6 `[C — scope]` (fences + oracle). NO module-content edits, ever — verbatim or not at all. NO loader/interpreter/engine/emitter changes — if a vendored closure trips a loader gap the census didn't (different code path), that module is DEFERRED WHOLE with a finding (the bail-out precedent), the batch lands minus it, disclosed. NO default-config changes — the demo/busy-tuesday configs are the author's own files; the new modules become AVAILABLE, not default. NO batch-2/3/4 modules, however tempting the adjacency. Standing untracked files untouched.

Steps Step 0 — Preflight + tag. Cwd ext4; tip `cd16fa9` or later-with-disclosure; pin-verify the checkout; full suite green baseline; oracle pre-digest (existing eleven). Execute AR-VB1-0. Step 1 — Rider (AR-VB1-5). Commit: `docs: the vital-sign row learns what the substance census showed (vendoring batch 1, AR-VB1-5)` Step 2 — Vendor + test, module by module (AR-VB1-2/3), red-then-green per module inside the step. ONE commit when all six land (or per the bail-out, all-that-landed): `feat: six everyday ailments join the mix — asthma, bronchitis, sleep-apnea, injuries, fibromyalgia, dementia (vendoring batch 1, AR-VB1-2/3)` Step 3 — Oracle roots (AR-VB1-4). Six first baselines recorded; existing eleven verified identical. Commit: `test: six new oracle roots — first baselines for the batch-1 closures (vendoring batch 1, AR-VB1-4)` Step 4 — ADR-0070 + record (AR-VB1-1). Rulings verbatim; the curation plan text; the NOTICE additions cross-checked against on-disk SHA-256 by fresh hash (audit evidence uses the mechanism it recommends); per-module seed/config table; index line; README count by `ls`; Done pointer `- 2026-08-07 — vendoring-batch-1 — ADR-0070`. Full oracle bracket (`cd16fa9` → tip): existing eleven identical, six new roots present as baselines. Successor tag debt recorded IN THE ADR (`stable-20260807-vendoring-batch-1` at this session's closing tip). Session record + prompt self-archive. Final commit: `docs: vendoring batch 1 record — the ailment mix doubles (ADR-0070)` Fences Everything AR-VB1-6 names. The curation plan is recorded, not extended — no re-ranking, no batch reshuffling mid-session. A closure file whose upstream content differs from what the census walked (pin drift, local checkout dirt) is STOP-AND-REPORT, never re-fetched from a later commit. `[C]` rulings that conflict with the live tree fix forward and disclose; `[A]` ones escalate. After landing: design channel verifies by fresh probe — NOTICE hashes re-derived against the vendored bytes, the census's produces-content claim spot-checked against each round-trip test's own witnessed output — then batch 2 (the chronic clinic tail) gets its prompt on the author's go, and the wellness-encounters design item waits its own pass.

## Deviation record

- **Prompt named six modules; five landed, one deferred whole.**
  `injuries.json` was assessed and DEFERRED WHOLE per AR-VB1-6's own
  bail-out clause — a real, standing `gmf-interpreter` `max-steps` gap
  the census's own narrow three-seed sample never sampled (confirmed
  at two layers: through `engine/run` across three horizons, and
  directly at the census's own interpreter-layer code path across 120
  well-mixed seeds). Full finding: `notes/adr/0070-vendoring-batch-
  1.md` and `components/sim/resources/sim/modules/NOTICE`'s own dated
  section.
- **Expected file count (≈15) matched the actual total (15) by
  coincidence, not by the estimate's own reasoning holding** — the
  estimate summed the census's own `:closure-file-count` (JSON modules
  only) and assumed `injuries.json` would land whole; the real total
  is eleven asthma files (three JSON plus eight lookup-table CSVs the
  estimate didn't count) plus four single-file modules, zero from
  injuries. Full account: ADR-0070's own "Expected-count disclosure"
  section.
- **Commit messages for Steps 2/3 were adapted from the prompt's own
  literal text** to name the actual landed set (five, not six) and the
  deferral, rather than land a commit message asserting six modules
  when only five did — fix-forward with disclosure, per this repo's
  own standing discipline for a checkpoint premise that doesn't fully
  hold against the live tree.
