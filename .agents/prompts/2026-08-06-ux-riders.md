2026-08-06 — ux riders: the arc opens — brief lands, tags land, the compaction pointers come home
Session prompt (design channel, 2026-08-06). Prior: the alignment arc closed and was design-channel-verified (`12d3aa3`, ADR-0055). A real first-contact CLI failure on 2026-08-06 opened the UX arc; its brief is in the working tree at `.agents/plans/2026-08-06-ux-arc-brief.md`, placed by the author — if absent or empty, STOP-AND-ESCALATE, do not reconstruct. This is the arc's small opening session: land the brief, execute the alignment arc's two pending tags, rotate the leftover compaction pointers. Docs and tags only. R30 ceremony. Read-first: the brief (all of it); `notes/adr/0055-alignment-arc-close.md` (the pending-tag commands and the scope-precision disclosure this session closes); the attic's format contract (`.agents/plans/roadmap-done-2026-08.md` header + the alignment-arc rotation as the fresh model).
Author rulings (record verbatim in ADR-0056)

1. AR-U0-1 (brief lands, verified not trusted). The brief lands with its `.agents/plans/README.md` index entry. Before landing, re-probe its probeable claims: no `:cli` alias in root `deps.edn` (nor in `components/sim/deps.edn`); the demo READMEs' `clojure -M:cli` fences exist as described (enumerate the files — the count goes in ADR-0056); `bin/ehrt`'s cd-to-root behavior per its own header. A claim that fails probe is corrected in the brief with a dated note, never silently kept or dropped.
2. AR-U0-2 (the two pending tags — R1). Per the reconciled mechanic: (a) annotated tag `stable-20260805-alignment-fixes-5` at `2b3bb2b`, message `alignment fixes 5 landed, design-channel-verified 2026-08-05 (ADR-0054)`; (b) annotated tag `stable-20260805-alignment-close` at `12d3aa3`, message `alignment arc closed, design-channel-verified 2026-08-06 (ADR-0055)`. Both pushed, both verified on origin with peeled refs resolving to the named commits. If either already exists at the exact commit/message (the author may have tagged directly), verify and disclose rather than re-create — the fixes-2 precedent.
3. AR-U0-3 (compaction pointers rotate — R2). The three Done pointers (scaffolding-compaction-a/b/c, ADR-0045/0046/0047) relocate verbatim from the roadmap's Done section to `.agents/plans/roadmap-done-2026-08.md` under a dated header (`## Scaffolding-compaction arc — closed 2026-08-05, rotated 2026-08-06 (ADR-0045–0047; rotation deferred at its own close, disclosed in ADR-0055)`), placed BEFORE the alignment-arc section so the attic reads chronologically. Relocation-not-rewrite. The Done section afterward holds exactly two entries: ADR-0055's pointer and, once Step 2 lands it, ADR-0056's.
4. AR-U0-4 (nothing else). No UX fixes of any kind land this session — not one help string, not one error message, however small the temptation. The audit session owns the survey; this session opens the arc, only.

Steps
Step 0 — Preflight. Cwd ext4; tip `12d3aa3` or later-with-disclosure; brief present; AR-U0-1's probes run and tabled; full suite green baseline; oracle pre-digest. Then AR-U0-2's two tag acts, verified.
Step 1 — Brief + rotation (AR-U0-1/3). The brief and its index entry; the rotation. ONE commit (both are `.agents/plans/` hygiene): `docs: the UX arc opens — its brief lands, the compaction pointers come home (ux riders, AR-U0-1/3)`
Step 2 — ADR-0056 + record. ADR-0056: rulings verbatim; the probe table; both tag verifications; the rotation's before/after Done listing. Index line; Done pointer `- 2026-08-06 — ux-riders — ADR-0056`. Oracle bracket (`12d3aa3` → tip): all ELEVEN batches identical — docs-only; any change is STOP-AND-ESCALATE. Full suite green (index/pointer gates hold through the rotation). Session record + prompt self-archive. Final commit: `docs: ux riders record — the arc opens clean (ADR-0056)`
Fences
Docs and tags only: no `src/`, no `test/`, no config, no gates, no help.clj, no error strings (AR-U0-4). Frozen archives untouched (ADR-0056 + index + the live-attic append sanctioned). The brief outranks this prompt's summaries of it; evidence outranks both.
After landing: design channel verifies by fresh probe, then drafts the UX audit session against the landed brief. This landing's own tag rides that session's Step 0.

## Deviation record

AR-U0-2's two tag acts were licensed but not executed by this session.
This workspace's standing ceremony law (`AGENTS.md`; `AUTHORS-GUIDE.md`
§1) reserves `stable-*` tags to the author alone in every ceremony
mode, including R30 — the same fence ADR-0055's own AR-AC-0 already
applied to these exact two tags before this session's own prompt was
authored. Step 0's own sequencing above ("Then AR-U0-2's two tag acts,
verified") is read against that standing law, not literally: this
session verified neither tag exists yet, prepared both commands exactly
as specified, and left execution to the author. Full disclosure and the
exact commands: `notes/adr/0056-ux-riders.md`.
