2026-08-07 — census substance: the vendoring arc opens — walks-and-produces is no longer walks-and-nothing
Session prompt (design channel, 2026-08-07; conventions and every claim below read at HEAD `b7ed686` against a fresh public clone). OPENS THE VENDORING ARC — module vendoring widening the ailment mix past the seven in-tree, RATIFIED by the author (ADR-0066 AR-BB1-R; scheduled after the player arc, which closed at `b7ed686`, ADR-0068). Sequencing per ADR-0068's own horizon note (design-channel recommendation, recorded there): the census substance qualifier FIRST — curation over 84 walking modules is impossible without distinguishing walks-but-produces-nothing from walks-with-real-content (the roadmap Deferred census-refinements row's own item (a): 26 of 42 pre-Wave-F `:ok-walked` modules produce zero events on every seed, `docs/gmf-interpreter.md` §15 AR-8b) — then the design-channel curation pass over the ranked catalog, then vendoring sessions batched by closure family. Probe-backed head start, verified at HEAD: `walk-one` (census.clj:322) ALREADY records `:event-count` per seed in every walk row — the substance DATA exists in every census artifact; this session adds only the qualifier, the tallies, the filename fix (refinement (c)), and the fresh labeled census run that becomes the curation pass's input. R30 ceremony. Read-first: `components/sim-trajectory/src/ehrt/sim_trajectory/census.clj` (`walk-one` ~322 and its `:event-count`; `census-one` ~344, the `:ok-walked` row shape; `summarize` ~400; `-main` ~440, the filename derivation refinement (c) targets); `components/sim-trajectory/test/ehrt/sim_trajectory/census_test.clj` (the 7 existing tests, the fixture style to extend); the roadmap Deferred "Census tool refinements" row in full (items (a)/(b)/(c), their history, the ADR-0044 relocation note); `docs/gmf-interpreter.md` §15 (AR-8b's substance note — the 26-of-42 figure this session's fresh run supersedes) and the AR-5 dated-append convention its census sections follow; `components/sim-trajectory/docs/census/2026-08-04-synthea-7e08387-wave-i2.edn` header (the parity artifact this run's verdicts are compared against). Author rulings (record verbatim in ADR-0069; `[A]` author-ruled, `[C]` channel-inferred — conflict rules per the session-prompt skill's own convention)

1. AR-VC-0 `[A — tag law ADR-0057 AR-T-1 case (ii); debt recorded in ADR-0068 itself]`. Annotated `stable-20260807-player-close` at `b7ed686`, message `player arc closed, design-channel-verified 2026-08-07 (ADR-0068)`; push; verify peeled ref.
2. AR-VC-1 `[A for the arc opening (the ratification, ADR-0066 AR-BB1-R, and the author's "Start vendoring," design channel 2026-08-07); C for the sequencing]`. ADR-0069 records the vendoring arc OPEN, first session this one, sequencing per ADR-0068's horizon note. The arc's brief is the recorded chain (ADR-0064 intake → ADR-0066 ratification → ADR-0068 horizon), not a new document — cite it, don't restate it.
3. AR-VC-2 `[C — executes the Deferred row's own item (a), its revisit trigger fired by this arc]` (the substance qualifier). `census-one`'s `:ok-walked` rows gain an ADDITIVE `:substance` key derived from the walks' own already-recorded `:event-count`s: `:zero-on-every-seed` iff every walk's count is 0, else `:produces-content`; alongside it, `:event-counts` (the per-seed vector, surfaced at row level for curation-time ranking without re-digging `:walks`). The verdict enum itself does NOT change (additive field — nothing downstream re-keys). `summarize` gains `:ok-walked-by-substance` (tally) . Red-first: `census_test` extensions — a fixture module producing zero events on every seed vs one producing content, both asserting the qualifier and the tally; the existing 7 tests must pass unmodified (additive means additive).
4. AR-VC-3 `[C — executes the Deferred row's own item (c)]` (the filename learns to disambiguate). `-main` accepts an optional third argument, a label, giving `<census-date>-synthea-<pin7>-<label>.edn` (unchanged shape when absent) — the hand-appended wave-suffix workaround (both prior collisions, per the row's own history) retires. Extract the filename derivation into a pure fn; red-first test on both shapes. No wall-clock beyond the existing `:census-date` (determinism law unchanged).
5. AR-VC-4 `[C — the run that feeds curation]` (the fresh census). Re-run the census at the pin against the author's own Synthea checkout, label `substance`, artifact into `components/sim-trajectory/docs/census/`; append a dated section to `docs/gmf-interpreter.md` per the AR-5 convention: the substance tally, and the FULL list of `:ok-walked` module ids with `:substance :zero-on-every-seed` (the AR-8b 26-of-42 figure's successor, now first-class) — this artifact + section is the curation pass's input, say so in the section. STOP-AND-REPORT if any module's VERDICT differs from the parity artifact (84/85 `:ok-walked` + `gallstones` out-of-scope) — a parity regression at the same pin is news, never routine; substance values are new information, not regressions.
6. AR-VC-5 `[C — AR-A-5 discipline, fix-forward form]` (the Deferred row). The census-refinements row rewrites with a dated note: (a) and (c) CLOSED this session (ADR-0069 — the closed items' own text lives on in that ADR's record, relocation-not-deletion); (b) (per-module census-seed override) STANDS alone with its original trigger, untouched. Same commit as the work that closes them.
7. AR-VC-6 `[C — scope]` (fences + oracle). Src edits ONLY in `census.clj` and `census_test.clj`. NO gmf loader or interpreter changes — a census observes the tree as it stands (the census tool's own founding fence, ADR-0034); any loader/interpreter gap the fresh run surfaces is a FINDING for the curation pass, recorded never fixed. NO module vendoring this session — the arc's build sessions follow curation, not precede it. The oracle bracket must show all ELEVEN batches identical (the census reads upstream modules and writes a docs artifact; emitted corpora are untouched); any digest change is STOP-AND-ESCALATE. Standing untracked files untouched.

Steps Step 0 — Preflight + tag. Cwd ext4; tip `b7ed686` or later-with-disclosure; full suite green baseline (fresh-clone green is the standing expectation — a failure is STOP-AND-REPORT); oracle pre-digest. Execute AR-VC-0. Step 1 — Red, witnessed (AR-VC-2/3 tests first). The substance-qualifier and filename reds captured for ADR-0069. Commit: `test: substance turns red — zero-event walks witnessed indistinguishable (census substance, AR-VC-2/3 red)` Step 2 — Green + the fresh run (AR-VC-2/3/4). Implementation lands; all green; `poly check` OK; full suite green; the labeled census artifact and the doc append land; the parity comparison recorded. Commit: `feat: the census tells substance from silence — and the catalog is ranked for curation (census substance, AR-VC-2/3/4)` Step 3 — ADR-0069 + record (AR-VC-1/5). Rulings verbatim; the red transcripts; the parity comparison; the Deferred row rewrite; index line; README count verified by `ls`; Done pointer `- 2026-08-07 — census-substance — ADR-0069`. Oracle bracket (`b7ed686` → tip) per AR-VC-6. This close's own successor tag debt recorded IN THE ADR (`stable-20260807-census-substance` at this session's closing tip, next session's Step 0). Session record + prompt self-archive. Final commit: `docs: census substance record — the vendoring arc opens with an honest catalog (ADR-0069)` Fences No vendoring. No loader/interpreter/emitter/engine edits. No verdict-enum change. The fresh census artifact is evidence, not a decision — curation is the design channel's next pass, and every vendoring choice after that is a ruled session. Frozen archives untouched (ADR-0069 + index + the sanctioned doc append). `[C]` rulings that conflict with the live tree fix forward and disclose; `[A]` ones escalate. After landing: design channel verifies by fresh probe — including re-deriving the substance tally from the artifact itself, not the ADR's summary of it — then the curation pass runs in the design channel over the ranked catalog (substance × closure-family × the recorded blockers: the vital-sign trio, stroke pending Wave E's register), producing the batched vendoring plan for the author's ruling.

## Deviation record

- **Citation correction, disclosed not blocking:** the prompt's own
  read-first list and AR-VC-4 both cite "`docs/gmf-interpreter.md` §15"
  for the AR-8b substance note and the dated-append target. That
  content actually lives in `gmf-interpreter-findings.md` §15 — moved
  there verbatim 2026-08-05 (`notes/ADRs.md` ADR-0043 AR-D-1);
  `gmf-interpreter.md` §9 keeps only an index table pointing at it. This
  session read the substance note from and appended the new dated
  subsection to the correct live file
  (`gmf-interpreter-findings.md`), and added the corresponding index
  row to `gmf-interpreter.md` §9, matching the convention as it
  actually stands rather than the prompt's own slightly-loose citation.
- **The Synthea checkout path was not named in the prompt** ("against
  the author's own Synthea checkout") — found live at
  `/home/mg/synthea-checkout`, confirmed `git rev-parse HEAD` matches
  the pin (`7e08387c68a7f0e21d13076609a159fd473fc902`) and the modules
  directory holds the expected 85 top-level files, before running the
  census.
- **No STOP-AND-REPORT fired.** The parity comparison (AR-VC-4's own
  gate) found zero verdict diffs and zero digest diffs against the Wave
  I2 artifact — full details in ADR-0069's own Verification section.
- **No other premise mismatch found.** Working tree was clean at tip
  `b7ed686` exactly as stated; the full suite was green (227 `Test
  results:` lines, 0/0) at Step 0, matching the standing expectation.
