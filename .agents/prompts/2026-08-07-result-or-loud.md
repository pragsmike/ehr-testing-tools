# 2026-08-07 — Result or loud — driving prompt

## Context

Repo `ehr-testing-tools`, ext4 clone (`~/src/ehr-testing-tools`).
Prompt's own stated HEAD: `93bd9a6` — confirmed live at session start,
no premise mismatch (see this date's own session record).

## Prompt, verbatim

2026-08-07 — result or loud: every I/O failure speaks the vocabulary — the register's top cluster closes Session prompt (design channel, 2026-08-07; conventions and every claim below read at HEAD `93bd9a6` against a fresh public clone). Third session of the quality-review arc: FIX SESSION 1 of the ruled mitigation plan. The author ruled on the first assessment's register today (design channel, 2026-08-07, all five rulings as recommended — AR-RL-5 records them verbatim below, their execution distributed across this session, fix session 2, and the arc close). This session closes the register's highest-severity cluster — ONE root cause surfacing in three dimensions (D4-1, D3-4, D8-2/D8-3, `.agents/plans/2026-08-07-repo-review-findings.md`): I/O calls living outside the repo's own Result vocabulary, including one DEMONSTRATED silent-success path (`mutate-command` returning `{:status :ok, :count 0}` for a failed directory listing). Also carried: a correction the design channel's own verification found IN the register — its summary arithmetic does not match its own rows (45 disposition-carrying rows, not 44; 28 close-as-fine, not 26; 5 ruling-needed, not 6; fix-candidate 9 and intake 3 correct) — the alignment register's own 51-vs-47 class, recurring in the quality instrument itself; corrected by dated note, append-don't-erase. CI-red policy note (this arc's own): red checkpoints are WITNESSED IN-SESSION and recorded in the ADR's transcripts; COMMITS LAND GREEN — no push carries a knowingly-failing test (supersedes the older red-commit checkpoint pattern for this and future sessions; disclose this as the pattern shift it is). R30 ceremony. Read-first: the register's D4-1, D3-4, D8-2, D8-3 rows in full (the site list and the traced silent-success path); `bases/cli/src/ehrt/cli/core.clj` `files-with-extension-in` (~620) and `mutate-command`; `components/kernel/src/ehrt/kernel/artifact.clj` `:123` (`.renameTo`), `:195`, `:227`; `components/sim/src/ehrt/sim/run.clj` `similar-sibling-config` (ADR-0076's fix — the pattern to generalize, including its retry idiom and naming comment); `ehrt.kernel.result`'s API (the vocabulary the helper returns); the remaining sites: `corpus/generate.clj:127`, `generator_source.clj:63`, `corpus-io/spool.clj:58`, `sink_write.clj:166`, `sim-trajectory/census.clj:173`, `judge-fhir-official/fhir.clj:471`, `bases/cli/core.clj:375,1039` (line numbers are the register's — re-locate by fresh read, never trust drift); `README.md`'s "What you get" fence (~77–84). Author rulings (record verbatim in ADR-0078; `[A]` author-ruled, `[C]` channel-inferred)

1. AR-RL-0 `[A — tag law, case (ii); debt recorded in ADR-0077]`. Annotated `stable-20260807-repo-review-1` at `93bd9a6`, message `repo review 1 landed, design-channel-verified 2026-08-07 (ADR-0077)`; push; verify peeled ref.
2. AR-RL-R `[C — the register correction]`. The findings register gains a dated correction note (bottom, append-don't-erase): the summary's 44/26/6 corrects to 45/28/5 by direct row count, method stated; found by the design channel's post-landing verification; the same-class precedent (the alignment register's 51-vs-47) cited. ADR-0078 records it. The repo-review SKILL amendment (next assessment re-derives its predecessor's summary as a standing probe) is FIX SESSION 2's cargo, not this session's — noted here so it isn't lost.
3. AR-RL-1 `[C — the helper]`. `ehrt.kernel` gains the guarded-I/O helper(s) the sweep needs: a directory-listing fn that distinguishes an I/O failure from an empty directory (retry-once-on-nil, then a `result/error` naming the path — generalizing `similar-sibling-config`'s own ADR-0076 idiom into the shared vocabulary) and a checked-rename fn (`.renameTo` false → `result/error :rename-failed {:from :to}`). Names, arities, and exact Result categories are the session's judgment, disclosed; the helper ns carries the D4-1 story in its docstring (maintainer voice). Red-first unit tests on the helper's own branches (nil path, non-directory, empty-vs-failure distinction — an injectable seam if a real I/O failure can't be provoked hermetically, disclosed).
4. AR-RL-2 `[C — the sweep, in priority order]`. Every register-listed site converts to the helper: (i) FIRST `bases/cli` `files-with-extension-in`/`mutate-command` — the demonstrated silent-success path; red-first via the helper's injectable seam: a listing failure must yield a categorized error envelope, never `{:count 0}`; (ii) the `:fail-if-exists`/`:out-dir-exists` guard sites (`generate.clj`, `generator_source.clj`, `spool.clj`, `sink_write.clj`, `artifact.clj` `extracted-already?`) — a defeated safety guard now errs loud; (iii) the remaining sites (`census.clj`, `fhir.clj`, `artifact.clj` `find-executable`, `cli` 375/1039) — each read in context; a site where nil is GENUINELY impossible or already handled is disclosed as such with reasoning, never converted blindly (the register's own read-in-context discipline); (iv) `artifact.clj:123`'s `.renameTo` → the checked rename. HAPPY PATHS BYTE-IDENTICAL — this session changes what failure looks like, never what success does.
5. AR-RL-3 `[C — the operator-facing pair]`. `corpus mutate` on a missing/unreadable PATH returns the same categorized envelope its siblings give (`:file-not-found`-class, exit 2, matching `gate`/`sim run` — fresh-read their category names and match exactly); red-first. The README "What you get" fence points at a real, generatable path (matching every other fence's runnable convention — the fence-path gate then covers it forever); the surrounding prose adjusts minimally.
6. AR-RL-4 `[C — the recurrence gate]`. A docs-tooling lint test: no bare `.listFiles`/`.list(`/`.renameTo` in `components/*/src` or `bases/*/src` outside the kernel helper's own namespace (allowlist by ns, stated in the test). Its natural red against the pre-sweep tree is WITNESSED IN-SESSION and transcribed into ADR-0078; the commit lands with the sweep, green (the CI-red policy above).
7. AR-RL-5 `[A — the author's five rulings on the register, recorded verbatim here, execution distributed]`. (1) The `state.md` staleness tripwire: ADOPTED — lands in fix session 2. (2) The generalized multi-surface-law drift scaffold: DEFERRED with a named trigger — a third law drifting the hard way builds the registry. (3) `defspec` seed policy: the middle path — seeds stay unpinned repo-wide; a spec that has actually flaked gets its seed pinned or durably logged (the engine spec now, in fix session 2); test.check's printed seed plus CI log retention is ruled sufficient for the rest, revisited on the next flake. (4) Pairing-as-data: RULED IN — the design pass opens in the design channel in parallel with this session; the register stops counting. (5) The multi-seed-once-flagged vendoring practice: ADOPTED as a rulings append at this arc's close.
8. AR-RL-6 `[C — scope]` (fences). Src edits ONLY in: the kernel helper ns (new), the register-listed call sites, `mutate-command`'s error path, and the README fence + its prose line. NO happy-path output changes anywhere — the oracle bracket must show all TWENTY-SEVEN batches identical, and any digest movement is STOP-AND-ESCALATE (error-path edits touch no emitted byte). No fix-session-2 cargo (the lint family beyond AR-RL-4's own gate, the tripwire, the seed pin, the skill amendment). Standing untracked files untouched.

Steps Step 0 — Preflight + tag. Cwd ext4; tip `93bd9a6` or later-with-disclosure; full suite green baseline; last-five CI conclusions disclosed; oracle pre-digest. Execute AR-RL-0. Step 1 — The register correction (AR-RL-R). Commit: `docs: the register's own arithmetic corrects -- 45/28/5, by the mechanism it recommends (result or loud, AR-RL-R)` Step 2 — Helper + gate + sweep + operator pair (AR-RL-1/2/3/4). Reds witnessed in-session and transcribed; everything lands green in ONE commit (or two if the cli cluster and the component sweep split more legibly — session's judgment, disclosed): `fix: I/O speaks Result or fails loud -- the silent count-of-zero dies, gated (result or loud, AR-RL-1/2/3/4)` Step 3 — ADR-0078 + record. Rulings verbatim (AR-RL-5's five included); the red transcripts; the site-by-site conversion table (site → old behavior → new category, including any disclosed not-converted sites); index line; README count by `ls`; Done pointer `- 2026-08-07 — result-or-loud — ADR-0078`. Oracle bracket (`93bd9a6` → tip). Successor tag debt IN THE ADR (`stable-20260807-result-or-loud` at the closing tip). Session record + prompt self-archive. Final commit: `docs: result or loud record -- an I/O failure can no longer impersonate an empty directory (ADR-0078)` Fences Everything AR-RL-6 names. A site whose conversion would change a HAPPY-path behavior is STOP-AND-REPORT, not converted. `[C]` rulings conflicting with the live tree fix forward and disclose; `[A]` ones escalate. After landing: design channel verifies by fresh probe — the gate re-run, the conversion table sampled site-by-site, the mutate error envelope read — then fix session 2 (the lint family, the tripwire, the seed pin, the skill amendment) gets its prompt, and the arc close follows with the rulings appends. The pairing-as-data design pass proceeds in the design channel throughout.
