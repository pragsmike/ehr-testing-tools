# 2026-08-08 — Fidelity payoff — driving prompt

## Context

Repo `ehr-testing-tools`, ext4 clone (`~/src/ehr-testing-tools`).
Prompt's own stated read-first: conventions read at HEAD `82d1753`
against a fresh public clone — this session ran directly after the
EncounterEnd fix landed. Confirmed live at session start: HEAD
`82d1753` (ADR-0082's own closing tip), working tree clean.

## Prompt, verbatim

2026-08-08 — fidelity payoff: anemia comes home — and colorectal's real blocker gets its true name Session prompt (design channel, 2026-08-08; conventions read at HEAD `82d1753` against a fresh public clone). Third session of the fidelity arc: the payoff rider, RESHAPED by the fix session's own evidence (ADR-0082). The brief named two modules; the evidence licenses ONE: anemia's violations extinguished completely post-fix (clean at the deferral's own seeds and population — the in-session proof this session now pins as a committed test), while `colorectal_cancer.json`'s violations are BYTE-IDENTICAL pre- and post-fix with zero dangling ends in its walks — ADR-0072's "same EncounterEnd gap" diagnosis was WRONG for colorectal, and its real blocker (`:clinical-content-only-when-admitted` violations downstream of clean trajectories) is a separate, undiagnosed defect. So: anemia vendors; colorectal reclassifies under its true name and waits for its own investigation. R30 ceremony. Read-first: ADR-0082 in full (the fix, the trace finding, the colorectal evidence — this session's ground truth); ADR-0071's deferral section (the NOTICE row content drafted-and-withdrawn there, now landing for real); ADR-0072's colorectal deferral (the diagnosis this session corrects by erratum); the vendoring mechanics exemplars (`vendored_hypothyroidism_test.clj` — anemia's closure shape is its sibling: root + the ALREADY-VENDORED `anemia/anemia_sub.json`); `components/oracle/src/ehrt/oracle/digest.clj`'s root-addition mechanics; the roadmap's narrowed Deferred row (which this session rewrites once more, to colorectal-only with the corrected diagnosis). Author rulings (record verbatim in ADR-0083; `[A]` author-ruled, `[C]` channel-inferred)

1. AR-FP-0 `[A — tag law, case (ii); debt recorded in ADR-0082]`. Annotated `stable-20260808-encounterend-fix` at `82d1753`, message `encounterend fix landed, design-channel-verified 2026-08-08 (ADR-0082)`; push; verify peeled ref.
2. AR-FP-1 `[C — the vendoring, standing mechanics]` (anemia comes home). `anemia___unknown_etiology.json` vendors VERBATIM from the pin-verified checkout (root file only — `anemia/anemia_sub.json` is already in-tree via hypothyroidism's own closure, verified byte-identical before reuse, the batch-1 shared-submodule precedent); SHA-256; one NOTICE row under a dated section citing ADR-0083 and the two-arc deferral story in one sentence. The committed round-trip test (`vendored_anemia_test.clj`) lands red-first in the ceremonial sense the CI policy allows — witnessed red against the missing resource in-session, committed green — at the deferral's own evidence parameters (the seeds and population ADR-0071 recorded failing, now passing; assert ALSO the `:suppressed-encounter-ends` counts the fix's walks predict, pinning the A5 arm's behavior forever). A new first-baseline oracle root joins `digest.clj`; the existing TWENTY-SEVEN stay byte-identical.
3. AR-FP-2 `[C — the reclassification, from ADR-0082's evidence]` (colorectal's true name). The Deferred row rewrites: colorectal is NOT EncounterEnd-blocked (zero dangling ends in its walks, violations byte-identical across the fix — cite ADR-0082); its live blocker is the separate downstream defect, named by its violation class (`:clinical-content-only-when-admitted` at 2-of-3 seeds, 300 patients), UNDIAGNOSED — revisit trigger: its own investigation session, intake for the arc close. ADR-0072's colorectal diagnosis receives a dated erratum (append-don't-erase; the correction lives in ADR-0083 with a one-line pointer where ADR-0072's record allows) — the same-root-cause claim was inference from the shared submodule, not from a trajectory scan; the scan (ADR-0082's) now exists and says otherwise. The audit-evidence-mechanism rule cited once more: the diagnosis was plausible-by-adjacency, not probe-verified, and this arc's own probe overturned it.
4. AR-FP-3 `[C — scope]` (fences). Src/resource changes ONLY: the anemia root file, its NOTICE row, its test, its oracle root. NO colorectal vendoring, NO investigation of its residual defect (intake, not act). NO other interpreter/engine/emitter changes. Existing 27 oracle batches identical; anemia's root is a declared first baseline. Standing untracked files untouched.

Steps Step 0 — Preflight + tag. Cwd ext4; tip `82d1753` or later-with-disclosure; pin-verify the checkout; full suite green baseline; last-five CI disclosed; oracle pre-digest (27). Execute AR-FP-0. Step 1 — Anemia (AR-FP-1): red witnessed, resource + NOTICE + test land green in one commit: `feat: anemia comes home — two arcs deferred, one fix later, pinned forever (fidelity payoff, AR-FP-1)` Step 2 — The root + the reclassification (AR-FP-1/2). Oracle root added, bracket run with the declared addition; the Deferred rewrite + erratum. Commit: `docs: colorectal's blocker gets its true name — and the oracle gains its twenty-eighth root (fidelity payoff, AR-FP-1/2)` Step 3 — ADR-0083 + record. Rulings verbatim; the deferral-to-landing story; the erratum text; index line; README count by `ls`; Done pointer `- 2026-08-08 — fidelity-payoff — ADR-0083`. Successor tag debt IN THE ADR (`stable-20260808-fidelity-payoff` at the closing tip). Session record + prompt self-archive; CI watched green. Final commit: `docs: fidelity payoff record — the oldest deferral closes, the misdiagnosis corrects (ADR-0083)` Fences Everything AR-FP-3 names. `[C]` rulings conflicting with the live tree fix forward and disclose; `[A]` ones escalate. After landing: design channel verifies by fresh probe — the NOTICE hash re-derived, the round-trip's pinned counter assertions read, the erratum's pointer resolved — then the FIDELITY ARC CLOSE follows (ADR-0084: rulings appends candidates from this arc's own lessons — the trace-first protocol, plausible-by-adjacency-is-not-a-diagnosis; state regeneration; budgets; rotation; the close's intake carrying colorectal's investigation, the truncation-layer absorbed-error finding, and review 2's accumulating watch-list).

## Deviation record

- **The vendoring order ran resource-then-test, not test-then-resource**
  — `anemia___unknown_etiology.json` was copied to disk before
  `vendored_anemia_test.clj` was authored (the reverse of the batch
  precedent's own sequencing). Red was still witnessed honestly, by
  moving the resource aside and re-running the freshly-authored test
  before restoring it — the same red-then-green discipline via a
  different mechanical path. Disclosed in the session record.
