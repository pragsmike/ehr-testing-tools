# 2026-08-07 — Lint family — driving prompt

## Context

Repo `ehr-testing-tools`, ext4 clone (`~/src/ehr-testing-tools`).
Prompt's own stated HEAD: `758f3af` — confirmed live at session start,
no premise mismatch (see this date's own session record).

## Prompt, verbatim

2026-08-07 — lint family: the small gates land together — tripwire, seed, protection, and three lints Session prompt (design channel, 2026-08-07; conventions and every claim below read at HEAD `758f3af` against a fresh public clone). Fourth session of the quality-review arc: FIX SESSION 2 of the ruled mitigation plan (all five rulings recorded verbatim in ADR-0078 AR-RL-5; this session executes the ones assigned to it). Cargo, all small, all mechanical, each with the register row it closes: (a) the `state.md` staleness tripwire (D2-4, RULED ADOPTED); (b) the façade surface-identity gate (D2-3); (c) the Deferred in-place-closure lint (D2-5); (d) the test-reads-live-path lint (D2-6); (e) `-text` protection for the four unprotected hash-recorded fixture files (D3-3); (f) the engine `defspec`'s seed pinned or durably logged (D3-2's ruled middle path — the one spec that has actually flaked, `every-churned-run-satisfies-the-invariant-catalog`); (g) the `rulings.md` citation correction `AR-F1-6a/b` → ADR-0050's real `A-3`/`D-3` labels (D7-1); (h) the `repo-review` SKILL amendment in both mirrors: the next assessment re-derives its predecessor's register summary as a standing probe (the AR-RL-R lesson). R30 ceremony; reds witnessed in-session, commits land green (the ADR-0078 pattern). Read-first: the register rows named above, each in full; `.agents/state.md`'s header (what the tripwire asserts: its cited regeneration ADR vs the newest `*-arc-close.md` on disk); `ehrt.sim.interface` (the frozen façade — `ns-publics` name+arity baseline); `.agents/plans/roadmap.md`'s Deferred section header (the contract the lint enforces) and the compliant-disclosure shape D7-3 blessed (relocation-disclosed sub-items are NOT violations — the lint must not false-positive on them); `components/corpus/test-fixtures/v2-nist/COVID19_ELR-v2.3.1/` and `v2/simhospital/LICENSE` (the four files gaining `-text`; re-verify their hashes still match their NOTICE/PROVENANCE rows BEFORE and AFTER the attribute change — the AR-VB3-R1 mechanism); `components/sim-engine/test/ehrt/sim_engine/engine_test.clj` (the flaked spec); `io_vocabulary_lint_test.clj` (the lint-family pattern to follow, including the allowlist-pinned-by-test move); both `repo-review` SKILL.md copies. Author rulings (record verbatim in ADR-0079; `[A]` author-ruled, `[C]` channel-inferred)

1. AR-LF-0 `[A — tag law, case (ii); debt recorded in ADR-0078]`. Annotated `stable-20260807-result-or-loud` at `758f3af`, message `result or loud landed, design-channel-verified 2026-08-07 (ADR-0078)`; push; verify peeled ref.
2. AR-LF-1 `[A — D2-4 ruled adopted; C for shape]` (the tripwire). A docs-tooling test asserting `state.md`'s own cited regeneration point IS the newest arc-close ADR on disk (parse both; a close landing without regeneration turns it red at that session's own full-suite run). It must be GREEN today (state.md cites ADR-0074, the newest close) — its red is witnessed by TEMPORARY simulation in-session (transcribed), not committed.
3. AR-LF-2 `[C — D2-3]` (the façade gate). A test reading `ehrt.sim.interface`'s public vars (name + arity via `ns-publics`) against a committed baseline literal; the baseline states its own provenance (frozen per AR-M4-3). Green today; simulated red witnessed.
4. AR-LF-3 `[C — D2-5 + D2-6]` (two lints, one family). (i) The Deferred lint: scan `roadmap.md`'s Deferred section for rows whose text closes-in-place ("RESOLVED"/"CLOSED"/"FIXED... see Done") WITHOUT the relocation-disclosure shape D7-3 blessed — the compliant precedent rows must pass (run against the live file first; a false positive on a blessed row means the pattern is wrong, not the row). (ii) The live-path lint: scan test sources for reads of repo paths outside tracked fixture roots (`test-fixtures`, `config/synthea`, resources) — the busy-weekday class; allowlist pinned by its own test, `io_vocabulary_lint_test`'s move.
5. AR-LF-4 `[C — D3-3]` (the protection). `.gitattributes` gains `-text` for the four files (or their parent globs, session's judgment); hashes re-verified against their NOTICE/PROVENANCE rows before and after; if `notice_verbatim_test`'s scope doesn't already cover the v2-nist NOTICE.md and simhospital PROVENANCE.md tables, note the gap as a register-style finding for the close — extend only if it lands small, disclosed.
6. AR-LF-5 `[A — D3-2's ruled middle path]` (the seed). The flaked engine spec gets its seed pinned (or, if pinning materially weakens what it explores, a durable seed log the session justifies — the ruling allows either for a flaked spec; pinning is the default). A comment cites the ruling and the flake (ADR-0076/0077's evidence).
7. AR-LF-6 `[C — D7-1 + the skill amendment]` (the small docs). The `rulings.md` citation corrects to ADR-0050's real labels with a dated note; both `repo-review` SKILL.md copies gain the predecessor-summary re-derivation probe line (mirror gate holds).
8. AR-LF-7 `[C — scope]` (fences). NO changes beyond the eight cargo items; the lints' allowlists are pinned by their own tests; no sweep of anything the lints newly flag beyond what this prompt names (a new hit is a FINDING for the close, not a fix here — run the lints against the live tree BEFORE landing and disclose what they find). Oracle: all TWENTY-SEVEN batches identical. Standing untracked files untouched.

Steps Step 0 — Preflight + tag. Cwd ext4; tip `758f3af` or later-with-disclosure; full suite green baseline; last-five CI disclosed; oracle pre-digest. Execute AR-LF-0. Step 1 — The gates and lints (AR-LF-1/2/3), reds witnessed in-session, landed green in one commit: `test: four small gates — the state can't stale, the facade can't drift, closures can't hide, tests can't wander (lint family, AR-LF-1/2/3)` Step 2 — Protection + seed + small docs (AR-LF-4/5/6). Commit: `fix: four files git can't rewrite, one seed that can't hide, two citations that resolve (lint family, AR-LF-4/5/6)` Step 3 — ADR-0079 + record. Rulings verbatim; the simulated-red transcripts; any new lint findings disclosed for the close; index line; README count by `ls`; Done pointer `- 2026-08-07 — lint-family — ADR-0079`. Oracle bracket (`758f3af` → tip). Successor tag debt IN THE ADR (`stable-20260807-lint-family` at the closing tip). Session record + prompt self-archive; CI watched green per policy. Final commit: `docs: lint family record — eight small debts, one sitting (ADR-0079)` Fences Everything AR-LF-7 names. `[C]` rulings conflicting with the live tree fix forward and disclose; `[A]` ones escalate. After landing: design channel verifies by fresh probe, then the ARC CLOSE follows (ADR-0080: rulings appends — the multi-seed codification ruled at AR-RL-5(5), plausibly "I/O speaks Result or fails loud" as this arc's own standing law; state regeneration; budgets; rotation; the wellness-encounters re-surface in the horizon note per D7-6; the intake watch-list for review 2). The pairing-as-data design pass continues in the design channel.
