2026-08-06 — ux fixes 2: errors that name their artifact — the config crash dies, and the fences actually run
Session prompt (design channel, 2026-08-06). Prior: ux fixes 1 landed and was design-channel-verified (`c998c7b`, ADR-0059) — with one residual the design channel's own probe caught: the swept demo fences kept their OLD cwd-relative `--config` paths, which under `bin/ehrt`'s forced root cwd resolve to nothing (verified: `docs/demos/` does not exist at root; the file lives at `components/sim/docs/demos/module-mix/config.edn`). Register A-2 verified grammar, not path existence — that verification-gap lesson is recorded here. This session fixes the fences (rider), then executes the error-surface cluster: C-1's `--config` crash adopts the Result vocabulary, U4's near-miss folds in, B-6/D-3's hint gains specificity, and the author's B-5 ruling (bare invocation exits 0) lands. FIRST src-behavior session of the arc; every fix is red-first-tested; the oracle bracket proves no emitted byte moved. R30 ceremony. Read-first: register rows C-1/C-2/C-5 (the defect and its two in-tree positive controls), U4, B-5/B-6/D-1/D-3; `components/sim/src/ehrt/sim/run.clj` `merge-config-file` (~194–208); `bases/cli/src/ehrt/cli/core.clj`'s dispatch and `result->exit-code`; the alignment arc's fixes-2 gates (ADR-0051) as test style models — NOTE: that citation means the ALIGNMENT arc's session, `notes/adr/0051`, not this arc's numbering (ADR-0059 disclosed the prior prompt's ambiguous same-name reference; this prompt disambiguates).
Author rulings (record verbatim in ADR-0060)

1. AR-U2-0 (tag, standing ceremony). Annotated `stable-20260806-ux-fixes-1` at `c998c7b`, message `ux fixes 1 landed, design-channel-verified 2026-08-06 (ADR-0059)`; push; verify.
2. AR-U2-R (rider — the fences actually run). Fresh enumeration of EVERY file-path-carrying flag in live doc command fences (`--config`, `--out-dir`, `--lockfile`, any others; multi-line backslash-continued fences included — a line-scoped grep undercounts, as the design channel's own did): each path must resolve from workspace root. Rewrite non-resolving paths root-relative (e.g. `--config components/sim/docs/demos/module-mix/config.edn`). Extend `invocation_lint_test` with a fence-path-resolution assertion: every path argument in a live doc fence exists on disk from root (output-dir flags whose targets are CREATED by the command are exempted by flag name, not by path). Natural red first. ADR-0060 records the A-2 lesson: grammar-validity is not path-validity; fence verification must resolve, not just parse.
3. AR-U2-1 (C-1 — the crash dies). `merge-config-file` adopts the Result pattern its siblings prove (C-2, C-5): missing file → `result/error :config-not-found {:path path}`; unreadable/malformed EDN → `result/error :config-unreadable {:path path :message <parse-message>}`. Propagates through every caller (`run-command`, `identifiers-command`, `generate-sim-command` — enumerate by fresh grep) exactly as `:missing-required-opt` does; exit code 2 via the existing `result->exit-code` mapping (verify the catch-all covers the new categories or add them explicitly — disclosed either way). Red-first: a failing test per category BEFORE the fix (missing path; malformed EDN), each asserting the category, the `:path` in the payload, and the exit-code mapping.
4. AR-U2-2 (U4 — the near-miss, folded). When `:config-not-found` fires and a sibling file with the same stem but different extension exists in the same directory, the payload gains `:did-you-mean <sibling-path>`. Check `ehrt.kernel` for an existing similar-file helper first (register's unconfirmed hint); reuse if real, else a small local fn — disclosed which. Red-first test: the founding incident's exact shape (`busy-weekday.md` present, `.edn` requested).
5. AR-U2-3 (B-6/D-3 — the hint knows the group). When the unknown-command token matches a real group name, the hint becomes `run: ehrt help <group>` instead of the generic `run: ehrt help`. Genuinely-unknown tokens keep the generic hint. Red-first test for both branches.
6. AR-U2-4 (B-5, ruled: exit 0). Bare `bin/ehrt` renders the same help text and exits 0, matching the spec's own `--help`-exits-0 convention (author ruling, 2026-08-06). Locate the `result/error :cli-help` construction the register cites (~core.clj:1476) and convert to the success path `help` itself uses. One factual line lands in the exit-codes table documenting that bare invocation, `help`, and `--help` all exit 0 — DISCLOSED overlap with session 4's voice rewrite: this line is a factual addition now, restyled later with everything else; keep it to plain words with no citations so the rewrite has nothing to relocate. Red-first test asserting the exit code.
7. AR-U2-5 (scope). C-4 (unknown-flag validation) is session 3; the voice rewrite is session 4; the wrap mechanism session 5. Nothing from those lands here, however adjacent the code. The oracle bracket must show all ELEVEN batches identical — error paths and help dispatch touch no emitted byte; any digest change is STOP-AND-ESCALATE.

Steps
Step 0 — Preflight + tag. Cwd ext4; tip `c998c7b` or later-with-disclosure; full suite green baseline; oracle pre-digest. Execute AR-U2-0.
Step 1 — Rider (AR-U2-R). Red on the path gate; enumeration table; rewrite; green. Commit: `docs: the fences actually run — paths resolve from root, gated (ux fixes 2, AR-U2-R)`
Step 2 — The error cluster (AR-U2-1..4). Red-first tests written and witnessed failing (transcripts for the ADR); fixes landed; all green; `poly check` OK; full suite green from workspace root (ADR-0059's cwd lesson). ONE commit: `fix: errors name their artifact — config crash dies, hints know their group, bare help succeeds (ux fixes 2, AR-U2-1/2/3/4)`
Step 3 — ADR-0060 + record. Rulings verbatim; the red transcripts per fix; the rider's enumeration and A-2 lesson; the `result->exit-code` disposition; the B-5 overlap disclosure. Index line; Done pointer `- 2026-08-06 — ux-fixes-2 — ADR-0060`. Oracle bracket (`c998c7b` → tip) per AR-U2-5. Session record + prompt self-archive. Final commit: `docs: ux fixes 2 record — the founding incident can no longer happen (ADR-0060)`
Fences
Src edits ONLY in: `merge-config-file` and its direct callers' error propagation; the dispatch sites for the hint and the bare-invocation exit; plus the one exit-codes-table line. No other help.clj text changes. No flag-parsing changes (C-4 is session 3). No gate weakening; the extended lint gate only adds. Frozen archives untouched (ADR-0060 + index sanctioned). If `result->exit-code` or the dispatch structure differs from the register's reading in a way that makes any fix non-local, STOP-AND-REPORT with the evidence — the register is an input, not a license against contrary structure.
After landing: design channel verifies by fresh probe — including re-running the founding incident's exact command shape against the fixed tree; session 3 (C-4 unknown-flag validation) follows, and this landing's tag rides its Step 0 under standing ceremony.
