# 2026-08-07 — ci current

Repo: `ehr-testing-tools`, WSL ext4 clone (`~/src/ehr-testing-tools`).
HEAD at prompt receipt: `cd6c56c` (vendoring arc close, ADR-0074).

## Prompt, verbatim

2026-08-07 — ci current: the derived docs catch up, the staleness guard comes home, preflight learns to look

Session prompt (design channel, 2026-08-07; conventions and every claim below read at HEAD `cd6c56c` against a fresh public clone). A ruled fix session between arcs, driven by the author's report: CI has been failing "for a while." Diagnosis, probe-backed: `test.yml`'s docsgen step regenerates the derived docs and `git diff --exit-code`s them; the committed `docs/cli.md` is STALE — it lacks the `--width` global-flag row (cli-spec changed at ux-epilogue, ADR-0065) and the `--board` row plus `--ticker`'s precedence sentence (player-board, ADR-0067) — so CI has been red since ADR-0065's landing, roughly twenty-five commits. ROOT CAUSE, in the repo's own words: `docsgen_test.clj`'s docstring says "the real staleness guard is CI" — the freshness check was DESIGNED CI-only (the pipeline/use-cases half genuinely needs python3 + the mermaid converter, outside a JVM test), and the design assumed a watched CI. Nobody watched: not the build sessions, and not the design channel's own verification loop — a named gap in BOTH disciplines, recorded honestly in ADR-0075. The fix is three-fold: the docs catch up; the JVM-derivable half of the staleness guard comes home as a local gate (cli.md and operators.md are pure-Clojure renders — locally checkable; pipeline/use-cases stay CI-only, the division documented where it lives); and Step-0 preflight learns to look at CI. UNKNOWN from the design channel's sandbox (API unreachable): whether `integration.yml` or other `test.yml` steps ALSO fail for unrelated reasons — enumerating that is this session's first probe. R30 ceremony. Read-first: `.github/workflows/test.yml` (the docsgen step, ~lines 36–84) and `integration.yml`; `components/docs-tooling/test/ehrt/docs_tooling/docsgen_test.clj` (the docstring this session amends, the renderer tests as the pattern); `Makefile`'s docsgen target; `ehrt.cli.help/write-cli-md!` and `ehrt.corpus.operators-doc/write-operators-md!` (both pure JVM renders — confirm by read); both `build-session` `SKILL.md` copies (the Step-0 checklist AR-CI-3 amends; the skill-mirror-currency gate keeps them synced). Author rulings (record verbatim in ADR-0075; `[A]` author-ruled, `[C]` channel-inferred)

1. AR-CI-0 `[A — tag law, case (ii); debt recorded in ADR-0074]`. Annotated `stable-20260807-vendoring-close` at `cd6c56c`, message `vendoring arc closed, design-channel-verified 2026-08-07 (ADR-0074)`; push; verify peeled ref.
2. AR-CI-1 `[C — the enumeration first]`. Check BOTH workflows' latest runs on main (gh CLI or the Actions API, from this machine). Expected: `test.yml` red at the docsgen step with exactly the `docs/cli.md` diff the author pasted. Anything ELSE failing — another step, `integration.yml`, a different diff — is STOP-AND-REPORT with the log excerpt before any fix lands: this session's fix scope is the docsgen staleness alone, and an unrelated failure needs its own ruling, not a rider.
3. AR-CI-2 `[C — the guard comes home]`. RED FIRST, before regenerating: `docsgen_test.clj` gains `cli-md-is-current-test` and `operators-md-is-current-test` — each renders from the live spec in-process and compares byte-for-byte against the committed file (`slurp`), no git involved. `cli-md-is-current-test` must fail against the pre-fix tree (the natural red — witness it). The namespace docstring's "the real staleness guard is CI" sentence rewrites to state the new division: cli.md/operators.md guarded locally AND in CI; pipeline/use-cases CI-only (the python dependency, named). THEN `make docsgen` regenerates; commit the regenerated files — expected: `docs/cli.md` alone changes, matching the author's pasted diff exactly; any other file changing is disclosed, and a diff beyond the expected three-line shape is STOP-AND-REPORT. Both new gates green after.
4. AR-CI-3 `[C — preflight learns to look]`. Both `build-session` `SKILL.md` copies gain one Step-0 checklist line: check the latest CI conclusion for main (one command, e.g. `gh run list --limit 2` or the equivalent API call) and DISCLOSE it in the session record; a red CI at preflight is a finding to report before proceeding, never silently passed. The design channel's own contract note in `.agents/state.md` is NOT edited here (state regenerates at arc close; ADR-0075's record carries the lesson until then — the same deferral AR-EP-5 used).
5. AR-CI-4 `[C — the proof uses the mechanism]`. The claim "CI is fixed" is proven by CI: after the final push, watch the Actions run for this session's own closing commit to conclusion; record the run URL and conclusion in the session record. A still-red run is STOP-AND-REPORT, not a note.
6. AR-CI-5 `[C — scope]`. NO cli-spec changes, NO workflow-file changes (CI did its job; the gap was watching it), NO Makefile changes, NO other doc edits beyond `make docsgen`'s own outputs. The oracle bracket must show all TWENTY-SEVEN batches identical. Standing untracked files untouched.

Steps Step 0 — Preflight + tag + enumeration. Cwd ext4; tip `cd6c56c` or later-with-disclosure; full suite green baseline; oracle pre-digest; AR-CI-1's CI-status enumeration recorded. Execute AR-CI-0. Step 1 — The guard, red (AR-CI-2 first half). Both new tests written; `cli-md-is-current-test` witnessed RED against the stale committed file. Commit: `test: the staleness guard comes home — cli.md's drift witnessed locally for the first time (ci current, AR-CI-2 red)` Step 2 — The catch-up, green (AR-CI-2 second half). `make docsgen`; the regenerated `docs/cli.md` lands; both gates green; full suite green; `poly check` OK. Commit: `fix: the derived docs catch up — width and board reach the manual, gated locally now (ci current, AR-CI-2)` Step 3 — Preflight amendment (AR-CI-3). Both skill copies, one line each, mirror gate green. Commit: `docs: preflight learns to look — CI status joins Step 0 (ci current, AR-CI-3)` Step 4 — ADR-0075 + record (AR-CI-4's watched run included). Root-cause narrative with the twenty-five-commit red span and BOTH disciplines' watching gap named; index line; README count by `ls`; Done pointer `- 2026-08-07 — ci-current — ADR-0075`. Oracle bracket (`cd6c56c` → tip). Successor tag debt IN THE ADR (`stable-20260807-ci-current` at the closing tip). Session record (CI run URL + conclusion) + prompt self-archive. Final commit: `docs: ci current record — red for twenty-five commits, watched again, guarded twice (ADR-0075)` Fences Everything AR-CI-5 names. An `integration.yml` or non-docsgen failure found at AR-CI-1 is reported, not fixed. `[C]` rulings conflicting with the live tree fix forward and disclose; `[A]` ones escalate. After landing: design channel verifies by fresh probe — the regenerated cli.md against the spec by the gate's own mechanism, the skill amendment in both copies — and the author sees CI green on main for the first time since ADR-0065. The horizon ruling (EncounterEnd design pass, Wave E, batch 4, publish-prep, pairing-as-data) remains open and unaffected.
