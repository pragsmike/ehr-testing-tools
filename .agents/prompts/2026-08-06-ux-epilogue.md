2026-08-06 — ux epilogue: muscle memory gets an answer, help gets a width
Session prompt (design channel, 2026-08-06). Prior: the UX arc closed and was design-channel-verified (`2e77096`, ADR-0064). This session exists because the founding incident's own command shape resurfaced live TODAY, from the author's own shell: `clojure -M:cli run --seed 42 ... --config ./config/busy-weekday.edn ...` → `FileNotFoundException ... run (No such file or directory)`. Diagnosis, probe-backed by the design channel against the fresh public clone: the file not found is `run`, NOT the config — no `:cli` alias exists in the root `deps.edn` (fresh grep, zero hits; `git log -S':cli' -- deps.edn` shows it never existed in the monorepo root), so the Clojure CLI drops the undeclared alias and `clojure.main` treats `run` as an init-script path. The alias DID exist pre-monorepo (`git show 906a954:.staging/deps.edn`: `:cli {:main-opts ["-m" "ehr-testing-sim.cli"]}`, its comment literally teaching the form) and died silently at consolidation (2026-07-28). ADR-0059 swept the form from every doc and gated the docs surface; the RUNTIME surface was never tombstoned — founding failure #1 at a layer the arc didn't cover, while failure #2's fix (ADR-0060) never got the chance to fire. The `[unverified-by-live-run]` link in that chain is clojure.main's script-load behavior itself (the design channel's sandbox cannot resolve Maven Central; the author's paste is the outcome evidence) — Step 2's own live probe closes it. This session lands the tombstone under standing law (errors name their artifact; unknown input rejected by name; two voices, two homes), executes the author's `--width`/COLUMNS ruling, probes the sibling muscle-memory path through the NEW entry point, and discharges three residuals from the arc-close verification. R30 ceremony. Read-first: `bin/ehrt`'s header (the cwd contract — WHY tombstone, not synonym); root `deps.edn` `:ehrt` alias (~149); `git show 906a954:.staging/deps.edn` (the historical alias, provenance for the tombstone's source comment); `bases/cli/src/ehrt/cli/help.clj` (`cli-spec`, `default-wrap-width` ~232, the width-arity render fns ~266–288); `bases/cli/src/ehrt/cli/core.clj` `unknown-command-error` (~1480) and `unknown-flag-error` (~1753); `bases/cli/test/ehrt/cli/help_wrap_test.clj` and `help_voice_test.clj` as test-style models; `.agents/rulings.md` "From the UX arc" (the three standing laws this session executes under); `.agents/plans/roadmap.md` Deferred rows "Procedure-duration gap" and "compile-trajectory/engine/emit full-pipeline gap" (both FIXED-marked, AR-EP-4's subjects). Author rulings (record verbatim in ADR-0065)

1. AR-EP-0 (tag, standing ceremony). Annotated `stable-20260806-ux-close` at `2e77096`, message `ux arc closed, design-channel-verified 2026-08-06 (ADR-0064)`; push; verify peeled ref. This discharges the mechanical debt the archived close prompt recorded — note in ADR-0065 that the debt was recorded in the archived prompt ONLY, not in ADR-0064 itself (AR-EP-5's second correction).
2. AR-EP-1 (the tombstone — ruled: reject by name, exit 2; explicitly NOT a synonym for `:ehrt`, which would resurrect the cwd-relative-path hazard `bin/ehrt`'s own header exists to prevent). Root `deps.edn` gains `:cli {:extra-deps {org.clojure/clojure {:mvn/version "1.12.5"}} :extra-paths ["bases/cli/src"] :main-opts ["-m" "ehrt.cli.retired"]}`. New namespace `bases/cli/src/ehrt/cli/retired.clj`: requires nothing beyond clojure.core; a pure `retired-message` fn plus a `-main` that prints it to stderr and `System/exit 2` (the operational-error contract). The message speaks operator language, names what was retired and what replaces it, and shows one worked example mirroring the founding shape (`bin/ehrt sim run --seed 42 --patients 200 --config config/busy-weekday.edn`), ending `run bin/ehrt help for commands`. NO citations, milestones, or internal names in the message — the consolidation history and this ruling's provenance live in the ns's source comment (two voices, two homes). Red-first, two gates: (a) `bases/cli/test/ehrt/cli/retired_test.clj` — `retired-message` passes the help-voice token patterns (reuse `help_voice_test`'s patterns; enumerate by fresh read), names `bin/ehrt`, contains no `clojure -M:cli`-teaching framing that `invocation_lint_test` would reject if this were a doc; (b) a docs-tooling gate (`cli_tombstone_test.clj`) reading root `deps.edn` as data and asserting the `:cli` alias exists with exactly these `:main-opts` — the tombstone must not be silently removable. LIVE PROBE after green, recorded in ADR-0065: the author's exact pasted command shape from workspace root → redirect message on stderr, exit 2. Check whether `invocation_lint_test`'s doc sweep would flag the worked example inside the retired ns source or its test; if the gate's file scope covers them, use the gate's own established exemption mechanism (fresh read; disclosed) — never weaken the gate.
3. AR-EP-2 (the sibling muscle-memory path, probe-then-fix). Probe `bin/ehrt run --seed 1 --patients 1` (the OLD top-level verb through the NEW entry point). Expected under ADR-0060 AR-U2-3: an `:unknown-command` error whose hint helps. If the hint does NOT point toward `sim run` (the near-miss crosses a group boundary — `run` is a verb name, not a group name), extend `unknown-command-error`: when the unknown token matches a VERB name in exactly one group, the payload gains `:did-you-mean "<group> <verb>"` and the hint names that group (`run: ehrt help sim`). Ambiguous (verb in multiple groups) or no-match tokens keep current behavior. Red-first if fixing; if the probe already passes, record the transcript in ADR-0065 and land nothing.
4. AR-EP-3 (`--width`/COLUMNS — author ruled the affordance IN, 2026-08-06, retiring ADR-0064's intake row; the shape below is the design channel's recommendation — STOP-AND-REPORT if the structure fights it). Scope: help rendering only (the ADR-0063 wrap mechanism); error payloads are data, untouched. Resolution order: explicit `--width N` > `COLUMNS` env var > `default-wrap-width` (80). Validation asymmetry, deliberate and disclosed: `--width` is user input — a value that is not an integer ≥ 40 is rejected by name (Result error naming the flag, the given value, and what was expected; exit 2; red-first test); `COLUMNS` is ambient — any value not parsing to an integer ≥ 40 falls back silently to 80 (a broken terminal variable must not break help). `--width` joins `cli-spec` wherever help's own flags live (fresh read; if no global-flag plumbing exists, scope to the help paths — bare, `help`, `help <group>`, `--help` — disclosed), with an operator-voice description (the `help_voice` gate walks `cli-spec`; it must pass unmodified). Extend `help_wrap_test`: every-line-fits and byte-content-preservation invariants hold at 40, 60, 120; resolution-order cases (flag beats env beats default); the rejection case. Red-first throughout.
5. AR-EP-4 (rider — AR-A-5 relocation, ruled). The two FIXED-marked Deferred rows (Procedure-duration / ADR-0032; closure full-pipeline / ADR-0033) relocate from the live roadmap's Deferred section into `.agents/plans/roadmap-done-2026-08.md`, notes intact (relocation, not rewrite), each with a dated note: closed 2026-08-03, relocated 2026-08-06 per AR-A-5 (this session; the rows' "see Done, below" pointers had dangled since the Done rotation). Same sanctioned-append class as ADR-0064's AR-UC-5. The `myocardial_infarction.json` known violation is NOT this rider's scope — it stays disclosed, untouched. Deferred row count after: 11, all live.
6. AR-EP-5 (corrections, recorded — the arc-close verification's two transcript-witnessed findings become repo-recorded here, per the standing transcript-witnessed rule). ADR-0065 records: (1) `.agents/state.md`'s `emit_hl7.clj` claim ("the `:require` form is still exactly [the two ehrt forms]") is imprecise — the form holds four requires; the two named are the only `ehrt.*` ones; the gated invariant HELD; the wording overreached ("audit evidence uses the mechanism it recommends" applies to claim phrasing too); state.md itself is NOT edited (it regenerates at arc close — this record is the correction's home until then). (2) The `stable-20260806-ux-close` debt was repo-recorded in the archived close prompt only, not ADR-0064 — discharged by AR-EP-0, citation-precision miss recorded.
7. AR-EP-6 (scope + oracle). `config/busy-weekday.md` stays untracked and untouched (standing disposition since ADR-0060). Nothing from the feature horizon (corpus-player, pairing-as-data, module vendoring) lands here. The oracle bracket must show all ELEVEN batches identical — a tombstone alias, help-width plumbing, and a hint touch no emitted byte; any digest change is STOP-AND-ESCALATE.

Steps Step 0 — Preflight + tag. Cwd ext4; tip `2e77096` or later-with-disclosure; working tree clean apart from the standing untracked `config/busy-weekday.md`; full suite green baseline; oracle pre-digest. Execute AR-EP-0. Step 1 — Rider (AR-EP-4). Relocate both rows; fresh count of remaining Deferred rows recorded. Commit: `docs: closed rows come home — two FIXED deferred rows join the attic (ux epilogue, AR-EP-4)` Step 2 — The tombstone (AR-EP-1). Red on both gates witnessed; ns + alias landed; green; the live probe transcript captured for ADR-0065. Commit: `fix: the retired invocation answers in words — clojure -M:cli redirects to bin/ehrt, gated (ux epilogue, AR-EP-1)` Step 3 — Width + the sibling path (AR-EP-2, AR-EP-3). Probe transcript first; red-first tests witnessed; fixes landed; all green; `poly check` OK; full suite from workspace root. ONE commit if both land small, split with disclosure if AR-EP-2's fix grows: `feat: help fits your terminal, old verbs get real hints (ux epilogue, AR-EP-2/3)` Step 4 — ADR-0065 + record. Rulings verbatim; the diagnosis chain with its `[unverified-by-live-run]` link closed by Step 2's probe; red transcripts per fix; AR-EP-5's two corrections; index line; Done pointer `- 2026-08-06 — ux-epilogue — ADR-0065`. Oracle bracket (`2e77096` → tip) per AR-EP-6. Session record + prompt self-archive. Final commit: `docs: ux epilogue record — muscle memory gets an answer, help gets a width (ADR-0065)` Fences Src edits ONLY in: the new `retired.clj`; root `deps.edn`'s new `:cli` alias; `help.clj`'s width resolution and `cli-spec`'s `--width` entry; `core.clj`'s `unknown-command-error` (only if AR-EP-2's probe fails) and whatever threads the resolved width into the existing width-arity render fns. No wrap-algorithm changes (the mechanism is ADR-0063's, done); no error-payload reformatting; no gate weakening anywhere — extended gates only add. Frozen archives untouched except ADR-0065 + index + the AR-EP-4 attic append. If the width plumbing or the near-miss extension turns non-local, STOP-AND-REPORT with the evidence — this prompt is an input, not a license against contrary structure. After landing: design channel verifies by fresh probe — including the author's exact pasted command against the fixed tree, `bin/ehrt run` behavior, and `bin/ehrt help` at three widths — and this landing's tag rides the next session's Step 0 under standing ceremony. The feature horizon's ruling comes next; nothing here forecloses it.

## Deviation record

- **AR-EP-2's fix was needed** (the probe did NOT already pass): `bin/ehrt run --seed 1 --patients 1` returned the generic `"run: ehrt help"` hint, not one naming `sim`. `unknown-command-error` extended with a new `verb-name-groups` helper, red-first, confirmed failing before the fix.
- **AR-EP-1's citation-lint check came back negative, not positive**: `invocation_lint_test`'s own scan surface does not reach `bases/cli/src` or `bases/cli/test` at all, so no exemption mechanism was needed for `retired.clj`'s own source comment naming `clojure -M:cli` for provenance.
- **AR-EP-3's `--width` joined `global-flags`**, not a scoped-to-help-paths list — fresh read found no separate "help flags" plumbing in `cli-spec`; global-flags was the established join point, with the scope difference disclosed in the flag's own `:doc` string rather than structural surgery. Not escalated as STOP-AND-REPORT: the structure did not fight this placement.
- **Extending `help_wrap_test` to 40/60/120 caught a live, pre-existing bug** outside any named AR: `render-top-level`'s own "Run `ehrt help <group>`..." line was a bare literal, never wrapped, invisible at 80 columns. Fixed within AR-EP-3's own fence (apply the existing wrap mechanism to one more line; no algorithm change).
- **Steps 2 and 3 landed as three separate commits, not the "ONE commit if both land small" default** — AR-EP-1 (the tombstone) and AR-EP-2/AR-EP-3 (the sibling path + width) were already separated by the Steps section itself (Step 2 vs Step 3); within Step 3, AR-EP-2 and AR-EP-3 landed together as ruled ("ONE commit if both land small").
- **Red-first was not strictly observed for `ehrt.cli.help`'s own two new pure functions** (`resolve-width`, `parse-width-flag`) — see the session record's own disclosure.
