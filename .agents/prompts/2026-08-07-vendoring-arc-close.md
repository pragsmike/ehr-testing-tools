## Appendix A — the original close prompt, verbatim

2026-08-07 — vendoring arc close: the mix more than tripled, the bytes cannot lie, the front door is open

Session prompt (design channel, 2026-08-07; conventions and every claim below read at HEAD `5e2afaf` against a fresh public clone). This session CLOSES the vendoring arc (ADR-0069–0074) per the standing close pattern (ADR-0068 is the model): rulings appends, the dependency-review cadence, `state.md` regeneration, budget re-derivation, Done rotation, and the closing ADR. Docs-only; anything new found is next-arc intake, never an act. The arc being closed, five sessions: census substance (ADR-0069, `cd16fa9` — the qualifier, the ranked catalog); batch 1 (ADR-0070, `d41a278` — five landed, injuries deferred on the broken_jaw loop); batch 2 (ADR-0071, `96424f8` — seven landed, anemia deferred on the EncounterEnd idiom, the scenarios home born); batch 3 (ADR-0072, `721adb6` — the verbatim-law rider with its gate, four landed, colorectal deferred on the SAME EncounterEnd gap); demos front door (ADR-0073, `5e2afaf` — the operator surface at the root, byte-witnessed, "See it run" in the README). Sixteen modules vendored, twenty-three ailments in-tree, oracle roots 11→27, and one law now mechanically enforced that was only prose before. R30 ceremony. Read-first: `notes/adr/0068-player-arc-close.md` (the pattern, regeneration-table format); `.agents/rulings.md` in full; `.agents/state.md` in full (regenerated at the player close, now FIVE sessions stale — wrong in at least: the module inventory, the gate inventory, the oracle root count, the demo/scenario paths its own text cites, and the `.gitattributes` posture); `.agents/reading-sets.edn`; ADR-0069 through 0073 in full (their findings feed the intake list); `.agents/plans/roadmap.md` (Done holds six pointers to rotate; the Deferred rows this arc touched). Author rulings (record verbatim in ADR-0074; `[A]` author-ruled, `[C]` channel-inferred — conflict rules differ per the session-prompt skill's convention)

1. AR-VAC-0 `[A — tag law ADR-0057 AR-T-1 case (ii); debt recorded in ADR-0073]`. Annotated `stable-20260807-demos-front-door` at `5e2afaf`, message `demos front door landed, design-channel-verified 2026-08-07 (ADR-0073)`; push; verify peeled ref.
2. AR-VAC-1 `[C — both appends are channel consolidations of this arc's executed discipline; the author may strike either]` (rulings appends). Under "From the vendoring arc (ADR-0069–0074)" in `.agents/rulings.md`: (a) vendored bytes are law, standing — upstream content vendors byte-verbatim at its named pin, `-text`-protected from git normalization, NOTICE-hashed per file, and gate-verified on every test run (`notice_verbatim_test`); an edit-tempting vendored file is STOP-AND-REPORT, never a fix (ADR-0070 mechanics; AR-VB3-R1's teeth; the uti_recurrence lesson — a hash and the bytes it names are only honest together). (b) the population-scale gate outranks the census sample, standing — a module joins the tree only with a witnessed content-producing engine-layer round trip at population scale; zero-substance modules are not vendorable; a census verdict is evidence for curation, never a vendoring license (the injuries, anemia, and colorectal precedents, ADR-0070–0072).
3. AR-VAC-2 `[A — standing cadence, rulings register A-3]`. `clojure -M:poly libs :outdated`; dated report in ADR-0074; no edit follows; urgencies are intake notes.
4. AR-VAC-3 `[A — state.md's regeneration contract, AR-C-1]` (state regenerates). Every `[V]` claim probe-backed THIS session; skeleton preserved; content updates at minimum: the vendored-module inventory (top-level roots, closure dirs, lookup tables — fresh `ls` counts, with the substance-census provenance one sentence); the gate inventory (`notice_verbatim_test` in docs-tooling; the `vendored_*` round-trip family's fresh count; the census tool's substance fields); the oracle posture (27 roots by fresh count, the first-baseline mechanics one sentence); the demos/scenarios geography (top-level `demos/` with `traces/`/`scenarios/`, the three pointer stubs, the `.gitattributes` `-text` protections at BOTH protected trees — every stale component-local demo path in state.md's own text sweeps); the deferred-modules picture (injuries; anemia + colorectal on the SHARED EncounterEnd gap — name it as a two-module blocker; the three zero-substance family members; the vital-sign row's corrected evidence); the suite/tag/ADR/Deferred/Next counts all by fresh count. Regeneration table (claim → old → new → probe) in ADR-0074; wrong-in-kind is STOP-AND-REPORT.
5. AR-VAC-4 `[A — standing budget rule]` (budgets). Re-derive every reading set whose member paths changed across `b7ed686..HEAD` (`git log --name-only`; `README.md` and `state.md` both changed, so `:onboarding` at minimum; the demos move may touch `:docs` — enumerate, don't assume), AFTER the regeneration lands.
6. AR-VAC-5 `[C — rotation mechanics per the three prior closes]` (rotation). Done holds six pointers (0068–0073, design-channel probed at `5e2afaf`). Rotate: ADR-0068's pointer joins the attic's player-arc section with a dated leftover note (the same class every close has handled for its predecessor); ADR-0069–0073 rotate under a new `## Vendoring arc — closed 2026-08-07 (ADR-0069–0074)` header. ADR-0074's pointer lands as the sole current entry (Step 3, after the index line — the sentinel-avoidance every close exercises). The Now section refreshes to one line. Relocation-not-rewrite throughout.
7. AR-VAC-6 `[A for cited/ratified items; C for composition]` (the closing ADR). `notes/adr/0074-vendoring-arc-close.md`: rulings verbatim; the arc summary one line per session with ADRs and tips; the arc narrative — the catalog ranked honestly, sixteen modules landed under a gate that refused three, the verbatim law given teeth by the very defect it then caught, and the whole surface moved to the operator's front door; the intake list, each item cited: the `anemia_sub` EncounterEnd gap (a TWO-module blocker now — anemia ADR-0071, colorectal ADR-0072 — the strongest ruled-fix candidate for the next arc, and the first candidate interpreter change since the coverage waves: a design pass precedes any fix), the census refinements (population-scale walk check and data-file closure counting, ADR-0070/0071 intake; standing item (b)), wellness-encounters (the named design item, ADR-0070), batch 4's remainder (the veteran family: `veteran` + hyperlipidemia + substance-abuse-treatment, concurred-plan composition), the zero-substance families and the 51-module zero list awaiting Wave E's register (with CHF/contraceptives' partial-substance evidence), the sim event-log input adapter (player future, ADR-0068 intake), and the pairing-as-data design pass (design channel, landing spot `judge`, ADR-0050 D-3 — carried through three closes now, still undone, say so plainly); the open Externals restated unchanged (NIST licensing gist, IG pinning, Clojars F-5/F-6, SETUP rewalk); this close's own successor tag debt recorded IN THE ADR (`stable-20260807-vendoring-close` at this session's closing tip, next session's Step 0); and the horizon note: "The horizon, for the author's ruling: the EncounterEnd interpreter design pass (unblocks two vendored-ready modules), Wave E's risk-attribute/vital-sign register (unblocks the attribute-blocked cluster), vendoring batch 4 (the veteran family), the pairing-as-data design pass, publish-prep (F-5/F-6 + F-7). `sim-emit-cda` on its trigger."

Steps Step 0 — Preflight + tag. Cwd ext4; tip `5e2afaf` or later-with-disclosure; full suite green baseline (fresh-clone green is the standing expectation — any failure is STOP-AND-REPORT); oracle pre-digest (27). Execute AR-VAC-0. Step 1 — Appends + cadence (AR-VAC-1/2). Commit: `docs: the vendoring arc's law is appended — bytes are law, the gate outranks the sample (arc close, AR-VAC-1/2)` Step 2 — State + budgets + rotation (AR-VAC-3/4/5). Full suite green through the rotation. Commit: `docs: the state regenerates, the budgets re-derive, four arcs rest in the attic (arc close, AR-VAC-3/4/5)` Step 3 — ADR-0074 + record. Everything per AR-VAC-6; index line; README count by `ls`; sole Done pointer. Oracle bracket (`5e2afaf` → tip): all TWENTY-SEVEN batches identical — docs-only; any change is STOP-AND-ESCALATE. Session record (disclosing the two-session close) + prompt self-archive (Appendix A + this preamble, one file). Final commit: `docs: the vendoring arc closes — twenty-three ailments, honest bytes, an open front door (ADR-0074)` Fences Docs-only: no `src/`, no `test/`, no config, no `.gitattributes`, no gates. The `libs :outdated` output is a report, full stop. The horizon note restates ruled directions and NAMES unruled intake; it decides nothing. Frozen archives untouched (ADR-0074 + index + the sanctioned attic appends). A state-regeneration discrepancy wrong in kind is STOP-AND-REPORT. `[C]` rulings conflicting with the live tree fix forward and disclose; `[A]` ones escalate. After landing: design channel verifies by fresh probe — sampled re-probes of the regenerated state.md per its contract — then the next arc's opening session tags `stable-20260807-vendoring-close` at this tip under standing ceremony, and the horizon is the author's to rule.

## Resumption preamble (Code session 2, verbatim)

2026-08-07 — vendoring arc close, RESUMPTION: the same close, second Code session — Steps 2–3 only

Session prompt (design channel, 2026-08-07). The vendoring arc close (ADR-0074) began in a prior Claude Code session that was KILLED MID-CEREMONY by an infrastructure-level request block (an API refusal unrelated to the work's content) immediately after Step 1 landed. The design channel has verified the seam by fresh public-clone probe: tip `beec395` (Step 1's own commit — the rulings appends and cadence commit, pushed and verified by the dead session before it died); tag `stable-20260807-demos-front-door` present and peeled-verified at `5e2afaf` (AR-VAC-0 EXECUTED — verify it exists, disclose, NEVER re-create, per tag law's already-present clause); `.agents/rulings.md` carries the "From the vendoring arc (ADR-0069–0074)" section (AR-VAC-1 EXECUTED); `.agents/state.md` still the player-close regeneration (AR-VAC-3 NOT started); roadmap Done still holds six pointers (AR-VAC-5 NOT started); no ADR-0074 (AR-VAC-6 NOT started). One casualty: the dead session's `libs :outdated` output was transcript-only and is lost — RE-RUN it fresh this session (AR-VAC-2 re-executed; the report landing in ADR-0074 must be a real run's output regardless). This session completes Steps 2–3 of the original prompt, whose remaining rulings are restated verbatim below and whose FULL original text is Appendix A — the session's prompt self-archive is Appendix A followed by this resumption preamble, one file, so the pairing gate sees the whole provenance. ADR-0074's own deviation record and the session record BOTH disclose the two-session close plainly: an infrastructure block, no work lost, the seam verified by the design channel before resumption. R30 ceremony. Read-first: unchanged from Appendix A's own list, plus `git log -3` (the seam), `.agents/rulings.md`'s new section (what Step 1 landed), and any working-tree residue the dead session left (disclose and clear scratch; STOP-AND-REPORT anything that looks like uncommitted repo work rather than scratch).

Rulings in force (verbatim from the original; provenance tags unchanged)

* AR-VAC-0 `[A]` — EXECUTED by the dead session. Verify the tag exists at `5e2afaf`, peeled; disclose; do not re-create.
* AR-VAC-1 `[C]` — EXECUTED. Verify the two appends read as the original specifies; a discrepancy is STOP-AND-REPORT, not a silent re-edit.
* AR-VAC-2 `[A]` — RE-RUN this session: `clojure -M:poly libs :outdated`; dated report in ADR-0074; no edit follows.
* AR-VAC-3 `[A]` — as the original states, in full (Appendix A ruling 4): state.md regenerates, every `[V]` claim probe-backed THIS session, regeneration table in ADR-0074, wrong-in-kind is STOP-AND-REPORT.
* AR-VAC-4 `[A]` — as the original states (Appendix A ruling 5): budgets re-derive across `b7ed686..HEAD` after the regeneration lands.
* AR-VAC-5 `[C]` — as the original states (Appendix A ruling 6): the six-pointer rotation, the sentinel-avoidance, relocation-not-rewrite.
* AR-VAC-6 `[A/C]` — as the original states (Appendix A ruling 7): ADR-0074 in full — arc summary, narrative, cited intake list, Externals, the successor tag debt IN THE ADR (`stable-20260807-vendoring-close` at this session's closing tip), and the horizon note verbatim — PLUS the two-session deviation record above.

Steps

Step 2 — State + budgets + rotation (AR-VAC-3/4/5). Full suite green through the rotation. Commit: `docs: the state regenerates, the budgets re-derive, four arcs rest in the attic (arc close, AR-VAC-3/4/5)`

Step 3 — ADR-0074 + record (AR-VAC-2's fresh report; AR-VAC-6). Index line; README count by `ls`; sole Done pointer. Oracle bracket (`5e2afaf` → tip, spanning BOTH sessions' commits): all TWENTY-SEVEN batches identical — docs-only; any change is STOP-AND-ESCALATE. Session record (disclosing the two-session close) + prompt self-archive (Appendix A + this preamble, one file). Final commit: `docs: the vendoring arc closes — twenty-three ailments, honest bytes, an open front door (ADR-0074)`

Fences

Unchanged from Appendix A, in full. Additionally: Step 0 and Step 1's landed work is READ-ONLY this session — verified, never redone. After landing: design channel verifies by fresh probe — sampled re-probes of the regenerated state.md per its contract — then the next arc's opening session tags `stable-20260807-vendoring-close` at this tip under standing ceremony, and the horizon is the author's to rule.
