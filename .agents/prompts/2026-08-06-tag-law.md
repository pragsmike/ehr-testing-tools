2026-08-06 — tag law: sessions tag, every surface says so, and the retired phrase can never return
Session prompt (design channel, 2026-08-06). Prior: ux riders landed and was design-channel-verified (`56a2c21`, ADR-0056). That session deferred its licensed tags citing the build-session skill's rule #8 — and the probe that followed found the real cause: the tag law lives on FOUR live surfaces (AGENTS.md, AUTHORS-GUIDE.md, the build-session skill + its `.claude/skills/` mirror, `.agents/state.md`), and ADR-0051's reconciliation amended exactly one. Sessions kept obeying the loudest unamended surface — correct behavior under contradictory law. The author now rules the contradiction gone in the session-executes direction. This session sweeps every surface, lands a gate against the retired phrasing, and executes the three pending tags under the fixed law. R30 ceremony. Read-first: `.agents/skills/build-session/SKILL.md` rule #8 verbatim and its ADR-0003 citation; `notes/adr/0003-*.md` (the original trust-boundary reasoning — this session supersedes its scope with a recorded why, not a silent overwrite); AGENTS.md's tag rule as amended by ADR-0051; `.agents/rulings.md`'s stable-tagging entries; ADR-0056's pending-tag commands.
Author rulings (record verbatim in ADR-0057)

1. AR-T-1 (the law, restated once, canonically). `stable-*` continuity tags are SESSION ACTS: a session creates and pushes them (i) when its prompt licenses a specific tag at a specific commit, and (ii) for its own predecessor's design-channel-verified stable point as standing ceremony, without bouncing to the author. Deferral of a licensed tag is now the DEVIATION and requires a disclosed reason. The author may always tag directly; a pre-existing tag at the exact commit/message is verified-and-disclosed, never re-created. Release/version tags (`v*`) remain AUTHOR ACTION — publication itself is author-gated, so its tags are too. [Author: this carve-out encodes "stable tags flow, releases stay yours"; strike it in your paste if you want literally all tags session-executable.] ADR-0003's trust-boundary reasoning is superseded in scope, not erased: the boundary made sense before the verification-gated license mechanic existed (ADR-0049 AR-AU-0, ADR-0051 AR-F2-0); the design channel's landing verification is now the trust boundary, and the tag is its mechanical consequence. ADR-0057 records this supersession with both citations.
2. AR-T-2 (every surface, one session — the propagation lesson practiced, third instance recorded). The canonical law from AR-T-1 lands on ALL surfaces that state tag law, enumerated by fresh grep (at minimum: `AGENTS.md`'s tag rule, strengthened so execution is the default and deferral the deviation; `AUTHORS-GUIDE.md` wherever it states tag/AUTHOR-ACTION law; `.agents/skills/build-session/SKILL.md` rule #8 rewritten — stable tags move OUT of the author-only list, `gh` repo mutations/git surgery/external documents stay IN; the `.claude/skills/build-session/` mirror updated identically, byte-compared after; `.agents/state.md`'s tag-mechanic claim updated with this session's probe). `.agents/rulings.md` gains a dated amendment entry superseding the prior stable-tagging wording, citing ADR-0057 — appended per fix-forward discipline, not edited in place. The sweep's surface table (file → old phrasing → new) goes in ADR-0057.
3. AR-T-3 (the gate — third instance earns mechanism). New deftest in the `docs-tooling` gate family: the retired formulations (`"stay author-only in every mode"` and `"AUTHOR ACTION in every ceremony mode"`) may not appear in live law surfaces in connection with tags — encode as: those phrases are forbidden outright on the enumerated live surfaces (AGENTS.md, AUTHORS-GUIDE.md, both skill copies, state.md, rulings.md), since after this session nothing true is stated by them anywhere (non-tag AUTHOR-ACTION items get rephrased by AR-T-2's rewrite without the retired formulations). Frozen archives, ADRs, session records, and dated one-shots are OUT of scope — they quote history. Red→green witnessed: run the gate against the unswept tree first (rule #8 must trip it), then sweep + gate land in ONE commit, green.
4. AR-T-4 (the three tags, under the fixed law). After AR-T-2's commit is pushed: (a) `stable-20260805-alignment-fixes-5` at `2b3bb2b`, message per ADR-0055/0056's prepared command; (b) `stable-20260805-alignment-close` at `12d3aa3`, message per ADR-0056; (c) `stable-20260806-ux-riders` at `56a2c21`, message `ux riders landed, design-channel-verified 2026-08-06 (ADR-0056)`. All annotated, pushed, verified on origin with peeled refs. Pre-existing-at-exact-commit → verify-and-disclose.

Steps
Step 0 — Preflight. Cwd ext4; tip `56a2c21` or later-with-disclosure; full suite green baseline; oracle pre-digest; fresh-grep enumeration of every tag-law surface (the AR-T-2 input; if it finds a FIFTH surface this prompt missed, include it and disclose).
Step 1 — Red, then sweep + gate (AR-T-2/3). Gate written; red captured against the unswept tree; the full-surface sweep; mirror byte-compare; rulings amendment appended. ONE commit, gate green, suite green: `docs: the tag law reads the same everywhere, now gated — sessions tag, deferral is the deviation (tag law, AR-T-1/2/3)`
Step 2 — The tags (AR-T-4). All three, verified. No commit (tags are refs); the verification transcript goes in ADR-0057.
Step 3 — ADR-0057 + record. ADR-0057: rulings verbatim (including the author's carve-out decision as pasted); the ADR-0003 supersession narrative; the surface table; the red transcript; the three tag verifications; the third-instance note joining the propagation lesson's register entry (a dated amendment to that entry citing this ADR). Index line; Done pointer `- 2026-08-06 — tag-law — ADR-0057`. Oracle bracket (`56a2c21` → tip): all ELEVEN batches identical — docs/tests only; any change is STOP-AND-ESCALATE. Session record + prompt self-archive. Final commit: `docs: tag law record — the boundary moves to verification, where it always was (ADR-0057)`
Fences
No `src/` changes. The skill rewrite touches rule #8 and any other tag-stating line ONLY — the rest of the ceremony (staging hygiene, message-via-file, gitleaks, push verification, premise-mismatch stops) is untouched and this fence says so explicitly. `gh` repo-level mutations, git surgery, and external-document placement REMAIN author-only — this session narrows nothing else. Frozen archives untouched (ADR-0057 + index + the rulings append sanctioned). The UX audit is NOT this session — help.clj and error strings stay exactly as they are, however close the sweep passes to them.
After landing: design channel verifies by fresh probe — including that a grep for the retired phrasings on live surfaces comes back empty — then this landing's own tag is the FIRST executed under standing ceremony rule (i): the next session tags `stable-20260806-tag-law` at this tip in its Step 0 without further license. The UX audit session follows.
