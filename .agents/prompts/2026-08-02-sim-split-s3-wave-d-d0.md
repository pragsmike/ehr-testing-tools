2026-08-02 — ehr-testing: Wave D stage D0 — sim split S3, extract `sim-emit-hl7`

Context
First session of GMF coverage Wave D, whose design was ruled in the design channel 2026-08-02 (R1–R7, captured verbatim by Step 0's ADR). D0 executes the sim split plan's S3 (`.agents/plans/2026-08-02-sim-split-plan.md`) as ruled by R1: the emitter is extracted BEFORE Wave D's ORU work extends it — a pure move, no behavior change, trajectory-before-CallSubmodule reasoning applied to the emitter. `emit_hl7.clj`, `v2_replay.clj`, and `site_profile.clj` (+ their tests) move from `components/sim` to a new `components/sim-emit-hl7`, namespaces `ehrt.sim.X` → `ehrt.sim-emit-hl7.X`, bodies byte-identical except the `ns` form. `emit_state.clj` does NOT move (it reads `engine` directly; its seam is S4's design problem, per the plan). `ehrt.sim.interface`'s public surface does not change (AR-3, ADR-0025; corpus depends on it). Method precedents: the S1+S2 extraction sessions (ADR-0025, their session records) — caller-map characterization, mechanical rename, interface from grep evidence only, deftest parity, golden byte-identity.
The golden oracle is at its strongest this stage: a fixed-seed run with HL7 emission enabled produces actual emitted `.hl7`/manifest files, and every output byte must be identical pre→post move — an emitter extraction that changes one emitted byte has failed.
Ceremony: R30-mode — commit and push at each checkpoint, unattended, with R30's safeguards (staged-scope `--stat` check per checkpoint, personal-info scan, message via the Write tool per the Wave B double-quote-hazard memory, session record before final push, hooks as backstop; tags and repo-level `gh` outside the grant). Work in the WSL ext4 clone; fast-forward to `origin/main` (at or past `83470b1`), record HEAD.
Read first

1. `.agents/plans/2026-08-02-sim-split-plan.md` — S3's ruled scope, R-1..R-8 risks (binding), verification baselines.
2. ADR-0025 in `notes/ADRs.md` (the S1+S2 record: dependency directions, forbidden-forever list — D0 extends both) and the S1/S2 session records for the extraction mechanics.
3. `components/sim/src/ehrt/sim/emit_hl7.clj` (note its requires: `config` — now `ehrt.sim-model` — and `site-profile`; and its message-type registry comments, which Wave D1 will extend and which move untouched today), `v2_replay.clj`, `site_profile.clj`, `run.clj` and `identifiers.clj` (expected callers — verify, don't trust).
4. The docs stale-path tripwire — gains the retired-namespace patterns for the three moved namespaces; confirm archive exclusions first, as before.
5. `.agents/plans/2026-08-02-gmf-coverage-plan.md` + roadmap (Step 0 restructures the Wave D rows).

Author rulings (ruled 2026-08-02, design channel)

* R1 — S3 executes now as Wave D's stage D0, front-running the trigger deliberately: emitter growth inside fat sim is the anti-pattern the split exists to prevent, and the move is oracle-guarded. Component name `sim-emit-hl7` (AR-1 family, ADR-0025).
* R2 — Wave D's IR additions, for the record now, built in D1/D2 not D0: (a) ONE new step type `:diagnostic-report` for the observation family — optional report codes plus a vector of observation-shaped children; both `MultiObservation` and `DiagnosticReport` compile into it, exact upstream coupling pinned by D1's characterization from Synthea source at the pin; (b) `:care-plan-start`/ `:care-plan-end` as a paired span mirroring the `:medication-order`/`:medication-end` precedent; (c) `VitalSign` dissolves into observation-flavored events (vital-sign category) — vitals live in the log, the `Vital Sign` condition reads clinical state, NO new persona compartment. Every IR addition co-lands the full chain: schema + compile mapping + engine handling (or explicit pass-through ruling) + emission decision + invariants.
* R3 — CarePlan is deliberately v2-silent: registry non-entry with the disclosed comment, the `:procedure`/`:medication-*` precedent; its natural rendering is a FHIR CarePlan when `sim-emit-fhir` exists.
* R4 — Closures may contain DATA-FILE members (lookup-table CSVs): the loader closure, NOTICE, provenance headers, and content-hash lineage all extend to them. Built in D3.
* R5 — `ImagingStudy` is OUT of Wave D: named in the coverage plan with a CHF trigger, not built.
* R6 — Wave D sequencing: D0 (this session), D1 (observation family
   * ORU^R01-with-OBR emission → sepsis, closure permitting), D2 (CarePlan family + `Active CarePlan` condition → MI and total_joint_replacement, closures permitting), D3 (`lookup_table_transition` + attribute-weighted `distributed_transition` weights + UTI closure re-characterization → UTI). Each stage: own session, own characterization gate, own payoff declared from fetched evidence.
* R7 — The stroke-risk data source is NOT Wave D scope: a calibration/content-provenance item, named in the coverage plan with its own row, unowned by any wave until ruled.

Steps

0. Records. Land the Wave D design ADR (next number) in `notes/ADRs.md`: R1–R7 verbatim (attributed: ruled 2026-08-02, design channel), the three-family design rationale in brief, and marked placeholders for D1/D2/D3 characterization findings. Coverage plan: dated note restructuring Wave D into D0–D3 with the payoff map above, the R5 ImagingStudy note, the R7 stroke-risk-source row. Roadmap: D0 → Now; D1–D3 → Deferred with payoffs; sim-split S3 row satisfied-by-D0 (cite ADR); S4 row unchanged. Commit: `docs: Wave D design ADR (R1-R7); D0-D3 plan restructure`.
1. Characterize. Caller map for the three moving namespaces (grep across all components and bases; expected: `run` and `identifiers` require `emit-hl7`; `v2-replay` and `site-profile` internal to the trio plus whoever else the grep actually shows — evidence decides). Confirm the façade re-exports none of them (or list which, handled per AR-3 delegation). Confirm `emitter_order_independence_test` stays in sim and what it requires. Golden baseline: fixed-seed run WITH HL7 emission enabled, sha256 of every output file recorded. Deftest recount (live count is the baseline). Commit: `docs: D0 caller map and golden baseline (characterization)`.
2. Extract. `poly create component name:sim-emit-hl7`; move the three sources + three tests; rename namespaces (bodies otherwise byte-identical — diff-audit each); `ehrt.sim-emit-hl7.interface` from caller evidence only, thin delegation; repoint residual-sim consumers; wire root `deps.edn` (`:dev` + `:test`), project `deps.edn`s, `workspace.edn`, re-derive `:necessary`; move any `components/sim/docs/` file whose SUBJECT is the emitter (list the ruling per file in the session record; sweep links); extend the tripwire with the three retired-namespace patterns (red→green recorded). Commit: `refactor: extract sim-emit-hl7 from sim (sim split S3 / Wave D D0)`.
3. Verify. `poly check` clean; `poly test :all skip:integration` green; both lanes green; golden re-run BYTE-IDENTICAL including every emitted HL7 file; deftest parity vs baseline; namespace diff list = renames only; seam CLI (`ehrt help`, the golden run command) byte-identical; structure-currency and reading-set gates green (self-catch bumps expected, dated notes as established).
4. Records. ADR-0025 gains a dated S3-executed note (fix-forward); the new ADR's D0 execution record; dependency directions recorded (`sim-emit-hl7 → {sim-model, kernel?}` — as measured; forbidden-forever: `sim-emit-hl7 → sim`, `sim-emit-hl7 → sim-trajectory`, any cycle); roadmap D0 → Done with shas; session record; self-archive this prompt to `.agents/prompts/`. Final commit: `docs: D0 records (ADR, roadmap; archives prompt)`.
