# 2026-08-07/08 — Quality arc close — driving prompt

## Context

Repo `ehr-testing-tools`, ext4 clone (`~/src/ehr-testing-tools`).
Prompt's own stated HEAD: `8eeafb2` — confirmed live at session start,
no premise mismatch (see this date's own session record).

## Prompt, verbatim

2026-08-07 — quality arc close: the repo examined itself, ruled on what it saw, and fixed the worst of it Session prompt (design channel, 2026-08-07; conventions and every claim below read at HEAD `8eeafb2` against a fresh public clone). This session CLOSES the quality-review arc per the standing close pattern (ADR-0074/0068 the models): rulings appends, the dependency-review cadence, `state.md` regeneration, budget re-derivation, Done rotation, and the closing ADR. Docs-only; anything new found is next-review intake, never an act. The arc being closed — the `repo-review` skill's first full cycle, seeded by an incident bridge: ci current (ADR-0075, `9acb79b` — the bridge: 32 commits of unwatched red, the derived docs caught up, preflight learned to look); quality riders (ADR-0076, `89c0d24` — the skill landed, the sibling flake fixed atomically, preflight widened to five runs); repo review 1 (ADR-0077, `93bd9a6` — 45 rows, eight lenses, the first scoreboard: 4 green / 3 yellow / 1 red); result or loud (ADR-0078, `758f3af` — `ehrt.kernel.io`, eleven sites converted, the demonstrated silent count-of-zero dead, gated); lint family (ADR-0079, `8eeafb2` — four gates, four protections, one pinned seed, one sitting). The register's ruled dispositions are fully executed or explicitly assigned; the scoreboard's RED dimension is structurally closed pending re-probe at review 2. R30 ceremony. Read-first: `notes/adr/0074-vendoring-arc-close.md` (the pattern); `.agents/rulings.md` in full; `.agents/state.md` in full (regenerated at the vendoring close, now FIVE landed sessions stale — wrong in at least: the gate inventory, the kernel component surface, the CI posture, the review instrument's existence, and the flake ledger); `.agents/reading-sets.edn`; ADR-0075 through 0079 (their findings and disclosures feed the close); the findings register (`.agents/plans/2026-08-07-repo-review-findings.md` — final dispositions land in ADR-0080; the register stays a dated artifact, the ADR-0055/0064 discipline); `.agents/plans/roadmap.md` (Done holds six pointers to rotate). Author rulings (record verbatim in ADR-0080; `[A]` author-ruled, `[C]` channel-inferred)

1. AR-QC-0 `[A — tag law, case (ii); debt recorded in ADR-0079]`. Annotated `stable-20260807-lint-family` at `8eeafb2`, message `lint family landed, design-channel-verified 2026-08-07 (ADR-0079)`; push; verify peeled ref.
2. AR-QC-1 `[A/C per append]` (rulings appends). Under "From the quality-review arc (ADR-0075–0080)": (a) `[A — ruled AR-RL-5(5)]` multi-seed-once-flagged, standing — a vendoring round-trip that flags a module re-runs at 2–3 well-mixed seeds at population scale before any verdict; codifies ADR-0071/ADR-0072's followed practice (register D6-4). (b) `[A — ruled AR-RL-5(3)]` the defspec seed policy, standing — seeds stay unpinned for generator diversity; a spec that has actually flaked pins or durably logs its seed (the engine spec, ADR-0079); the printed-seed-plus-CI-retention default is sufficient otherwise; revisited on the next flake. (c) `[C — the arc's own executed discipline; the author may strike]` I/O speaks Result or fails loud, standing — a production I/O call that can fail routes through `ehrt.kernel.io` or handles its failure mode by name; an I/O failure never impersonates an empty result (ADR-0078, gated by `io_vocabulary_lint_test`). (d) `[C — likewise]` CI is watched, never waited on, and commits land green, standing — preflight discloses the last five runs' conclusions; watch-to-conclusion is reserved for sessions whose own claim is about CI; no push carries a knowingly-failing test (ADR-0075/0076/0078's own pattern shift, superseding the older red-checkpoint-commit pattern).
3. AR-QC-2 `[A — standing cadence]`. `clojure -M:poly libs :outdated`; dated report in ADR-0080; no edit follows.
4. AR-QC-3 `[A — state.md's regeneration contract]` (state regenerates). Every `[V]` claim probe-backed THIS session; skeleton preserved; content updates at minimum: the kernel component's new `io` surface and the I/O law; the gate inventory by fresh count (docs-tooling grew the io-vocabulary lint, the staleness tripwire, the Deferred lint, the live-path lint; `sim` grew the façade surface gate; `bases/cli`/`corpus` carry docsgen currency gates since ADR-0075); the CI posture in full (five-deep preflight, watch-reserved, commits-land-green, local docsgen gates, the author's notification-email Externals row — probe whether the author has toggled it, disclose either way); the review instrument as standing equipment (the skill in both mirrors, the register as a dated artifact, the scoreboard as review 2's baseline — restate it: 4 green / 3 yellow / 1 red with the RED structurally closed pending re-probe); the flake ledger (sibling fix SOAKING — count CI runs since `9cc3563` at close time against the one-in-five-to-seven target; engine spec pinned `-60645`); the demos/derived-docs posture unchanged; every count (ADR files, tags, Deferred, Next, oracle roots, NOTICE rows) by fresh count. Regeneration table in ADR-0080; wrong-in-kind is STOP-AND-REPORT.
5. AR-QC-4 `[A — standing budget rule]` (budgets). Re-derive every reading set whose member paths changed across `cd6c56c..HEAD` (`README.md` changed at ADR-0078; `state.md` regenerates here; `rulings.md` changed twice — enumerate by `git log --name-only`, don't assume), AFTER the regeneration lands.
6. AR-QC-5 `[C — rotation mechanics per the four prior closes]` (rotation). Done holds six pointers (0074–0079, design-channel probed at `8eeafb2`). Rotate: ADR-0074's pointer joins the attic's vendoring-arc section with the dated leftover note (the standing class); ADR-0075–0079 rotate under `## Quality-review arc — closed 2026-08-07 (ADR-0075–0080)` with one line noting ADR-0075's bridge role (the incident that seeded the arc rides with the arc it seeded — the ux-epilogue precedent, inverted: this bridge belongs to what FOLLOWED it). ADR-0080's pointer lands as the sole current entry (Step 3, sentinel-avoidance). Now refreshes to one line. Relocation-not-rewrite.
7. AR-QC-6 `[A for cited items; C for composition]` (the closing ADR). `notes/adr/0080-quality-arc-close.md`: rulings verbatim; the arc summary one line per session with ADRs and tips; the register's FINAL disposition tally (re-derive from the register's rows, not its corrected summary — the AR-RL-R mechanism, one more time, on principle); the arc narrative — the repo's improvement passes became procedure: a rotating eight-lens instrument, a register, a ruled plan, executed fixes each with its recurrence gate; review 2's inherited watch-list, explicit: the sibling-flake soak (with the run-count at close), the `notice_verbatim` coverage gap (v2-nist NOTICE.md + simhospital PROVENANCE.md table shapes, ADR-0079's disclosed finding), the yellow dimensions' open rows (D2-7's deferred scaffold with its third-drift trigger; D6-1's census closure-count, escalated), wellness-encounters RE-SURFACED (register D7-6 executed here — it reappears in this horizon note so it stops sliding), and the register summary re-derivation as standing probe (the skill now says so); the open Externals restated (NIST gist, IG pinning, Clojars F-5/F-6, SETUP rewalk, the notification toggle if still unfired); this close's own successor tag debt IN THE ADR (`stable-20260807-quality-close` at this session's closing tip); and the horizon note: "The horizon, for the author's ruling: the EncounterEnd interpreter design pass (two vendored-ready modules blocked, the oldest ruled-fix candidate), the pairing-as-data registry session (design pass RUNNING in the design channel, four shape questions pending the author), Wave E's risk-attribute/vital-sign register, vendoring batch 4 (the veteran family), the census closure-count refinement, publish-prep (F-5/F-6 + F-7). Review 2 on the author's cadence call. `sim-emit-cda` on its trigger."

Steps Step 0 — Preflight + tag. Cwd ext4; tip `8eeafb2` or later-with-disclosure; full suite green baseline; last-five CI disclosed; oracle pre-digest (27). Execute AR-QC-0. Step 1 — Appends + cadence (AR-QC-1/2). Commit: `docs: the quality arc's law is appended — seeds, loudness, and a watched green (arc close, AR-QC-1/2)` Step 2 — State + budgets + rotation (AR-QC-3/4/5). Full suite green through the rotation — note the staleness tripwire (ADR-0079's own gate) fires red mid-step the moment the newest close ADR exists without state.md citing it; sequence the regeneration and the ADR landing so the gate's own logic is satisfied at every commit boundary (the tripwire asserts against COMMITTED state; a wrong-order landing is exactly what it exists to catch — let it govern the order, disclosed). Commit: `docs: the state regenerates, the budgets re-derive, five arcs rest in the attic (arc close, AR-QC-3/4/5)` Step 3 — ADR-0080 + record. Everything per AR-QC-6; index line; README count by `ls`; sole Done pointer. Oracle bracket (`8eeafb2` → tip): all TWENTY-SEVEN batches identical — docs-only; any change is STOP-AND-ESCALATE. Session record + prompt self-archive; CI watched green. Final commit: `docs: the quality arc closes — the improvement pass is now a procedure, and the procedure has teeth (ADR-0080)` Fences Docs-only: no `src/`, no `test/`, no config, no `.gitattributes`, no gates. The `libs :outdated` output is a report. The horizon note restates ruled directions and NAMES unruled intake; it decides nothing. Frozen archives untouched (ADR-0080 + index + sanctioned attic appends). Wrong-in-kind regeneration discrepancies are STOP-AND-REPORT. `[C]` rulings conflicting with the live tree fix forward and disclose; `[A]` ones escalate. After landing: design channel verifies by fresh probe — sampled re-probes of the regenerated state.md per its contract, the tripwire's own first real exercise inspected — then the next arc's opening session tags `stable-20260807-quality-close` at this tip under standing ceremony, and the horizon is the author's to rule. The pairing-as-data design pass continues in the design channel; its four shape questions remain open.
