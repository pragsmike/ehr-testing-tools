# 2026-08-08 — The EncounterEnd fix — driving prompt

## Context

Repo `ehr-testing-tools`, ext4 clone (`~/src/ehr-testing-tools`).
Prompt's own stated instruction: Step 0 resolves the actual tip and tag
from ADR-0081's own record, never from the prompt's own memory — this
session ran AFTER fidelity riders landed. Confirmed live at session
start: HEAD `c2bcb67` (ADR-0081's own closing tip), working tree clean.

This session's own AR-EE-1 (the blast-radius probe, required before
any fix code) found a real, nonzero mover — `hypothyroidism`, already
vendored — and stopped per the prompt's own R3 bar, before writing any
fix code. The author's ruling on that STOP-AND-REPORT, which amended
R3 and licensed the mover, is archived below as a second verbatim
block — it drove Steps 2-4 exactly as much as the original prompt
drove Step 1.

## Prompt, verbatim

2026-08-08 — encounterend fix: the interpreter learns the five arms — predicted, tracked, counted, proven Session prompt (design channel, 2026-08-08; conventions read at HEAD `42cd1e0`; this session runs AFTER fidelity riders lands — its Step 0 resolves the actual tip and tag from ADR-0081's own record, never from this prompt's memory). Second session of the fidelity arc: THE INTERPRETER FIX, licensed by the author's three recorded rulings (ADR-0081: R1 openness-only with disclosed divergence; R2 count-and-surface; R3 predict-and-confirm identical, any mover escalates) executing the landed design brief (`.agents/plans/2026-08-08-encounterend-design.md` — read it FIRST and in FULL; it is the specification, this prompt is its execution order). This is the first interpreter semantics change since the GMF coverage waves — the ceremony is proportionate. One structural note the brief's red-first plan needs resolved here: the two real modules (anemia, colorectal) are NOT in-tree until the payoff rider, and committed tests cannot depend on the author's checkout (the live-path lint, this repo's own law) — so the COMMITTED reds use a small synthetic test-fixture module exercising the close-if-open idiom, while the two real modules' before/after proof runs IN-SESSION against the pin-verified checkout, transcribed into ADR-0082, and becomes committed round-trips only in the vendoring rider when their resources land. R30 ceremony. Read-first: the brief in full; ADR-0081 (the rulings verbatim, the recorded tag debt); `gmf_interpreter.clj` ~1690–1712 (the compile dispatch), ~1209 (`index-of-last-open-encounter`, the openness-blind fn this session RETIRES), ~1930–1955 (the Wave H fold whose pairing discipline moves into the walk); `census.clj` `walk-one` (where the suppressed-end counter surfaces); ADR-0071/ADR-0072's deferral evidence (the violation shapes the in-session proof must reproduce, then extinguish); `docs/gmf-interpreter.md`'s section conventions (where the arm-treatment section lands). Author rulings (record verbatim in ADR-0082; `[A]` author-ruled, `[C]` channel-inferred)

1. AR-EE-0 `[A — tag law, case (ii)]`. Execute the successor tag debt ADR-0081 records (`stable-20260808-fidelity-riders` at ADR-0081's own closing commit — resolve the SHA from that ADR's text, verify it is an ancestor of HEAD, tag, push, verify peeled ref).
2. AR-EE-1 `[A — R3, the prediction FIRST]`. Before ANY code: the blast-radius probe. For each of the 27 oracle roots, walk its own recorded seed/config through the CURRENT interpreter and count unmatched `:encounter-end` emissions (an end whose reference is nil OR whose referenced encounter already has a matching earlier end). The PREDICTION TABLE (root → unmatched count → predicted identical/moves) lands in ADR-0082 verbatim. Expected: zero unmatched everywhere, all 27 predicted identical. ANY nonzero count is STOP-AND-REPORT with the walk evidence before a line of the fix lands — R3's own bar.
3. AR-EE-2 `[A — the ruled design, executed]`. Per the brief: (i) walk-state openness tracking — the open-encounter index set on `:encounter`, cleared on its matched `:encounter-end`, one in-flight encounter (the subset's own invariant, assert it); (ii) the compile dispatch — A1 open→emit referencing the TRACKED index; A5 none-open→NO EVENT, ordinary transition, `:suppressed-encounter-ends` incremented (R2); wellness arms by openness alone with the disclosed-divergence paragraph landing in `docs/gmf-interpreter.md` as a dated section (R1); A4 disclosed unreachable, the Wave G multi-module trigger extended to name it; (iii) `index-of-last-open-encounter` RETIRED — deleted, not left as a trap (its one caller is the site being fixed; verify by fresh grep, disclose any other). (iv) The counter surfaces: census `walk-one` rows carry `:suppressed-encounter-ends` when nonzero; round-trip metadata likewise (fresh-read where round-trips report walk stats; lands-small, disclosed).
4. AR-EE-3 `[C — the reds]`. COMMITTED red-first: a synthetic test-fixture module (a minimal GMF JSON under the tracked test-fixture root — the live-path lint's blessed home) whose walk reaches an EncounterEnd with no open encounter; tests assert (pre-fix, witnessed in-session) the dangling end exists, then (post-fix, committed green) no dangling end, the ordinary transition taken, the counter = 1, and the A1 path still emits with the tracked reference. Unit tests on the tracking itself (open→close→open sequences; the never-nests assertion). IN-SESSION proof, transcribed: anemia and colorectal loaded from the pin-verified checkout, the ADR-0071/0072 violation shapes REPRODUCED against the pre-fix interpreter (the honest red), then extinguished post-fix — population scale, the same seeds the deferrals recorded, invariant catalog green. Commits land green throughout (the standing CI policy).
5. AR-EE-4 `[A — R3, confirmed after]`. The post-fix oracle bracket must match AR-EE-1's prediction EXACTLY (27 identical); any divergence from prediction is STOP-AND-ESCALATE even if it "looks fine." A labeled census re-run (`encounterend`) lands: parity must hold (84/85 + gallstones), substance movement cataloged in ADR-0082 (movement here is INFORMATION — post-fix walks may legitimately differ for modules leaning on the idiom — but every changed row is listed, none silently absorbed), suppressed-end counts appear as new census data.
6. AR-EE-5 `[C — the paper trail]`. The brief gains a dated executed-note pointing at ADR-0082 (fix-forward style, the plan stays a dated artifact); the roadmap's two-module-blocker Deferred row gains a dated note: the interpreter gap CLOSED, the two modules now awaiting only their vendoring rider.
7. AR-EE-6 `[C — scope]` (fences). Src edits ONLY in `gmf_interpreter.clj` (the dispatch, the tracking, the retirement) and `census.clj` (the counter surfacing) plus the new test files and fixture. NO vendoring (the rider is next). NO engine/emitter/player changes. NO other interpreter improvements however adjacent — a tempting fix found mid-move is a FINDING (AR-P-4's law). Standing untracked files untouched.

Steps Step 0 — Preflight + tag. Cwd ext4; tip per ADR-0081's record or later-with-disclosure; full suite green baseline; last-five CI disclosed; oracle pre-digest (the 27 this session must keep identical). Execute AR-EE-0. Step 1 — The prediction (AR-EE-1). Probe run, table drafted for the ADR. NO commit (the table lands with ADR-0082; a nonzero finding stops here). Step 2 — The fix (AR-EE-2/3). Reds witnessed in-session and transcribed; fixture + tests + implementation + doc section land green in ONE commit: `fix: the interpreter learns the five arms — ends match opens or stay silent, counted (encounterend fix, AR-EE-2/3)` Step 3 — The confirmation (AR-EE-4/5). Oracle bracket vs prediction; the labeled census + its artifact; the brief's executed-note + the Deferred note. Commit: `docs: the prediction confirms — 27 roots identical, the census re-ranks, the gap closes on paper too (encounterend fix, AR-EE-4/5)` Step 4 — ADR-0082 + record. Rulings verbatim; the prediction table and its confirmation; the transcribed in-session proofs; index line; README count by `ls`; Done pointer `- 2026-08-08 — encounterend-fix — ADR-0082`. Successor tag debt IN THE ADR (`stable-20260808-encounterend-fix` at the closing tip). Session record + prompt self-archive; CI watched green. Final commit: `docs: encounterend fix record — the first semantics change since the waves, predicted and proven (ADR-0082)` Fences Everything AR-EE-6 names. The brief is the specification — a conflict between this prompt and the brief is STOP-AND-REPORT (the design channel reconciles; the session never picks silently). `[C]` rulings conflicting with the live tree fix forward and disclose; `[A]` ones escalate. After landing: design channel verifies by fresh probe — the prediction table re-derived, the census artifact re-read, the retirement grep re-run — then the payoff rider vendors anemia and colorectal with their round-trips pinned forever, and the arc closes.

## Ruling on the STOP-AND-REPORT, verbatim

Author ruling on the STOP-AND-REPORT (design channel, 2026-08-08, recorded verbatim in ADR-0082):

AR-EE-1a — the trace first. Before any fix code: trace ONE of hypothyroidism's five dangling walks (walk #72's shape is fine) through compile-trajectory → engine → emit against the CURRENT tree, and record in ADR-0082 exactly why today's population-scale round-trip passes despite the dangling :encounter-end — identify the layer that drops, tolerates, or absorbs the nil-referenced end. If the answer is a silent drop or silent tolerance, record it as a D4-family absorbed-error finding that this fix closes structurally; do not fix it separately — the arm dispatch makes the case unreachable.

AR-EE-1b — R3 amended, the mover licensed. R3's bar amends to: 26 roots predicted-and-confirmed identical + hypothyroidism a PREDICTED, EVIDENCED, LICENSED mover — a correction, not a regression (its current digests freeze trajectories containing events upstream semantics forbid). Requirements riding the license: the before/after digests for hypothyroidism recorded side by side in ADR-0082; its round-trip test re-witnessed green post-fix at its own seed/population; its post-fix walks showing :suppressed-encounter-ends = the predicted counts (5/300 at the oracle seed); and any root OTHER than hypothyroidism moving remains STOP-AND-ESCALATE — the license names one mover, not a class.

AR-EE-1c — the erratum. ADR-0071's "its own call path never reaches the hazardous state" claim receives a dated correction note (append-don't-erase, in ADR-0082 with a pointer line where ADR-0071's record allows): the 3000-patient check verified content production, not end-matching at the oracle seed — the probe's evidence (5/300, end-any-active-encounter-just-in-case, references=nil) supersedes it. The audit-evidence-mechanism rule applies: the check verified a different property than the one later cited.

Proceed with Steps 2–4 under the amended bar. All other rulings unchanged.
