2026-08-06 — ux fixes 4: the help speaks to operators — the approved rewrite lands, gated
Session prompt (design channel, 2026-08-06). Prior: ux fixes 3 landed and was design-channel-verified (`7665baa`, ADR-0061). The author has APPROVED the design-channel rewrite draft in full, including judgment calls J1–J4 as drafted. The draft is in the working tree at `.agents/plans/2026-08-06-help-rewrite-draft.md`, placed by the author — if absent or empty, STOP-AND-ESCALATE; do not reconstruct or improvise wording. The draft's §3 is the complete replacement text; this session applies it verbatim, co-lands its §5 gate, and regenerates the generated region of `docs/cli.md`. The words are the author-ruled artifact: NO wordsmithing, however tempting — a string the session believes is wrong is a STOP-AND-REPORT, never an edit. R30 ceremony. Read-first: the draft (all of it); register rows B-1/B-2/B-3/B-4(a); `help.clj` in full; the render path enough to regenerate `docs/cli.md` per existing convention.
Author rulings (record verbatim in ADR-0062)

1. AR-U4-0 (tag, standing ceremony). Annotated `stable-20260806-ux-fixes-3` at `7665baa`, message `ux fixes 3 landed, design-channel-verified 2026-08-06 (ADR-0061)`; push; verify.
2. AR-U4-1 (the draft lands, verified not trusted). The draft lands in `.agents/plans/` with its index entry. Pre-landing verification: every string §3 marks `[unchanged]` must currently be free of §5's token patterns (spot-check them all — an `[unchanged]` claim that fails is a draft defect: STOP-AND-REPORT, the design channel corrects the draft, the author re-approves the delta).
3. AR-U4-2 (apply §3 verbatim). Every rewritten string replaces its original exactly as drafted; every `;;` relocation comment lands where shown; `[unchanged]` strings stay byte-identical. Completeness invariant, tabled in ADR-0062: every token removed from a rendered string appears in an adjacent comment — the count must reconcile to the draft's 38 (24 ADR + 14 milestone) or the delta is disclosed with each divergent token named. Zero deletions.
4. AR-U4-3 (the §5 gate, co-landed). The voice gate exactly as §5 specifies: walks `cli-spec` as data (rendered-string positions only — `:doc`, `:meaning`, `:positional-doc`, `:default`, top-level-doc's value); word-bounded patterns; pre-verified against every post-rewrite string AND against the current clean strings ("MSH-7", "msg-%03d", dates must survive) — any false positive is a STOP, never a silently loosened pattern. Natural red witnessed first against the unrewritten tree (expected: 38 token hits); rewrite + gate land in ONE commit, green.
5. AR-U4-4 (the rendered surface proves it). Regenerate `docs/cli.md`'s generated region per existing convention (nothing outside it). Attach to ADR-0062: a before/after excerpt of `bin/ehrt help` and `bin/ehrt help corpus` transcripts — the register's B-2 exemplars — showing what an operator now reads.
6. AR-U4-5 (scope). J1–J4 are ruled as drafted and recorded. No render-function changes (wrap mechanics are session 5); no `core.clj`; no error strings. The oracle bracket must show all ELEVEN batches identical — help text is not wire output; any change is STOP-AND-ESCALATE.

Steps
Step 0 — Preflight + tag. Cwd ext4; tip `7665baa` or later-with-disclosure; draft present; AR-U4-1's spot-checks tabled; full suite green baseline from workspace root; oracle pre-digest. Execute AR-U4-0.
Step 1 — Draft lands (AR-U4-1). Commit: `docs: the approved rewrite draft lands — the words are ruled (ux fixes 4, AR-U4-1)`
Step 2 — Red, then the rewrite + gate + regen (AR-U4-2/3/4). Gate written; the 38-token red witnessed and transcripted; §3 applied verbatim; comments placed; `docs/cli.md` regenerated; gate green; full suite green; `poly check` OK; the help transcripts captured. ONE commit: `docs: the help speaks to operators — 38 tokens relocated, zero deleted, gated (ux fixes 4, AR-U4-2/3/4)`
Step 3 — ADR-0062 + record. Rulings verbatim; the token-relocation table; the red transcript; the before/after help excerpts; the `[unchanged]` verification table. Index line; Done pointer `- 2026-08-06 — ux-fixes-4 — ADR-0062`. Oracle bracket (`7665baa` → tip) per AR-U4-5. Session record + prompt self-archive. Final commit: `docs: ux fixes 4 record — maintainer memory kept, operator surface cleared (ADR-0062)`
Fences
`help.clj` edits are ONLY: the §3 replacement strings and the `;;` relocation comments — no render functions, no structural changes, no def-docstring edits. The one new file is the gate test; the one regenerated file is `docs/cli.md`'s generated region. NO wordsmithing beyond the draft (the STOP governs). No gate weakening. Frozen archives untouched (ADR-0062 + index sanctioned). If applying any §3 string is non-mechanical because the surrounding structure differs from the draft's reading, STOP-AND-REPORT — the draft is author-ruled text, and reconciling it against contrary structure is the design channel's job, not the session's.
After landing: design channel verifies by fresh probe — including reading the rendered `bin/ehrt help` transcripts against the draft; session 5 (the wrap mechanism, B-4b) follows as the arc's last fix, then the arc close. This landing's tag rides session 5's Step 0 under standing ceremony.
