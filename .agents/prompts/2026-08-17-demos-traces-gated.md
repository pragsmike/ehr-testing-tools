# Archived prompt: demos-traces-gated (2026-08-17)

Archived verbatim as received. One premise did not hold against the live
tree and was corrected rather than adapted around: the prompt places the
freshness-population assertion in `docsgen_test.clj`, "how the population
is derived so the new paths join it, not a hand list." No such derivation
exists -- `docsgen_test` covers `render-cli-md` only, and ADR-0136's
widening of CI's diff list was prose discipline in a workflow comment,
never a gate. `ehrt.docs-tooling.traces-fresh-test` is the first
mechanical check of either surface.

Two of the prompt's own conditional branches fired:

- The census's `module-mix` divergence was an "other", which this
  prompt's fence makes a STOP. Probed before reporting -- re-running its
  README command at `f07684c`, the commit that last wrote the file,
  produces today's bytes -- which collapsed the two candidate readings
  to one and made it fix-forward with disclosure instead.
- `make traces` cold measured 83.65 s against the prompt's 90 s ceiling,
  so the per-push-vs-integration tier ruling was not forced. The margin
  is thin and is recorded rather than glossed.

The FHIR bundle was included on the prompt's "mechanical-only variance"
branch (one line re-wrapped at the default print margin), not left as
residue. Reasoning in `notes/adr/0149-demos-traces-gated.md`, ceremony in
`.agents/session-records/2026-08-17-demos-traces-gated.md`.

---

Session prompt -- demos/traces regenerated by the build and
freshness-diffed by CI, plus two overdue rulings rows -- ADR-0149

## Context

Claude Code under R30 in ehr-testing-tools. HEAD at handoff: 922e55a
(ADR-0148 addendum; tree clean; CI green at 854a0df per the addendum's
own `gh run 32078358238`; last tag `stable-20260817-exercised-sources-
coverage` @854a0df, no tag owed). Roadmap row `roadmap.md#demos-traces-
ungated` (OPEN PRIORITY 6): "`demos/traces/**`'s seven committed
`messages*.txt` traces are produced by `bin/ehrt sim run` and regenerated
by nothing (no Makefile target, no workflow, no test), so they drift
silently and did. ... proposed shape is a `docsgen` target plus the CI
freshness diff every other generated surface gets." It runs BEFORE
`#event-log-shape-defects` (P1) because the S-row and Z-segment fixes
there move these traces, and today nothing would notice.

Channel probe at 922e55a (re-derive every one):

* Six trace dirs, seven `messages*.txt` (site-profiles has two), six
  `ground-truth.edn` (all pprint-shaped, 71..10,302 lines), one
  `emit-state/fhir-bundle-patient1.json`. Each README carries its
  producing `bin/ehrt sim run` fence(s); persona-enriched's README
  discloses at :28 that its `messages.txt` predates the PV1 additions and
  is NOT a byte match today.
* ADR-0142 section "Gating verified negative": no Makefile target,
  workflow, or test touches `demos/traces`; only `.gitattributes:27`
  (`-text`).
* The CLI's two bare stdout modes are `--format er7` (messages joined by
  one blank line) and `--format ground-truth` (`pr-str`, one line, NOT
  pretty) -- `bases/cli/src/ehrt/cli/core.clj:2246-2261`. So a CLI-only
  capture reproduces `messages*.txt` bytes directly, but NOT the
  committed pretty `ground-truth.edn` bytes without a pprint step.
* Pattern to copy: `bin/demo-exerciser-ed-tuesday` (taught commands
  verbatim between BEGIN/END markers) + `demo_exerciser_fresh_test`
  (README fences == script's taught block, per push) + one
  `exercised-sources.edn` row per source (`R-exercised-implies-gated`;
  ADR-0148's `exercised_sources_coverage_test` will FORCE the row for any
  cited script -- do not fight it, feed it).
* Freshness diff population: `.github/workflows/test.yml:106-127` (`make
  docsgen` then `git diff --exit-code <paths>`), gated by `docsgen_test`
  (population from the make graph).
* Rider evidence: `rulings.md:178` `R-unrelayed-tag-condition-stops`
  still reads "...STOPs and reports the run id; never substitute your own
  check -- ADR-0134", while the standing ruling of 2026-08-17 ("sessions
  verify CI via gh"; two-branch in-session tag payment) lives only in the
  0148 prompt's verbatim block and the 0147/0148 addenda. F-3-narrowed
  (STOP-AND-REPORT binds only where two readings are both defensible;
  mechanical = fix-forward-with-disclosure) appears in ADR-0146 (:370)
  and ADR-0148 (:64, :255) and in NO rulings row.
  `R-law-surface-propagation` (ADR-0055) says both owe the register.

Small session: one script, one make target, one fresh test, one CI path
list, register rows; docs-tooling + bin + Makefile + workflow only.

## Read first

1. `demos/traces/README.md` and every `demos/traces/*/README.md` (each
   fence, verbatim -- these are the taught commands).
2. `bin/demo-exerciser-ed-tuesday` (whole file: markers, `expect`,
   per-step capture), `components/docs-tooling/src/ehrt/docs_tooling/
   demo_exerciser_fresh.clj` (`readme-command-lines`,
   `script-command-lines`, `check`) and its test.
3. `components/docs-tooling/resources/docs-tooling/exercised-sources.edn`
   (header contract, row shape); `exercised_sources.clj`;
   `strip_fresh.clj` `check-all`; ADR-0148 (what the coverage gate now
   forces).
4. `Makefile` `docsgen` and its leaves (:185-228); `.github/workflows/
   test.yml:35-60,106-127`; `docsgen_test.clj` (how the population is
   derived so the new paths join it, not a hand list).
5. `bases/cli/src/ehrt/cli/core.clj:2242-2305` (bare formats).
6. ADR-0142 section "Gating verified negative" and its trace-movers table
   (:431-446); `rulings.md` header, rows 46-52 (SUPERSEDED-BY shape),
   :176-180; ADR-0146 :360-372; ADR-0148 :294-320 (addendum).
7. `rulings.md#R-population-closure`, `#R-exercised-implies-gated`,
   `#R-taught-shell-lines-use-expect-eval`, `#R-law-surface-propagation`,
   `#R-tag-law`; build-session skill; `:docs` reading set.

## Author rulings, verbatim

* Next: "go" on `[demos-traces-ungated]` with the rulings rider
  (channel-recommended option (a), author "Accept recommendations. Go.",
  2026-08-17). Standing: sessions verify CI via gh; F-3 narrowed.
* Tag: no tag owed at Step 0 (854a0df tagged, 922e55a is its addendum).
  This session's own close tag: pay in-session if its tip run concludes
  success while open, else next Step 0 -- say which.

## Step 0

Fresh clone, tip 922e55a; `bin/preflight`; baseline `make test` unpiped,
MAKE_EXIT captured, reconcile vs ADR-0148's recorded 344 blocks / 3,906
tests / 17,548 assertions; `poly check`; reading sets vs baselines. Then
the census this session needs, written into the ADR before any change:
for each trace dir, the README's fenced commands (count and text), the
derived files present, and -- by RUNNING each README's own `--emit hl7`
command with `--format er7` (and site-profiles' two) into a temp dir --
which of the seven `messages*.txt` are byte-identical to the tree today
and which diverge (`cmp`, bytes differing). PREDICTION to record before
running: persona-enriched diverges (its README says so); the other six
were regenerated or predicted frozen at ADR-0142 and are predicted
identical. Prediction vs actual is a row of the ADR. Wall-clock each run.

## Step 1 -- red (docs-tooling test commit)

Add `traces_fresh_test.clj` (or extend `demo_exerciser_fresh_test`, say
which and why): for EACH `demos/traces/*/README.md`, README command lines
== the taught block for that trace in `bin/regen-traces` (see Step 2 for
the marker/row question), pinned counts per README. Add to `docsgen_test`
(or wherever the population lives) the assertion that `make docsgen`'s
graph includes the new target and that the CI diff path list contains
`demos/traces/`. Both red against 922e55a. Push red together with Step
2's green (`R-red-pushed-with-green`). Commit: "test: red -- demos/traces
regeneration is taught, targeted, and freshness-diffed (ADR-0149)"

## Step 2 -- green (bin + Makefile + workflow + register)

(a) `bin/regen-traces`: `set -uo pipefail`, `expect`-shaped like the
demo-exerciser, one BEGIN/END block per trace README carrying that
README's fences VERBATIM (every fence, in order, continuation lines kept
-- the extractor's rules), and per-step capture writing:

* `messages*.txt` from the `--format er7` step's stdout, bytes untouched
  (`.gitattributes` `-text` already protects them);
* `ground-truth.edn`: CLI-only capture via `--format ground-truth` piped
  through ONE repo-owned pretty-print step housed in the script (a
  `clojure -M -e` pprint of `edn/read`, or a docs-tooling entry -- say
  which). If the result differs from the committed file ONLY in
  whitespace / map-key order, that is mechanical: fix forward, commit the
  regenerated bytes, disclose per file (F-3 narrowed). If it differs in
  content, STOP -- two readings (stale trace vs engine drift) and it is
  P1's business, not this session's.
* `emit-state/fhir-bundle-patient1.json`: include iff a CLI-only capture
  (`--emit fhir --json` plus a stated extraction) reproduces it
  byte-for-byte or in mechanical-only variance; else LEAVE IT, name it in
  the roadmap row's residue with the reason.

Marker/row question, probe before deciding: if `exercised-sources.edn`
`check-all` can key six `:source` rows (one per README) to ONE `:script`
with distinct marker pairs, do that; if the extractor or the register
assumes one script per source, split into `bin/regen-trace-<name>` x6
called by `bin/regen-traces`. Record which and the evidence.

(b) Makefile: `traces:` leaf target running `bin/regen-traces`, added to
`docsgen`'s dependency list; echoes what it regenerated like its
siblings.

(c) `.github/workflows/test.yml`: add `demos/traces/` to the freshness
`git diff --exit-code` list; update the comment block's population note.

(d) `exercised-sources.edn`: the row(s), `:witness {:adr "ADR-0149" :date
"2026-08-17"}`; the ADR-0148 coverage gate must be green over them.

(e) Run `make traces` from a clean `out/`; commit the regenerated files.
Time ceiling: if `make traces` cold exceeds 90 s wall on your machine,
STOP with the number -- per-push vs integration tier is a ruling.

Commit: "feat: demos/traces regenerated by `make traces` (docsgen leaf)
and freshness-diffed by CI; taught commands verbatim per README, gated by
traces-fresh and the exercised-sources register (ADR-0149)"

## Step 3 -- rulings rider (register-only commit)

In `.agents/rulings.md`, three-line rows, lint green:

* `R-unrelayed-tag-condition-stops` KEEPS its row, gains `SUPERSEDED-BY
  R-session-verifies-ci-via-gh (ADR-0148)`.
* New `R-session-verifies-ci-via-gh` -- a tag licence's CI condition is
  met by the executing session's own `gh run view <id>` concluding
  success, id and conclusion in the session record; author relay
  sufficient, not required; a licensed tag whose tip run concludes
  success while the session is open is paid in session, else deferred to
  the next Step 0 saying which -- ADR-0148.
* New `R-stop-only-on-two-defensible-readings` -- STOP-AND-REPORT binds
  where two readings are both defensible; a mechanical conflict with one
  defensible reading is fix-forward with disclosure -- ADR-0146.

Cite the addenda/prompt lines these come from in the ADR; if you find
either ruling ALREADY captured under another slug (grep first), cite that
row instead of adding one and say so. Commit: "docs: rulings register
carries the 2026-08-17 tag-licence and F-3 narrowings as standing rows
(R-law-surface-propagation, ADR-0149)"

## Close (self-archive FIRST)

Archive this prompt to `.agents/prompts/2026-08-17-demos-traces-gated.md`
and open the paired session record BEFORE the rest of the close, so an
interrupted close still leaves provenance. Then: ADR-0149 (census,
prediction-vs-actual per trace file, the marker/row decision,
ground-truth handling, timings, rider sources); roadmap:
`#demos-traces-ungated` -> CLOSED under `## Done`, and if any derived
file was left ungated, a NEW row naming it (six-line cap,
`R-register-hygiene-at-close`); session record with `gh run view` id and
conclusion; full `make test` unpiped, MAKE_EXIT and the three counts
recorded and reconciled vs Step 0; `bin/post-push-verify`; tag per
ruling. Commit: "docs: ADR-0149 -- demos/traces gated close"

## Fences

src: docs-tooling (tests + at most one small pprint entry), `bin/`,
`Makefile`, `.github/workflows/test.yml`, `exercised-sources.edn`,
`.agents/rulings.md`, `.agents/plans/roadmap.md`, `demos/traces/**`
regenerated bytes ONLY -- no README prose edits beyond a dated errata
line where a README's own staleness note (persona-enriched :28) becomes
false; NO engine/emitter/CLI production change; oracle IDENTICAL; no
schema-version change; no test deletions; exit codes unpiped; `out/`
cleared before runs; anchored register edits; R-RP. READ-BACK, not
diffstat, names the fence: the ADR states, per regenerated file, the byte
size before/after and whether the change class is mechanical
(whitespace/order), ADR-0142-disclosed (PV1 / OBR-7 / OBX-14), or other
-- an "other" is a STOP.
