2026-08-07 — player board: the whiteboard exists — the accumulator meets the pacer

Session prompt (design channel, 2026-08-07). Session 2 of the player arc. Prior: player fold landed and was design-channel-verified (`01d9459`, ADR-0066) — the fold is total over the emitter's trigger set (A17/A40 included), self-anchored in absolute epoch millis, and the coherence property over full `sample-profile` churn is its spec. This session builds what that enabled: the bed board — a state-snapshot-at-intervals display (ADR-0014's own deferred surface) wired into `ehrt play`. The component-graph change it needs was RULED APPROVED by the author (design channel, 2026-08-06, recorded ADR-0066 AR-BB1-R): `v2-replay` exports through `ehrt.sim-emit-hl7.interface` and `corpus` gains the `sim-emit-hl7` edge — its first external caller, exactly the event the interface's own grep discipline (AR-6) keys on. One rider, RULED by the author 2026-08-07: "Tests should build their own directories as needed" — the standing principle resolving the fresh-clone-red finding ADR-0066 disclosed (`merge-config-file-suggests-a-same-stem-sibling-file` reads the LIVE `config/` directory and depends on the untracked `config/busy-weekday.md`; the public repo's suite has never been green on a fresh clone since that test landed, and any real config file the author keeps in `config/` can break it — probe-verified by the design channel, run_test.clj:298). Design note the general-input ruling forces, stated here so the session doesn't rediscover it: FOREIGN traffic may carry triggers outside the fold's handled set (a real feed's A08, A05, ...); the fold stays STRICT (`:unsupported-trigger`, the coherence law's own boundary) — it is the board SINK that treats an unfoldable message as a counted, cued skip, never a crash and never a silent absorb (the pacer's own cue rule, ADR-0014). R30 ceremony. Read-first: `components/corpus/src/ehrt/corpus/player.clj` (`plan`, `message-timestamp-ms`, the sink-map shape); `bases/cli/src/ehrt/cli/core.clj` `play-command` (~1351: the `sink-result` map `{:sink-fn :close-fn :cue-fn}`, the injectable `:sleep-fn`/`:println-fn`/`:now-ms-fn` seams, `run-plan!`); `help.clj` `cli-spec`'s play group (~200) and `--ticker`'s own "ignored when --sink is given" precedence wording; `components/sim-emit-hl7/src/ehrt/sim_emit_hl7/v2_replay.clj` (`fold-message`'s post-ADR-0066 2-arg signature, the entry shape: `:status`/`:class`/`:location {:ward :bed}`/`:attending`/`:persona`/`:admitted-at`, the `:merged` tombstone) and `interface.clj`'s header (the AR-6 grep discipline this session's export must honor in its own comment); `components/sim/test/ehrt/sim/run_test.clj` ~295–312 (the rider's subject and, immediately above it, the temp-dir pattern its neighbors already use); `.agents/rulings.md` (two-voices; errors-name-their-artifact; evidence-mechanism). Author rulings (record verbatim in ADR-0067)

1. AR-BB2-0 (tag, standing ceremony). Annotated `stable-20260806-player-fold` at `01d9459`, message `player fold landed, design-channel-verified 2026-08-06 (ADR-0066)`; push; verify peeled ref.
2. AR-BB2-R (rider — tests build their own directories, RULED). Rewrite `merge-config-file-suggests-a-same-stem-sibling-file` to construct its fixtures in a temp directory (create `<tmp>/busy-weekday.md`, request `<tmp>/busy-weekday.edn`, assert `:did-you-mean` names the temp `.md` path — the same pattern its own neighboring tests already use), and correct its docstring (it currently claims the untracked file is "a pre-existing fixture in this repo," which is false — say what the test now actually does). This makes the suite green on a fresh clone for the first time since ADR-0060 and makes `config/` safe for the author's real configs. Then ENUMERATE, by fresh grep of every test tree, any OTHER test reading a live mutable repo directory (the `config/` class — tracked test-fixtures directories are fine and out of scope): fix any additional offender only if it lands as small as this one, otherwise record it as a finding with the enumeration table. The author's ruling sentence — "Tests should build their own directories as needed" — is STANDING LAW: record it verbatim in ADR-0067; its `.agents/rulings.md` append waits for the arc close per that register's own contract (note this explicitly, don't append mid-arc without license). `config/busy-weekday.md`'s disposition after this rider is ceremonial: still untracked, still untouched, no longer load-bearing for any test.
3. AR-BB2-1 (the export and the edge — executing the standing approval). `ehrt.sim-emit-hl7.interface` exports EXACTLY what the player's board sink calls — expected: `fold-message` (2-arg) and nothing more; if implementation needs another var, export it and say why — with the interface header's own grep-discipline comment updated to name the new caller (it currently states `v2-replay` has "NO real external caller at all"; that sentence dies here, cited). `components/corpus/deps.edn` gains `poly/sim-emit-hl7 {:local/root ...}`; every `projects/*/deps.edn` and the development project already carrying or newly needing the edge reconciles the same commit — `clojure -M:poly check` is the arbiter. Record the fresh caller-grep in ADR-0067 (the AR-6 mechanism, exercised not asserted).
4. AR-BB2-2 (the `--board` flag). `ehrt play PATH --board N`: N = stream-minutes per snapshot (positive integer; validated by the same Result pattern `--rate`/`--idle-cap` already use — reject by name, exit 2). Board is DISPLAY-ONLY (ADR-0013's display-vs-wire law): when `--sink` is given, `--board` is ignored under exactly the precedence wording `--ticker`'s own `:doc` already establishes ("ignored when --sink is given"); when both `--board` and `--ticker` are given, `--board` wins, documented in both flags' `:doc` strings. The `cli-spec` entry speaks operator language (the help-voice gate walks it; the wrap gate wraps it — both must pass unmodified). The result envelope gains `:snapshot-count` and `:unfolded-count` when board mode ran.
5. AR-BB2-3 (the board sink — fold meets pacer). A board sink closes over the accumulator: each paced event folds via the exported `fold-message`; a message whose fold throws `:unsupported-trigger` (foreign triggers outside the emitter's set) is SKIPPED with a cue through the existing `cue-fn` channel and counted (`:unfolded-count`) — never a crash, never silent (the cue rule: cues go to the display, never into a data sink; board IS the display, so the cue prints inline). Snapshot cadence in STREAM time: render when a message's own timestamp (`message-timestamp-ms` — reuse, don't re-derive) first crosses each successive N-minute boundary measured from the FIRST parseable timestamp, plus one final snapshot at stream end regardless of boundary position. Unparseable-timestamp messages fold but never trigger a snapshot (they carry no position in stream time — same lenient posture the pacer itself takes).
6. AR-BB2-4 (the renderer — pure, and it speaks whiteboard). A pure function (accumulator map + snapshot instant → string), living in `corpus` (a small new namespace or `player.clj` itself — fresh-read judgment, disclosed; do NOT entangle `ehrt.corpus.display`'s own roadmap placement question). Renders: a header naming the snapshot instant (ISO-8601 from the absolute millis, UTC); occupied beds grouped by ward (wards sorted, beds sorted within), one line per patient — bed, patient name, MRN, class, attending when present; then a one-line tally: inpatients / active outpatients / discharged / merged (tombstones are COUNTED, never listed as occupying anything). Operator voice throughout — no citation, milestone, or internal-namespace token anywhere in rendered output (mirror `help_voice_test`'s patterns in this surface's own gate). Red-first unit tests: the renderer against a hand-built accumulator (empty; one ward; multi-ward with an outpatient, a discharge, and a merged tombstone); the sink against a hermetic paced run using the existing injectable seams (`:sleep-fn`/`:println-fn`/`:now-ms-fn`) — boundary-crossing snapshot count, final-snapshot-always, unfolded-skip counting, `--sink`-precedence (board ignored, bytes still byte-identical).
7. AR-BB2-5 (scope + oracle). `v2_replay.clj` src UNTOUCHED (the fold is done; only `interface.clj` changes in that component). Emitter untouched. No pacer-algorithm changes (`plan` is done; the board is a sink, not a scheduler). The oracle bracket must show all ELEVEN batches identical; any digest change is STOP-AND-ESCALATE. LIVE PROBE, recorded in ADR-0067: generate a small churn corpus (`bin/ehrt corpus generate sim --seed 42 --patients 8 --churn --out-dir ...` — `sample-profile` churn makes A17/A40 traffic plausible; if the seed produces none, pick one that does, disclosed), then `bin/ehrt play <out-dir> --board 60 --rate 100000` — transcript of at least two snapshots plus the final one; and `bin/ehrt play <out-dir> --board 60 --sink file:...` confirming the sink path still writes byte-identical output with board ignored.

Steps Step 0 — Preflight + tag. Cwd ext4; tip `01d9459` or later-with-disclosure; full suite baseline — EXPECTED: the one disclosed `merge-config-file-suggests-a-same-stem-sibling-file` failure and nothing else (it dies in Step 1); oracle pre-digest. Execute AR-BB2-0. Step 1 — Rider (AR-BB2-R). The rewritten test green hermetically; the enumeration grep recorded; full suite FULLY green for the first time — state the count. Commit: `test: tests build their own directories — the founding fixture retires from the suite (player board, AR-BB2-R)` Step 2 — Red, witnessed (AR-BB2-3/4 tests first, atop AR-BB2-1's export so they compile). Interface export + edge land WITH the red tests (the export alone changes no behavior; the red tests are the behavior spec); renderer and sink reds witnessed failing; transcripts for ADR-0067. Commit: `test: the whiteboard's spec turns red — snapshots, cues, and counts witnessed absent (player board, AR-BB2-1 + AR-BB2-3/4 red)` Step 3 — Green (AR-BB2-2/3/4). `--board` flag, sink, renderer land; all green; `poly check` OK; full suite green from workspace root; the live probe transcripts captured. Commit: `feat: the bed board lives — fold meets pacer at every Nth stream-minute (player board, AR-BB2-2/3/4)` Step 4 — ADR-0067 + record. Rulings verbatim (the rider's standing-law sentence included, with its arc-close append note); red transcripts; the caller-grep; the live probe transcripts; enumeration table; index line; Done pointer `- 2026-08-07 — player-board — ADR-0067`. Oracle bracket (`01d9459` → tip) per AR-BB2-5. Session record + prompt self-archive. Final commit: `docs: player board record — a stranger can watch the hospital breathe (ADR-0067)` Fences Src edits ONLY in: `components/sim-emit-hl7/src/ehrt/sim_emit_hl7/interface.clj` (export + header comment); `components/corpus/deps.edn` and any `projects/*/deps.edn`/development reconciliation `poly check` demands; the board renderer/sink namespace in `corpus` and its tests; `bases/cli` `core.clj` `play-command` + `help.clj` `cli-spec`'s play entry + their tests; `components/sim/test/ehrt/sim/run_test.clj` (the rider's one test, plus any additional offender ONLY under AR-BB2-R's lands-small clause). `v2_replay.clj` src frozen. Emitter frozen. No gate weakening. Frozen archives untouched except ADR-0067 + index. If the sink-map seam or the fold's entry shape resists the board's needs in a way that tempts a `v2_replay.clj` or pacer edit, STOP-AND-REPORT with the evidence — the fence moves by ruling, not by momentum. After landing: design channel verifies by fresh probe — including, for the first time, running the rewritten sibling test against the FRESH CLONE itself (the rider's whole point, verified by the mechanism it recommends) — then the arc close follows (rulings appends including the rider's standing law and the UX-arc-era corrections, state regeneration, budgets, rotation, tags), and the vendoring arc's design pass opens per the author's standing ratification.
