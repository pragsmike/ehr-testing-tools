2026-08-06 — ux fixes 3: the typo that succeeded — unknown flags rejected, near-misses named
Session prompt (design channel, 2026-08-06). Prior: ux fixes 2 landed and was design-channel-verified (`e222908`, ADR-0060). This session executes register row C-4, the audit's most user-hostile find: unknown flags are silently accepted — `--patiens 200` today produces a successful-looking run with the default patient count, the typo echoed into the manifest as if intended. After this session, every flag token must be declared for its verb or the command rejects it by name, with a near-miss suggestion when one exists. R30 ceremony. Read-first: register rows C-4 and C-2 (the rendering pattern to follow); `bases/cli/src/ehrt/cli/core.clj`'s option-parsing path in full BEFORE designing the validation point; the ADR-0013 spec-coverage infrastructure the register cites as already holding the per-verb flag data; ADR-0060's U4 near-miss helper as the suggestion-shape precedent.
Author rulings (record verbatim in ADR-0061)

1. AR-U3-0 (tag, standing ceremony). Annotated `stable-20260806-ux-fixes-2` at `e222908`, message `ux fixes 2 landed, design-channel-verified 2026-08-06 (ADR-0060)`; push; verify.
2. AR-U3-1 (the flag universe is the spec's). The set of valid flags per verb derives from `cli-spec` itself (per-verb declared flags plus any global set) — no hand-maintained duplicate list anywhere. If the ADR-0013 coverage infrastructure exposes this enumeration, reuse it; if not, a small derivation fn beside the spec — disclosed which.
3. AR-U3-2 (the validation). At the point where the parser has resolved the verb and is consuming option tokens: any token the PARSER ITSELF treats as a flag (not a value — see AR-U3-3) that is not in the verb's valid set → `result/error :unknown-flag {:flag <token> :verb <group+verb>}`, plus `:did-you-mean <declared-flag>` when a near match exists (nearest declared flag within Levenshtein distance 2; ties broken alphabetically; no match → no key). Exit 2 via the standard mapping; rendered through the same path as ADR-0060's categories (C-2's pattern). Parsing stops at the first unknown flag — one clear error beats a cascade.
4. AR-U3-3 (values are not flags — the one hazard). Flag VALUES may begin with `-` (negative offsets, date-like strings). The validator judges only tokens in flag position per the parser's own consumption logic — it must not reclassify a value. Read the parser first; if its structure conflates flag and value positions such that a local validator would misfire on any legal value in the spec, STOP-AND-REPORT with the case — restructuring the parser is not licensed here.
5. AR-U3-4 (red-first, three ways). Witnessed failing tests BEFORE the fix: (a) the founding-adjacent case — `--patiens 200` currently succeeds; the test asserts rejection naming `--patiens`, suggesting `--patients`, exit 2; (b) an unknown flag with no near match rejects without `:did-you-mean`; (c) the acceptance property — iterate the spec itself: every declared flag of every verb, supplied with a plausible value, parses without `:unknown-flag` (this test is the guard against the validator breaking a legal flag, and it must be spec-derived so future flags are covered automatically). Transcripts in ADR-0061.
6. AR-U3-5 (scope). No help.clj changes at all this session. No voice rewrite (session 4), no wrap (session 5). The oracle bracket must show all ELEVEN batches identical — flag validation touches no emitted byte; any change is STOP-AND-ESCALATE.

Steps
Step 0 — Preflight + tag. Cwd ext4; tip `e222908` or later-with-disclosure; full suite green baseline from workspace root; oracle pre-digest. Execute AR-U3-0.
Step 1 — Red, then the validator (AR-U3-1..4). Parser read; validation point chosen and recorded with reasoning; the three red transcripts; the fix; all green; `poly check` OK; live sanity check against `bin/ehrt` (the `--patiens` case) transcripted. ONE commit: `fix: unknown flags are rejected by name — the silent typo dies (ux fixes 3, AR-U3-1/2/3/4)`
Step 2 — ADR-0061 + record. Rulings verbatim; the parser-structure reading and validation-point rationale; the three reds; the live transcript; the derivation-fn disposition. Index line; Done pointer `- 2026-08-06 — ux-fixes-3 — ADR-0061`. Oracle bracket (`e222908` → tip) per AR-U3-5. Session record + prompt self-archive. Final commit: `docs: ux fixes 3 record — every flag is spoken for (ADR-0061)`
Fences
Src edits only in the option-parsing/validation path and its rendering; no parser restructuring (AR-U3-3's STOP governs); no help.clj; no gate weakening; tests co-landed in the owning bricks' trees. Frozen archives untouched (ADR-0061 + index sanctioned). If the spec's flag data turns out NOT to be derivable per-verb (contrary to the register's ADR-0013 reading), STOP-AND-REPORT — a hand-maintained flag list is a ruling the author must make, not a default.
After landing: design channel verifies by fresh probe; session 4 is the help-spec voice rewrite — the design channel's draft arrives for author ruling before that session is prompted; this landing's tag rides its Step 0 under standing ceremony.
