# 2026-08-08 — Fidelity riders — driving prompt

## Context

Repo `ehr-testing-tools`, ext4 clone (`~/src/ehr-testing-tools`).
Prompt's own stated HEAD: `42cd1e0` — confirmed live at session start,
no premise mismatch. The design brief this prompt names as Appendix A
was already present, untracked, at session start (a prior "encounterend
fix" session — recorded in this workspace's own memory system, not the
repo — had found the brief there, unlanded, and stopped rather than
proceed without its own prerequisite capture session; this session IS
that prerequisite).

## Prompt, verbatim

2026-08-08 — fidelity riders: the arc opens — the brief lands, the debts clear, the backlog grows two rows Session prompt (design channel, 2026-08-08; conventions and every claim below read at HEAD `42cd1e0` against a fresh public clone). OPENS THE FIDELITY ARC — the EncounterEnd interpreter design pass and its consequences, ruled by the author (design channel, 2026-08-08: "go" on the design brief; the arc's shape per ADR-0080's horizon note, where this item is the oldest ruled-fix candidate). A DOCS-ONLY opening: the design brief lands as a plan artifact (Appendix A, verbatim), the standing debts clear, and two author-added backlog rows record — the interpreter fix itself waits for the author's R1–R3 rulings on the brief, posed in the design channel in parallel with this session. R30 ceremony. Read-first: ADR-0080 (the horizon note this executes); the brief itself (Appendix A — read what you land); ADR-0071/0072's deferral sections (the incident record the brief cites); `.agents/plans/roadmap.md` Next section (where the two new rows land) and its Deferred two-module-blocker row (which gains a pointer to the brief). Author rulings (record verbatim in ADR-0081; `[A]` author-ruled, `[C]` channel-inferred)

1. AR-FR-0 `[A — tag law, case (ii); debt recorded in ADR-0080]`. Annotated `stable-20260807-quality-close` at `42cd1e0`, message `quality arc closed, design-channel-verified 2026-08-08 (ADR-0080)`; push; verify peeled ref. One line in this session's record discharges the transcript-only near-miss the design channel flagged: the quality-close session created this same tag prematurely, caught its own contradiction against ADR-0080's text and the tag law, and deleted it before it reached the record — disclosed in the design channel 2026-08-07, repo-recorded here (the transcript-witnessed rule, honored one session late by the first session able to).
2. AR-FR-1 `[A — the author's "go"; C for landing mechanics]` (the brief lands). `.agents/plans/2026-08-08-encounterend-design.md` lands EXACTLY as Appendix A states (the design channel authored it against upstream `State.java` at the pin and the live interpreter; a session-spotted factual error in it is STOP-AND-REPORT, never a silent fix — the brief is the design channel's to correct). The roadmap's Deferred two-module-blocker row gains one dated line pointing at the brief. ADR-0081 records the arc opening and the brief's three rulings — POSED by the brief and RULED by the author before this session ran (design channel, 2026-08-08, all three as recommended): R1 openness-only with disclosed divergence; R2 count-and-surface; R3 predict-and-confirm identical, any mover escalates. Record them verbatim; the fix session is LICENSED.
3. AR-FR-2 `[A — the author's backlog additions, design channel 2026-08-08, recorded verbatim]` (two Next rows). (a) Fixture relocation: move test fixtures from components (the `components/corpus/test-fixtures/v2/simhospital` tree and its v2-nist sibling named explicitly) to a top-level home so demos can use them — recording the design channel's flagged wrinkles in the row: the trees are NOTICE/PROVENANCE-hashed and `-text` protected, so the demos-front-door mechanic applies (same-commit `.gitattributes` moves, byte-witnessing, pointer stubs), and the live-path lint's blessed roots update with the move. (b) ADR references in user-facing documentation: remove or convert to clickable footnote links — recording the fork (strip-to-dev-docs vs footnotes; footnotes keep provenance) and the prerequisite (an inventory of the user path per ADR-0010's audience fork), unruled, awaiting its own design moment.
4. AR-FR-3 `[C — scope]` (fences). Docs-only: no `src/`, no `test/`, no config, no gates. NO interpreter work — the brief DESCRIBES the fix; nothing implements it before R1–R3 are ruled. NO fixture moves, NO footnote work — both are backlog rows today, not acts. Oracle: all TWENTY-SEVEN batches identical. Standing untracked files untouched.

Steps Step 0 — Preflight + tag. Cwd ext4; tip `42cd1e0` or later-with-disclosure; full suite green baseline; last-five CI disclosed; oracle pre-digest. Execute AR-FR-0, near-miss line included in the record. Step 1 — The brief + the rows (AR-FR-1/2). Commit: `docs: the fidelity arc opens — the EncounterEnd brief lands, two backlog rows record (fidelity riders, AR-FR-1/2)` Step 2 — ADR-0081 + record. Rulings verbatim; the pending-rulings note; index line; README count by `ls`; Done pointer `- 2026-08-08 — fidelity-riders — ADR-0081`. Oracle bracket (`42cd1e0` → tip). Successor tag debt IN THE ADR (`stable-20260808-fidelity-riders` at the closing tip). Session record + prompt self-archive; CI watched green. Final commit: `docs: fidelity riders record — the design is on the table, the rulings are the gate (ADR-0081)` Fences Everything AR-FR-3 names. `[C]` rulings conflicting with the live tree fix forward and disclose; `[A]` ones escalate. After landing: design channel verifies by fresh probe — then the fix session (already licensed by the recorded R1–R3 rulings) runs: openness tracking, the arm dispatch, the suppressed-end counter per R2, the blast-radius protocol per R3, anemia/colorectal red-first, and the payoff vendoring rider.

Appendix A — `.agents/plans/2026-08-08-encounterend-design.md`, verbatim: see that file directly (landed byte-for-byte by this session's own Step 1, `6cb4627`) — not duplicated a second time in this archive.
