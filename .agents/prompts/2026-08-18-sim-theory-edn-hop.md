# Archived prompt: sim-theory-edn-hop (2026-08-18)

Session prompt -- sim-theory.edn is the single source of truth:
`sim-theory-equations.txt` becomes GENERATED, the head hop joins the
registered chain; rider: `:execute`'s two event-contract laws --
ADR-0152

## Context

Claude Code under R30 in ehr-testing-tools. HEAD at handoff: bde5f37
(ADR-0151 addendum; tree clean; CI green at 8d4fac2 and bde5f37 per
addendum; last tag `stable-20260818-reason-nil-drop` @8d4fac2, no tag
owed). Roadmap row `roadmap.md#sim-theory-edn-hop` (OPEN PRIORITY 3):
"`sim-theory.edn` -> `sim-theory-equations.txt` is a hand derivation at
the head of an otherwise registered chain, so the `.edn` can drift while
every downstream artifact regenerates byte-perfectly from the stale half
and CI stays green. Open question for the author: translator, or
checked-in agreement gate? ADR-0139 finding C-1 holds the evidence and
the contradicting live sentence." RULED: translator; `.edn` single
source of truth; the equations file STAYS (committed, generated).

## Channel probe at bde5f37 (re-derive)

* `components/sim/docs/sim-theory.edn` (155 lines, 12 stages + external
  stages, `:schema-version 1`) says of itself: "keeps that repo's
  pipeline.edn schema shape exactly, so its equation->diagram machinery
  consumes this file unchanged." `ehrt.docs-tooling.pipeline/
  pipeline->equations-text` and `write-equations-txt!` (`pipeline.clj:92,
  :141`, `-X`-invokable with `:pipeline-edn`/`:out`) ARE that machinery
  -- `make pipeline` (Makefile:74-79) already runs them on
  `pipeline.edn`. The translator is a second call site, not new code.
* `sim-theory-equations.txt` (46 lines, 13 equation lines, blank line
  between each, two `# planned:` comments at :35/:38, an 18-line hand
  header) says "no Clojure translator exists here ... edit both
  together" and "LINE COUNT IS LOAD-BEARING: `%% Arrow N` numbering
  derives from THIS file's line numbering". The translator emits one
  line per equation with NO blank lines and a `# planned:` line
  IMMEDIATELY before its stage. So the generated file's layout WILL
  differ from the hand file's; arrow numbers in `.mermaid` (and the
  `.md`'s embedded block) renumber ONCE. Predict the exact new numbering
  before regenerating (count lines of the translator's output).
* Downstream: `make sim-theory` (Makefile:129-138) python converter ->
  `.mermaid` -> awk splice into `sim-theory-diagram.md`; both are on
  CI's freshness list (`test.yml:138-139`); the equations file is NOT
  (it is hand-maintained today). `docsgen` depends on `sim-theory`
  (:249).
* Misdescription: `components/sim-trajectory/docs/trajectory-computation.
  md:249-250` says the diagram is "mechanically regenerated from
  `sim-theory.edn`" -- false today, TRUE after this session; leave the
  sentence, note it in the ADR as made-true.
* Links to the equations file: `.agents/**` records/prompts/plans only
  (history) and the roadmap row; no `docs/**` reader link (`git grep
  sim-theory-equations -- ':!Makefile'` at Step 0 to confirm).
* Header helper for generated files: `docsgen.clj:58` (`<!-- GENERATED
  by `make X` from Y -- do not hand-edit -->`) is HTML-comment shaped;
  the equations file's comment syntax is `#`. Emit a `#`-prefixed
  equivalent (the converter ignores `#` lines) -- three or four lines,
  content: generated by `make sim-theory` from `sim-theory.edn`, do not
  hand-edit, edit the `.edn` and regenerate. Say in the ADR why the
  helper was not reused verbatim (comment syntax), or generalise the
  helper with a comment-prefix arg -- your call, say which.
* Rider evidence: `:execute` node `:laws` has 3 entries (determinism,
  decide/evolve, order-profiles/provider-pool catalytics); none states
  the event-log contract. ADR-0141 states two run-level properties: the
  ground-truth log is a vector of `Event` valid at `schema-version`
  (`valid-log?`/`event-schema-test`), and `:t` is monotone within a run
  as a RUN-level property (`run-t-monotone?`, ADR-0141:89-90), never
  per-event, no run-boundary marker. Laws do not render into equations,
  so the rider does not move the diagram -- assert that.

## Read first

1. `sim-theory.edn` header + `:execute` node; `sim-theory-equations.txt`
   whole; `sim-theory-diagram.md` header + embedded block; `.mermaid`.
2. `pipeline.clj` :40-160; `pipeline_test.clj`; `docsgen.clj` :40-70;
   `traces_fresh_test.clj` claims (c)/(d) (the make-graph and CI-list
   assertions ADR-0149 established -- extend, don't duplicate).
3. Makefile :74-79, :113-140, :249; `test.yml` :106-145.
4. ADR-0139 C-1 (:301-323), ADR-0136 (registered derivations), ADR-0135
   (arrow-renumbering precedent, `.agents/prompts/2026-08-15-review-3-
   fix-a-register-derivations.md` :26,:44), ADR-0141 (:80-95).
5. `rulings.md#R-session-verifies-ci-via-gh`, `#R-full-suite-before-
   push`, `#R-red-pushed-with-green`, `#R-law-surface-propagation`,
   `#R-register-hygiene-at-close`; build-session skill; `:docs` reading
   set.

## Author rulings, verbatim

* "(a) edn single-source-of-truth, keep equations. Rider." (2026-08-18)
  -- translator; `sim-theory-equations.txt` stays committed as a
  GENERATED file; the `:execute` two-laws rider rides this session.
* Tag: no tag owed at Step 0. This session's own close tag: pay
  in-session if its tip run concludes success while open, else next
  Step 0 -- say which.

## Step 0

Fresh clone, tip bde5f37; `bin/preflight`; baseline `make test`
unpiped, MAKE_EXIT captured, reconcile vs ADR-0151's 346 blocks / 3,928
tests / 17,648 assertions; `poly check`; reading sets vs baselines.
Predictions into the ADR before any edit: (a) the translator's output
for `sim-theory.edn` -- run `write-equations-txt!` to `target/`, `diff`
against the committed file, classify every hunk (header / blank lines /
`# planned:` placement / equation text). If ANY equation LINE differs in
content (not position), STOP -- that is `.edn`/`.txt` drift, the very
defect the row names, and the author decides which half is truth. (b)
the `%% Arrow N` renumbering: line numbers of each equation in the
generated file vs the hand file, hence the predicted `.mermaid` diff.
(c) `docsgen` freshness population before/after. (d) `sim-theory`
wall-clock before/after (adds one `clojure -X`, ~JVM start).

## Step 1 -- red

Tests: (i) the generated equations file equals `pipeline->equations-text`
of `sim-theory.edn` byte-for-byte after the header (a `docs-tooling`
test reading both from the tree); (ii) `sim-theory-equations.txt` is in
CI's freshness list and `make sim-theory` writes it (extend ADR-0149's
make-graph/CI-list assertions in the same shape); (iii) rider red: the
`:execute` node's `:laws` contains both event-contract laws (a test
loading the `.edn` -- there is a Pipeline Malli, use it -- and asserting
substrings you will write verbatim in Step 3). Commit: "test: red --
sim-theory-equations.txt is generated from sim-theory.edn and freshness-
diffed; :execute states the event-contract laws (ADR-0152)"

## Step 2 -- green (translator)

Makefile `sim-theory`: first step `clojure -X:dev ehrt.docs-tooling.
pipeline/write-equations-txt! :pipeline-edn '"components/sim/docs/sim-
theory.edn"' :out '"components/sim/docs/sim-theory-equations.txt"'` with
the `#` GENERATED header prepended (in the fn via a `:header` option, or
a wrapper -- say which; `pipeline.edn`'s own `target/` output must be
byte-unchanged -- assert). Then the existing converter/splice steps
unchanged. `test.yml`: add the equations file to the freshness list;
comment updated. Run `make sim-theory`: the equations file, `.mermaid`,
and `.md` block regenerate; actual diff vs predictions (a)/(b) exact or
STOP. Rewrite the `sim-theory-diagram.md` header's provenance line iff
it names the equations file as hand-derived. `sim-theory.edn` header:
replace "so its equation->diagram machinery consumes this file
unchanged" claim with the now-true "and `make sim-theory` renders it"
(dated). Full `make test`; push red+green together. Commit: "feat:
sim-theory-equations.txt GENERATED from sim-theory.edn by `make
sim-theory`; head hop registered and freshness-diffed; arrows renumbered
once, disclosed (ADR-0152, C-1)"

## Step 3 -- rider (edn content commit)

`:execute` `:laws` gains two entries, verbatim as tested in Step 1:
"event-log contract (ADR-0141): the ground-truth log is a vector of
Event valid at `ehrt.sim-engine.event-schema/schema-version` (stamped in
every manifest as `:event-schema-version`); additive changes keep the
version, anything else bumps it (event-schema-test)"; "`:t` is monotone
within a run as a RUN-level property (`run-t-monotone?`), never a
per-event constraint; the log carries no run-boundary marker -- a run IS
the vector". Run `make sim-theory`: equations/`.mermaid`/`.md`
byte-unchanged (laws do not render) -- assert with `git status`. Commit:
"docs: sim-theory.edn :execute states the two ADR-0141 event-contract
laws (rider, ADR-0152)"

## Step 4 -- register hygiene

Roadmap: `#sim-theory-edn-hop` -> CLOSED under `## Done`; review-4
watch-list row C-1 marked closed with the ADR (find where the watch-list
lives -- `.agents/plans/2026-08-15-repo-review-findings.md` or the
review-4 row -- and edit the one the row cites). ADR-0139 C-1: dated
"closed by ADR-0152" addendum line, not an edit.

## Close (self-archive FIRST)

Archive to `.agents/prompts/2026-08-18-sim-theory-edn-hop.md`; open the
session record; then ADR-0152 (predictions a-d vs actuals; the arrow
renumbering table old->new; header decision; rider text), roadmap,
session record with `gh run view` id/conclusion, full `make test`
reconciled per namespace vs Step 0, `bin/post-push-verify`, tag per
ruling. Commit: "docs: ADR-0152 -- sim-theory head hop registered,
close"

## Fences

Files: `Makefile` `sim-theory` target only; `test.yml` freshness list +
comment; `pipeline.clj` (header option) + tests; `sim-theory-equations.
txt` (now generated), `.mermaid`, `sim-theory-diagram.md` (regenerated +
header line), `sim-theory.edn` (header sentence + `:execute` laws ONLY
-- no stage, resource, or equation-bearing key changes); NO change to
`pipeline.edn` or `docs/dev/pipeline.md` (assert byte-identical); NO
python converter change; oracle IDENTICAL (docs-only; assert with one
`bin/regression-oracle` run); no test deletions; exit codes unpiped;
anchored register edits; R-RP. READ-BACK names the fence: per
regenerated artifact, bytes before/after and class (header /
layout+renumber / rider-none) -- an equation whose TEXT changed is a
STOP.
