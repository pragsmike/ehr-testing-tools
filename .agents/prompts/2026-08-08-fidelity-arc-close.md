## Appendix A — the original close prompt, verbatim

2026-08-08 — fidelity arc close: the interpreter matches upstream, the misdiagnosis is corrected, the oldest deferral is home

Session prompt (design channel, 2026-08-08; conventions and every claim below read at HEAD `a13bc0b` against a fresh public clone). This session CLOSES the fidelity arc per the standing close pattern (ADR-0080/0074 the models): rulings appends, the dependency-review cadence, `state.md` regeneration, budget re-derivation, Done rotation, and the closing ADR. Docs-only; anything new found is intake. The arc being closed, three sessions: fidelity riders (ADR-0081, `c2bcb67` — the brief landed, R1–R3 ruled, two author backlog rows); encounterend fix (ADR-0082, `82d1753` — the five arms, the pure openness fold, the counted no-op; the blast-radius protocol's STOP-AND-REPORT caught hypothyroidism's five shipped dangling ends, the author's trace-then-license ruling found the truncation layer absorbing them, and the licensed mover held byte-identical at oracle granularity anyway); fidelity payoff (ADR-0083, `a13bc0b` — anemia vendored after two arcs deferred, its suppressed-end counts pinned empirically; colorectal's misdiagnosis corrected by erratum, its true blocker named and undiagnosed). R30 ceremony. Read-first: `notes/adr/0080-quality-arc-close.md` (the pattern); `.agents/rulings.md` in full; `.agents/state.md` in full (regenerated at the quality close, now FIVE landed sessions stale — wrong in at least: the module inventory (24 now), the oracle root count (28), the interpreter's EncounterEnd semantics and counter, the census artifact set, and the Deferred picture); `.agents/reading-sets.edn`; ADR-0081–0083 in full; `.agents/plans/roadmap.md` (Done holds four pointers to rotate; the reclassified colorectal row). Author rulings (record verbatim in ADR-0084; `[A]` author-ruled, `[C]` channel-inferred)

1. AR-FC-0 `[A — tag law, case (ii); debt recorded in ADR-0083]`. Annotated `stable-20260808-fidelity-payoff` at `a13bc0b`, message `fidelity payoff landed, design-channel-verified 2026-08-08 (ADR-0083)`; push; verify peeled ref.
2. AR-FC-1 `[C — both appends consolidate this arc's executed discipline; the author may strike either]` (rulings appends). Under "From the fidelity arc (ADR-0081–0084)": (a) semantics changes are predicted before they are made, standing — an interpreter/engine/emitter semantics change runs a blast-radius probe over every oracle root FIRST, lands a per-root identical-or-moves prediction, and any mover is STOP-AND-REPORT for an explicit license naming that mover alone (ADR-0082's own executed protocol, R3 as ruled and exercised — including the trace-then-license resolution the author ruled when the probe fired). (b) plausible-by-adjacency is not a diagnosis, standing — a defect attributed to a shared mechanism without a direct probe of the failing artifact is `[unverified]` until the probe exists; ADR-0072's colorectal diagnosis was inference from a shared submodule, overturned by the first trajectory scan (ADR-0082/0083's own erratum chain).
3. AR-FC-2 `[A — standing cadence]`. `clojure -M:poly libs :outdated`; dated report in ADR-0084; no edit follows.
4. AR-FC-3 `[A — state.md's regeneration contract]` (state regenerates). Every `[V]` claim probe-backed THIS session; skeleton preserved; content updates at minimum: the interpreter's EncounterEnd posture (the five arms, the A5 no-op, `open-encounter-index`, the `:suppressed-encounter-ends` counter surfacing in census rows and walk output, the disclosed wellness divergence, the retired openness-blind indexer); the module inventory (24 by fresh `ls`, anemia's two-arc story one sentence); the oracle posture (28 roots, the batch mechanics unchanged, hypothyroidism's licensed-mover-that-held story one sentence); the census artifact set (the `encounterend` label beside `substance`); the deferred-modules picture (injuries on broken_jaw; colorectal on its TRUE blocker with the violation classes named, undiagnosed; the zero-substance families; wellness-encounters); the truncation-layer absorbed-error finding (recorded as a documented mechanism with its new significance, not a defect chased); the flake-soak count by fresh CI enumeration; every count fresh. Regeneration table in ADR-0084; wrong-in-kind is STOP-AND-REPORT.
5. AR-FC-4 `[A — standing budget rule]` (budgets). Re-derive every reading set whose member paths changed across `42cd1e0..HEAD` (`state.md` regenerates here; `rulings.md` changed; enumerate the rest by `git log --name-only`), AFTER the regeneration lands.
6. AR-FC-5 `[C — rotation mechanics per the five prior closes]` (rotation). Done holds four pointers (0080–0083, design-channel probed at `a13bc0b`). Rotate: ADR-0080's pointer joins the attic's quality-arc section with the dated leftover note; ADR-0081–0083 rotate under `## Fidelity arc — closed 2026-08-08 (ADR-0081–0084)`. ADR-0084's pointer lands as the sole current entry (Step 3, sentinel-avoidance). Now refreshes to one line. Relocation-not-rewrite.
7. AR-FC-6 `[A/C]` — as the original states (ruling 7): ADR-0084 in full — the arc narrative, the cited intake list (colorectal's investigation at the top; the pairing-as-data second-carry said plainly), the Externals, the successor tag debt IN THE ADR (`stable-20260808-fidelity-close` at this session's closing tip), the horizon note verbatim — PLUS the two-session deviation record and the interruption-pattern note above.

Steps Step 0 — Preflight + tag. Cwd ext4; tip `a13bc0b` or later-with-disclosure; full suite green baseline; last-five CI disclosed; oracle pre-digest (28). Execute AR-FC-0. Step 1 — Appends + cadence (AR-FC-1/2). Commit: `docs: the fidelity arc's law is appended — predictions precede semantics, adjacency is not diagnosis (arc close, AR-FC-1/2)` Step 2 — State + budgets + rotation (AR-FC-3/4/5). Full suite green through the rotation; the staleness tripwire governs the citation-vs-file landing order (the ADR-0080 sequencing precedent). Commit: `docs: the state regenerates, the budgets re-derive, six arcs rest in the attic (arc close, AR-FC-3/4/5)` Step 3 — ADR-0084 + record (AR-FC-2's fresh report; AR-FC-6). Index line; README count by `ls`; sole Done pointer. Oracle bracket (`a13bc0b` → tip, spanning BOTH sessions' commits): all TWENTY-EIGHT batches identical — docs-only. Session record (two-session close disclosed) + prompt self-archive (Appendix A + this preamble, one file); CI watched green. Final commit: `docs: the fidelity arc closes — the interpreter tells upstream's truth, and the record tells its own (ADR-0084)` Fences Unchanged from Appendix A, in full. Additionally: Steps 0–1's landed work is READ-ONLY this session — verified, never redone. After landing: design channel verifies by fresh probe — sampled re-probes of the regenerated state.md per its contract — then the next arc's opening session tags `stable-20260808-fidelity-close` at this tip under standing ceremony, and the horizon is the author's to rule.

Author rulings (record verbatim in ADR-0084; `[A]` author-ruled, `[C]` channel-inferred)

1. AR-FC-0 `[A — tag law, case (ii); debt recorded in ADR-0083]`. Annotated `stable-20260808-fidelity-payoff` at `a13bc0b`, message `fidelity payoff landed, design-channel-verified 2026-08-08 (ADR-0083)`; push; verify peeled ref.
2. AR-FC-1 `[C — both appends consolidate this arc's executed discipline; the author may strike either]` (rulings appends). Under "From the fidelity arc (ADR-0081–0084)": (a) semantics changes are predicted before they are made, standing — an interpreter/engine/emitter semantics change runs a blast-radius probe over every oracle root FIRST, lands a per-root identical-or-moves prediction, and any mover is STOP-AND-REPORT for an explicit license naming that mover alone (ADR-0082's own executed protocol, R3 as ruled and exercised — including the trace-then-license resolution the author ruled when the probe fired). (b) plausible-by-adjacency is not a diagnosis, standing — a defect attributed to a shared mechanism without a direct probe of the failing artifact is `[unverified]` until the probe exists; ADR-0072's colorectal diagnosis was inference from a shared submodule, overturned by the first trajectory scan (ADR-0082/0083's own erratum chain).
3. AR-FC-2 `[A — standing cadence]`. `clojure -M:poly libs :outdated`; dated report in ADR-0084; no edit follows.
4. AR-FC-3 `[A — state.md's regeneration contract]` (state regenerates). Every `[V]` claim probe-backed THIS session; skeleton preserved; content updates at minimum: the interpreter's EncounterEnd posture (the five arms, the A5 no-op, `open-encounter-index`, the `:suppressed-encounter-ends` counter surfacing in census rows and walk output, the disclosed wellness divergence, the retired openness-blind indexer); the module inventory (24 by fresh `ls`, anemia's two-arc story one sentence); the oracle posture (28 roots, the batch mechanics unchanged, hypothyroidism's licensed-mover-that-held story one sentence); the census artifact set (the `encounterend` label beside `substance`); the deferred-modules picture (injuries on broken_jaw; colorectal on its TRUE blocker with the violation classes named, undiagnosed; the zero-substance families; wellness-encounters); the truncation-layer absorbed-error finding (recorded as a documented mechanism with its new significance, not a defect chased); the flake-soak count by fresh CI enumeration; every count fresh. Regeneration table in ADR-0084; wrong-in-kind is STOP-AND-REPORT.
5. AR-FC-4 `[A — standing budget rule]` (budgets). Re-derive every reading set whose member paths changed across `42cd1e0..HEAD` (`state.md` regenerates here; `rulings.md` changed; enumerate the rest by `git log --name-only`), AFTER the regeneration lands.
6. AR-FC-5 `[C — rotation mechanics per the five prior closes]` (rotation). Done holds four pointers (0080–0083, design-channel probed at `a13bc0b`). Rotate: ADR-0080's pointer joins the attic's quality-arc section with the dated leftover note; ADR-0081–0083 rotate under `## Fidelity arc — closed 2026-08-08 (ADR-0081–0084)`. ADR-0084's pointer lands as the sole current entry (Step 3, sentinel-avoidance). Now refreshes to one line. Relocation-not-rewrite.
7. AR-FC-6 `[A for cited items; C for composition]` (the closing ADR). `notes/adr/0084-fidelity-arc-close.md`: rulings verbatim; the arc summary one line per session with ADRs and tips; the arc narrative — the first interpreter semantics change since the waves, predicted before it was made, licensed where it moved, absorbed where the tree already hid it, and honest about the diagnosis it overturned; the intake list, each cited: colorectal's true blocker (the `:clinical-content-only-when-admitted` + early `:discharge-follows-admission` violations downstream of clean trajectories — needs its own investigation session, the arc's top handoff), the truncation-layer absorbed-error (documented mechanism, newly load-bearing, a candidate for a future fidelity pass), the review-2 watch-list accumulating (the sibling-flake soak count at close, the census closure-count refinement, wellness-encounters restated so it cannot slide), vendoring batch 4 (the veteran family), Wave E, the author's two backlog rows (fixture relocation; ADR-footnotes, both unruled), and the pairing-as-data design pass (ruled in, still paused on its four unanswered shape questions (granularity, the storefront fixture, the taxonomy snapshot, mutation-adequacy as consumer); say plainly this is its second close carried since the ruling); the open Externals restated; this close's own successor tag debt IN THE ADR (`stable-20260808-fidelity-close` at the closing tip); and the horizon note: "The horizon, for the author's ruling: colorectal's investigation, the pairing-as-data registry (four questions from a ruling), vendoring batch 4, Wave E's register, the fixture-relocation and ADR-footnote rows, publish-prep (F-5/F-6 + F-7), review 2 on cadence. `sim-emit-cda` on its trigger."

Steps Step 0 — Preflight + tag. Cwd ext4; tip `a13bc0b` or later-with-disclosure; full suite green baseline; last-five CI disclosed; oracle pre-digest (28). Execute AR-FC-0. Step 1 — Appends + cadence (AR-FC-1/2). Commit: `docs: the fidelity arc's law is appended — predictions precede semantics, adjacency is not diagnosis (arc close, AR-FC-1/2)` Step 2 — State + budgets + rotation (AR-FC-3/4/5). Full suite green through the rotation; the staleness tripwire governs the citation-vs-file landing order (the ADR-0080 sequencing precedent). Commit: `docs: the state regenerates, the budgets re-derive, six arcs rest in the attic (arc close, AR-FC-3/4/5)` Step 3 — ADR-0084 + record. Everything per AR-FC-6; index line; README count by `ls`; sole Done pointer. Oracle bracket (`a13bc0b` → tip): all TWENTY-EIGHT batches identical — docs-only; any change is STOP-AND-ESCALATE. Session record + prompt self-archive; CI watched green. Final commit: `docs: the fidelity arc closes — the interpreter tells upstream's truth, and the record tells its own (ADR-0084)`

Fences

Docs-only: no `src/`, no `test/`, no config, no gates. The horizon note restates and names; it decides nothing. Frozen archives untouched (ADR-0084 + index + sanctioned attic appends). Wrong-in-kind regeneration discrepancies are STOP-AND-REPORT. `[C]` rulings conflicting with the live tree fix forward and disclose; `[A]` ones escalate. After landing: design channel verifies by fresh probe — sampled re-probes of the regenerated state.md per its contract — then the next arc's opening session tags `stable-20260808-fidelity-close` at this tip under standing ceremony, and the horizon is the author's to rule.

## Resumption preamble (Code session 2, verbatim)

2026-08-08 — fidelity arc close, RESUMPTION: the same close, second Code session — Steps 2–3 only

Session prompt (design channel, 2026-08-08). The fidelity arc close (ADR-0084) began in a prior Claude Code session KILLED MID-CEREMONY by an infrastructure-level request block after Step 1 landed — the SECOND such interruption, and the second to hit an arc close specifically (the first: the vendoring close, ADR-0074, resolved by the same resumption pattern now being exercised again). The design channel has verified the seam by fresh public-clone probe: tip `e7961b9` (Step 1's commit — the rulings appends + cadence commit, pushed, CI-watched green by the dead session); tag `stable-20260808-fidelity-payoff` present and peeled-verified at `a13bc0b` (AR-FC-0 EXECUTED — verify, disclose, never re-create); `.agents/rulings.md` carries "From the fidelity arc (ADR-0081–0084)" (AR-FC-1 EXECUTED — verify both appends read as the original specifies; discrepancy is STOP-AND-REPORT); `.agents/state.md` still the quality-close regeneration (AR-FC-3 NOT started); roadmap Done holds four unrotated pointers (AR-FC-5 NOT started); no ADR-0084. The dead session's `libs :outdated` output is transcript-only and lost — RE-RUN fresh (AR-FC-2 re-executed; the report landing in ADR-0084 must be a real run's output). This session completes Steps 2–3 of the original prompt, restated below with its full text as Appendix A; the prompt self-archive is Appendix A followed by this preamble, one file, for the pairing gate. ADR-0084's deviation record and the session record BOTH disclose the two-session close plainly — AND note the pattern: two of the last three arc closes were killed by the same infrastructure block class; the design channel's `[C]` intake suggestion (record in ADR-0084's intake, decide nothing): future close prompts may pre-split into two smaller sessions by design, reducing exposure. R30 ceremony. Read-first: unchanged from Appendix A, plus `git log -3` (the seam), the new rulings section (what Step 1 landed), and any working-tree residue (disclose and clear scratch; STOP-AND-REPORT anything resembling uncommitted repo work).

Rulings in force (verbatim from the original; tags unchanged)

* AR-FC-0 `[A]` — EXECUTED. Verify the tag at `a13bc0b`, peeled; disclose; do not re-create.
* AR-FC-1 `[C]` — EXECUTED. Verify both appends against the original's text; discrepancy is STOP-AND-REPORT, not a re-edit.
* AR-FC-2 `[A]` — RE-RUN this session: `clojure -M:poly libs :outdated`; dated report in ADR-0084; no edit follows.
* AR-FC-3 `[A]` — as the original states in full (Appendix A ruling 4): state.md regenerates, every `[V]` claim probe-backed THIS session; regeneration table in ADR-0084; wrong-in-kind is STOP-AND-REPORT.
* AR-FC-4 `[A]` — as the original states (ruling 5): budgets re-derive across `42cd1e0..HEAD` after the regeneration lands.
* AR-FC-5 `[C]` — as the original states (ruling 6): the four-pointer rotation, the fidelity attic header, sentinel-avoidance, relocation-not-rewrite.
* AR-FC-6 `[A/C]` — as the original states (ruling 7): ADR-0084 in full — the arc narrative, the cited intake list (colorectal's investigation at the top; the pairing-as-data second-carry said plainly), the Externals, the successor tag debt IN THE ADR (`stable-20260808-fidelity-close` at this session's closing tip), the horizon note verbatim — PLUS the two-session deviation record and the interruption-pattern note above.

Steps

Step 2 — State + budgets + rotation (AR-FC-3/4/5). Full suite green through the rotation; the staleness tripwire governs the citation-vs-file landing order (the ADR-0080 sequencing precedent). Commit: `docs: the state regenerates, the budgets re-derive, six arcs rest in the attic (arc close, AR-FC-3/4/5)`

Step 3 — ADR-0084 + record (AR-FC-2's fresh report; AR-FC-6). Index line; README count by `ls`; sole Done pointer. Oracle bracket (`a13bc0b` → tip, spanning BOTH sessions' commits): all TWENTY-EIGHT batches identical — docs-only. Session record (two-session close disclosed) + prompt self-archive (Appendix A + this preamble, one file); CI watched green. Final commit: `docs: the fidelity arc closes — the interpreter tells upstream's truth, and the record tells its own (ADR-0084)`

Fences

Unchanged from Appendix A, in full. Additionally: Steps 0–1's landed work is READ-ONLY this session — verified, never redone. After landing: design channel verifies by fresh probe — sampled re-probes of the regenerated state.md per its contract — then the next arc's opening session tags `stable-20260808-fidelity-close` at this tip under standing ceremony, and the horizon is the author's to rule.
