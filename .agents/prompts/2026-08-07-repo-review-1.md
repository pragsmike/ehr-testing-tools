# 2026-08-07 — Repo review 1 — driving prompt

## Context

Repo `ehr-testing-tools`, ext4 clone (`~/src/ehr-testing-tools`).
Prompt's own stated HEAD: `89c0d24` — confirmed live at session start,
no premise mismatch (see this date's own session record).

## Prompt, verbatim

2026-08-07 — repo review 1: the first assessment — every lens, nothing moved Session prompt (design channel, 2026-08-07; conventions and every claim below read at HEAD `89c0d24` against a fresh public clone). Second session of the quality-review arc: the FIRST RUN of the `repo-review` skill — its steps 1 through 4 ONLY (history scan, probe battery, dated register, scoreboard). Steps 5–7 (the plan's rulings, the fix sessions, the close) follow AFTER the author rules on the register — this session is a SURVEY, and the survey discipline is ADR-0049/0058's: examined, registered, NOTHING MOVED. Invoke the `repo-review` skill and follow it as authority; this prompt adds only the session-specific scoping below. R30 ceremony. Read-first: `.agents/skills/repo-review/SKILL.md` in full (the instrument — landed ADR-0076, byte-verified against its design-channel source); the audit-register format exemplars (`.agents/plans/2026-08-05-alignment-audit-findings.md`, `2026-08-06-ux-audit-findings.md`); every arc-close ADR (0047, 0055, 0064, 0068, 0074) and every session record's "Deviations, disclosed" section landed since the repo's start — the DISTILLED incident record; the full per-ADR read is NOT required (76 files is the wrong unit; the closes and deviations are where incidents live), but any incident a close cites is chased to its own ADR; ADR-0075/0076 (the freshest incident cluster and the seeded watch-list). Author rulings (record verbatim in ADR-0077; `[A]` author-ruled, `[C]` channel-inferred)

1. AR-RR-0 `[A — tag law, case (ii); debt recorded in ADR-0076]`. Annotated `stable-20260807-quality-riders` at `89c0d24`, message `quality riders landed, design-channel-verified 2026-08-07 (ADR-0076)`; push; verify peeled ref.
2. AR-RR-1 `[A — the author's review mandate, design channel 2026-08-07; C for scoping]` (the survey). Run the skill's steps 1–4 in full. First-run specifics: the history window is the repo's whole life, read through the closes-and-deviations lens above; the scoreboard has NO prior column — record "first assessment" explicitly so the second run has its baseline. The register lands at `.agents/plans/2026-08-07-repo-review-findings.md` in the established audit-register format: one row per finding — dimension, evidence (gathered by the mechanism it recommends — re-derive, re-hash, re-run, never re-read), severity, PROPOSED disposition (fix-session candidate / ruling-needed / close-as-fine / intake). Clean probes are recorded too — a green probe is inheritance, not noise.
3. AR-RR-2 `[C — the seeded watch-list; the probe battery starts here but does not end here]`. Items the record already queues for specific dimensions, to be probed not presumed: the sibling-flake SOAK status (D3 — CI runs since `9cc3563`, fires vs the one-in-five-to-seven rate ADR-0075 recorded); the engine property test's unpinned generator seed (D3/D6 — ADR-0076's own new finding; ALSO generalize the probe: enumerate EVERY `defspec` repo-wide and record which pin or log their seeds — an irreproducible CI failure is a failure you cannot chase); the two-module EncounterEnd gap (D6/intake, ADR-0071/0072); the census's JSON-only closure count and three-seed power (D6, ADR-0070); carried-item aging (D7 — pairing-as-data is now FOUR closes old; the vital-sign/Wave-E cluster, wellness-encounters, batch 4, the publish-prep Externals — age each explicitly); the laws-to-gates map (D2 — every standing ruling in `.agents/rulings.md` mapped to its enforcing test or named gateless); the CI-only and author-machine-only check inventory (D2/D3 — the synthea checkout, the NIST mirror, anything else); the operators-registry shared-mutable-state pattern (D3 — ADR-0075 found one polluter class; enumerate other shared atoms tests mutate).
4. AR-RR-3 `[C — probe mechanics]`. Dimension 8's fence-execution probe runs every LIVE doc command fence and the CLI error matrix against the built `bin/ehrt` from workspace root — outputs into `out/` scratch, cleaned or disclosed after; the two-commands-to-demo path runs for real. Dimension 1's count/hash/path re-derivations cover `state.md`, both NOTICEs (modules and the v2-nist fixture), `reading-sets.edn`, and the ADR/README counts. Dimension 5 regenerates every derived doc and diffs. Long probes may run while the register drafts — but every row's evidence is captured before the register lands.
5. AR-RR-4 `[C — scope]` (fences). SURVEY ONLY: no src, no test, no doc-content fixes, no gate changes, no flake-hardening, no tidying-in-passing however small — a fix-shaped temptation becomes a register row with a proposed disposition, exactly the discipline that made the alignment and UX audits trustworthy. The ONLY tree changes this session: the register file itself, ADR-0077 + indexes, the Done pointer, the session record + prompt archive. The oracle bracket must show all TWENTY-SEVEN batches identical. Standing untracked files untouched.

Steps Step 0 — Preflight + tag. Cwd ext4; tip `89c0d24` or later-with-disclosure; full suite baseline (disclose any intermittent fire — it is itself D3 evidence, captured not chased); last-five CI conclusions disclosed; oracle pre-digest. Execute AR-RR-0. Step 1 — The survey (AR-RR-1/2/3). History scan, then the full probe battery, working notes in scratch. NO commit — the register lands whole in Step 2, the ADR-0049 pattern (survey artifact lands once, complete). Step 2 — The register (AR-RR-1). The dated findings register + first-assessment scoreboard land. Commit: `docs: the first assessment lands — eight lenses, every probe recorded, nothing moved (repo review 1, AR-RR-1)` Step 3 — ADR-0077 + record. Rulings verbatim; the probe-battery summary with per-dimension row counts; the scoreboard restated; index line; README count by `ls`; Done pointer `- 2026-08-07 — repo-review-1 — ADR-0077`. Oracle bracket (`89c0d24` → tip). Successor tag debt IN THE ADR (`stable-20260807-repo-review-1` at the closing tip). Session record + prompt self-archive. Final commit: `docs: repo review 1 record — the repo examined itself and wrote down what it saw (ADR-0077)` Fences Everything AR-RR-4 names. The register's dispositions are PROPOSALS — the author rules in the design channel after landing; nothing in this session anticipates those rulings. `[C]` rulings conflicting with the live tree fix forward and disclose; `[A]` ones escalate. After landing: design channel verifies by fresh probe — sampled re-derivations of register rows by their own stated mechanisms — then distills the register into the mitigation plan for the author's rulings (the skill's step 5, design-channel work), and the ruled fix sessions follow.
