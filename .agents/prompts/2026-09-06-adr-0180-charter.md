# Session prompt — ADR-0180: indexes ride the fold — the generate-quadratic program charter — 2026-09-06

Archived verbatim. The session it drove is
[`../session-records/2026-09-06-adr-0180-charter.md`](../session-records/2026-09-06-adr-0180-charter.md).

---

Session: ADR-0180 -- indexes ride the fold: the generate-quadratic program charter -- 2026-09-06

Context: the performance measurement (.agents/plans/2026-09-05-performance-measurement/
measurements.md:216-307) put 70% of top-of-decade generate in four sites of one shape --
a per-event scan over a population that is only ever appended to: waiting-boarder 30.5%,
run/select-person 20.0% (on no list), occupancy-board 17.1%, last-uncancelled-index 10.8%.
Check is linear and 5% of the pair. fold/apply-events already carries four indexes
maintained per event (:reinstate-index :citation-index :registration-index :bed-index,
fold.clj:540-600); the fix for all four sites is that carrier. This session writes the
ADR that charters the program and its rows; it enacts NO site. Fresh clone at 3d9f5c24
or later. WSL only. No sub-agents.

Read first: AGENTS.md; SKILL.md (:87-88, :142); measurements.md:216-307 and :308-380;
notes/adr/0169-*.md (method, R-move-not-improve, F-3); notes/adr/0179-*.md (shape of a
recent ADR; :345-360 the R-queue witness); roadmap.md:1-17 and :19-30 (P1 row);
fold.clj:540-600; decide.clj:975-1015 (waiting-boarder, admit), :1341; run.clj
(select-person -- locate it); log_index.clj:96-113; components/sim-model/docs/charter.md
:170-182 ("the occupancy board is a definition, not a cache").

Author rulings, verbatim and binding:
 R-fold-carrier: "every remaining generate quadratic is fixed the same way -- an index
   maintained at fold/apply-events, with the from-scratch definition kept as the gate
   that proves the index equal at every replay entry."
 R-equivalence: "each site is its own session; every commit bracket-proven; draw- or
   allocation-affecting sites carry an explicit equivalence argument in the ADR before
   the session, not a bracket after it."
 R-order: "sites in measured order: waiting-boarder, select-person, occupancy-board,
   last-uncancelled-index. Check's fourteen replays last."
 R-gate-gaps: "dense-7500's exerciser joins make integration; the Scale table gets a gate."
 R-edit, R-cap, R-pins (standing).

Steps (one gate each; commit message given):
1. bin/preflight. Write notes/adr/0180-indexes-ride-the-fold.md: the measured basis
   (cite the record's tables by line); the four sites with their scan, the index that
   replaces each, and -- per R-equivalence -- for each whether it is draw-affecting
   (channel expectation: waiting-boarder's sort order and select-person's filterv order
   feed a draw; occupancy-board and the cancel index do not -- correct each from the code)
   and the equivalence argument; the from-scratch-equals-index law as one test shape
   reused four times; sequencing per R-order; what is NOT in scope (constant factors,
   :persons, emitters). Index row in notes/ADRs.md. Gate: make state-derived diff clean.
   Commit: "docs: ADR-0180 indexes ride the fold -- generate-quadratic program charter".
2. Rows. roadmap.md: P1 row re-scoped to point at ADR-0180 and its four sites; a NEW row
   for run/select-person (it has none) citing measurements.md:216-240 -- keep it a
   pointer (R-cap); a NEW row for the two gate gaps (exerciser not in make integration;
   Scale table ungated) citing the 2026-09-06 session record. Gate: make test
   (row-cap, index). Commit: "docs: roadmap -- P1 re-scoped to ADR-0180; select-person
   and gate-gap rows".
3. Census correction. scenario_census.clj:109's :result-after-merge counts a result on a
   :merged subject, which R-queue makes impossible; the scenario is a result whose cited
   order's subject differs from the result's, resolved through a :merge (R-inv). Fix the
   predicate; re-run over the eight cells' logs if still in ~/perf-out (else the 7,500
   log regenerated); the merge column must now be non-zero where the ADR-0179 addendum
   says 21. Update measurements.md:338-366 with a dated correction, not a rewrite.
   Gate: the corrected count matches the ADR's 21 at 7,500. Commit: "plans: scenario
   census -- result-after-merge predicate corrected to R-inv's condition".
4. Session record; archive prompt; close ceremony; push; verify CI; close-marker commit.
   Commit: "docs: record CI success at <sha> -- ADR-0180 charter close".
