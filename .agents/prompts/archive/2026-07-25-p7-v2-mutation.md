# P7 — v2 mutation: locator grammar, operator catalog, contract pairing

You are working in `ehr-testing-tools` (public). This session starts P7's
one unblocked item: v2 mutation over the fixture corpus — the v2 locator
grammar, a seed catalog of v2 defect operators, format dispatch in
`corpus.mutate` and the CLI, and a v2 contract-pairing suite proving every
seeded operator produces a mutant `judge.v2` actually convicts. P7's other
two items remain externally blocked and get no work: IG pinning (awaiting
the motivating team's target profile artifact) and EXP-D3 (awaiting the
NIST license inquiry, F1/F15). This is the first capability session
conducted entirely in the post-refactor vocabulary — judges convict,
workflows decide; write it that way from birth.

Read first: `AGENTS.md`, `AUTHORS-GUIDE.md`, `notes/ADRs.md` (ADR-0004,
ADR-0009, ADR-0010), `.agents/plans/corpus-foundations.md` (the P7 row and
the "v2 mutation deferral, updated (P5)" note), `docs/experiments/EXP-B2*`
(the PipeParser round-trip findings — the representation question is
already answered there), `src/ehr_testing_tools/locator.clj` (the
envelope; the v2 grammar was explicitly deferred to arrive "with
mutation"), `src/ehr_testing_tools/corpus/{operators,mutate,intake}.clj`
(the FHIR catalog's shape, the EXP-B2 applied-decision-rule docstrings,
intake's `:v2-er7` detection and normalization),
`src/ehr_testing_tools/judge/v2.clj` (its base-structural detection tier
— this bounds what operators can promise), `test/fixtures/v2/*.hl7` (the
five ADT fixtures — this session's corpus),
`test-integration/ehr_testing_tools/contract_pairing_test.clj` (the FHIR
pairing suite this session's v2 twin mirrors), `notes/facts-register.md`,
`deps.edn` (the `:test`/`:coverage` alias comments). Test-first per
ADR-0006 throughout. Ritual: commit → `git push origin`. Save this prompt
to `.agents/prompts/2026-07-XX-p7-v2-mutation.md`; final commit archives
it.

Author rulings in effect: Substrate — v2 mutation operates on plain
delimiter-split data (segments split on segment terminators, fields on
`|`, components on `^`, subcomponents on `&`, repeats on `~`, honoring
MSH-1/MSH-2 as the source of those characters), never a HAPI-parsed tree
— the same applied decision rule EXP-B2 imposed on FHIR (HAPI round-trips
disqualified there by silent `resource.id` drops; here PipeParser's
trailing-delimiter canonicalization is the analogous hazard for a
mutation substrate, though it remains fine for judging). Record the
decision and its EXP-B2 citation in the ns docstring, as the FHIR catalog
does. Operator scope — the seed catalog contains only `:violates`
operators within `judge.v2`'s base-structural detection tier; a defect
the resident judge cannot convict has no contract to pair and does not
ship. Corpus — the five fixtures are this session's corpus; the still-
awaited foreign v2 sample remains the P7 row's note, unchanged. Untouched
— `:indeterminate` stays reserved; no report-format, verdict, or
policy-layer changes; no IG/EXP-D3 work.

## Step 0 — Register hygiene: plan closure and two tool facts

1. `.agents/plans/judge-gate-refactor.md`: add a status line at the top —
   Phases 0–3 executed and Done (R1–R3, archived prompts linked); Phase 4
   is a parking register, not scheduled work; the plan is spent as an
   execution driver and retained as record.
2. `notes/facts-register.md`, next F-rows, evidence-first:
   * Cognitect test-runner discovery: `cognitect.test-runner` does not
     derive test directories from the alias's `:extra-paths`; `:dirs`
     defaults to `["test"]` and must name every test root explicitly
     (discovered R2, when `palgebra/test` was silently absent from
     discovery until `:dirs` was set). Evidence: the `deps.edn`
     `:test`/`:coverage` alias comments and a minimal reproduction
     (temporarily drop `"palgebra/test"` from `:dirs` locally, observe
     the test count fall 375 → the ehr-only count, restore). Cite the
     runner's documented `:dirs` default.
   * The exclusion-key fact: the author recalls a session spending time
     on a plural-vs-singular exclusion key — possibly cognitect's
     `:excludes` (metadata-based, plural, cf. `:excludes [:integration]`
     in `deps.edn`) or a cloverage exclusion flag. Reconstruct what
     actually happened from `.agents/prompts/archive/` and `git log -p
     -- deps.edn Makefile` around the coverage/test aliases; reproduce
     the minimal misspelled-key behavior (what silently happens with the
     wrong key is the load-bearing part); record the fact as verified,
     not as remembered — if reconstruction shows the recollection
     conflates two incidents, record what the evidence supports and note
     the correction.
3. `corpus-foundations.md`: P7 row → In progress, prompt column pointing
   at this prompt's archive path; blocked items noted as
   untouched-by-design.

Commit: `P7: plan closure noted; facts-register gains test-runner :dirs
and exclusion-key rows (evidence-based)`.

## Step 1 — The v2 locator grammar (red first)

1. Failing tests in `locator_test.clj`: parse and validate v2 paths —
   segment (`PID`), segment+repeat (`OBX[2]`), field (`PID-3`),
   field+repeat (`PID-3[2]`), component (`PID-3.1`), subcomponent
   (`PID-3.1.2`), MSH's off-by-one convention handled explicitly (MSH-1
   is the field separator itself, MSH-2 the encoding characters — decide
   and document how the grammar addresses them); rejects: unknown
   segment-name shapes, zero/negative indices, trailing separators.
2. Implement as the `:v2` format-dispatched grammar the locator
   docstring promised — envelope unchanged, grammar validity behind
   `make`/a `parse` returning the structured path (the operator fns
   consume the structured form, not the string).

Commit: `Locator: v2 segment/field/component grammar (pattern nursery #4
pays off)`.

## Step 2 — The ER7 substrate: split, join, round-trip

1. Failing tests: split→join over all five fixtures is byte-identical
   (modulo only whatever line-ending normalization `corpus.intake`
   already performs — reuse its rule, don't invent a second one); the
   two adversarial fixtures (repeated identifiers; trailing empty
   fields) round-trip exactly — they exist to catch precisely the
   canonicalization hazard EXP-B2 found in PipeParser.
2. Implement the split/join in a `corpus.er7` (or similar) namespace:
   MSH-1/MSH-2-driven delimiters, repeats, no normalization beyond
   intake's. Content identity: sha256 of the joined ER7 string
   (mirroring `corpus.mutate/content-hash`'s hash-what-gets-persisted
   rationale — state it).

Commit: `corpus.er7: delimiter-split substrate, byte-identical round-trip
over adversarial fixtures (EXP-B2 applied)`.

## Step 3 — The v2 seed operator catalog (contracts first)

1. For each candidate operator, write the pairing expectation first:
   which base-structural constraint it `:violates` and what `judge.v2`
   finding class convicts it. Seed set (adjust to what judge.v2
   demonstrably detects — verify, don't assume): drop a required segment
   (PID from an ADT); blank a required MSH field (e.g. MSH-9 message
   type); corrupt the segment name (PID→PIX); corrupt MSH-2 encoding
   characters; truncate a segment below its required field count. Any
   candidate judge.v2 cannot convict is recorded in the session notes as
   out-of-tier and dropped, not shipped unconvictable.
2. Register entries in `corpus.operators` with `:format :v2`, versions,
   contracts, `:locator-required?` per operator — same shapes as the
   FHIR seeds; operator `:fn`s take the split ER7 structure + parsed
   locator.

Commit: `corpus.operators: v2 seed catalog, every operator paired with a
judge.v2-convictable contract`.

## Step 4 — Format dispatch: mutate core and CLI

1. `corpus.mutate`: dispatch on datum format — FHIR path unchanged; v2
   path uses the er7 substrate and its content-hash; lineage records
   carry format. Its docstring's "FHIR only this session / v2 deferred"
   paragraph is now history — rewrite to the current story, keeping the
   EXP-B2 rationale.
2. CLI `ehr corpus mutate`: accepts the v2 fixtures end-to-end (intake
   detection → mutate → mutant + lineage on disk). Red→green at both
   layers.

Commit: `corpus.mutate + CLI: v2 format dispatch (FHIR path untouched)`.

## Step 5 — v2 contract pairing

Mirror the FHIR pairing suite for v2: each seed operator applied to a
fixture yields a mutant `judge.v2` convicts (`:rejected`, findings
implicating the injected defect — not incidental noise), and the
unmutated fixtures do not convict on the injected constraint. Placement:
`judge.v2` runs in-process (HAPI HL7v2 via deps, no external engine
subprocess) — if the suite runs at unit speed, it lives in `test/` with
the pairing tests; put it in `test-integration/` only if runtime
genuinely demands it. Note in the suite's docstring, once: this is the
polarity regression's v2 twin — `:rejected` here is success, the
workflow supplies the polarity (ADR-0009).

Commit: `v2 contract pairing: every seed operator's mutant convicted by
judge.v2; clean fixtures pass`.

## Step 6 — Close out

Full suite + coverage + both lints green; golden regeneration check
(`make pipeline && make use-cases && git diff --exit-code
docs/pipeline.md docs/use-cases.md`) — expected untouched; if this
session added no stage to the equations, the diagrams don't change, and
if you find yourself editing `pipeline.edn`, stop: that's scope creep.
FHIR integration suite as courtesy pass only if time permits (no
judge.fhir changes were made). Docs: README maturity table's v2 mutation
entry; `docs/components.md` if it states v2 mutation as deferred;
`corpus-foundations.md` P7 row updated (v2 mutation over fixtures: done;
foreign-corpus sample: still awaited; IG pinning + EXP-D3: still blocked,
untouched) and the v2-deferral note gains its closing update. Archive
this prompt.

Commit: `P7 (v2 mutation) complete: locator grammar, er7 substrate, seed
catalog, contract pairing (archives prompt)`.
