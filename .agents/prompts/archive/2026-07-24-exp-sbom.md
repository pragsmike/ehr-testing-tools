Task: EXP-SBOM — license and provenance inventory (plus small housekeeping)

You are working in `ehr-testing-tools`. This is a small, mostly-
investigative session: fix the session-interruption problem, close two
skill flags, then execute experiment EXP-SBOM — the license/provenance
inventory that gates the v2 gate architecture and the go-public gate.

Read first: `AGENTS.md`, `AUTHORS-GUIDE.md`, `notes/facts-register.md` (F1
especially), `docs/components.md` (NIST engine, CDC wrapper, IGAMT
sections), `docs/experiments.md`, `docs/experiments/results-template.md`
and `results-rubric.md`. Commits from WSL. Network available and needed.

Save this prompt to `.agents/prompts/2026-07-24-exp-sbom.md` (live);
final commit moves it to `.agents/prompts/archive/`.

## Step 0 — Session permissions allowlist

Create a committed `.claude/settings.json` (project-scoped; note
`.claude/settings.local.json` stays personal/gitignored) with a
permissions allowlist covering this repo's standard session toolset, so
future sessions don't interrupt for routine actions: `make` targets,
`git` (all subcommands the hook permits), `clojure`/`clj`, file
edits/creation within the repo, `gh api`/`gh gist`, `curl` to
github.com/api.github.com/raw.githubusercontent.com and Maven Central,
`jq`, `python3`, `wsl`. Consult the current Claude Code settings
documentation for exact rule syntax (https://code.claude.com/docs — do
not guess field names from memory). Keep the allowlist scoped to command
families, not blanket bypass. Document its existence and intent in
`AGENTS.md` (one paragraph: what's pre-approved and why; anything outside
the list still prompts).

Commit: `Add committed session permissions allowlist`.

## Step 1 — Skill flags from P3 (author rulings received)

1. scenario roster: the `scenarios` and `probe` skills hard-stop without
   `agent/scenario-roster.md`. The cyberneutics repo is public:
   https://github.com/pragsmike/cyberneutics — locate the roster file it
   references (search the repo tree via raw/API), copy it verbatim to the
   path the skills expect within this repo (creating the directory), and
   note its upstream origin + fetch date in a one-line header comment.
   Ruling: copy now, adapt at first real use. If the file genuinely
   doesn't exist upstream, flag loudly and leave the skills dormant.
2. Config-path harmonization: standardize on `.agents/`-rooted config.
   Patch `scenarios` and `probe` to read
   `.agents/cyberneutics-config.yaml` (or, if the committee skill's
   `.agents/committee-config.yml` is the same kind of config, unify on
   one file — read all three skills and choose the minimal coherent
   scheme; report the choice). Note in each patched SKILL.md that this
   diverges from upstream cyberneutics paths, with date.

Commit: `Skill housekeeping: scenario roster, .agents-rooted config`.

## Step 2 — EXP-SBOM protocol

Create `docs/experiments/EXP-SBOM.md` in the same protocol shape as
EXP-A4 (objective, decision informed, apparatus, procedure, expected
artifacts, acceptance, stop condition). Substance:

* Objective: determine, with primary-source evidence, the license and
  artifact-provenance status of (a) NIST v2-validation
  (usnistgov/v2-validation), (b) NIST IGAMT (usnistgov/hl7-igamt), (c)
  CDC lib-hl7v2-nist-validator, including the full dependency closure the
  CDC wrapper pulls (especially anything from the NIST Nexus), evaluated
  against this repo's Apache-2.0 distribution target (ADR-0001).
* Decision informed: v2 gate architecture (NIST full gate vs HAPI-light +
  local rules); the go-public gate's licensing condition; resolution of
  facts-register F1 and the guide repo's F3.
* Acceptance: every component and dependency classified as one of:
  license-verified-compatible / license-verified-incompatible /
  license-unstated (with the exact evidence absence documented) — plus,
  for unstated cases, a drafted inquiry the author can send.
* Stop condition: this is inventory, not adjudication — where the
  evidence is absent, record absence; do not construe US-Government
  status from vibes. Effort cap: one session.

## Step 3 — Execute

1. For each of the three repos: enumerate license evidence — LICENSE/
   COPYING files at root and in subprojects, source-file headers (sample
   systematically, don't cherry-pick), pom.xml/build.sbt/gradle license
   metadata, README statements, any NIST/CDC policy pages the repos link.
   Record exact paths/URLs and quote-minimal findings (locations and
   characterizations, not wholesale text).
2. CDC wrapper dependency closure: from its build files, list every
   dependency coordinate, its source repository (Maven Central vs NIST
   Nexus vs other), and license per its POM metadata or upstream. Flag
   the SSL-verification-disabled repository config with its exact
   location. Determine whether the NIST-hosted artifacts are also on
   Maven Central under any coordinates.
3. Check for any published US-Government/public-domain declaration
   covering NIST's Systems Interoperability Group software (NIST website
   software/licensing policy pages count as evidence if they explicitly
   cover these artifacts; general agency policy without artifact linkage
   is recorded as suggestive-not-sufficient).
4. Draft `docs/experiments/EXP-SBOM-inquiry-draft.md`: a short, polite
   email to the appropriate NIST contact (find the actual contact from
   the repos/NIST tool pages) asking for license clarification for the
   named repositories, written for the author to review and send. Do not
   send anything.
5. Write `docs/experiments/EXP-SBOM-results.md` per the results template,
   self-scored against the rubric. Classification only — the architecture
   decision happens in the design channel. Update the EXP-SBOM row in
   `docs/experiments.md` (executed, link results).
6. Facts register: update F1's status line to reflect the executed
   inventory (still pending only if the inquiry remains the missing
   piece); add rows for any newly verified license facts the inventory
   established (e.g., specific CDC-wrapper dependencies verified
   Apache/EPL/etc.), house style, evidence links, dated.

Commits: `EXP-SBOM protocol` and `EXP-SBOM executed: results, inquiry
draft, register updates` (split as sensible).

## Step 4 — Finalize

`make test` green (nothing here should touch code, so this is a
regression check); move prompt to archive; `make pack-push`; verify gist
`updated_at` via `gh api`. Report: allowlist rules as committed;
roster/config outcomes; per-component license classification table;
dependency-closure summary (counts by source and license); the
suggestive-vs-sufficient evidence line for US-Gov status; F-row changes;
commits.

## Out of scope

No capability code, no deps.edn changes, no architecture conclusions
(classify, don't decide), no sending the inquiry, no guide-repo edits
(its F3 update happens after the author reviews results), no edits to
ADRs.
