2026-08-06 — ux fixes 1: every doc teaches the real invocation — swept, paired, gated
Session prompt (design channel, 2026-08-06). Prior: the UX audit landed and was design-channel-verified (`bc66bd6`, ADR-0058). This session executes the register's first ruled cluster: U1's invocation sweep, the module-mix flag-pairing fix, and the invocation-lint gate. Mechanical by the register's own evidence (A-2 confirmed every corrected form valid against `docs/cli.md`'s grammar). Docs plus one test file; nothing else. R30 ceremony. Read-first: register rows U1, U4→n/a, A-1/A-2/A-3 in `.agents/plans/2026-08-06-ux-audit-findings.md`; the register's U1 evidence column (the 11 file:line groups, verbatim); the fixes-2 gates as style models.
Author rulings (record verbatim in ADR-0059)

1. AR-U1-0 (tag, standing ceremony). Annotated `stable-20260806-ux-audit` at `bc66bd6`, message `ux audit landed, design-channel-verified 2026-08-06 (ADR-0058)`; push; verify.
2. AR-U1-1 (the sweep). Re-derive the stale-invocation inventory by fresh grep (`clojure -M:cli` across `README.md`, `AUTHORS-GUIDE.md`, `docs/**`, `components/*/docs/**` — .md AND .edn header comments); the register's 11 file:line groups / 14 instances are the expectation — a delta in either direction is disclosed, not silently absorbed. Every instance corrects to the `bin/ehrt` form (`clojure -M:cli run` → `bin/ehrt sim run`, preserving each command's own flags). In the same pass: `components/sim/docs/demos/module-mix/README.md:13` and its `config.edn:5-6` twin gain the missing `--emit hl7` (register U1's pairing defect — per `docs/cli.md`, `--format er7` requires it). LICENSED-OPTIONAL, per register A-3: if touching `docs/simulate-your-facility.md` anyway, the line-202 placeholder mention may become a real fence with concrete values — cosmetic, disclose the choice either way.
3. AR-U1-2 (the gate, co-landed). New deftest in the `docs-tooling` gate family: the string `clojure -M:cli` is forbidden across the sweep's exact surface set (the four roots above, `.md` and `.edn` files both); frozen archives, `.agents/prompts/`, `.agents/session-records/`, dated one-shot plan files, and the audit registers are OUT of scope — they quote history. Docstring cites AR-U1-2 and register U1. Red→green is NATURAL: run the gate against the unswept tree first (all 11 groups trip), capture the red, land sweep + gate in ONE commit, green.
4. AR-U1-3 (nothing else). No help.clj, no error strings, no `src/` — sessions 2–5 own those. A newly-noticed defect is a note in ADR-0059 for the arc's intake, never an act.

Steps
Step 0 — Preflight + tag. Cwd ext4; tip `bc66bd6` or later-with-disclosure; full suite green baseline; oracle pre-digest. Execute AR-U1-0.
Step 1 — Red, then sweep + gate (AR-U1-1/2). Gate written; red captured; sweep executed with its per-file table drafted for the ADR; ONE commit, gate green, suite green, `poly check` OK: `docs: every doc teaches the real invocation — the stale alias dies, gated (ux fixes 1, AR-U1-1/2)`
Step 2 — ADR-0059 + record. ADR-0059: rulings verbatim; the fresh-grep delta table vs the register's expectation; the red transcript; the A-3 disposition. Index line; Done pointer `- 2026-08-06 — ux-fixes-1 — ADR-0059`. Oracle bracket (`bc66bd6` → tip): all ELEVEN batches identical — docs and one test only; any change is STOP-AND-ESCALATE. Session record + prompt self-archive. Final commit: `docs: ux fixes 1 record — the invocation surface is coherent (ADR-0059)`
Fences
Docs + one test file, nothing else. Command semantics unchanged — this session edits what docs SAY, and the corrected fences must match `docs/cli.md`'s grammar exactly (register A-2 is the pre-verification; a fence the session cannot map cleanly is a STOP, not a guess). No gate weakening. Frozen archives untouched (ADR-0059 + index sanctioned). The register is read-only.
After landing: design channel verifies by fresh probe; session 2 (error surfaces: C-1 + U4 + B-6/D-3 + the B-5 exit-0 ruling) follows, and this landing's tag rides its Step 0 under standing ceremony.
