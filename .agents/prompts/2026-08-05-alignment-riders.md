2026-08-05 — alignment riders: small debts paid, the audit brief lands, stable tags go live
Session prompt (design channel, 2026-08-05). Prior: scaffolding compaction arc closed and verified (`89e327f`). This is the small session opening the alignment arc: it lands the arc's brief and pays three pre-ruled debts BEFORE the audit session runs, so the audit starts clean. Docs-only — no `src/` is touched at any point. R30 ceremony. The audit brief is already present in the working tree at `.agents/plans/2026-08-05-alignment-audit-brief.md`, placed there by the author from the design channel's delivered file; if it is absent or empty, STOP-AND-ESCALATE — do not reconstruct it from memory.
Author rulings (record verbatim in ADR-0048)

1. AR-R-1 (brief lands, verified not trusted). The alignment-audit brief lands with an index entry in `.agents/plans/README.md`. Doer-checker inversion, lightweight: before landing, re-probe the brief's probeable seeded claims against the live tree — S1 (both `components/sim-model/resources/sim/` and `components/sim/resources/sim/` exist; `comm -12` of their sorted file lists is empty), S2's tag inventory (`git tag` shows exactly `stable-bootstrap`, `stable-ehrt-1`, `stable-pre-monorepo`), S4 (zero live roadmap rows matching `mllp|bed board|accumulator|census sink|player`, case-insensitive). A claim that fails probe is corrected in the brief with a dated note — never silently kept, never silently dropped.
2. AR-R-2 (stable-* tagging adopted — STANDING). Live `stable-*` tagging is adopted: the author tags after each design-channel-verified landing, format `stable-YYYYMMDD-<session-slug>`, matching the existing `^stable-.*` pattern in `workspace.edn` (no config change). The three legacy tags stay — frozen history, superseded by the first new stable point. Tagging remains the author's act alone (R30); this session tags nothing. Recorded in ADR-0048 as a standing ruling; the register append (AR-R-5) cites it.
3. AR-R-3 (player backlog resurfaces). The corpus player's remaining work returns to the live roadmap, each row citing `notes/ADRs.md` ADR-0014: bed board + census sink, accumulator wiring, and the sim event-log input adapter as Next rows; the `:mllp` transport sink as a Deferred row carrying ADR-0014's own bail-out reasoning verbatim-by-citation and revisit trigger "a session needs wire transport and a lands-small shape is identified". Row text is written fresh against ADR-0014's actual deferral language, read in this session — not from this prompt's summary of it.
4. AR-R-4 (stray Deferred row comes home). The `myocardial_infarction.json` row relocates from Deferred to the Done attic `.agents/plans/roadmap-done-2026-08.md`, notes intact (relocation, not rewrite — AR-A-5), with a dated relocation note naming this session and the drift's original disclosure (ADR-0047 / `.agents/state.md`'s finding). The attic file's own header contract is read before appending; the append matches its format. Deferred's live-row count drops 13 → 12; state.md is NOT edited (it regenerates at arc close only, AR-C-1 — the count divergence is expected and disclosed in the record).
5. AR-R-5 (rulings register append). Two appends to `.agents/rulings.md`, under a new "From ADR-0048" section: (a) transcript-witnessed ≠ repo-recorded — only repo artifacts are citable; chat-witnessed events are `[unverified]` until a record captures them — citing ADR-0047 Step 0 as the evidencing event (it caught the prior design channel twice, 2026-08-05); (b) AR-R-2's stable-tagging discipline, citing ADR-0048. The register's contract says appends happen at arc close; this mid-arc append is author-licensed here, disclosed in the ADR as a deviation-with-license, not silently normalized.

Steps
Step 0 — Preflight and verify. Confirm cwd is the ext4 clone (a `/mnt/c` default is a NEW regression — STOP-AND-REPORT, AR-C-3). Tip = `89e327f` or later-with-disclosure. Confirm the brief file is present. Run AR-R-1's probes; record a claim → probe → held/corrected table for the ADR. Full suite green baseline (`clojure -M:poly test :all skip:integration`); note the deftest/assertion shape for the bracket.
Step 1 — Land the brief (AR-R-1). Brief committed with its `.agents/plans/README.md` index entry (format matching the existing per-file entries). Index-completeness gate green. Commit: `docs: the alignment-audit brief lands — the arc opens with its map (alignment riders, AR-R-1)`
Step 2 — Roadmap hygiene (AR-R-3/AR-R-4). Read `notes/adr/0014-corpus-player.md`'s deferral text first; write the four restored rows from it. Relocate the myocardial row per AR-R-4. One commit, both edits — they are the same artifact's hygiene. Commit: `docs: the player's backlog resurfaces; a stray Deferred row comes home (alignment riders, AR-R-3/4)`
Step 3 — Rulings append (AR-R-5). Both appends; the deviation-with-license note goes in ADR-0048, not the register itself. Commit: `docs: rulings append — witnessed is not recorded; stable tags go live (alignment riders, AR-R-5)`
Step 4 — ADR-0048 + record. Author `notes/adr/0048-alignment-riders.md` (rulings verbatim, Step 0's verification table, the AR-R-5 deviation note, the state.md count-divergence disclosure); append its index line to `notes/ADRs.md`; Done pointer `- 2026-08-05 — alignment-riders — ADR-0048` (done-pointer gate). Oracle bracket (`89e327f` → tip): all ELEVEN batches identical — docs-only session; ANY digest change is STOP-AND-ESCALATE, no `--declared-digest-change` is licensed here. Full suite green, shape unchanged from Step 0. Session record + prompt self-archive (pairing gate). Final commit: `docs: alignment riders record — small debts paid before the audit (ADR-0048)`
Fences
Docs-only: no `src/`, no `test/`, no `workspace.edn`, no `deps.edn`, no new gates. Frozen archives untouched (`notes/adr/0048` and the index line are the sanctioned additions; `roadmap-done-2026-08.md` is a live attic, append-only per its header). No audit work smuggled in — every finding beyond AR-R-1's probe table belongs to the audit session, recorded as a finding if stumbled on, never acted on. The brief's content outranks this prompt's summaries of it; ADR-0014's text outranks both for the row wording. Evidence outranks all three.
After landing: the design channel verifies by fresh probe; on verification the author tags `stable-YYYYMMDD-alignment-riders` — the new discipline's first tag.
