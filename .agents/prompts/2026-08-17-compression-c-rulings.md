# Archived prompt: compression-c-rulings (2026-08-17)

Archived verbatim as received. The one departure from it is recorded in
`notes/adr/0145-rulings-standing-register.md` (the `:onboarding` budget did
not fall) and in `.agents/session-records/2026-08-17-compression-c-rulings.md`.

---

# Session prompt -- compression arc, session C: the rulings register
# becomes standing-rules-only; build-session skill split; reading-
# sets header attic'd -- ADR-0145

## Context
Claude Code under R30 in ehr-testing-tools. HEAD: e0cd075 (ADR-0144,
session B). Channel probe at e0cd075 (re-derive before use):
`.agents/rulings.md` 1,757 lines / 46 blocks / in no reading set,
its own header contract "standing decisions only, NOT a history"
violated by every block since ~ADR-0057; `build-session/SKILL.md`
309 lines, 17 steps, history inline, in every set; four sets sit
above their ratchet baselines by formula (ADR-0144 close) so this
session is on the critical path; `.agents/reading-sets.edn` 531
lines of which 415 are comment. Nothing deleted: verbatim moves with
dated headings, read-back verified.

## Read first
1. `.agents/rulings.md` header (:1-27 -- its own definition of
   "standing"), block headings, the ADR-0047 seed contract.
2. `notes/adr/0144-roadmap-row-contract.md` (or its actual name --
   find it): the migration-script + `--verify` read-back pattern, the
   numstat-vs-read-back finding, the "owed to the author" list.
3. `.agents/skills/build-session/SKILL.md` whole; `handoff`,
   `session-prompt` skills' references to rulings; `shared-skill-
   layout` for the `.claude/skills` mirror.
4. `.agents/reading-sets.edn` header; `reading_set_budget_test.clj`;
   `.agents/reading-sets-baseline.edn`.
5. `AGENTS.md` registers section; `.agents/rulings.md` "From ADR-0143/
   0144".

## Author rulings, verbatim
- "C, then UX." Q1-Q5: (a) throughout.        [EDIT IF OTHERWISE]
- B's owed items: (1) `intake-staging-dir` trigger: ___ ; (2) the five
  mis-sectioned rows: leave, list them again in ADR-0145; (3) PRIORITY
  values: order per the ADR-0141 handoff queue as recorded in the
  compression-arc row's own text; (4) review-3 tag `b96c246`: author
  pushes it; session verifies it peels at Step 0 (STOP if absent
  and no ruling).                               [EDIT IF OTHERWISE]
- Tag: `e0cd075` (ADR-0144 close), case (i), pay
  `stable-20260817-roadmap-row-contract` ONLY on relayed CI success
  for run 32023934757; else STOP with the run id.

## Step 0
Fresh, tip e0cd075. Tag per license. Baseline `make test` unpiped,
MAKE_EXIT captured, reconcile against ADR-0144's 336 / 17,278. Reading
sets measured vs budgets AND vs baseline file (expect four above
baseline: that is this session's charter, not a STOP -- disclose).
`poly check` OK. Apply owed rulings (1)-(4) as one docs commit if any
change the tree.

## Step 1 -- census (docs-only commit)
1. Every block in `rulings.md`: heading, ADR(s) owned, line count.
   Every bullet inside: proposed class STANDING | ARC-LOCAL |
   SUPERSEDED (by which), with the sentence that decides it quoted.
   Ambiguous ones are FINDINGS for the author, listed. Expect
   hundreds of bullets; the table is generated by the migration
   script (B's discipline), not typed.
2. Every reference to `rulings.md` (scan-root live surfaces, as
   ADR-0144's include-list defines them) -- anchors that must survive.
3. `build-session/SKILL.md`: per step, the imperative sentence vs the
   history sentences (cite-with-story, near-miss anecdotes), line
   counts of each.
4. `reading-sets.edn`: comment lines by dated section.
Open ADR-0145. Commit: "docs: ADR-0145 opens -- rulings-register
census (standing/arc-local/superseded), build-session split map,
reading-sets header census (compression arc, session C)"

## Step 2 -- red first (tests only)
`rulings_lint_test`: every row is `- **R-<slug>** -- <rule> -- ADR-NNNN`
(optionally `SUPERSEDED-BY R-<slug> (ADR-NNNN)`); slug unique; ADR
file exists; row <= 3 lines; no `## From` block headings remain; a
SUPERSEDED row names a successor that exists. `reading_set_budget_
test`: `.agents/reading-sets.edn` comment lines <= 20 (header only).
Sanity cases for each regex (B's `\b` lesson: assert the pattern
matches a known-good and rejects a known-bad row). Witness RED with
counts. Commit: "test: red -- rulings register row contract, header
cap on reading-sets.edn"

## Step 3 -- green: migration
1. Script `bin/rulings-migrate-0145` (with `--dry-run`, `--verify`):
   move each block verbatim to its owning ADR under the dated heading
   (arc blocks -> arc close ADR; the header's own seed prose stays as
   the new file's preamble, trimmed only by moving, not rewriting);
   write the standing rows from the census classes, each with its
   ADR cite; SUPERSEDED rows kept as rows. `--verify`: every block
   found as an exact contiguous block at its destination.
2. `build-session/SKILL.md`: history sentences moved verbatim to
   `.agents/skills/build-session/HISTORY.md` under per-step dated
   headings; each step keeps imperative + one cite. Mirror to
   `.claude/skills`, diff zero. HISTORY.md is NOT added to any set.
3. `reading-sets.edn`: comment history moved verbatim to
   `.agents/plans/reading-sets-history.md`; <= 20-line header
   pointing at it.
4. Q5: add `.agents/rulings.md` to `:onboarding` `:paths`; re-measure
   all five sets; every budget and baseline re-set DOWN to the
   standing formula at the new actuals; no set above baseline at
   close (that is C's exit condition).
5. Rewrite references (Step 1.2) that pointed at block headings to
   row ids; each in-place edit enumerated.
Green: lint tests, full `make test` unpiped MAKE_EXIT=0 reconciled;
oracle IDENTICAL 35/35 (no src outside docs-tooling); docsgen no
drift; mirrors zero.
Commit: "feat: rulings register is standing-rules-only -- one row per
rule with ADR cite, blocks moved verbatim into their ADRs; build-
session skill split into rules and HISTORY; reading-sets header
attic'd; onboarding set gains the register; budgets ratcheted down
(ADR-0145)"

## Step 4 -- records
ADR-0145 (census, classification findings for the author, moved-line
ledger by read-back, before/after per file and per set). Rulings:
this session's own rulings land as ROWS in the new form (first use).
Roadmap: compression row (C LANDED; D queued), in the row contract.
AGENTS.md registers section: one line on the rulings row contract.
Session record; prompt archived.
Commit: "docs: ADR-0145 -- compression arc session C: standing rulings
register, build-session split, reading-sets header; D chartered"

## Fences
src: docs-tooling only; oracle IDENTICAL. ADR files append-only except
enumerated reference rewrites. Nothing deleted; read-back is the
instrument (not numstat -- ADR-0144's finding). No budget increase;
exit condition: all five sets at or below baseline. Exit codes
unpiped; `out/` cleared; anchored edits; R-RP. STOP on: a rulings
bullet you cannot classify AND cannot list; a reference you cannot
resolve; a set still above baseline after Step 3.

## Self-archive
`.agents/prompts/2026-08-17-compression-c-rulings.md` in Step 4.
