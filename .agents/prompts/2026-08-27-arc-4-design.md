Session prompt -- arc 4 design: emission add-ons (ADR-0175)

Context. HEAD 8439416. Arc 3 is complete: the skeleton contract (event
schema 1.7.0) carries persons, encounters, the bed cycle and scheduling,
all judged by check.clj. Arc 4 is R-mix-7 (`rulings.md:301`): chatter and
fan-out are emission add-ons DOWNSTREAM of the fact generators; ratios are
emission config; NOTHING reshuffles. Roadmap `[emission-add-ons]` P5
(:57-62), `[corpus-player-slices]` P7 (:63-77, `:mllp` and accumulator
wiring UNPRICED). This session is the design ADR only, no code; its proof
shape for every later sweep is fixed here: ground truth byte-identical
(oracle IDENTICAL on `engine/run`), only `emit-hl7` output moves, and it
moves only for configs that opt in. Payload session; last arc of the
program.

Read first: ADR-0168 §4 and the plan's Arc 4 paragraph; R-mix-7 and
R-skeleton-or-emission (:303); `emit_hl7.clj` :50-140 (the kind->message
registry, with its own comments on what does NOT reach the wire and why:
`:demographic-update`/`:coverage-change` at :137-139, scheduling's four
kinds at :91+, the `bed-status-message` sibling precedent :76-90);
`single-subject-message`, `pv1-segment`, `in1-segment` :369, `emit-wire`
and `plan-latency` (`run.clj:450-460`: `:latency` rides `:config`
byte-identically -- the emission-config precedent every add-on follows);
`corpus-io` `framing.clj`/`spool.clj` (MLLP framing exists; a transport
does not); `components/corpus/board.clj` (message input only); ADR-0172
row 8 (households have no wire surface); ADR-0174 §2(d) (MSH-12 `"2.3"`
vs the vendored v2.4 structures -- unresolved, owned by this arc);
`hapi-structures-v24` in the classpath, which is the only structure
library the tree has -- read message structures from it, never from
memory.

Step 1. Census from the tree: (i) every skeleton kind with NO wire entry
today and the registry's stated reason; (ii) every segment the emitter
renders and its blank-field count; (iii) every place a consumer reads the
wire (player, board, judge-v2-hapi, judge-v2-nist, corpus intake) -- what
each would do with a message family it has never seen; (iv) the vendored
trigger/structure table for ADT A08, A31, A28, DFT P03, SIU S12-S26, ORU
R01 status values, and what 2.3 vs 2.4 changes for each.

Step 2. Design, each with rejected alternatives and the classification
test (R-skeleton-or-emission) applied explicitly: (a) RE-STATEMENT
CHATTER -- A08 on `:demographic-update`, A31 on person-level changes,
IN1-only updates on `:coverage-change`, plus periodic A08 re-statement at
a config ratio (`:chatter {:restatement-rate ...}`); every A08 derivable
from state-at-t (that is what makes it emission). (b) STATUS LADDERS --
order/result status progression (ORC-5 / OBR-25 / OBX-11 values from the
v2.4 tables) as ORU/ORM restatements between `:order-placed` and
`:result-available`, ratio-configured, derivable from the two skeleton
events' times. (c) DFT P03 charges per encounter close, derivable from
the encounter record. (d) NK1 from household state -- the FIRST wire
surface for ADR-0172 row 8; say which rows it lifts. (e) SIU: settle
MSH-12 -- the emitter declares 2.3, the structures are 2.4; options:
declare 2.4 (one MSH-12 change, moves every message once -- a declared
emission sweep of its own), or keep 2.3 and emit only what 2.3 defines;
rule it, do not straddle. (f) FAN-OUT -- a subscriber table (`:fan-out
[{:name :filter :transform}]`): per-subscriber message subsets, filtered
by MSH-9 and by patient class, each its own spool; the accumulator wiring
row. (g) TRANSPORT -- `:mllp` as a corpus-io sink over the existing
framing (a socket target, ACK handling per the v2 MSA rules), the
player's `--transport` flag; what is skeleton (nothing) and what is
judged (ACK pairing, framing). (h) GATING AT SCALE -- the plan's own
ruling item: 10^6 messages under NIST is hours-class; propose the
sampled/stratified policy (full on skeleton kinds, sampled on chatter)
and the gate that asserts the sample is drawn deterministically.

Step 3. Rulings, lettered, recommendation each -- at least: (A) MSH-12
2.3 vs 2.4; (B) which add-ons are v1 (channel lean: A08/A31/IN1 chatter +
NK1 + DFT first; ladders second; SIU only after A; fan-out and MLLP as
the player slices, priced); (C) chatter ratios as run config vs a
separate emission profile file; (D) gating policy at scale; (E) landing
order -- each add-on its own opt-in key, dark IDENTICAL then on with ONLY
emitted files moving (the arc-0 shape applied to the wire: ground truth
digests identical, message digests declared). ADR-0175 Proposed; roadmap
rows one line each; record one page; push; CI; no tag.

Fences. No `components/*/src` change. No skeleton kind proposed --
anything downstream invariants must respect is arc-3 territory and a
STOP. Every structure claim cites the jar. Every line from your clone.
