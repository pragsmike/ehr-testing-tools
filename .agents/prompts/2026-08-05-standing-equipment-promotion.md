2026-08-05 — standing-equipment promotion: census into sim-trajectory, oracle digest into a component (J2 closed)
Session prompt (design channel, 2026-08-05). Prior arcs: sim split B (ADR-0043, complete + verified) and the docs coherence pass (2026-08-05, complete + verified). This session promotes the two pieces of standing equipment the cleanup-arc seeds named — the census tool and the regression oracle's digest — into the tested tree, and closes the J2 deferred row structurally. R30 ceremony throughout. Move-don't-improve: relocations verbatim; the digest's interface-repoint is the one sanctioned improvement (it is what makes the promotion possible); NO census behavior changes (AR-P-4).
Context
Census (`development/src/ehrt/sim_trajectory/census.clj`, 447 lines; `development/test/.../census_test.clj`, 7 deftests): ADR-0034's sim-trajectory dev entry point. Its tests are invisible to `poly test` (Wave I finding). It requires sim-trajectory INTERNALS (`gmf`, `gmf-interpreter`) — fine in dev, illegal as a foreign component's src — and writes dated EDN artifacts into `components/sim-trajectory/docs/census/`, which it already treats as home.
Oracle digest (`bin/oracle-src/ehrt/oracle/digest.clj`, 258 lines): the byte-digest producer `bin/regression-oracle` reads from the CURRENT checkout against both worktrees — the J2 deferred row's incompatibility, worked around live twice (ADR-0033's compile error; M2's split-mode bracket). Design-channel call-site census at `f9830ec` (session re-derives fresh): every sim-trajectory need except `interp/dob-epoch-day` is ALREADY on `ehrt.sim-trajectory.interface` (`load-module`, `load-closure`, `singleton-closure`, `run-module`); `engine/run` and `emit-hl7/emit` are interface vars; the two apparent internals calls (`urinary-tract-infections-engine`, `emit-hl7/test`) are comment-text artifacts, not calls. Digest is therefore interface-clean modulo one var.
The structural J2 fix: once digest lives in a component, each worktree's synthetic classpath resolves that worktree's OWN digest — producer-shape changes mid-span stop being compile errors against the baseline. The comparability guarantee the old design bought (one digest source for both sides) is preserved by encoding M2's soundness condition into the script: diff the two sides' digest sources; proceed silently if identical or ns/require-only, demand a declared expected-change (or abort) otherwise.
Read first

* `.agents/plans/roadmap.md` — the J2 deferred row (~line 219) and census-refinements row, whose triggers this session must cite and NOT re-open.
* `development/src/ehrt/sim_trajectory/census.clj` + its test.
* `bin/regression-oracle` (whole script) + `bin/oracle-src/ehrt/oracle/digest.clj`.
* `components/sim-trajectory/src/ehrt/sim_trajectory/interface.clj`.
* `notes/ADRs.md` ADR-0030 (J-series), ADR-0033 execution note, ADR-0034 (census), ADR-0043 M2 record (split-mode precedent).
* How census is invoked today (find the launcher: bin/, Makefile, docs — Step 0 records it; the invocation must survive).

Author rulings (record verbatim in a new ADR)

1. AR-P-1 (census home). Census moves INTO `components/sim-trajectory` as `ehrt.sim_trajectory/census.clj` (ns `ehrt.sim-trajectory.census`); its test moves into the component's test tree. Internals access becomes intra-component (legal); the interface itself does NOT grow for census — it is equipment, not API; invocation is via `-m ehrt.sim-trajectory.census` or the existing launcher, repointed. Census's deps (`data.json`, `clojure.java.shell` is core) enter sim-trajectory's deps.edn as needed, evidenced.
2. AR-P-2 (digest home). Digest becomes `components/oracle` (`ehrt.oracle.digest` + `ehrt.oracle.interface` exposing the entry point(s) the script calls). Its requires repoint to interfaces: `ehrt.sim-trajectory.interface` (adding `dob-epoch-day` to that interface iff the fresh call-site census confirms the need — one var, caller-evidenced, recorded), `ehrt.sim-engine.interface`, `ehrt.sim-emit-hl7.interface`, `ehrt.sim-model.interface`. The interface-repoint is this session's sanctioned improvement; digest LOGIC moves verbatim (the byte-comparison in Step 5 is the proof). `bin/oracle-src/` retires with a dated pointer in the script's header.
3. AR-P-3 (script redesign — J2 closes). `bin/regression-oracle` changes: per-worktree classpath now includes that worktree's own `components/oracle` (each side runs its own digest); a cross-side digest-source equivalence check runs FIRST — identical or ns/require-only diff proceeds; anything else requires an explicit `--declared-digest-change` flag (which is recorded in the manifest header) or aborts. The J2 deferred row closes with a dated note citing this session; ADR-0030's own J2 precedent gets a dated amendment, fix-forward style. Brackets spanning PRE-promotion refs: the script detects a side lacking `components/oracle` and falls back to that side's digest.clj at `bin/oracle-src` if present (one transitional branch, commented with its own retirement condition: remove when no bracket needs a pre-promotion baseline).
4. AR-P-4 (no census improvements). The deferred census refinements — substance qualifier, per-module seeds, the same-day filename overwrite bug — stay Deferred with their triggers intact. Promotion is relocation + test-exercise, nothing else; if the overwrite bug's fix tempts during the move, it is a FINDING.
5. AR-P-5 (this session's own bracket — declared transitional split). The bracket spanning this session runs old-mechanism@pre (bin/oracle-src at `f9830ec`) vs new-mechanism@post (the component), manifests compared; soundness condition: digest's cross-side diff is ns/require/interface-repoint lines only, asserted and recorded. All ELEVEN batches byte-identical, expected-change set NONE. This is the last split-mode bracket; the ADR says so.

Steps
Step 0 — Characterize (evidence, no edits). Verify tip = `f9830ec` (STOP-AND-ESCALATE on mismatch). Fresh call-position census of digest's every dependency var (cross-check the list above, both-direction deltas recorded — including whether `dob-epoch-day` is truly needed and truly absent from the interface). Census's call-position census likewise. Locate census's launcher and every reference to `bin/oracle-src` repo-wide. Form-anchored deftest counts. Record all.
Step 1 — Census promotion (AR-P-1). Move census + test into sim-trajectory; deps evidenced into the component's deps.edn; launcher repointed; workspace bookkeeping (test path additions, coverage alias gains the paths if absent, structure-currency). `poly test` now exercises the 7 tests — show them in the run output. Full suite green, both lanes. Commit: `refactor(sim-trajectory): census comes home — dev equipment enters the tested tree (promotion, AR-P-1)`
Step 2 — Oracle component (AR-P-2). Create `components/oracle`; digest moves with interface repoints (each repoint listed in the commit body); `dob-epoch-day` interface addition iff evidenced; `bin/oracle-src` retires with the dated pointer. Workspace bookkeeping. Digest's pure helpers now under `poly test` (write NO new tests this session beyond what moves — if digest has none, the gap is a recorded finding with a trigger, not new scope). Full suite green. Commit: `refactor(oracle): the digest becomes a component — each checkout carries its own producer (promotion, AR-P-2)`
Step 3 — Script redesign (AR-P-3). The per-worktree resolution, the equivalence check, the `--declared-digest-change` flag, the transitional pre-promotion fallback with its retirement comment. Red→green: run the script against a same-ref bracket (`f9830ec f9830ec` via the fallback branch, proving it) and against `<step-2-tip> <step-2-tip>` (via the component branch) — both must report IDENTICAL; record both. Commit: `feat(oracle): brackets read each side's own digest — J2 closes structurally (promotion, AR-P-3)`
Step 4 — ADR + roadmap (AR-P-3/4/5). New ADR: rulings verbatim, the J2 closure narrative (two live workarounds cited), ADR-0030's dated amendment, the census-refinements re-cite. Roadmap: J2 row closes dated; census-tool-tests row closes; refinement triggers stand. Commit: `docs: promotion ADR — standing equipment is tested equipment; J2 deferred row closes (promotion, AR-P-4/5)`
Step 5 — Verification + record. AR-P-5's declared transitional split bracket, soundness asserted, ELEVEN batches identical. Deftest parity: +7 (census tests now counted — state the counting definition per AR-D-6's rule) plus whatever moved with digest; ledger states each delta. Façade seam byte-identical. `poly check` clean; both lanes green; all gates green. Session record `.agents/session-records/2026-08-05-standing-equipment-promotion.md`; prompt self-archives. Final commit: `docs: promotion session record — the equipment tests itself now`
Fences
No census behavior changes (AR-P-4 — the overwrite bug survives this session on purpose). No digest logic changes — interface repoints are the entire licensed diff; Step 5's soundness condition is the proof. No new tests beyond the 7 that move (gaps are findings with triggers). No oracle-script features beyond AR-P-3's list. Deferred rows re-cited, not re-opened. Frozen archives untouched. If `dob-epoch-day` turns out to be on the interface already or unneeded, follow the evidence and record the delta — the ruling's iff-clause governs.

Deviation record

- **Test-fixture staleness (Step 1), fixed forward, not merely disclosed.** Running census's 7 tests under `poly test` for the first time surfaced two stale fixtures (GMF coverage Wave VS had already made `VitalSign`/`Vital Sign` real). Read as within Step 1's own "full suite green, show them in the run output" requirement rather than a census-behavior change under AR-P-4 — see the session record's own Judgment calls section for the reasoning.
- **"Wave I finding" citation not found.** This prompt's own Context section attributes the `poly test` invisibility to "the roadmap's own Wave I finding" — a direct grep of `.agents/plans/roadmap.md` found no such row; GMF coverage Wave I/I2 (ADR-0040/ADR-0041) is an unrelated arc. The underlying claim was independently re-confirmed by a live before/after `poly test` run instead (202 blocks before, 204 after) and the ADR records the corrected provenance.
- **Roadmap wording**: this prompt's own Step 4 says "census-tool-tests row closes" — no such distinct row was found either (only the single "Census tool refinements" row, whose own three triggers a/b/c stand untouched per AR-P-4). The J2 row is the one that actually closes; the census-tool-refinements row instead gained a dated note distinguishing the fixture finding from its own three named triggers.
- **`poly/oracle` added to the root `deps.edn`'s `:dev` alias** — not named explicitly in AR-P-2, done so `poly check`/`poly test`/the dev REPL have a real project-level edge to the new component, the same way every other component already has one.
- **`bin/regression-oracle`'s Step 2 edit was the minimal classpath swap only, not yet AR-P-3's full redesign** — between Step 2's and Step 3's commits (same session), the script could not resolve a pre-promotion baseline ref at all; accepted deliberately, disclosed in the Step 2 commit's own header-comment edit, resolved by the very next commit.

See `.agents/session-records/2026-08-05-standing-equipment-promotion.md` for the full account, evidence, and findings.
