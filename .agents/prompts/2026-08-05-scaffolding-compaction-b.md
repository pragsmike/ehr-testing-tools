2026-08-05 — scaffolding compaction B: the ADR split and the roadmap rotation
Session prompt (design channel, 2026-08-05). Prior: compaction A landed and independently verified (tip:  3495181 — STOP-AND-ESCALATE on mismatch). This is the arc's big relocation: history moves to the attic in compact, indexed, byte-verbatim form; nothing is deleted. Author rulings AR-B-1..5 approved 2026-08-05 (design channel); AR-B-1's rulings-register half and AR-B-5's continuity register are session C's scope (design-channel-authored), NOT this session's — B is mechanical relocation plus gates. R30 ceremony throughout. Every move's proof is a byte-identity extraction diff, the gmf-findings-split precedent.
Author rulings (record verbatim in the new ADR)

1. AR-B-1 (ADR split, per-ADR files). Every `## ADR-NNNN` entry in `notes/ADRs.md` moves VERBATIM to `notes/adr/NNNN-<slug>.md` (slug from the entry's own title, lowercased-hyphenated). `notes/ADRs.md` becomes the INDEX: header explaining the split (dated), then one line per ADR — number, title, file link, and the entry's own status/arc if its header states one. The standing-rulings register is session C's deliverable, not built here.
2. AR-B-2 (citation continuity, zero-sweep). `notes/ADRs.md` REMAINS the citation target: every existing "notes/ADRs.md ADR-NNNN" citation repo-wide (including frozen archives) stays resolvable — the reader lands on the index, the index points to the file. Bare `ADR-NNNN` continues per the citation-qualification rule. NO citation sweep is performed or needed; the ADR states this explicitly. No renumbering (standing ruling, re-cited). NEW execution-record appends go directly to the per-ADR file; the index line's status updates when an arc closes.
3. AR-B-3 (roadmap rotation). `.agents/plans/roadmap.md`'s Done history moves VERBATIM to `.agents/plans/roadmap-done-2026-07.md` and `roadmap-done-2026-08.md` (split by each entry's own date; entries straddling months stay whole in their start month). The live roadmap keeps: Now, Next, Deferred, and a Done section holding ONLY the current (compaction) arc as one-line pointers per AR-B-4, plus a dated header line pointing at the archive files. Attic files get a two-line header (what they are, moved verbatim, date).
4. AR-B-4 (canonical hierarchy + gate). Recorded as standing rule in the ADR: the ADR execution record is the sole narrative of a session; the session record is the ceremony/verification log; the roadmap Done entry is a one-line pointer (date, slug, ADR number); the prompt archive is provenance. One new docs-tooling deftest: every Done one-liner in the LIVE roadmap cites an ADR number that exists in the index (both-direction checks are C's scope if wanted; this gate is one-direction, cheap). Red→green proven live (corrupt one pointer, watch it fail, restore).
5. AR-B-5 (deferred to C, recorded). The continuity register (`.agents/state.md`) is session C's deliverable, design-channel-authored with per-claim citations against THIS session's landed layout. B records the ruling verbatim so the ADR trail shows the approval date.

Steps
Step 0 — Characterize. Verify tip. Enumerate every `## ADR-` boundary in notes/ADRs.md (count, line ranges, titles — the split map, recorded in full). Enumerate Done entries with dates (the rotation map). Reading-set membership check: which sets include notes/ADRs.md or roadmap.md (their budgets re-derive in Step 3 per AR-D-3's formula, membership unchanged — if the index should join or leave a set, that is a FINDING for the author, not an edit). Form-anchored deftest counts.
Step 1 — The ADR split (AR-B-1/2). The per-file moves, the index. PROOF, recorded verbatim in the session record: concatenating the per-ADR files in index order, minus each file's added header line(s), byte-equals the original entries' extraction — script the diff, show the command and its silence. `poly check` + full suite green (budget gate: take the licensed temporary bump if needed, re-derive properly in Step 3, record which). Commit: `docs(adr): the register becomes an index — forty-plus entries move to the attic verbatim (compaction B, AR-B-1/2)`
Step 2 — The roadmap rotation (AR-B-3). Same proof discipline: extraction diff per archive file. The live roadmap's new Done section. Commit: `docs(roadmap): the Done history moves to dated attic files — the live map is Now again (compaction B, AR-B-3)`
Step 3 — Budgets + the pointer gate (AR-B-4). Reading-set budgets re-derive (AR-D-3 formula, one dated note); the new deftest, red→green live. All gates green. Commit: `chore: budgets re-derive for the compacted surfaces; Done pointers grow a gate (compaction B, AR-B-4)`
Step 4 — ADR + record. The new ADR (rulings verbatim including AR-B-5's deferral note, the split map, both proofs cited); the live roadmap's own Done pointer for this session (the gate's first real subject). Session record `.agents/session-records/2026-08-05-scaffolding-compaction-b.md`; prompt self-archives. Oracle bracket (pre-B tip → landing): all ELEVEN batches byte-identical — this session touches no src at all; any digest change is STOP-AND-ESCALATE. Deftest parity: +1 (the gate), ledgered, counting definition stated. Final commit: `docs: compaction B session record — history in the attic, indexed and verbatim`
Fences
Nothing is deleted — every byte relocates or stays; the extraction diffs are the proof. No content rewrites: index lines and attic headers are the ONLY new prose. No citation sweep (AR-B-2 makes it unnecessary — performing one anyway is scope creep). No rulings register, no state.md (session C). No reading-set membership changes. Frozen archives untouched — `notes/adr/` is a NEW attic, not a modification of the frozen `notes/sim`/`notes/tools`/ `notes/prompts` tombstones, and the ADR says so. If any ADR entry's boundaries are ambiguous (nested `##`, malformed header), that is a STOP-AND-ESCALATE, not a judgment call.
