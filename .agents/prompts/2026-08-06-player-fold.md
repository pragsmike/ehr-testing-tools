2026-08-06 — player fold: the accumulator learns two-participant messages and absolute time Session prompt (design channel, 2026-08-06). OPENS THE PLAYER ARC (bed-board slice, roadmap Next's first corpus-player named future, ADR-0014's own deferral). Prior: the ux epilogue landed and was design-channel-verified (`03a8698`, ADR-0065). The author ruled the arc's shape today (design channel, 2026-08-06 — transcript-witnessed until THIS session records it, AR-BB1-R): (1) the fold extension comes FIRST; (2) the component-graph change the wiring needs (exporting `v2-replay` through `ehrt.sim-emit-hl7.interface`, a new corpus→sim-emit-hl7 edge) is approved for session 2, not taken here; (3) timing goes GENERAL — the bed board must serve foreign traffic, so the fold self-anchors from the wire, no reference-date parameter. Also ruled: module vendoring (ADR-0064 intake, recorded `[unverified]`) is RATIFIED as real and scheduled after this arc — the author ratification ADR-0064's own close record said was owed is hereby given; record it. Why the fold first, probe-backed: `ehrt.sim-emit-hl7.v2-replay/fold-message` throws `:unsupported-trigger` on A17/A40 by documented scope boundary, while bare `--churn`'s `sample-profile` emits both (`:bed-swap 0.03`, `:merge 0.01` — `ehrt.sim-engine.churn/sample-profile`) — a bed board fed default churn traffic would crash, and a board is precisely the surface that cares about bed swaps. This session makes the fold total over the emitter's real trigger set and self-anchoring in time; session 2 wires it into the player and builds the board sink. R30 ceremony. Read-first: `components/sim-emit-hl7/src/ehrt/sim_emit_hl7/v2_replay.clj` WHOLE (the ns docstring's own scope-boundary paragraph is what this session retires; `hl7-instant->seconds` is what generalizes; `project-to-wire-visible-fields` includes `:admitted-at`/`:discharged-at`/observation `:t` — times ARE compared); `emit_hl7.clj` `bed-swap-message` (~474: ONE message, MSH/EVN then TWO PID+PV1 pairs in participant order, then Z-segments) and `merge-message` (~499: PID carries the SURVIVING mrn, MRG-1 the merged-away one); `v2_replay_test.clj` (the `no-bed-swap-no-merge-churn` exclusion ~117 and `coherent-at-every-boundary?` — the property this session extends into the spec); `ehrt.sim-engine.engine`'s merge/bed-swap decide+apply and its PatientState schema (`:status` enum already carries `:merged`; `:mrns`/`:active-mrn` — read, align, do NOT modify player code this session). Author rulings (record verbatim in ADR-0066)

1. AR-BB1-0 (tag, standing ceremony). Annotated `stable-20260806-ux-epilogue` at `03a8698`, message `ux epilogue landed, design-channel-verified 2026-08-06 (ADR-0065)`; push; verify peeled ref.
2. AR-BB1-R (rider — the arc's rulings become repo fact). ADR-0066 records verbatim: the author's three arc-shape rulings and the vendoring ratification stated in this prompt's own preamble (fold-first; graph-change approved for session 2; timing general; module vendoring ratified, scheduled after this arc). The roadmap's Next row for module vendoring may gain a one-line dated ratification note citing ADR-0066; nothing else moves — vendoring stays next-arc scope.
3. AR-BB1-1 (A17 — the fold learns two participants). `fold-message` handles trigger "A17": walk the parsed message's segments in order, pair each PID with its own immediately-following PV1 (two pairs; stop pairing at any non-PID/PV1 segment — Z-segments may trail), and fold each pair as a location/attending update onto that PID-3's own entry (the A02 treatment, per pair) — never-seen MRNs self-initialize (bootstrap-from-empty holds, foreign traffic may open mid-stream). Red-first unit test against a real rendered A17 (build it via the emitter's own `bed-swap-message` path, not a hand-typed string — audit evidence uses the mechanism it recommends).
4. AR-BB1-2 (A40 — the fold learns identity reassignment). `fold-message` handles trigger "A40": PID-3 is the surviving MRN, MRG-1 the merged-away one. The surviving entry absorbs per the engine's own merge semantics; the merged-away MRN's entry becomes a tombstone whose shape is chosen by fresh read of `ehrt.sim-engine.engine`'s own merge apply and the PatientState `:merged` status (the wire-side mirror of what the engine's log-fold does — NOT invented independently). Whatever shape lands must satisfy AR-BB1-3's extended coherence property; that property is the arbiter, this prompt's prose is not. Red-first unit test against a real rendered A40, same mechanism rule.
5. AR-BB1-3 (the property becomes the spec). The emitter-coherence defspec's churn profile extends to the FULL `sample-profile` set — the `no-bed-swap-no-merge-churn` exclusion dies. Red-first, witnessed: BEFORE the fold changes land, run the extended property and record a failing seed whose traffic contains an A17 or A40 (if 150 trials at the default sizes don't reliably produce one, pin a generator or seed that does, disclosed — a property that cannot exercise the new triggers is not red, it is absent). Green after. The `v2_replay` ns docstring's scope-boundary paragraph rewrites from "documented not silent" exclusion to supported-with-citation; `v2_replay_test.clj`'s own header comment likewise. Grep for any OTHER surface stating the A17/A40 exclusion (docs, ADR cross-references are frozen history and stay; live docs/docstrings reconcile) — the law-surface propagation lesson applies.
6. AR-BB1-4 (general time — the fold self-anchors from the wire). `hl7-instant->seconds` and `fold-message`'s `reference-date` parameter are RETIRED: MSH-7 (and observation timestamps) parse to an ABSOLUTE epoch instant read from the wire alone — honoring an explicit trailing offset (`±ZZZZ`/`Z`) when present, treating a naive timestamp as UTC otherwise, lenient on truncated precision to the same tolerance `ehrt.corpus.player/parse-dtm-lenient` already implements (read it; align the contract; extract nothing across the component boundary this session — a shared helper, if ever wanted, is session-2-or-later scope, disclosed as a finding not taken, per promotion-moves-not-improves). Unit: epoch MILLIS, matching the player's own representation. The coherence property compares via a TEST-SIDE adapter converting the engine's run-relative seconds to absolute millis using the run's own reference-date — `project-to-wire-visible-fields` itself does not change shape and the production fold never sees a reference-date again. `v2-replay`'s callers are zero outside its own component (interface header's own grep discipline, confirmed at tip) — the signature change is free, verify by fresh grep and record the grep.
7. AR-BB1-5 (scope + oracle). NO player, corpus, CLI, or interface changes — `v2-replay` stays unexported this session (the export and the corpus→sim-emit-hl7 edge are session 2's approved change, not this one's). NO emitter changes: `bed-swap-message`/`merge-message` render correctly today and their bytes are oracle-covered; if the fold work surfaces an emitter defect, that is a FINDING, recorded, not fixed (the projection's own LOINC narrowing precedent). The oracle bracket must show all ELEVEN batches identical — the fold is read-side only; any digest change is STOP-AND-ESCALATE. `config/busy-weekday.md` stays untracked and untouched.

Steps Step 0 — Preflight + tag. Cwd ext4; tip `03a8698` or later-with-disclosure; working tree clean apart from the standing untracked `config/busy-weekday.md`; full suite green baseline; oracle pre-digest. Execute AR-BB1-0. Step 1 — Red, witnessed (AR-BB1-1/2/3 tests first). The two unit reds (real rendered A17/A40 through the old fold → `:unsupported-trigger`); the extended property red with a recorded A17/A40-bearing failing seed; transcripts captured for ADR-0066. Commit: `test: the fold's scope boundary turns red — A17/A40 traffic witnessed unfolded (player fold, AR-BB1-1/2/3 red)` Step 2 — The fold extension + general time (AR-BB1-1/2/3/4). Implementation lands; all reds green; full suite green from workspace root; `poly check` OK; docstring scope-boundary rewrites + the propagation grep recorded. ONE commit: `feat: the accumulator learns two-participant messages and absolute time (player fold, AR-BB1-1/2/3/4)` Step 3 — ADR-0066 + record. Rulings verbatim (AR-BB1-R's author statements included); red transcripts; the merge-tombstone shape and its engine-side justification; the zero-external-callers grep; index line; Done pointer `- 2026-08-06 — player-fold — ADR-0066`. Oracle bracket (`03a8698` → tip) per AR-BB1-5. Session record + prompt self-archive. Final commit: `docs: player fold record — the coherence property is the spec, and it holds over full churn (ADR-0066)` Fences Src edits ONLY in `components/sim-emit-hl7/src/ehrt/sim_emit_hl7/v2_replay.clj` and its own test tree. Interface untouched. Emitter untouched. Player untouched. No gate weakening — the property extension only widens what must hold. Frozen archives untouched except ADR-0066 + index. If the engine's merge semantics make the wire-side tombstone genuinely ambiguous (the coherence property satisfiable two ways), STOP-AND-REPORT with both shapes and the evidence — that is a design-channel decision, not a session guess. After landing: design channel verifies by fresh probe — re-running the extended property, reading the tombstone shape against the engine's own — then session 2 (accumulator wiring, the interface export and corpus→sim-emit-hl7 edge under ruling (2), the `--board` cadence flag, the board renderer and its gates) gets its prompt, and this landing's tag rides its Step 0 under standing ceremony.

---

**Deviation disclosed (recorded by the executing session, not part of the original prompt):** Step 0's stated premise ("working tree clean apart from the standing untracked `config/busy-weekday.md`") did not hold — the live tree instead carried two DIFFERENT untracked scratch files, `config/busy-tuesday.edn` and `config/busy-weekday.edn` (neither is `config/busy-weekday.md`, which does not exist on disk at all), and their presence broke `merge-config-file-suggests-a-same-stem-sibling-file`'s own sibling-file assumption. Flagged to the author before proceeding; the author relocated both files out of `config/` themselves. `config/busy-weekday.md`'s own absence is a separate, pre-existing, unrelated gap — disclosed in ADR-0066 AR-BB1-5, left unfixed (out of this session's fenced scope).
