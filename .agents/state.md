# State of the project — continuity register

Regenerated by the design channel 2026-08-05 at the alignment arc's own
close (`notes/adr/0055-alignment-arc-close.md`, AR-AC-3), landed against
tip `992e0a5` — every `[V]` claim below was re-probed against the live
tree THIS session before landing; nothing carried forward on the prior
version's own authority (this file's own regeneration contract,
ADR-0047 AR-C-1, restated `.agents/rulings.md`). The prior version was
authored 2026-08-05 against tip `53edcad` (scaffolding compaction C,
ADR-0047) — everything below supersedes it. Regenerated at each arc
close (standing rule, unchanged). Provenance tags: `[A]` author-ruled,
`[C]` channel-inferred, `[V]` probe-verified at landing.

## What this repo is

Clojure/Polylith workspace generating deterministic synthetic hospital
traffic (Synthea GMF at pinned commit `7e08387c...f902`), injecting
defects, judging conformance (HL7v2/FHIR). Formats are emitters of the
patient state machine `[A]` (`notes/adr/` index: ADR-0043's own
"sibling emitters over one state machine" doctrine). Book repo
`ehr-testing-guide` is separate, permanently out of scope (ADR-0001
R2).

## Component graph (post sim-split-B, post alignment fixes 3) `[V @992e0a5]`

18 `components/*` directories, 1 `bases/*` directory (`bin/regression-
oracle`'s own worktree-count convention re-run: `ls -d components/*/ |
wc -l` → 18, `ls -d bases/*/ | wc -l` → 1) — unchanged in count since
the prior regeneration; the arc's own src-touching session (alignment
fixes 3, ADR-0052) renamed a resource directory, not a component.

`sim-engine` (engine/churn/order-profiles) ← {`sim-check`,
`sim-emit-fhir`, residual `sim` (run/identifiers/version/manifest-
build/façade), `oracle`}. Fresh grep of every real
`ehrt.sim-engine.interface` requirer (`grep -rl "ehrt.sim-engine.interface"
components/*/src/`) found the same five callers the prior regeneration
recorded, still exactly five: `sim-check/check.clj`, `sim-emit-fhir/
emit_fhir.clj`, `sim/identifiers.clj`, `sim/run.clj`, dev-adjacent
`oracle/digest.clj`. `sim-emit-hl7/emit_hl7.clj`'s own `:require` form
(read directly, not grepped for text) is exactly `[ehrt.sim-model.
interface :as sim-model]` and its own `[ehrt.sim-emit-hl7.site-profile
:as site-profile]` — matching AGENTS.md's own constraint and now a
GATED invariant (`ehrt.docs-tooling.sim-emit-hl7-dependency-test`,
alignment fixes 2, ADR-0051 AR-F2-2), not merely an observed fact. This
component graph claim is therefore stronger than the prior
regeneration's own citation: it no longer depends on a session's own
fresh grep holding by convention alone.

`provenance` ← {`corpus`, `sim`}, forbidden forever from any `ehrt.*`
require beyond itself — now GATED (`ehrt.docs-tooling.provenance-leaf-
law-test`, alignment fixes 2, ADR-0051 AR-F2-3), same strengthening.
`provenance`'s own `deps.edn` confirmed live: `{:deps {metosin/malli
{:mvn/version "0.20.1"}}}`, nothing else. `oracle` = the digest
component (each worktree carries its own producer via its own
synthetic classpath, ADR-0044 AR-P-2/AR-P-3). Census lives in
`sim-trajectory` (confirmed live, `census_test.clj`). Façade
`ehrt.sim.interface` surface frozen `[A]` (`.agents/plans/
2026-08-02-sim-split-plan.md` AR-3; corpus depends on this interface's
own stability in-process, ADR-0012).

**Resource nesting, closed and gated (alignment fixes 3, ADR-0052).**
The seven `components/*/resources` directories workspace-wide
(`judge`, `kernel`, `provenance`, `sim-engine`, `sim-model`, `sim-
trajectory`, `sim` — confirmed live, `for d in components/*/resources;
do ls $d; done`) now ALL nest under their own brick name;
`sim-model`'s own former `resources/sim/` tolerance (ADR-0025, once
disclosed as deliberate and temporary) closed — `resources/sim-model/`
now, matching every sibling. Gated: `ehrt.docs-tooling.resource-
nesting-test`, no allowlist.

## Where history lives `[V @992e0a5]`

* `notes/ADRs.md` = INDEX (99 lines, confirmed live); entries at
  `notes/adr/NNNN-slug.md`. **File count: 52**, confirmed
  (`ls notes/adr/*.md | grep -v README | wc -l`) — the prior
  regeneration's own "45" was accurate only for its own landing
  instant (ADR-0047); seven more attic files were added by this same
  arc (ADR-0048 alignment riders through ADR-0054 alignment fixes 5).
  This ADR's own file (`0055-alignment-arc-close.md`) will make it 53
  once it lands — the exact staleness-at-the-moment-of-count pattern
  the prior regeneration named explicitly; noted here rather than
  chased, since chasing it is definitionally impossible for a count
  that includes the file stating the count.
* Roadmap = live map only (Now/Next/Externals/Deferred/Done-current-
  arc-only); Done history at `.agents/plans/roadmap-done-2026-07.md`
  (0 entries, unchanged) and `roadmap-done-2026-08.md`. The attic
  file's own `## Done (...)` header count is **34** (33 from
  compaction B's own rotation, ADR-0046, plus one added by alignment
  riders, ADR-0048 AR-R-4 — a pre-existing stray Deferred-row closure,
  `myocardial_infarction.json`, relocated verbatim, not this arc's own
  session-narrative content). This arc's close (AR-AC-5, this session)
  adds a DIFFERENTLY-shaped section — `## Alignment arc — closed
  2026-08-05 (ADR-0048–0055)` — holding the arc's own one-line Done
  pointers (ADR-0048 through ADR-0054) relocated verbatim from the live
  roadmap; this is a dated arc header, not a `## Done (this session,
  ...)` entry, so the 34-count above is unaffected by it. **Scope note,
  disclosed:** AR-AC-5 names only ADR-0048 through ADR-0054 for
  relocation; the live roadmap's own Done section also still carries
  three older one-line pointers from the scaffolding-compaction arc
  (ADR-0045/0046/0047), a separate, already-closed arc whose own
  pointers were never rotated at ITS close. Those three are OUT of this
  ruling's own named range and stay in place — "ADR-0055's own pointer
  lands as the Done section's sole current entry" is read as sole
  current entry FOR THE ALIGNMENT ARC, not a license to also relocate a
  different arc's own leftover pointers unnamed by this ruling. A
  future arc-close session inherits the compaction pointers as a small,
  named cleanup, not silently swept here.
* Session narrative hierarchy `[A, AR-B-4]`: ADR execution record =
  sole narrative; session record = ceremony log; roadmap Done = one-
  line pointer (gated: `ehrt.docs-tooling.done-pointer-adr-test`);
  prompt archive = provenance (gated: `ehrt.docs-tooling.prompt-record-
  pairing-test`).

## Standing gates `[V @992e0a5]`

`components/docs-tooling/test/ehrt/docs_tooling/` holds **19 test
files** (confirmed live, up from the prior regeneration's implicit
14-ish baseline before this arc's own five new gates): structure-
currency; index-completeness; reading-set budgets (`:onboarding`
re-derived this session, AR-AC-4, 1095→1160 — the only set whose member
paths this arc touched; `:corpus`/`:sim`/`:judge`/`:docs` unchanged);
stale-path
tripwire; prompt/record pairing; done-pointer→ADR; readme-presence;
skill-mirror-currency; docsgen/pipeline/usecases/quickstart-fresh/lint
(doc-generation gates, unrelated to this arc); notes-prompts-frozen.
**Five gates this arc added, each red→green witnessed against the live
tree before landing (ADR-0051/0052/0054):** `sim-emit-hl7-dependency-
test` (AR-F2-2); `provenance-leaf-law-test` (AR-F2-3); `root-alias-
completeness-test` (AR-F2-4); `resource-nesting-test` (AR-F3-2);
`license-text-pointer-test` (AR-F5-3). Oracle: `bin/regression-oracle`,
per-worktree digest via `components/oracle`, cross-side source-
equivalence check, `--declared-digest-change` flag, all-eleven-batches
identity the standing expectation for structural/docs sessions — held
across every one of this arc's own seven ADRs (0048–0054), confirmed
by direct read of each ADR's own Verification section, never merely
asserted here.

## Live work `[V @992e0a5]`

* Deferred = LIVE rows only (**13**, confirmed by direct count,
  unchanged from the prior regeneration — this arc touched zero
  Deferred rows). The `myocardial_infarction.json` drift the prior
  regeneration disclosed (an in-place closure note rather than an
  actual relocation) is CLOSED — alignment riders (ADR-0048 AR-R-4)
  relocated it verbatim to the attic; see "Where history lives," above.
* Next (roadmap, titles abbreviated — read the roadmap for detail):
  lookup `time` gap, Wave G attachment trigger, pairing-as-data
  registry (landing spot now RULED — `judge`, `.agents/rulings.md`,
  D-3, this arc's own append), storefront fixture, quickstart→nightly,
  generator-source split, corpus.display placement, table-helper
  dedup. Unchanged in membership since the prior regeneration.
* Externals (author-only): NIST licensing inquiry — **citation
  corrected**: the prior regeneration's own source ADR (ADR-0053) cites
  `docs/experiments/EXP-SBOM-inquiry-draft.md`; no such file exists.
  The real evidence doc is `components/corpus/docs/experiments/
  EXP-SBOM-results.md` (92-line protocol at the sibling `EXP-SBOM.md`,
  200-line results); the inquiry draft ITSELF is explicitly "maintained
  privately by the author," not a repo artifact — confirmed by direct
  read of the results doc's own "Artifacts produced" table. The
  inquiry's own posture is narrowed, not resolved, by that doc's own
  classification work (per-coordinate license findings, two disclosed
  scope-limit caveats — `v2-validation`'s full-text search blocked by
  GitHub 401, three Maven Central deps' evidence "one hop removed" from
  an underreferenced parent POM) — still open, still author-only (send
  the drafted gist). IG pinning: still open. Clojars publish: ruled,
  deferred, unchanged. SETUP rewalk: still open. `/mnt/c` disposition:
  closed (ADR-0047 AR-C-3, unchanged).

## Environment `[A, 2026-08-05]`

Claude Code project root = UNC path to the ext4 clone, unchanged, no
new incident this arc (`[V]`: fresh grep of this arc's own seven
session records for `mnt/c`/`dual-clone`/`clone stale` — zero hits).
The three-cluster `/mnt/c` incident ledger the prior regeneration
recorded stands, historical, unrevisited.

**Tag mechanic, reconciled and exercised (this arc's own main
structural theme).** `AGENTS.md`'s tag rule (alignment fixes 2, ADR-0051
AR-F2-0) now states the same law `.agents/rulings.md`'s own AR-R-2 and
ADR-0049's AR-AU-0 state: a session MAY create and push a `stable-*`
continuity tag when its own prompt licenses a SPECIFIC tag at a
SPECIFIC commit, issued by the design channel only after verifying that
landing. **Six `stable-*` continuity tags live** as of this session's
own Step 0 (confirmed, `git tag -l 'stable-*'`, excluding the three
frozen legacy tags `stable-bootstrap`/`stable-ehrt-1`/`stable-pre-
monorepo`): `stable-20260805-alignment-riders`, `-alignment-audit`,
`-alignment-fixes-1`, `-alignment-fixes-2`, `-alignment-fixes-3`,
`-alignment-fixes-4`. `-alignment-fixes-5` is licensed by this arc's
own close but not yet tagged (AUTHOR ACTION, prepared not executed —
see `notes/adr/0055-alignment-arc-close.md` AR-AC-0); `-alignment-close`
itself is licensed only after design-channel verification of this
session's own landing, per the standing after-landing sequence.

**NIST supply-chain posture, mechanized (alignment fixes 4, ADR-0053).**
`deps.edn`'s own `nist-hit` Nexus stays live-network at build time, but
redistribution is foreclosed (ADR-0005's 2026-07-24 amendment): `bin/
mirror-nist` builds a USER-SIDE `~/.ehrt/nist-mirror/` from the user's
own `~/.m2` cache, sha256-verified against `artifacts.lock.edn`; `bin/
verify-nist-lock` is wired as `make test`'s own third line (confirmed
live, `Makefile` lines 38–41), so a lockfile/cache sha256 drift now
fails the per-push lane instead of passing silently. The licensing
inquiry itself stays open (see "Live work," above).

**`workspace.edn`, slimmed (alignment fixes 1, ADR-0050 AR-F1-4).**
33 lines (confirmed live, down from the ~70-line pre-fix shape); the
40-line `:necessary`-override narrative relocated verbatim into
ADR-0050. `"development"` project carries a documented `:necessary
["oracle"]` override (REPL-uniformity rationale, inline comment citing
ADR-0050 register row A-1).

**The alignment arc's own pointer.** Register at `.agents/plans/
2026-08-05-alignment-audit-findings.md` (dated audit artifact, 47
rows); dispositions final per `notes/adr/0055-alignment-arc-close.md`
— closed-this-arc, deferred-to-publish-prep, standing-ruling-recorded,
and recommendation-only rows, per that ADR's own tally.

## Design-channel contract (how sessions run)

Two channels: design (chat) plans/verifies vs public clone; Code
executes pasted prompts, self-archives, R30 ceremony. Evidence over
ruling — live probes have overturned survey rows, channel claims, and
the author's own summaries repeatedly across this arc (three separate
precision gaps in ADR-0054 alone, caught before code was written; the
EXP-SBOM citation drift caught by this very regeneration, above).
Continuity: new design sessions read THIS file + roadmap + latest ADR
execution record — structural claims in any continuity prompt carry
citations or an explicit `[unverified]` tag `[A, AR-B-5]`.
