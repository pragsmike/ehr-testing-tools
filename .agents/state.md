# State of the project — continuity register

Regenerated by the design channel 2026-08-06 at the UX arc's own close
(`notes/adr/0064-ux-arc-close.md`, AR-UC-3), landed against tip `f5af489`
(the UX arc's own predecessor tip — every `[V]` claim below was
re-probed against the live tree THIS session before landing; nothing
carried forward on the prior version's own authority (this file's own
regeneration contract, ADR-0047 AR-C-1, restated `.agents/rulings.md`).
The prior version was authored 2026-08-05 against tip `992e0a5`
(alignment arc close, ADR-0055) — everything below supersedes it.
Regenerated at each arc close (standing rule, unchanged). Provenance
tags: `[A]` author-ruled, `[C]` channel-inferred, `[V]` probe-verified
at landing.

## What this repo is

Clojure/Polylith workspace generating deterministic synthetic hospital
traffic (Synthea GMF at pinned commit `7e08387c...f902`), injecting
defects, judging conformance (HL7v2/FHIR). Formats are emitters of the
patient state machine `[A]` (`notes/adr/` index: ADR-0043's own
"sibling emitters over one state machine" doctrine). Book repo
`ehr-testing-guide` is separate, permanently out of scope (ADR-0001
R2).

## Component graph (unchanged this arc — the UX arc touched `bases/cli`
and docs-tooling gates, never a component) `[V @f5af489]`

18 `components/*` directories, 1 `bases/*` directory (`ls -d
components/*/ | wc -l` → 18, `ls -d bases/*/ | wc -l` → 1) — unchanged
in count since the prior regeneration; the UX arc's own five src-
behavior sessions (ADR-0060/0061/0062/0063) edited `bases/cli` and its
own tests only, never a component boundary.

`sim-engine` (engine/churn/order-profiles) ← {`sim-check`,
`sim-emit-fhir`, residual `sim` (run/identifiers/version/manifest-
build/façade), `oracle`}. Fresh grep of every real
`ehrt.sim-engine.interface` requirer found the same five external
callers the prior regeneration recorded, still exactly five:
`sim-check/check.clj`, `sim-emit-fhir/emit_fhir.clj`,
`sim/identifiers.clj`, `sim/run.clj`, dev-adjacent `oracle/digest.clj`.
`sim-emit-hl7/emit_hl7.clj`'s own `:require` form (read directly) is
still exactly `[ehrt.sim-model.interface :as sim-model]` and
`[ehrt.sim-emit-hl7.site-profile :as site-profile]` — HELD, still
gated (`ehrt.docs-tooling.sim-emit-hl7-dependency-test`).

`provenance` ← {`corpus`, `sim`}, forbidden forever from any `ehrt.*`
require beyond itself — HELD, still gated
(`ehrt.docs-tooling.provenance-leaf-law-test`); `provenance`'s own
`deps.edn` confirmed live, unchanged: `{:deps {metosin/malli
{:mvn/version "0.20.1"}}}`, nothing else. `oracle` = the digest
component (each worktree carries its own producer via its own
synthetic classpath, ADR-0044 AR-P-2/AR-P-3). Census lives in
`sim-trajectory` (confirmed live, `census_test.clj`, untouched this
arc). Façade `ehrt.sim.interface` surface frozen `[A]` (`.agents/plans/
2026-08-02-sim-split-plan.md` AR-3; corpus depends on this interface's
own stability in-process, ADR-0012).

**Resource nesting, closed and gated (alignment fixes 3, ADR-0052),
HELD unchanged.** The seven `components/*/resources` directories
workspace-wide (confirmed live, unchanged set: `judge`, `kernel`,
`provenance`, `sim-engine`, `sim-model`, `sim-trajectory`, `sim`) all
still nest under their own brick name. Gated:
`ehrt.docs-tooling.resource-nesting-test`, no allowlist.

## Where history lives `[V @f5af489]`

* `notes/ADRs.md` = INDEX (108 lines, confirmed live); entries at
  `notes/adr/NNNN-slug.md`. **File count: 61**, confirmed
  (`ls notes/adr/*.md | grep -v README | wc -l`) — this ADR's own file
  (`0064-ux-arc-close.md`) will make it 62 once it lands, the exact
  staleness-at-the-moment-of-count pattern the prior two regenerations
  both named explicitly; noted here rather than chased, since chasing
  it is definitionally impossible for a count that includes the file
  stating the count.
* Roadmap = live map only (Now/Next/Externals/Deferred/Done-current-
  arc-only); Done history at `.agents/plans/roadmap-done-2026-07.md`
  (0 entries, unchanged) and `roadmap-done-2026-08.md`. **This
  session's own rotation (AR-UC-5):** ADR-0055's own pointer — left in
  place as the live roadmap's sole current entry at the alignment
  arc's own close, a disclosed leftover of the same class ADR-0055
  itself named for the scaffolding-compaction pointers — relocates
  into the attic's existing `## Alignment arc — closed 2026-08-05
  (ADR-0048–0055)` section with a dated append note, closing that
  section's own range for real. A new `## UX arc — closed 2026-08-06
  (ADR-0056–0064)` header holds the UX arc's own eight one-line Done
  pointers (ADR-0056 through ADR-0063), relocated verbatim from the
  live roadmap. `.agents/plans/roadmap.md`'s own Done section now holds
  ADR-0064's own pointer as its sole current entry — no leftover this
  time (the alignment-arc leftover pattern was checked for and closed,
  not repeated).
* Session narrative hierarchy `[A, AR-B-4]`: ADR execution record =
  sole narrative; session record = ceremony log; roadmap Done = one-
  line pointer (gated: `ehrt.docs-tooling.done-pointer-adr-test`);
  prompt archive = provenance (gated:
  `ehrt.docs-tooling.prompt-record-pairing-test`).

## Standing gates `[V @f5af489]`

`components/docs-tooling/test/ehrt/docs_tooling/` holds **21 test
files**, confirmed live (`ls ... | wc -l`), up from the prior
regeneration's 19 — **two new this arc**: `invocation_lint_test.clj`
(created ux-fixes-1, ADR-0059, then EXTENDED with a fence-path-
resolution assertion by ux-fixes-2's own rider, ADR-0060 AR-U2-R —
grammar-validity is not path-validity, the lesson `.agents/rulings.md`
now records standing); `tag_law_test.clj` (created tag-law, ADR-0057,
forbidding the retired author-only tag phrasing on any live surface).
Reading-set budgets: `:onboarding` re-derived this session (AR-UC-4,
1160→1205 — the only set whose member paths this arc touched;
`:corpus`/`:sim`/`:judge`/`:docs` unchanged, confirmed by diffing every
set's own `:paths` against `git log 12d3aa3..HEAD --name-only`).
Structure-currency; index-completeness; stale-path tripwire;
prompt/record pairing; done-pointer→ADR; readme-presence;
skill-mirror-currency; docsgen/pipeline/usecases/quickstart-fresh/lint
(doc-generation gates, unrelated to this arc); notes-prompts-frozen;
the five alignment-arc gates (`sim-emit-hl7-dependency-test`,
`provenance-leaf-law-test`, `root-alias-completeness-test`,
`resource-nesting-test`, `license-text-pointer-test`) — all HELD,
unchanged, none touched this arc.

**Two more gates land OUTSIDE the docs-tooling family this arc, in
`bases/cli/test/ehrt/cli/`, confirmed live:** `help_voice_test.clj`
(created ux-fixes-4, ADR-0062 — walks `cli-spec` as rendered data,
word-bounded patterns, rejects any ADR/milestone/ruling token or bare
keyword/internal-namespace leak into a user-facing string);
`help_wrap_test.clj` (created ux-fixes-5, ADR-0063 — 80-column wrap
width plus byte-content-preservation). Unknown-flag rejection
(ux-fixes-3, ADR-0061) and the config-crash-dies error vocabulary
(ux-fixes-2, ADR-0060) landed as new `deftest`s inside the base's own
pre-existing `core_test.clj`/`run_test.clj`/`identifiers_test.clj`,
not new gate files — confirmed by this session's own fresh read of
each, not merely inferred from the ADRs' own claims.

Oracle: `bin/regression-oracle`, per-worktree digest via
`components/oracle`, cross-side source-equivalence check,
`--declared-digest-change` flag, all-eleven-batches identity the
standing expectation for structural/docs/error-vocabulary/help-voice
sessions — held across every one of this arc's own eight ADRs
(0056–0063), confirmed by direct read of each ADR's own Verification
section, never merely asserted here; this session's own Step 0
pre-digest (`bin/regression-oracle f5af489 f5af489`) reconfirms all
eleven roots IDENTICAL at this arc's own closing tip.

## Live work `[V @f5af489]`

* Deferred = LIVE rows only (**13**, confirmed by direct count,
  unchanged from the prior regeneration — the UX arc touched zero
  Deferred rows; it is a `bases/cli`/docs arc, not a sim-trajectory
  one).
* Next (roadmap, **11** rows by fresh count, titles abbreviated — read
  the roadmap for detail): lookup `time` gap, Wave G attachment
  trigger, pairing-as-data registry (landing spot `judge`, ruled
  ADR-0050 D-3), storefront fixture, quickstart→nightly, generator-
  source split, corpus.display placement, table-helper dedup, three
  corpus-player named futures (bed board/census sink, accumulator
  wiring, sim event-log input adapter). Unchanged in membership since
  the prior regeneration — the UX arc added no Next row (its own
  register rows landed as fix sessions or standing rulings, not
  backlog).
* Externals (author-only), unchanged this arc, restated: NIST
  licensing inquiry — narrowed, not resolved (per-coordinate
  classification in `components/corpus/docs/experiments/
  EXP-SBOM-results.md`), still author-only (send the drafted gist). IG
  pinning: still open. Clojars publish: ruled, deferred — F-5/F-6 are
  its own remaining decisions (unchanged since the alignment arc's own
  tally). SETUP rewalk: still open. `/mnt/c` disposition: closed
  (ADR-0047 AR-C-3), unchanged.
* **Both audit registers' pointers, with final-tally status.**
  Alignment audit register (`.agents/plans/
  2026-08-05-alignment-audit-findings.md`, 54 rows by the alignment
  arc's own fresh count): dispositions FINAL per
  `notes/adr/0055-alignment-arc-close.md` — 17 closed across five fix
  sessions, 2 became standing rulings, 2 deferred to publish-prep, 3
  disclosed methodology caveats, 29 needed no action, 1 (D-4) an
  honest next-arc-intake miss, never chased. UX audit register
  (`.agents/plans/2026-08-06-ux-audit-findings.md`, **21 rows carrying
  a disposition by this session's own fresh count** — matches the
  register's own stated "21 total rows carrying a disposition"
  exactly, `grep -E '^\| [A-Z]-?[0-9]'` over the table rows; UNLIKE the
  alignment register, which the alignment arc close found internally
  inconsistent (51-vs-47), this register's own summary arithmetic
  checks out clean, no drift found): dispositions FINAL per
  `notes/adr/0064-ux-arc-close.md` — full tally: 8 close-as-fine
  (U5/A-1/A-2/A-3/C-2/C-3/C-5/D-2, no action needed, verified none
  regressed); 1 ruling-needed (B-5, plus D-1's identical citation) —
  RULED and EXECUTED (AR-U2-4, ADR-0060, bare invocation exits 0); 6
  fix-session-candidate (U1, U4, B-4's mechanism half, B-6, C-1, C-4,
  plus D-3's identical citation) — ALL SIX CLOSED, across ADR-0059
  (U1's sweep), ADR-0060 (U1's own path-resolution rider, U4, B-6,
  C-1), ADR-0061 (C-4), ADR-0063 (B-4's mechanism half, the wrap); 4
  design-channel-draft (B-1, B-2, B-3, B-4's content half) — ALL FOUR
  CLOSED, ADR-0062, the approved rewrite applied verbatim; 1 incomplete
  (D-4, the artifact-fetch-dependent Quickstart remainder) — STANDING,
  not actionable by this register's own framing (a judgment row noting
  RUN output is clean, not a defect), no arc action owed, next-session
  intake if full Quickstart fidelity is ever wanted.

## Environment `[A, 2026-08-06]`

Claude Code project root = UNC path to the ext4 clone, unchanged, no
new incident this arc (`[V]`: fresh grep of this arc's own eight
session records for `mnt/c`/`dual-clone`/`clone stale` — zero hits;
`df -T .` confirms `ext4` at this session's own Step 0).

**Tag mechanic, HELD unchanged (tag law, `notes/adr/0057-tag-law.md`
AR-T-1/AR-T-2, 2026-08-06).** `stable-*` continuity tags are a SESSION
ACT: a session creates and pushes one (i) when its own prompt licenses
a SPECIFIC tag at a SPECIFIC commit, issued by the design channel only
after verifying that landing, or (ii) for its own predecessor's
design-channel-verified stable point, as standing ceremony. Deferring
a licensed tag is the deviation, not the default. **This session's own
AR-UC-0 exercised case (ii) directly** (`stable-20260806-ux-fixes-5` at
`f5af489`) — the mechanic held under direct exercise, not merely
re-read. Every surface stating this law (`AGENTS.md`, both
`build-session` `SKILL.md` copies, this file, `.agents/rulings.md`)
confirmed reconciled by fresh read this session — no drift found, the
tag-law session's own sweep (ADR-0057) has held for eight subsequent
sessions. **`stable-*` tag census, fresh
(`git tag -l 'stable-*'`, excluding the three frozen legacy tags):**
**16 live**, up from the prior regeneration's 8 — the alignment arc's
own eight (`-alignment-riders`, `-alignment-audit`, `-alignment-fixes-1`
through `-5`, `-alignment-close`) plus this UX arc's own eight
(`-tag-law`, `-ux-riders`, `-ux-audit`, `-ux-fixes-1` through `-5`,
this session's own `-ux-fixes-5` just created and pushed at AR-UC-0).

**The founding incident, closed structurally — the UX arc's own
central theme, confirmed by fresh live probe this session, not merely
read from an ADR:** all four failures the 2026-08-06 founding
conversation named are now gate-guarded, re-verified live against the
built `bin/ehrt` at this session's own Step 0/close:
1. **Stale invocation** (`clojure -M:cli run ...` taught in demo docs)
   → swept to `bin/ehrt` form (ux-fixes-1, ADR-0059) AND gated so the
   fences actually RUN, not just parse (ux-fixes-2's own rider,
   AR-U2-R, ADR-0060) — `invocation_lint_test.clj`'s
   fence-path-resolution assertion. Confirmed live: zero
   `clojure -M:cli` occurrences anywhere in the doc tree.
2. **Opaque config crash** (`--config <missing-or-malformed>` → raw JVM
   stack trace, exit 1) → `merge-config-file` adopts the Result
   vocabulary (ux-fixes-2, ADR-0060 AR-U2-1/AR-U2-2). Confirmed live,
   the founding incident's own exact command shape:
   `bin/ehrt sim run --seed 1 --patients 1 --config
   config/busy-weekday.edn` → `{:status :error, :category
   :config-not-found, :payload {:path "config/busy-weekday.edn",
   :did-you-mean "config/busy-weekday.md"}}`, exit 2.
3. **Silent typo** (`--patiens` absorbed, run succeeds wrong) →
   spec-derived flag validation rejects by name with a near-miss
   suggestion (ux-fixes-3, ADR-0061). Confirmed live:
   `bin/ehrt sim run --patiens 200 --seed 1` → `{:status :error,
   :category :unknown-flag, :payload {:flag "--patiens", :verb "sim
   run", :did-you-mean "--patients"}}`, exit 2.
4. **Agent-voice help** (24 ADR + 14 milestone + 3 ruling citations, 3
   bare keywords, 2 internal-namespace leaks, 4 strings over 250 chars
   with no wrap structure) → the approved rewrite relocates all 36
   real token hits to source comments (ux-fixes-4, ADR-0062), and a
   real hanging-indent line-wrap mechanism replaces raw terminal
   soft-wrap at 80 columns, content byte-preserved (ux-fixes-5,
   ADR-0063). Confirmed live: `bin/ehrt help` carries zero
   `ADR-[0-9]{4}`/`ruling [0-9]`/`D1[0-9]` tokens; every rendered line
   ≤80 columns.

Two smaller register rows closed alongside: the unknown-command hint
now names the real group when one matches (`bin/ehrt sim` →
`:hint "run: ehrt help sim"`, ux-fixes-2 AR-U2-3/ADR-0060); bare
`bin/ehrt` now exits 0, matching the `--help`/`help` convention
(ux-fixes-2 AR-U2-4/ADR-0060) — confirmed live, `bin/ehrt` (no args)
exits 0.

**NIST supply-chain posture, HELD unchanged (alignment fixes 4,
ADR-0053).** `bin/verify-nist-lock` still wired as `make test`'s own
third line (confirmed live, `Makefile` line 41). The licensing inquiry
itself stays open (see "Live work," above) — untouched this arc.

**`workspace.edn`, HELD unchanged (alignment fixes 1, ADR-0050
AR-F1-4).** 33 lines (confirmed live), untouched this arc.

**The UX arc's own pointer.** Register at `.agents/plans/
2026-08-06-ux-audit-findings.md` (dated audit artifact, 21
disposition-carrying rows); dispositions final per
`notes/adr/0064-ux-arc-close.md` — full tally above, "Live work."

## Design-channel contract (how sessions run)

Two channels: design (chat) plans/verifies vs public clone; Code
executes pasted prompts, self-archives, R30 ceremony. Evidence over
ruling — live probes have overturned survey rows, channel claims, and
the author's own summaries repeatedly across both arcs now: the
alignment arc's own EXP-SBOM citation drift and three ADR-0054
precision gaps; this arc's own A-2 grammar-vs-path verification gap
(ADR-0060's own founding lesson: "grammar-validity is not
path-validity") and the draft's own 38-vs-36 token-count method drift
(ADR-0062) — the standing rule these two same-arc instances now
generalize into `.agents/rulings.md`: **audit evidence uses the
mechanism it recommends**, not a cheaper proxy for it. Continuity: new
design sessions read THIS file + roadmap + latest ADR execution record
— structural claims in any continuity prompt carry citations or an
explicit `[unverified]` tag `[A, AR-B-5]`.
