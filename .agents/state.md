# State of the project — continuity register

Authored by the design channel 2026-08-05 against tip `53edcad`,
landed by the compaction C build session the same day (arc close,
`notes/ADRs.md` ADR-0047) — every `[V]` claim below was re-probed
against the live tree at landing time and corrected or tagged
`[unverified]` where the draft didn't hold; corrections are marked
inline. Regenerated by the design channel at each arc close (standing
rule, this file's own contract, ADR-0047 AR-C-1). Provenance tags:
`[A]` author-ruled, `[C]` channel-inferred, `[V]` probe-verified at
landing.

## What this repo is

Clojure/Polylith workspace generating deterministic synthetic hospital
traffic (Synthea GMF at pinned commit `7e08387c...f902`), injecting
defects, judging conformance (HL7v2/FHIR). Formats are emitters of the
patient state machine `[A]` (`notes/adr/` index: ADR-0043's own
"sibling emitters over one state machine" doctrine). Book repo
`ehr-testing-guide` is separate, permanently out of scope (ADR-0001
R2).

## Component graph (post sim-split-B) `[V @53edcad]`

`sim-engine` (engine/churn/order-profiles) ← {`sim-check`,
`sim-emit-fhir`, residual `sim` (run/identifiers/version/manifest-
build/façade)}. **Correction to the draft:** `sim-emit-hl7` is NOT a
dependent of `sim-engine` — fresh grep of every real
`ehrt.sim-engine.interface` requirer found exactly five callers
(`sim-check/check.clj`, `sim-emit-fhir/emit_fhir.clj`,
`sim/identifiers.clj`, `sim/run.clj`, plus dev-only
`oracle/digest.clj`); `sim-emit-hl7/emit_hl7.clj`'s own requires are
`ehrt.sim-model.interface` and its own `site-profile` only, matching
AGENTS.md's own constraint ("`sim-emit-hl7` must never depend on
anything but `sim-model`"). The two emitters are still ADR-0043's own
"sibling rendering accents" in narrative terms (`sim-emit-hl7` reads
the event log residual `sim` hands it directly; `sim-emit-fhir` reads
folded state via `sim-engine`'s own `replay`) — siblings in role, not
in the require graph.

`provenance` ← {`corpus`, `sim`} (the manifest schemas' single home;
confirmed live: both `corpus/manifest.clj`/`corpus/interface.clj` and
`sim/manifest.clj` require `ehrt.provenance.interface`; `provenance`
itself depends on nothing but `malli`, forbidden forever from any
`ehrt.*` require, ADR-0043 AR-2). `oracle` = the digest component
(each worktree carries its own producer via its own synthetic
classpath, ADR-0044 AR-P-2/AR-P-3). Census lives in `sim-trajectory`
(src + 7 tested `deftest`s, confirmed live in `census_test.clj`).
Façade `ehrt.sim.interface` surface frozen `[A]` (`.agents/plans/
2026-08-02-sim-split-plan.md` AR-3, honored through every split stage
including M4's own docstring-only retirement note; corpus depends on
this interface's own stability in-process, ADR-0012).

## Where history lives `[V @53edcad]`

* `notes/ADRs.md` = INDEX (91 lines); entries at `notes/adr/NNNN-
  slug.md`. **Correction to the draft's "43 files":** by this arc's
  own close (ADR-0047 landing), the count is **45 files** — ADR-0001
  through ADR-0047, minus two never-issued numbers (0019, 0020) that
  predate this workspace's own numbering and were never assigned here.
  The draft's "43" was accurate only for the instant AR-B-1's split
  completed, before ADR-0046 (compaction B itself) and ADR-0047
  (compaction C) each added themselves as one more file — a citation
  that goes stale the moment the arc that produced it closes, the
  exact failure mode this file's own regeneration contract exists to
  catch. Citation rule: bare `ADR-NNNN` resolves via the index;
  frozen-era origin-qualified (`sim/ADR-0008` etc.) — see the index's
  own header.
* Roadmap = live map only (Now/Next/Externals/Deferred/Done-current-
  arc-only); Done history at `.agents/plans/roadmap-done-2026-07.md`
  (0 entries — nothing predates 2026-08) and `roadmap-done-2026-08.md`
  (33 entries, confirmed by `## Done` header count, verbatim, compaction
  B, ADR-0046 AR-B-3).
* Session narrative hierarchy `[A, AR-B-4]`: ADR execution record =
  sole narrative; session record = ceremony log; roadmap Done = one-
  line pointer (gated: `done_pointer_adr_test`); prompt archive =
  provenance (gated: prompt/record pairing test, confirmed live — a
  7-slug pre-cutover allowlist, `ehrt.docs-tooling.prompt-record-
  pairing-test`, all seven resolving to real `notes/prompts/*.md`
  files).

## Standing gates `[V @53edcad]`

Structure-currency; index-completeness (`.agents/plans`,
`.agents/prompts`, `.agents/session-records`, `notes`,
`.agents/skills` — both directions, confirmed against
`index_completeness_test.clj` directly; `.agents/` top level itself is
NOT a gated index, so `state.md`/`rulings.md` living there un-indexed
is correct, not an omission); reading-set budgets (re-derived
2026-08-05, actual×1.15 round-to-5, AR-D-3's formula, re-applied by
AR-B-4; `:onboarding` confirmed at 1095 in `.agents/reading-sets.edn`);
stale-path tripwire (learns each retired ns, per-stage); prompt/record
pairing; Done-pointer→ADR; readme-presence; skill-mirror-currency
(`.agents/skills/` ↔ `.claude/skills/` drift gate). Oracle:
`bin/regression-oracle`, per-worktree digest via `components/oracle`,
cross-side source-equivalence check, `--declared-digest-change` flag
(confirmed live in the script), all-eleven-batches identity is the
standing expectation for structural sessions `[A, ADR-0044]`.

## Live work

* Deferred = LIVE rows only (13, confirmed by direct count against the
  live roadmap at `53edcad`; header states the contract) `[V @53edcad]`:
  all amendments/extensions to existing mechanisms — no new capability
  classes `[C, ruled useful 2026-08-05]`. **Finding, disclosed not
  fixed (out of this arc's own fence):** one of the 13 rows
  (`myocardial_infarction.json`) carries its own in-place "RESOLVED...
  see Done, below" note rather than having been relocated — a
  pre-existing drift from 2026-08-03, predating compaction A's own
  AR-A-5 sweep, which evidently didn't catch this one (its closure
  note is embedded mid-paragraph, not a standalone closed-with-note
  row like the four AR-A-5 did relocate). Not this arc's scope to fix;
  named here so a future Deferred-triage session doesn't have to
  rediscover it.
* Next (roadmap): lookup `time` gap, Wave G attachment trigger,
  pairing-as-data registry, storefront fixture, quickstart→nightly,
  generator-source split, corpus.display placement, table-helper
  dedup `[V @53edcad, titles abbreviated — read the roadmap]`.
* Externals (author-only): NIST gist send; IG pinning; Clojars publish
  (ruled, deferred — do NOT re-raise Clojars-vs-Central); SETUP
  rewalk (unspoiled human); skill upstreaming; **/mnt/c disposition —
  RULED 2026-08-05 (ADR-0047 AR-C-3): retired.** `bin/sync-mnt-c`
  deleted; the guarded-mirror doctrine and dual-clone hazard language
  retired from `.agents/skills/build-session/SKILL.md` (both the
  canonical file and its `.claude/skills/` mirror) with dated notes;
  the two roadmap Externals rows that posed this question closed with
  a pointer to this ADR. **Correction to AR-C-3's own stated scope:**
  the ruling named "AGENTS.md" as a surface carrying this language to
  retire — fresh grep found zero mentions of `mnt/c`/`dual-clone`/
  `guarded-mirror` anywhere in `AGENTS.md`; there was nothing there to
  retire. Disclosed rather than silently no-opped.

## Environment `[A, 2026-08-05]`

Claude Code project root = UNC path to the ext4 clone
(`\\wsl.localhost\Ubuntu\home\mg\src\ehr-testing-tools`, confirmed live
via `wslpath -w .` against the actual session cwd
`/home/mg/src/ehr-testing-tools`). The `/mnt/c` mirror is RETIRED by
author ruling (ADR-0047 AR-C-3). **Correction to the draft's own
"four-incident ledger":** the draft named "M2 mis-target, M4
staleness, promotion memory note, compaction-A 345-line stale read" —
fresh grep of every sim-split-B session record (M2, M3, M4) and the
standing-equipment-promotion session record found **zero** mentions of
`/mnt/c`, `dual-clone`, or `clone` staleness in any of the four; those
four labels do not check out against any recorded evidence. The real,
evidence-backed ledger is **three** dated clusters, not four labeled
incidents: **(1)** Wave D stage D3, 2026-08-02 (`notes/ADRs.md`
ADR-0030's own context) — the `feedback-dual-clone-edit-hazard` fired
FOUR times within that single stage alone, the finding that produced
the J4 guardrails (read-only `/mnt/c`, reject-all hooks,
`bin/sync-mnt-c`, the `build-session` preflight rule) in the first
place; **(2)** migration session 6, 2026-08-02 — the session ran
natively FROM `/mnt/c` and found both migration session 5's own record
and this repo's persisted agent memory stale, claiming `/mnt/c` was
still five sessions behind when it had in fact already been
fast-forwarded outside any recorded session; **(3)** scaffolding
compaction A, 2026-08-05 — the session's own default working directory
resolved to `/mnt/c`, 345 lines stale (1127 vs. 1472 on the ext4 clone
of record), worked around by routing every read/edit through `wsl -e
bash -lc` per the `build-session` skill's own preflight rule. The
guardrails (mechanical read-only + reject-all hooks) held throughout —
no edit or commit ever actually landed on `/mnt/c` — but vigilance-
adjacent staleness (memory, default-cwd resolution) kept recurring
regardless of the mechanical guard, which is the reasoning this
session's own UNC-path resolution (Claude Code's project root pointed
directly at the ext4 clone via its UNC path, never at `/mnt/c` at all)
closes structurally rather than procedurally.

## Design-channel contract (how sessions run)

Two channels: design (chat) plans/verifies vs public clone; Code
executes pasted prompts, self-archives, R30 ceremony. Evidence over
ruling — live probes have overturned survey rows, channel claims, and
the author's own summaries (the citation corrections in this very file
are further instances of the same pattern; "Wave I" provenance,
ADR-0044, remains the canonical example). Continuity: new design
sessions read THIS file + roadmap + latest ADR execution record —
structural claims in any continuity prompt carry citations or an
explicit `[unverified]` tag `[A, AR-B-5]`.
