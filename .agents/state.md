# State of the project — continuity register

Regenerated by the design channel 2026-08-08 at the fidelity arc's own
close (`notes/adr/0080-quality-arc-close.md`, AR-FC-3) — **this citation
moves to ADR-0084 in Step 3's own commit**, the same staleness-tripwire
sequencing precedent ADR-0080's own Step 2 established (the tripwire
checks COMMITTED state; `notes/adr/0084-fidelity-arc-close.md` does not
exist on disk until Step 3, so this Step 2 commit deliberately holds
the citation above at the newest arc-close file ACTUALLY on disk at
this commit boundary, then Step 3 moves it in the same commit that
creates that file). Landed against tip `e7961b9` (this arc's own Step 1
commit, the rulings appends) — every `[V]` claim below was re-probed
against the live tree THIS session before landing; nothing carried
forward on the prior version's own authority (this file's own
regeneration contract, ADR-0047 AR-C-1, restated `.agents/rulings.md`).
The prior version was authored 2026-08-07/08 against tip `9eb7da9`
(quality-review arc close, ADR-0080) — everything below supersedes it.
Regenerated at each arc close (standing rule, unchanged). Provenance
tags: `[A]` author-ruled, `[C]` channel-inferred, `[V]` probe-verified
at landing.

## What this repo is

Clojure/Polylith workspace generating deterministic synthetic hospital
traffic (Synthea GMF at pinned commit `7e08387c68a7f0e21d13076609a159fd473fc902`),
injecting defects, judging conformance (HL7v2/FHIR). Formats are
emitters of the patient state machine `[A]` (`notes/adr/` index:
ADR-0043's own "sibling emitters over one state machine" doctrine).
Book repo `ehr-testing-guide` is separate, permanently out of scope
(ADR-0001 R2). A stranger can watch that traffic breathe as a bed board
(`ehrt play PATH --board N`, player arc, ADR-0066–0067), or generate
and replay a demo from a top-level operator front door (`demos/`,
demos front door, ADR-0073), without reading a line of this project's
own source. **Twenty-four** Synthea GMF ailments are now vendored
in-tree at the project's own pinned commit, byte-verbatim,
NOTICE-hashed, and gate-verified on every test run (vendoring arc,
ADR-0069–0074, joined this arc by `anemia___unknown_etiology.json`,
fidelity payoff, ADR-0083). **New this arc (fidelity, ADR-0081–0084):**
the interpreter's first semantics change since the GMF coverage waves —
`EncounterEnd` now tracks real per-walk openness instead of a
module-filtered guess, upstream's five arms collapsing to the two this
project's one-module-per-walk scope actually needs — closing the
oldest deferred-vendoring blocker and correcting a misdiagnosis
(`colorectal_cancer.json`) that had stood, unprobed, since 2026-08-07.

## Component graph `[V @e7961b9]`

18 `components/*` directories, 1 `bases/*` directory (`ls -d
components/*/ | wc -l` → 18, `ls -d bases/*/ | wc -l` → 1) — HELD
unchanged since the vendoring arc's own close. **The fidelity arc added
no brick and no new require edge** — fresh `git log 42cd1e0..HEAD
--name-only` shows zero `deps.edn` touches and zero `interface.clj`
touches; every `src/` edit landed inside `gmf_interpreter.clj`,
`census.clj` (both `sim-trajectory`), and `digest.clj` (`oracle`,
purely additive — one new producer function, one new `roots` map
entry).

**New this arc: `open-encounter-index`**
(`components/sim-trajectory/src/ehrt/sim_trajectory/gmf_interpreter.clj`,
EncounterEnd fix, ADR-0082) — a pure fold over the walk's own
trajectory-so-far, replacing the retired, openness-blind
`index-of-last-open-encounter` (zero remaining call sites, fresh grep
confirmed; the name survives only as a historical citation inside the
new function's own docstring). `:suppressed-encounter-ends` joins
`ctx` as a threaded integer counter (R2, the zero-cost diagnostic for
the A5 no-op arm) — every ctx-folding site that threads
`:attributes`/`:vital-signs` now threads this alongside them, including
the four independent submodule-call loops (`walk-module`/
`run-submodule`/`call-submodule-step`/`run-module`) so a called
submodule's own suppression reaches its caller's own walk. Surfaces in
`census.clj`'s `walk-one` as an additive field when nonzero — the sole
existing round-trip-metadata surface this repo has for per-walk stats.
Gated informally by three committed tests (`gmf_interpreter_test.clj`:
the fixture round trip, the open→close→open→close non-staleness proof,
the nesting assert) — no new lint gate, this is interpreter behavior,
not a repo-hygiene law.

`sim-engine` (engine/churn/order-profiles) ← {`sim-check`,
`sim-emit-fhir`, residual `sim` (run/identifiers/version/manifest-
build/façade), `oracle`} — HELD, still exactly five external callers.
`corpus` → `sim-emit-hl7` (player board's own edge) — HELD, unchanged.
`sim-emit-hl7/emit_hl7.clj`'s own four-require `ns` form — HELD,
unchanged. `provenance` ← {`corpus`, `sim`}, forbidden forever from any
`ehrt.*` require beyond itself — HELD, still gated
(`ehrt.docs-tooling.provenance-leaf-law-test`). Façade
`ehrt.sim.interface`'s surface — still `[A]`-frozen (AR-M4-3),
mechanically enforced (`interface_surface_test.clj`) — HELD, no
`sim`-façade edit this arc.

**Resource nesting, closed and gated, HELD unchanged.** The seven
`components/*/resources` directories workspace-wide, unchanged set, all
still nest under their own brick name. Gated:
`ehrt.docs-tooling.resource-nesting-test`, no allowlist.

## Vendored module inventory `[V @e7961b9]`

**Twenty-four modules, twenty-eight oracle roots** (fresh `ls
components/sim/resources/sim/modules/*.json | wc -l` → 24; fresh count
of `digest.clj`'s own `roots` map → 28), up from twenty-three/twenty-
seven at the quality-review arc's own close — **one module joined this
arc**: `anemia___unknown_etiology.json` (fidelity payoff, ADR-0083),
vendored verbatim from the pin-verified checkout, its own oracle root
(`anemia-pair`, race-weighted `:persona-config`) a FIRST BASELINE,
purely additive against the twenty-seven pre-existing roots (confirmed
both by manual pre/post manifest diff and the official
`bin/regression-oracle 841df9a 85ba040 --declared-digest-change`
bracket). `components/sim/resources/sim/modules/NOTICE` holds **70
provenance rows** (fresh `grep -c '^\| \`'`, up from 69 — one new row,
the anemia entry).

**The two-module `EncounterEnd` blocker, CLOSED this arc, its own
diagnosis corrected in the process.** The interpreter gap itself closed
structurally (ADR-0082): real per-walk openness tracking
(`open-encounter-index`) replaces the module-filtered guess, and the
compile dispatch no-ops (`A5`, counted) rather than emitting a dangling
`:encounter-end` when nothing is open. `anemia___unknown_etiology.json`
is confirmed clean post-fix (0 violations at all three of ADR-0071's own
seeds) and vendors this arc (above). `colorectal_cancer.json` was
**never actually blocked by this gap** — a dated erratum (ADR-0083,
append-don't-erase on ADR-0072's own deferral section) corrects
ADR-0072's own "same root cause, not a new gap" diagnosis, which was
plausible BY ADJACENCY (the same shared `anemia/anemia_sub.json`
submodule) but never itself probe-verified by a trajectory scan the way
anemia's own finding always was. `colorectal_cancer.json` stays
deferred whole, NOT vendored, under its own true, still-undiagnosed
name: `:clinical-content-only-when-admitted` violations (plus one early
`:discharge-follows-admission`) one compile layer downstream of the
interpreter (`compile-trajectory` or the engine, mechanism unknown) —
see Live work, below, for the revisit trigger.

**The truncation-layer absorbed-error finding, a new documented
mechanism, not a defect chased (ADR-0082 AR-EE-1a).** Tracing
`hypothyroidism`'s own real, pre-fix dangling `:encounter-end` (found
by the blast-radius probe, five of its own 300 oracle-seed walks
affected) through `compile-trajectory` found the dangling event was
ALREADY silently absorbed, pre-fix, by two PRE-EXISTING, unrelated
mechanisms: the `:pre-horizon` drop gate (3 of 5 walks, every event in
span predates registration) and `compile-trajectory`'s own
`encounter-closed?` single-encounter-per-run scope (2 of 5 walks — this
project's own standing "compile through the end of the FIRST
horizon-phase encounter, then stop" rule, sim/ADR-0007 point 3, drops
EVERYTHING after that point unconditionally, dangling or not). This is
why the fix's own oracle bracket came back byte-identical even for
`hypothyroidism` (the licensed mover) — the fix's real effect (proven
at the raw-trajectory-digest granularity directly) is invisible at the
engine/ground-truth granularity for this specific seed/population,
explained precisely rather than left mysterious. Newly significant, not
newly discovered: this truncation rule now has a documented second job
(silently absorbing a class of interpreter-level reference errors) it
was never designed for — a candidate for a future fidelity pass,
carried to the horizon.

## Where history lives `[V @e7961b9]`

* `notes/ADRs.md` = INDEX; entries at `notes/adr/NNNN-slug.md`. **File
  count: 81**, confirmed (`ls notes/adr/*.md | grep -v README | wc -l`)
  — this ADR-0084's own file will make it 82 once it lands in Step 3,
  the same staleness-at-count-instant pattern every prior regeneration
  has named.
* Roadmap = live map only; Done history at `.agents/plans/
  roadmap-done-2026-07.md` (0 entries, unchanged) and `roadmap-
  done-2026-08.md`. **This session's own rotation (AR-FC-5), landing in
  the same commit as this file:** ADR-0080's own pointer — left in
  place as the live roadmap's sole current entry at the quality-review
  arc's own close — relocates into the attic's EXISTING quality-review
  section with a dated leftover note. A new `## Fidelity arc — closed
  2026-08-08 (ADR-0081–0084)` header holds ADR-0081–0083's own Done
  pointers, relocated verbatim. `.agents/plans/roadmap.md`'s own Done
  section holds an HTML-comment sentinel (not a pointer) recording that
  ADR-0084's own pointer is deferred to Step 3 — the same
  dangling-reference sentinel-avoidance every prior close (ADR-0055/
  0064/0068/0074/0080) has disclosed. Deferred rows: **13** (up from 12
  — the `EncounterEnd` row's own closure split off a new colorectal-
  only row, net +1, per ADR-0083). Next rows: **11** (up from 9 — the
  two AR-FR-2 author backlog additions, fixture relocation and the
  ADR-footnote fork, both still unbuilt).
* Session narrative hierarchy `[A, AR-B-4]`: unchanged — ADR execution
  record = sole narrative; session record = ceremony log; roadmap Done
  = one-line pointer; prompt archive = provenance.

## Standing gates `[V @e7961b9]`

`components/docs-tooling/test/ehrt/docs_tooling/` holds **27 test
files**, confirmed live (`ls ... | wc -l`) — HELD unchanged since the
quality-review arc's own close; no new gate landed this arc (the
EncounterEnd fix is interpreter behavior + committed unit tests, not a
repo-hygiene lint).

**The façade surface-identity gate** (`interface_surface_test.clj`,
`components/sim/test/`, AR-LF-2) — HELD, unchanged.

**Local docsgen-currency gates** (`cli-md-is-current-test`,
`operators-md-is-current-test`) — HELD, unchanged; `docs/dev/
pipeline.md`/`docs/use-cases.md` stay CI-only, unchanged.

**Oracle: 28 roots, fresh-confirmed IDENTICAL** (`bin/regression-oracle
e7961b9 e7961b9`, this session's own Step 0 pre-digest — soundness "yes
outside ns form"), up from 27 at the quality-review arc's own close —
the fidelity payoff session's own new `anemia-pair` root (ADR-0083),
purely additive; the twenty-seven pre-existing roots stayed byte-
identical across the entire arc (confirmed batch by batch, ADR-0082's
own bracket for the fix and ADR-0083's own bracket for the vendoring).

**Vendored round-trip family: 28 files** — up from 27, the new
`vendored_anemia_test.clj` (ADR-0083).

`bases/cli/test/ehrt/cli/` holds 7 files — HELD, unchanged; this arc
touched no CLI surface. **Reading-set budgets: only `:onboarding`
re-derived this close (AR-FC-4)** — diffing every set's own `:paths`
against `git log 42cd1e0..HEAD --name-only` found four `:onboarding`
members touched (`roadmap.md`'s own Now/Done churn across three
fidelity-arc sessions plus this close's own rotation; the plans/
prompts/session-records READMEs' own new entries) and zero touched
members in `:corpus`/`:sim`/`:judge`/`:docs` (this arc's own src/test
edits touched `sim-trajectory`, `sim-emit-hl7`, and `oracle`, none of
them a `:paths` member of those four sets) — the first close since the
quality-review arc's own "all five moved together" regeneration where
only ONE set moves. Structure-currency; index-completeness; stale-path
tripwire; prompt/record pairing; done-pointer→ADR; readme-presence;
skill-mirror-currency; docsgen/pipeline/usecases/quickstart-fresh/lint;
notes-prompts-frozen; tag-law; invocation-lint; io-vocabulary-lint;
state-staleness-tripwire; roadmap-deferred-closure-lint;
test-source-live-path-lint; the five alignment-arc gates — all HELD,
unchanged, none touched this arc beyond what's named above.

## The quality-review arc's own instrument `[V, HELD since @9eb7da9]`

The `repo-review` skill and its first survey's own dated register
(45 disposition-carrying rows, 28 close-as-fine / 9 fix-session-
candidate EXECUTED / 5 ruling-needed RULED / 3 intake — 1 executed, 2
still open) — unchanged this arc, no review 2 has run yet. See
`notes/adr/0080-quality-arc-close.md` for the full account; restated
here only as still-live context, not re-derived (this arc's own review
re-probe would be review 2's job, not a docs-only close's).

## Live work `[V @e7961b9]`

* Deferred = LIVE rows only (**13**, up from 12 — see Where history
  lives, above, for the split).
* Next (roadmap, **11** rows, up from 9 — see Where history lives,
  above).
* **The sibling-flake SOAK** (`merge-config-file-suggests-a-same-stem-
  sibling-file`, ADR-0076's own fix, landed `9cc3563`), fresh count
  this close: **25 `test`-workflow push runs on `main` since `9cc3563`
  landed** (fresh `gh run list --json` enumeration, `9a34409` through
  this session's own `e7961b9`, excluding the separate scheduled
  `Integration` workflow), **zero recurrence of the named test** — two
  unrelated failures in that span, both already disclosed and closed
  the same session they landed in (`ac6ef5f2`, the `index-completeness-
  test` gap ADR-0077 named and closed; `deabbbdb`, the
  `roadmap-deferred-closure-lint-test` gap ADR-0082 named and closed
  within 15 minutes). Twenty-five runs, still not declared closed (the
  fix's own target was never "N clean runs, then stop counting") —
  carried to review 2 as the next re-probe point.
* **The engine `defspec` flake — pinned, not soaking:** `every-churned-
  run-satisfies-the-invariant-catalog` runs at a fixed `{:num-tests 150
  :seed -60645}` (confirmed live, fresh `grep`) — HELD, unchanged.
* **A NEW, undisclosed-until-now finding from this session's own
  preflight, genuinely unrelated to this arc's own work:**
  `ehrt.conformance.mutate-stdout-stdin-loopback-test`'s own
  `^:integration`-tagged `mutate-stdout-into-intake-stdin-real-
  loopback-test` (`projects/conformance/test/`) failed once under
  `clojure -M:poly test :all skip:integration` this session
  (`:malformed-mllp-frame`, "expected 0x0B start-of-block" at byte 0) —
  `skip:integration` skips the separate `integration` PROJECT, not
  individual `^:integration`-tagged deftests living inside the
  `conformance` project, so this test runs under the standard full-
  suite invocation despite its own docstring's "run via `make
  integration`" framing. The SAME piped shell command, run standalone
  outside the JVM test harness (twice, identically), succeeds cleanly
  both times — real MLLP-framed bytes, correct catalog, exit 0 — and a
  second full `poly test :all skip:integration` run (this session, see
  the session record for its own result) is the live disambiguation:
  reproducible defect vs. a load/timing-sensitive flake in a
  subprocess-piping test running concurrently with the rest of a large
  JVM test suite. Untouched by any fidelity-arc commit (`git log --all
  -- '*mutate_stdout_stdin_loopback_test*'` shows no touch since the
  pre-monorepo era) — named here as intake for the next session that
  owns test-suite hygiene, not investigated or fixed (docs-only fence).
* **CI posture, in full, this close:** preflight discloses the last
  FIVE runs' conclusions — all green at this session's own Step 0 (see
  the session record). Commits land green — no push this arc carried a
  knowingly-failing test.
* **D2-7's own deferred scaffold** (the generalized multi-surface-law-
  drift registry): DEFERRED with a named trigger, unfired — HELD,
  unchanged.
* **D6-1's census `:closure-file-count` undercount, escalated, still
  open:** `sim-trajectory/census.clj` still counts JSON modules only,
  never CSV lookup tables — confirmed still live in code this session
  (fresh `grep`, line 185's own `.json`-suffix filter unchanged) — HELD,
  unchanged.
* **Wellness-encounters** — HELD, restated unchanged from the
  quality-review arc's own re-surfacing (D7-6); no session touched it
  this arc.
* **Vital-sign channel row, HELD unchanged** — `congestive-heart-
  failure`/`contraceptives` both `:produces-content` post-Wave-VS;
  `covid19` alone `:zero-on-every-seed`, still fully blocked.
* **`notice_verbatim_test`'s own coverage gap, HELD, still open** — the
  v2-nist `NOTICE.md` table (2-column) and the simhospital
  `PROVENANCE.md` hash (prose) both sit outside the gate's own
  recognized shapes; both hashes still manually verified correct.
* **Census artifact set, updated:** ten `.edn` files under
  `components/sim-trajectory/docs/census/` (fresh `ls`, up from nine at
  the vendoring arc's own close) — one new this arc,
  `2026-08-08-synthea-7e08387-encounterend.edn` (ADR-0082's own labeled
  re-run, 85 modules censused, `{:ok-walked 84, :out-of-scope-by-ruling
  1}`, parity holds exactly; diffed against the substance census,
  exactly ONE row changed — `anemia-unknown-etiology`'s own middle seed,
  the fully-explained effect of one suppressed `:encounter-end`).
* Externals (author-only), unchanged this arc, all six rows restated:
  the GitHub workflow-failure notification-email toggle (still
  unconfirmed); NIST licensing inquiry (narrowed, not resolved); IG
  pinning (still open); Clojars publish (ruled, deferred; F-5/F-6
  remain open); SETUP rewalk (still owed); `/mnt/c` disposition (closed,
  unchanged).
* **Both audit registers' pointers, restated unchanged, neither touched
  this arc.** Alignment audit register (54 rows, FINAL per ADR-0055).
  UX audit register (21 rows, FINAL per ADR-0064).

## Environment `[A, 2026-08-08]`

Claude Code project root = UNC path to the ext4 clone, unchanged, no
new incident this arc (`uname -a` confirms Linux/WSL2 at this session's
own Step 0).

**Tag mechanic, HELD unchanged (tag law, ADR-0057 AR-T-1/AR-T-2).**
**`stable-*` tag census, fresh (`git tag -l 'stable-*'`, excluding the
three frozen legacy tags): 36 live**, up from the quality-review arc's
own 32 — four new this arc: `-quality-close` (ADR-0081's own AR-FR-0),
`-fidelity-riders` (ADR-0082's own AR-EE-0), `-encounterend-fix`
(ADR-0083's own AR-FP-0), `-fidelity-payoff` (this session's own AR-FC-0,
verified at `a13bc0b`) — each one session's own tag-law case-(ii)
execution for its own predecessor's verified stable point, exactly the
standing chain every prior regeneration has traced.

**The founding incident, still SIX guarded failures — HELD, unchanged.**
This arc touched no CLI/help-flag surface; all six mechanisms stand
unre-probed-for-regression by this session's own full-suite green run,
not independently re-walked live.

**CLI surface, HELD unchanged.** No new flag landed this arc.

**Full suite posture, fresh this session (Step 0 baseline against
`e7961b9`):** `clojure -M:poly test :all skip:integration` — **275**
`Testing ...` namespace announcements (up from 273 at the quality-
review arc's own close — `vendored_anemia_test.clj`, new this arc,
appears once per lane), final project-lane aggregate **521 passes, 0
failures, 0 errors** (development/conformance lane; the `ehrt-cli`
lane's own count matches). One run this session surfaced the
`mutate-stdout-into-intake-stdin-real-loopback-test` flake named above
in Live work; a second, independent full run — the disclosed disambig-
uation — came back clean at this exact 275/521/0/0 shape, confirming
the flake, not a regression, before this session committed anything.

**NIST supply-chain posture, `workspace.edn`, HELD unchanged** — both
untouched this arc.

**This arc's own pointers.** `notes/adr/0081-fidelity-riders.md` (the
design brief re-verified, three rulings recorded), `0082-encounterend-
fix.md` (the fix, the STOP-AND-REPORT, the trace, the license, the
fifteen-minute CI-red window disclosed), `0083-fidelity-payoff.md`
(anemia comes home, colorectal's misdiagnosis corrects). This arc's own
close record is `notes/adr/0084-fidelity-arc-close.md` (landing Step 3).

## Design-channel contract (how sessions run)

Two channels: design (chat) plans/verifies vs public clone; Code
executes pasted prompts, self-archives, R30 ceremony. Evidence over
ruling — live probes have overturned survey rows, channel claims, and
the author's own summaries repeatedly across every arc, and this arc
added its own instance: ADR-0072's colorectal diagnosis stood
unprobed for a full session before the first trajectory scan (ADR-0082/
0083) overturned it — the standing lesson ("plausible-by-adjacency is
not a diagnosis") this arc's own rulings append names explicitly.
Continuity: new design sessions read THIS file + roadmap + latest ADR
execution record — structural claims in any continuity prompt carry
citations or an explicit `[unverified]` tag `[A, AR-B-5]`.
