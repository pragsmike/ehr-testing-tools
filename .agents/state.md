# State of the project — continuity register

Regenerated by the design channel 2026-08-07 at the vendoring arc's own
close (`notes/adr/0074-vendoring-arc-close.md`, AR-VAC-3), landed
against tip `beec395` (this closing session's own predecessor tip —
every `[V]` claim below was re-probed against the live tree THIS
session before landing; nothing carried forward on the prior version's
own authority (this file's own regeneration contract, ADR-0047 AR-C-1,
restated `.agents/rulings.md`). The prior version was authored
2026-08-07 against tip `f9e4afc` (player arc close, ADR-0068) —
everything below supersedes it. **This close spans two Code sessions**:
an infrastructure-level API request block killed the first mid-
ceremony, immediately after its own Step 1 landed at `beec395`; no work
was lost, and the design channel verified the seam by fresh public-
clone probe (tip, tag, rulings append) before this session resumed at
Step 2 — full account in `.agents/session-records/2026-08-07-vendoring-
arc-close.md` and ADR-0074's own deviation record. Regenerated at each
arc close (standing rule, unchanged). Provenance tags: `[A]`
author-ruled, `[C]` channel-inferred, `[V]` probe-verified at landing.

## What this repo is

Clojure/Polylith workspace generating deterministic synthetic hospital
traffic (Synthea GMF at pinned commit `7e08387c...f902`), injecting
defects, judging conformance (HL7v2/FHIR). Formats are emitters of the
patient state machine `[A]` (`notes/adr/` index: ADR-0043's own
"sibling emitters over one state machine" doctrine). Book repo
`ehr-testing-guide` is separate, permanently out of scope (ADR-0001
R2). A stranger can now watch that traffic breathe as a bed board
(`ehrt play PATH --board N`, player arc, ADR-0066–0067), or generate
and replay a demo from a top-level operator front door (`demos/`,
demos front door, ADR-0073), without reading a line of this project's
own source. Twenty-three Synthea GMF ailments are vendored in-tree at
the project's own pinned commit — sixteen landed this arc (vendoring
arc, ADR-0069–0074), byte-verbatim, NOTICE-hashed, and gate-verified on
every test run.

## Component graph `[V @beec395]`

18 `components/*` directories, 1 `bases/*` directory (`ls -d
components/*/ | wc -l` → 18, `ls -d bases/*/ | wc -l` → 1) — HELD
unchanged since the player arc's own close. **The vendoring arc added
no brick and no new require edge** — fresh `git log b7ed686..HEAD
--name-only` shows zero `deps.edn` touches and zero `interface.clj`
touches anywhere except `components/sim-trajectory/src/ehrt/
sim_trajectory/census.clj` (the substance qualifier, an internal
function addition, not a new external edge) — the arc's own substance
was vendored JSON/CSV resource content plus tests plus docs, not
component wiring.

`sim-engine` (engine/churn/order-profiles) ← {`sim-check`,
`sim-emit-fhir`, residual `sim` (run/identifiers/version/manifest-
build/façade), `oracle`} — HELD, still exactly five external callers,
unchanged. `corpus` → `sim-emit-hl7` (player board's own edge,
ADR-0067 AR-BB2-1) — HELD, unchanged since the player arc.
`sim-emit-hl7/emit_hl7.clj`'s own four-require `ns` form — HELD,
unchanged. `provenance` ← {`corpus`, `sim`}, forbidden forever from any
`ehrt.*` require beyond itself — HELD, still gated
(`ehrt.docs-tooling.provenance-leaf-law-test`). Census lives in
`sim-trajectory` (confirmed live, `census_test.clj`, grown this arc —
see "Vendored module inventory," below). Façade `ehrt.sim.interface`
surface frozen `[A]`, unchanged.

**Resource nesting, closed and gated, HELD unchanged.** The seven
`components/*/resources` directories workspace-wide, unchanged set,
all still nest under their own brick name. Gated:
`ehrt.docs-tooling.resource-nesting-test`, no allowlist —
`components/sim/resources/sim/modules/` (the vendored-content tree,
this arc's own primary surface) sits inside this nesting, unaffected by
the law (a resource directory, not an interface).

## Vendored module inventory `[V @beec395]` (new this arc)

**The arc's own delivery: sixteen modules vendored across three
batches, twenty-three ailments in-tree total** (the sixteen new plus
seven pre-arc: sinusitis, appendicitis, sore-throat, ear-infections,
sepsis, uti, total-joint-replacement — confirmed by de-duplicating the
27 oracle roots below into ailment concepts and subtracting the
non-ailment `death-fixture` root). `components/sim/resources/sim/
modules/NOTICE` holds **69 provenance rows** (fresh `grep -c '^| \`'`),
each an upstream URL + pinned commit SHA + SHA-256 of the vendored
bytes as fetched, all at the SAME pin (`7e08387c...f902`).

* **Batch 1** (ADR-0070, `d41a278`): asthma, bronchitis, sleep-apnea,
  fibromyalgia, dementia landed (5); `injuries.json` assessed and
  **DEFERRED WHOLE** — a `gmf-interpreter` `max-steps` runaway: the
  closure's own `Dental Referral` state sets an attribute no state ever
  clears, so `Check for Dental Visit`/`Wait for Dental Visit` loops
  permanently once reached (reproduced on 4 of 120 well-mixed-seed
  walks, ~3.3% — a near-certainty at 300-patient population size).
* **Batch 2** (ADR-0071, `96424f8`): hypothyroidism, rheumatoid-
  arthritis, osteoarthritis, osteoporosis, attention-deficit-disorder,
  allergic-rhinitis, dermatitis landed (7); `anemia___unknown_
  etiology.json` assessed and **DEFERRED WHOLE** — the shared
  `anemia/anemia_sub.json` submodule's own `End Any Active Encounter
  Just In Case` state compiles as an UNCONDITIONAL `:encounter-end`
  (upstream's "close if open, else no-op" idiom has no no-op path
  here), tripping `sim-check`'s `:discharge-follows-admission`
  invariant at every seed tried (12/17/6 violations of 300 patients).
  `anemia/anemia_sub.json` itself stays vendored — landed clean via
  `hypothyroidism.json`'s own closure, whose call path never reaches
  the hazardous state.
* **Batch 3** (ADR-0072, `721adb6`): metabolic-syndrome-care,
  vhd-pulmonic, vhd-tricuspid, med-rec landed (4); `colorectal_
  cancer.json` assessed and **DEFERRED WHOLE** — the SAME shared
  `anemia_sub.json` `:encounter-end` gap, this time non-universal (2 of
  3 seeds rejected at 300 patients, 4 violations each) but real at
  population scale. **`anemia___unknown_etiology.json` (batch 2) and
  `colorectal_cancer.json` (batch 3) are now a two-module blocker on
  the SAME gap** (roadmap Deferred's own "EncounterEnd no-op-when-
  nothing-open" row). This batch also found and fixed a real pre-arc
  defect: `lookup_tables/uti_recurrence.csv` had never actually been
  byte-verbatim on disk — the root `.gitattributes` rule silently
  normalized its CRLF upstream bytes to LF on checkout, undetected by
  every prior "re-verified" claim. Fixed with `-text` protection
  (below) and a new mechanical gate (`notice_verbatim_test.clj`).
  **Three zero-substance family siblings recorded as not-vendorable-
  under-the-gate**, joining the attribute-blocked set:
  `metabolic_syndrome_disease.json`, `vhd_aortic.json`,
  `vhd_mitral.json` (all `:zero-on-every-seed` in the census substance
  artifact) — revisit trigger: multi-module patient assignment (Wave G)
  giving family pairing a runtime meaning.

**Census substance qualifier** (ADR-0069, `cd16fa9`, opening the arc):
`ehrt.sim-trajectory.census`'s `walk-one`/`census-one`/`summarize` gain
a `:substance` field (`:zero-on-every-seed` iff every seed's
`:event-count` is 0, else `:produces-content`), derived from data
already recorded since ADR-0034. Fresh labeled run against the pin:
**84 `:ok-walked`, 1 `:out-of-scope-by-ruling` (`gallstones`), 0
`:load-failed`, 0 `:walk-failed`; substance tally 51
`:zero-on-every-seed` / 33 `:produces-content`** — the ranked catalog
the design channel's curation pass batched from. Artifact:
`components/sim-trajectory/docs/census/2026-08-07-synthea-7e08387-
substance.edn` (confirmed present, 63330 bytes).

## Where history lives `[V @beec395]`

* `notes/ADRs.md` = INDEX (118 lines, confirmed live); entries at
  `notes/adr/NNNN-slug.md`. **File count: 71**, confirmed (`ls
  notes/adr/*.md | grep -v README | wc -l`) — five landed this arc
  (0069–0073); this ADR's own file (`0074-vendoring-arc-close.md`) will
  make it 72 once it lands, the same staleness-at-count-instant pattern
  every prior regeneration has named, noted rather than chased.
* Roadmap = live map only; Done history at `.agents/plans/
  roadmap-done-2026-07.md` (0 entries, unchanged) and `roadmap-
  done-2026-08.md`. **This session's own rotation (AR-VAC-5):**
  ADR-0068's own pointer — left in place as the live roadmap's sole
  current entry at the player arc's own close — relocates into the
  attic's EXISTING `## Player arc — closed 2026-08-07 (ADR-0066–0068)`
  section with a dated append note, the disclosed-leftover class every
  close has handled for its own predecessor. A new `## Vendoring arc —
  closed 2026-08-07 (ADR-0069–0074)` header holds ADR-0069–0073's own
  Done pointers, relocated verbatim. `.agents/plans/roadmap.md`'s own
  Done section now holds ADR-0074's own pointer as its sole current
  entry (landed Step 3, after this file's own regeneration — the
  sentinel-avoidance every close exercises).
* Session narrative hierarchy `[A, AR-B-4]`: unchanged — ADR execution
  record = sole narrative; session record = ceremony log; roadmap Done
  = one-line pointer; prompt archive = provenance.

## Standing gates `[V @beec395]`

`components/docs-tooling/test/ehrt/docs_tooling/` holds **23 test
files**, confirmed live (`ls ... | wc -l`), up from the prior
regeneration's 22 — **one new this arc**: `notice_verbatim_test.clj`
(vendoring batch 3, ADR-0072 AR-VB3-R1) — reads the NOTICE file's own
provenance table and re-hashes each named vendored file's on-disk bytes
against its recorded SHA-256, the exact mechanism promoted from a
design-channel probe to a standing gate after it caught the
`uti_recurrence.csv` CRLF/LF divergence. 141 assertions, 0 failures,
confirmed at batch 3's own landing.

**Vendored round-trip family: `vendored_*_test.clj`, 27 files
confirmed live** (fresh `find . -iname "vendored_*_test.clj" | wc -l`)
— 20 in `components/sim-emit-hl7/test/ehrt/sim_emit_hl7/`, 7 in
`components/sim-trajectory/test/ehrt/sim_trajectory/`; each a
population-scale (300-patient, seed `20260807`/`20260802` convention)
red-then-green round trip, the real filter the census's own three-seed
sample cannot substitute for (the standing law: "the population-scale
gate outranks the census sample," `.agents/rulings.md`).

**Oracle: 27 roots, fresh-confirmed IDENTICAL** (`bin/regression-oracle
beec395 beec395`, this session's own Step 0 pre-digest — soundness "yes
outside ns form"), up from 11 pre-arc — 16 new roots, this arc's own
first-baseline mechanics: each batch's own landing session ran
`bin/regression-oracle <pre> <post> --declared-digest-change` and
witnessed `DIFFERS`, EXPECTED (new roots only, zero movement among the
pre-existing set), per-batch dated notes in `components/oracle/src/
ehrt/oracle/digest.clj`.

`bases/cli/test/ehrt/cli/` holds 7 files — HELD, unchanged since the
player arc; the vendoring arc touched no CLI surface. Reading-set
budgets: `:onboarding` re-derived this session (AR-VAC-4, below — the
one set whose member path this arc's own span touched); `:corpus`/
`:sim`/`:judge`/`:docs` unchanged, confirmed by diffing every set's own
`:paths` against `git log b7ed686..HEAD --name-only`. Structure-
currency; index-completeness; stale-path tripwire; prompt/record
pairing; done-pointer→ADR; readme-presence; skill-mirror-currency;
docsgen/pipeline/usecases/quickstart-fresh/lint; notes-prompts-frozen;
tag-law; invocation-lint; the five alignment-arc gates — all HELD,
unchanged, none touched this arc.

## Demos & scenarios geometry `[V @beec395]` (new this arc)

Top-level `demos/` (demos front door, ADR-0073 AR-DM-1/2), confirmed
live: `demos/scenarios/` (one scenario, `busy-tuesday/` — a RUNNABLE
`config.edn` + `README.md`, no captured trace, population-scale output)
and `demos/traces/` (six captured-transcript directories confirmed by
fresh `find`: `boarding-transfer`, `emit-state`, `module-mix`,
`order-result`, `persona-enriched`, `site-profiles`). Three vacated
component-local directories each keep one pointer README, confirmed
live: `components/sim/docs/scenarios/README.md`,
`components/sim/docs/demos/README.md`,
`components/sim-emit-hl7/docs/demos/README.md`.

**`.gitattributes` `-text` protection at BOTH protected trees,
confirmed live in the same file:** `components/sim/resources/sim/
modules/** -text` (the vendored-bytes law, AR-VB3-R1 — no
reformatting, no line-ending normalization, ever) and
`demos/traces/**/messages*.txt -text` (the HL7 v2 ER7 wire-format
transcripts, moved in the SAME commit as the pattern that protects
them, AR-DM-1). Two older `-text` rules (`components/corpus/
test-fixtures/v2/*.hl7`, `.../v2-nist/covidELR/*.txt`) HELD, unchanged.

Top-level `README.md` carries a "See it run" section reaching the bed
board in two commands (AR-DM-2, design-channel-verified 2026-08-07);
`demos/README.md` is the front door's own index. Sweep of this file's
own prior text for stale component-local demo paths: none found — the
prior regeneration (player arc close) never cited a component-local
demo path in its own prose, so no correction was owed here.

## Live work `[V @beec395]`

* Deferred = LIVE rows only (**12**, up from 11 — **one new this
  arc**: the `EncounterEnd` no-op-when-nothing-open row, opened by
  batch 2's `anemia___unknown_etiology.json` finding and dated-noted by
  batch 3's `colorectal_cancer.json` finding as a second blocked
  module on the same gap. Confirmed by direct fresh count,
  `awk '/^## Deferred/,/^## Done/' roadmap.md | grep -c '^- '`).
* Next (roadmap, **9** rows, HELD — the vendoring arc touched zero Next
  rows; membership unchanged since the player arc's own close: lookup
  `time` gap, Wave G attachment trigger, pairing-as-data registry,
  storefront fixture, quickstart→nightly, generator-source split,
  corpus.display placement, table-helper dedup, the sim event-log
  input adapter).
* **Vital-sign channel row, dated note this arc (AR-VB1-5):** the
  substance census shows the blockage is now PARTIAL, post-Wave-VS —
  `congestive-heart-failure` `[0 117 0]` and `contraceptives`
  `[0 89 0]` both `:produces-content`; `covid19` alone `[0 0 0]`,
  `:zero-on-every-seed`, still fully blocked. Revisit trigger unchanged
  (a real vital-sign baseline register, Wave E).
* Externals (author-only), unchanged this arc, restated: NIST licensing
  inquiry — narrowed, not resolved, still author-only. IG pinning:
  still open. Clojars publish: ruled, deferred — F-5/F-6 remain open.
  SETUP rewalk: still open. `/mnt/c` disposition: closed, unchanged.
* **Both audit registers' pointers, restated unchanged, neither
  touched this arc.** Alignment audit register (54 rows, dispositions
  FINAL per ADR-0055). UX audit register (21 rows, dispositions FINAL
  per ADR-0064). Full tallies in each closing ADR, not reproduced here.

## Environment `[A, 2026-08-07]`

Claude Code project root = UNC path to the ext4 clone, unchanged, no
new incident this arc (`df -T .` confirms `ext4` at both this session's
own and its predecessor session's Step 0).

**Tag mechanic, HELD unchanged (tag law, ADR-0057 AR-T-1/AR-T-2).**
**`stable-*` tag census, fresh (`git tag -l 'stable-*'`, excluding the
three frozen legacy tags): 26 live**, up from the prior regeneration's
20 — six new since: `-player-close` (AR-VAC-0, tagged by this arc's own
FIRST, killed session before it died, verified — not re-created — by
this session), `-census-substance` (ADR-0069), `-vendoring-batch-1`
(ADR-0070), `-vendoring-batch-2` (ADR-0071), `-vendoring-batch-3`
(ADR-0072), `-demos-front-door` (ADR-0073).

**The founding incident, still SIX guarded failures — HELD, unchanged.**
The vendoring arc touched no CLI/help surface; all six mechanisms from
the player arc's own regeneration stand unre-probed-for-regression by
this session's own full-suite green run (below), not independently
re-walked live (a docs-only-adjacent arc re-confirms via the suite it
would break, not by re-running every prior arc's own live transcript).

**CLI surface, HELD unchanged since the player arc.** `--width N` and
`ehrt play PATH --board N` — no new flag landed this arc.

**Full suite posture, fresh this session (Step 0 baseline):**
`clojure -M:poly test :all skip:integration` — **261 `Test results:`
lines, 0 `FAIL`/`ERROR`/`Exception` anywhere**, up from the player
arc's own 227 namespaces (34 new — the vendored round-trip family plus
the census substance tests plus `notice_verbatim_test.clj`).

**NIST supply-chain posture, `workspace.edn`, HELD unchanged** — both
untouched this arc.

**The vendoring arc's own pointers.** `notes/adr/0069-census-
substance.md` (the qualifier), `0070`/`0071`/`0072-vendoring-batch-
{1,2,3}.md` (the three landing sessions), `0073-demos-front-door.md`
(the operator surface move) — full account in each; this arc's own
close record is `notes/adr/0074-vendoring-arc-close.md`.

## Design-channel contract (how sessions run)

Two channels: design (chat) plans/verifies vs public clone; Code
executes pasted prompts, self-archives, R30 ceremony. Evidence over
ruling — live probes have overturned survey rows, channel claims, and
the author's own summaries repeatedly across every arc. **This arc adds
a continuity instance, not just an evidence one:** an infrastructure-
level API block killed the vendoring arc close's own first Code session
mid-ceremony, immediately after its own Step 1 landed — the design
channel verified the landed seam (tip, tag, rulings append) by fresh
public-clone probe BEFORE authoring this session's own resumption
prompt, and the resumption explicitly marked Step 0/Step 1's work
READ-ONLY: verified, never redone. The standing discipline this
instance confirms: a killed session's own landed, pushed, verified work
is never repeated on a hunch — only re-probed. Continuity: new design
sessions read THIS file + roadmap + latest ADR execution record —
structural claims in any continuity prompt carry citations or an
explicit `[unverified]` tag `[A, AR-B-5]`.
