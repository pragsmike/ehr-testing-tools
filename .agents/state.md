# State of the project — continuity register

Regenerated by the design channel 2026-08-07 at the player arc's own
close (`notes/adr/0068-player-arc-close.md`, AR-PC-3), landed against
tip `f9e4afc` (this closing session's own predecessor tip — every `[V]`
claim below was re-probed against the live tree THIS session before
landing; nothing carried forward on the prior version's own authority
(this file's own regeneration contract, ADR-0047 AR-C-1, restated
`.agents/rulings.md`). The prior version was authored 2026-08-06
against tip `f5af489` (UX arc close, ADR-0064) — everything below
supersedes it, including two corrections recorded transcript-side by
the UX epilogue (ADR-0065 AR-EP-5) that never made it into the prior
version's own text. Regenerated at each arc close (standing rule,
unchanged). Provenance tags: `[A]` author-ruled, `[C]` channel-inferred,
`[V]` probe-verified at landing.

## What this repo is

Clojure/Polylith workspace generating deterministic synthetic hospital
traffic (Synthea GMF at pinned commit `7e08387c...f902`), injecting
defects, judging conformance (HL7v2/FHIR). Formats are emitters of the
patient state machine `[A]` (`notes/adr/` index: ADR-0043's own
"sibling emitters over one state machine" doctrine). Book repo
`ehr-testing-guide` is separate, permanently out of scope (ADR-0001
R2). A stranger can now watch that traffic breathe as a bed board
(`ehrt play PATH --board N`, player arc, ADR-0066–0067) without reading
a line of this project's own source.

## Component graph `[V @f9e4afc]`

18 `components/*` directories, 1 `bases/*` directory (`ls -d
components/*/ | wc -l` → 18, `ls -d bases/*/ | wc -l` → 1) — unchanged
in count since the prior regeneration; the player arc added no new
brick, only a new REQUIRE EDGE between two existing ones (below).

**NEW this arc: `corpus` → `sim-emit-hl7`, `corpus`'s first real
external edge into that component** (player board, `notes/ADRs.md`
ADR-0067 AR-BB2-1, ruled approved the session before by ADR-0066
AR-BB1-R(2)). `ehrt.sim-emit-hl7.interface` now exports exactly
`fold-message` (2-arg) — confirmed live (`grep -n "fold-message"
components/sim-emit-hl7/src/ehrt/sim_emit_hl7/interface.clj`): the
interface wraps it as `(defn fold-message [acc message] (v2-replay/
fold-message acc message))`, header comment naming `corpus`'s board
sink as the first real external caller (the `site-profile` half of the
interface still has none). Fresh caller-grep confirms the site:
`components/corpus/src/ehrt/corpus/board.clj` requires
`ehrt.sim-emit-hl7.interface`; `components/corpus/src/ehrt/corpus/
interface.clj` re-exports it as `board-fold-event`/`board-render-
snapshot`. **Finding, not an edit:** no `components/corpus/deps.edn`
edge was needed — `clojure -M:poly check` (the arbiter ADR-0067's own
ruling nominated) stayed green with zero `deps.edn` changes anywhere,
since every project that already carries `poly/corpus` already carried
`poly/sim-emit-hl7` transitively (fresh grep of every `components/*/
deps.edn`: none declares a `poly/*` local-root edge at all — that
wiring lives only in root `deps.edn`'s aliases and each `projects/*/
deps.edn`).

`sim-engine` (engine/churn/order-profiles) ← {`sim-check`,
`sim-emit-fhir`, residual `sim` (run/identifiers/version/manifest-
build/façade), `oracle`}. Fresh grep of every real
`ehrt.sim-engine.interface` requirer found the same five external
callers the prior regeneration recorded, still exactly five:
`sim-check/check.clj`, `sim-emit-fhir/emit_fhir.clj`,
`sim/identifiers.clj`, `sim/run.clj` (two aliased requires of the same
interface, one file), dev-adjacent `oracle/digest.clj` — HELD, the
player arc touched `sim-emit-hl7` and `corpus`, never `sim-engine`
itself.

**`sim-emit-hl7/emit_hl7.clj`'s own `:require` form, REWORDED this
session — a correction owed since the UX epilogue** (`notes/adr/
0065-ux-epilogue.md` AR-EP-5, disclosed there and discharged here per
its own promise). Fresh read of the file's own `ns` form: it holds
FOUR requires — `com.nervestaple.hl7-parser.parser`, `clojure.string`,
and exactly two `ehrt.*` ones, `[ehrt.sim-model.interface :as
sim-model]` and `[ehrt.sim-emit-hl7.site-profile :as site-profile]`.
The prior version's own wording read as if those two `ehrt.*` requires
were the entire form; they are in fact the only `ehrt.*` requires in a
four-require form — which is what the GATED invariant
(`ehrt.docs-tooling.sim-emit-hl7-dependency-test`) actually checks and
what HELD throughout, unaffected by the wording gap. "Audit evidence
uses the mechanism it recommends" (the UX arc's own third standing
law, `.agents/rulings.md`) applies to claim PHRASING too, not only to
verification method — the lesson this correction exists to close.

`provenance` ← {`corpus`, `sim`}, forbidden forever from any `ehrt.*`
require beyond itself — HELD, still gated
(`ehrt.docs-tooling.provenance-leaf-law-test`); `provenance`'s own
`deps.edn` confirmed live, unchanged: `{:deps {metosin/malli
{:mvn/version "0.20.1"}}}`, nothing else. `oracle` = the digest
component (each worktree carries its own producer via its own
synthetic classpath, ADR-0044 AR-P-2/AR-P-3). Census lives in
`sim-trajectory` (confirmed live, `census_test.clj`, untouched this
arc). Façade `ehrt.sim.interface` surface frozen `[A]` (`.agents/plans/
2026-08-02-sim-split-plan.md` AR-3; corpus depends on this interface's
own stability in-process, ADR-0012).

**Resource nesting, closed and gated (alignment fixes 3, ADR-0052),
HELD unchanged.** The seven `components/*/resources` directories
workspace-wide (confirmed live, unchanged set: `judge`, `kernel`,
`provenance`, `sim-engine`, `sim-model`, `sim-trajectory`, `sim`) all
still nest under their own brick name. Gated:
`ehrt.docs-tooling.resource-nesting-test`, no allowlist.

## Where history lives `[V @f9e4afc]`

* `notes/ADRs.md` = INDEX (112 lines, confirmed live); entries at
  `notes/adr/NNNN-slug.md`. **File count: 65**, confirmed
  (`ls notes/adr/*.md | grep -v README | wc -l`) — three landed this
  arc (0066/0067/the UX epilogue's own 0065, which precedes and
  rotates with the UX arc, not this one — see rotation, below); this
  ADR's own file (`0068-player-arc-close.md`) will make it 66 once it
  lands, the same staleness-at-count-instant pattern every prior
  regeneration has named, noted rather than chased for the same reason.
* Roadmap = live map only (Now/Next/Externals/Deferred/Done-current-
  arc-only); Done history at `.agents/plans/roadmap-done-2026-07.md`
  (0 entries, unchanged) and `roadmap-done-2026-08.md`. **This
  session's own rotation (AR-PC-5):** ADR-0064's own pointer — left in
  place as the live roadmap's sole current entry at the UX arc's own
  close — relocates into the attic's EXISTING `## UX arc — closed
  2026-08-06 (ADR-0056–0064)` section with a dated append note, the
  same disclosed-leftover class both prior closes handled for their own
  predecessors. ADR-0065 (the UX epilogue) ALSO joins that same UX-arc
  section, with a dated note naming it the arc's own epilogue — it
  patched that arc's own surface (the tombstone, `--width`), so it
  rests with that arc rather than this one. A new `## Player arc —
  closed 2026-08-07 (ADR-0066–0068)` header holds ADR-0066 and
  ADR-0067's own Done pointers, relocated verbatim. `.agents/plans/
  roadmap.md`'s own Done section now holds ADR-0068's own pointer as
  its sole current entry.
* Session narrative hierarchy `[A, AR-B-4]`: ADR execution record =
  sole narrative; session record = ceremony log; roadmap Done = one-
  line pointer (gated: `ehrt.docs-tooling.done-pointer-adr-test`);
  prompt archive = provenance (gated:
  `ehrt.docs-tooling.prompt-record-pairing-test`).

## Standing gates `[V @f9e4afc]`

`components/docs-tooling/test/ehrt/docs_tooling/` holds **22 test
files**, confirmed live (`ls ... | wc -l`), up from the prior
regeneration's 21 — **one new this arc**: `cli_tombstone_test.clj`
(created ux epilogue, `notes/adr/0065-ux-epilogue.md` AR-EP-1 — reads
root `deps.edn` as EDN, asserts the `:cli` alias exists with exactly
the given `:main-opts`/`:extra-paths`; the tombstone is not silently
removable). The UX epilogue precedes and belongs to the UX arc's own
rotation (see "Where history lives," above) — its gate is recorded here
because it is live NOW, the same way this file has always recorded live
gates regardless of which arc's rotation their own landing ADR belongs
to. Reading-set budgets: `:onboarding` and `:corpus` re-derived this
session (AR-PC-4, below — the two sets whose member paths this arc's
own span touched); `:sim`/`:judge`/`:docs` unchanged, confirmed by
diffing every set's own `:paths` against `git log 2e77096..HEAD
--name-only`. Structure-currency; index-completeness; stale-path
tripwire; prompt/record pairing; done-pointer→ADR; readme-presence;
skill-mirror-currency; docsgen/pipeline/usecases/quickstart-fresh/lint
(doc-generation gates, unrelated to this arc); notes-prompts-frozen;
tag-law; invocation-lint; the five alignment-arc gates
(`sim-emit-hl7-dependency-test`, `provenance-leaf-law-test`,
`root-alias-completeness-test`, `resource-nesting-test`,
`license-text-pointer-test`) — all HELD, unchanged, none touched this
arc.

**`bases/cli/test/ehrt/cli/` holds 7 files, confirmed live** (`ls ...`
— `claude_md_presence_test.clj`, `core_test.clj`,
`executable_bits_test.clj`, `help_test.clj`, `help_voice_test.clj`,
`help_wrap_test.clj`, `retired_test.clj`), up from the prior
regeneration's 6 — **one new this arc**: `retired_test.clj` (created ux
epilogue, ADR-0065 AR-EP-1 — the tombstone message clears
`help-voice-test`'s own agent-speak pattern, names `bin/ehrt`, never
re-teaches the retired `clojure -M:cli run` form). `core_test.clj` and
`help_wrap_test.clj` both GREW rather than gained new files: the
`--width`/COLUMNS dispatch-layer tests and the 40/60/120-column wrap
invariants (ux epilogue, AR-EP-3) landed inside `core_test.clj`/
`help_wrap_test.clj`; the `--board` cadence/precedence/cue tests (player
board, ADR-0067 AR-BB2-2/3/4) landed inside `core_test.clj` too — none
of the three earned its own file. **`components/corpus/test/ehrt/
corpus/` gained `board_test.clj`** (player board, ADR-0067 AR-BB2-3/4 —
the renderer and fold-event tests, file presence confirmed live), the
one new file in that component's own test tree this arc;
`components/sim/test/ehrt/sim/run_test.clj` grew (the AR-BB2-R
rider's own one test) without gaining a new file.

Oracle: `bin/regression-oracle`, per-worktree digest via
`components/oracle`, cross-side source-equivalence check,
`--declared-digest-change` flag, all-eleven-batches identity the
standing expectation for structural/docs/read-side/display-sink
sessions — held across every one of this arc's own ADRs (0065 precedes
and belongs to the UX arc; 0066/0067 are this arc's own), confirmed by
direct read of each ADR's own Verification section; this session's own
Step 0 pre-digest (`bin/regression-oracle f9e4afc f9e4afc`) reconfirms
all eleven roots IDENTICAL at this arc's own closing tip, soundness
"yes outside ns form."

## Live work `[V @f9e4afc]`

* Deferred = LIVE rows only (**11**, confirmed by direct count, HELD
  since the prior regeneration — the UX epilogue's own AR-EP-4
  relocated two long-dangling FIXED-marked rows to Done, which is why
  11 is the number both before and after this arc: the epilogue's own
  relocation happened before this arc opened, and the player arc itself
  touched zero Deferred rows).
* Next (roadmap, **9** rows by fresh count, down from the prior
  regeneration's 11 — **two corpus-player named futures (bed board/
  census sink, accumulator wiring) moved to Done in ADR-0067**, this
  arc's own headline delivery; the remaining corpus-player row (the sim
  event-log input adapter, ADR-0014) stays Next, unbuilt, and is this
  arc's own named intake item for whichever session takes it up).
  Membership otherwise unchanged: lookup `time` gap, Wave G attachment
  trigger, pairing-as-data registry (landing spot `judge`, ruled
  ADR-0050 D-3), storefront fixture, quickstart→nightly, generator-
  source split, corpus.display placement, table-helper dedup.
* Externals (author-only), unchanged this arc, restated: NIST
  licensing inquiry — narrowed, not resolved (per-coordinate
  classification in `components/corpus/docs/experiments/
  EXP-SBOM-results.md`), still author-only (send the drafted gist). IG
  pinning: still open. Clojars publish: ruled, deferred — F-5/F-6 are
  its own remaining decisions (unchanged since the alignment arc's own
  tally). SETUP rewalk: still open. `/mnt/c` disposition: closed
  (ADR-0047 AR-C-3), unchanged.
* **Both audit registers' pointers, with final-tally status — restated
  unchanged, neither register touched this arc.** Alignment audit
  register (`.agents/plans/2026-08-05-alignment-audit-findings.md`, 54
  rows by the alignment arc's own fresh count): dispositions FINAL per
  `notes/adr/0055-alignment-arc-close.md`. UX audit register
  (`.agents/plans/2026-08-06-ux-audit-findings.md`, 21 rows carrying a
  disposition): dispositions FINAL per `notes/adr/0064-ux-arc-close.md`.
  Full tallies in each closing ADR — not reproduced here again this
  arc, since neither register moved.

## Environment `[A, 2026-08-07]`

Claude Code project root = UNC path to the ext4 clone, unchanged, no
new incident this arc (`[V]`: fresh grep of this arc's own session
records for `mnt/c`/`dual-clone`/`clone stale` — zero hits; `df -T .`
confirms `ext4` at this session's own Step 0).

**Tag mechanic, HELD unchanged (tag law, `notes/adr/0057-tag-law.md`
AR-T-1/AR-T-2, 2026-08-06).** `stable-*` continuity tags are a SESSION
ACT: a session creates and pushes one (i) when its own prompt licenses
a SPECIFIC tag at a SPECIFIC commit, issued by the design channel only
after verifying that landing, or (ii) for its own predecessor's
design-channel-verified stable point, as standing ceremony. **This
session's own AR-PC-0 exercised case (ii) again**
(`stable-20260807-player-board` at `f9e4afc`) — the mechanic has now
held under direct exercise across every session since the tag-law
sweep (ADR-0057), including the UX arc's own close, the UX epilogue,
player fold, and this one, with zero drift found on any of the fresh
reads this file's own regenerations have run each time.
**`stable-*` tag census, fresh (`git tag -l 'stable-*'`, excluding the
three frozen legacy tags): 20 live**, up from the prior regeneration's
16 — four new since: `-ux-close` (UX arc close, ADR-0064 AR-UC-0),
`-ux-epilogue` (ADR-0065 AR-EP-0), `-player-fold` (ADR-0066 AR-BB1-0),
and this session's own `-player-board` (ADR-0067's own landing, tagged
under AR-PC-0).

**The founding incident, now SIX guarded failures — confirmed by fresh
live probe this session, not merely read from an ADR.** The 2026-08-06
founding conversation named four; two more surfaced and were closed
since the UX arc's own close:

1. **Stale invocation** (`clojure -M:cli run ...` taught in demo docs)
   → swept to `bin/ehrt` form (ux-fixes-1, ADR-0059) AND gated so the
   fences actually RUN (ux-fixes-2's own rider, ADR-0060) —
   `invocation_lint_test.clj`. Confirmed live: zero `clojure -M:cli`
   occurrences taught anywhere in the doc tree (occurrences that NAME
   the retired form for provenance, e.g. in ADR text, are not this —
   see item 5).
2. **Opaque config crash** (`--config <missing-or-malformed>` → raw JVM
   stack trace, exit 1) → `merge-config-file` adopts the Result
   vocabulary (ux-fixes-2, ADR-0060 AR-U2-1/AR-U2-2). Confirmed live,
   the founding incident's own exact command shape: `bin/ehrt sim run
   --seed 1 --patients 1 --config config/busy-weekday.edn` →
   `{:status :error, :category :config-not-found, :payload {:path
   "config/busy-weekday.edn"}}`, exit 2. **Correction from the prior
   regeneration's own recorded transcript:** that transcript showed a
   `:did-you-mean "config/busy-weekday.md"` key in the payload; this
   session's own fresh probe on a tree where the sibling `.md` fixture
   is now ceremonial (item 6, below — untracked, not guaranteed
   present) shows the payload WITHOUT that key when the sibling does
   not exist on disk. Both shapes are correct: `:did-you-mean` appears
   only when a same-stem sibling is actually found, exactly as
   designed — the prior transcript simply reflected a session where the
   author's own scratch file happened to be present. No behavior
   changed; the claim is now probed against the leaner, correct
   assumption (fresh clone, no author scratch files).
3. **Silent typo** (`--patiens` absorbed, run succeeds wrong) →
   spec-derived flag validation rejects by name with a near-miss
   suggestion (ux-fixes-3, ADR-0061). Confirmed live:
   `bin/ehrt sim run --patiens 200 --seed 1` → `{:status :error,
   :category :unknown-flag, :payload {:flag "--patiens", :verb "sim
   run", :did-you-mean "--patients"}}`, exit 2.
4. **Agent-voice help** (24 ADR + 14 milestone + 3 ruling citations, 3
   bare keywords, 2 internal-namespace leaks, 4 strings over 250 chars
   with no wrap structure) → the approved rewrite relocates all 36 real
   token hits to source comments (ux-fixes-4, ADR-0062), and a real
   hanging-indent line-wrap mechanism replaces raw terminal soft-wrap,
   content byte-preserved (ux-fixes-5, ADR-0063), now resizable live
   (item 6 note: `--width`/COLUMNS, ux epilogue). Confirmed live:
   `bin/ehrt help` carries zero `ADR-[0-9]{4}`/`ruling [0-9]`/`D1[0-9]`
   tokens; every rendered line ≤ its own resolved width.
5. **NEW, closed by the UX epilogue: the runtime half of failure #1 was
   never actually tombstoned.** The founding command shape resurfaced
   live from the author's own shell the same day the UX arc closed
   (`clojure -M:cli run ...` → `FileNotFoundException`) — no `:cli`
   alias had ever existed in the monorepo root `deps.edn`, so
   `clojure.main` silently dropped it and tried to load the bare
   positional `run` as an init-script path. ADR-0059/ux-fixes-1 had
   swept the DOCS surface and gated it; the RUNTIME surface was a gap
   the UX audit register never named. Fixed (ux epilogue, `notes/adr/
   0065-ux-epilogue.md` AR-EP-1): root `deps.edn` gains a real `:cli`
   alias pointing at a new `ehrt.cli.retired` namespace — prints a
   named redirect to stderr and exits 2, never resurrected as a
   `bin/ehrt` synonym (that would reopen the cwd-relative-path hazard
   `bin/ehrt`'s own header exists to prevent). Gated two ways:
   `retired_test.clj` (the message's own voice and content) and
   `cli_tombstone_test.clj` (the alias itself is not silently
   removable). Confirmed live this session:
   `clojure -M:cli run --seed 42 --patients 200 --config
   ./config/busy-weekday.edn` → `clojure -M:cli is retired.` /
   `The CLI moved: use bin/ehrt instead.` / a worked `bin/ehrt sim run
   ...` example / `run bin/ehrt help for commands`, exit 2.
6. **NEW, closed by the player arc: the founding fixture's own
   load-bearing status retired.** `merge-config-file-suggests-a-
   same-stem-sibling-file` had read the LIVE `config/` directory and
   depended on the untracked, author-held `config/busy-weekday.md`
   (standing since ADR-0060) — the suite had never been green on a
   fresh clone since that test landed, because a fresh clone never
   carries the author's own scratch file. Fixed (player board, `notes/
   adr/0067-player-board.md` AR-BB2-R, the author's own verbatim
   ruling: *"Tests should build their own directories as needed"*): the
   test now builds its own temp-dir fixture. Confirmed live this
   session: the full non-integration suite (`clojure -M:poly test :all
   skip:integration`) — 227 namespaces, 227 `Test results:` lines, 0
   `FAIL`/`ERROR`/`Exception` anywhere — is green on THIS session's own
   fresh Step 0 baseline, the first time this file can make that claim
   since ADR-0060 landed. `config/busy-weekday.md`'s own disposition is
   now ceremonial: still untracked, still untouched, no longer
   load-bearing for any test (confirmed absent from disk at this
   session's own Step 0, same as every session since ADR-0060).

Two smaller register rows closed earlier, HELD: the unknown-command
hint names the real group when one matches (`bin/ehrt run` →
`:hint "run: ehrt help sim"`, ux-fixes-2/epilogue's own sibling-path
extension, ADR-0060/ADR-0065); bare `bin/ehrt` still exits 0.

**CLI surface, grown twice since the prior regeneration.**
`--width N` (ux epilogue, ADR-0065 AR-EP-3): help-rendering-only,
resolution order explicit flag beats `COLUMNS` env beats the 80-column
default; a bad `--width` value is rejected by name (`:invalid-width`,
exit 2), a bad `COLUMNS` value falls back silently (ambient input must
not break help). Confirmed live: `bin/ehrt help --width abc` →
`{:status :error, :category :invalid-width, :payload {:flag "--width",
:value "abc", :expected "an integer >= 40"}}`, exit 2.
`ehrt play PATH --board N` (player board, ADR-0067 AR-BB2-2/3/4): N
stream-minutes per snapshot, display-only (ignored when `--sink` is
given, wins over `--ticker` when both given); renders occupied beds
grouped by ward via `ehrt.corpus.board/render-snapshot`, folding
through the exported `fold-message`; a message outside the emitter's
handled trigger set is skipped-with-cue and counted
(`:unfolded-count`), never crashes. ADR-0067's own live probe (full
transcript there, and in `.agents/session-records/
2026-08-07-player-board.md`) exercised a real seed-5 churn corpus
through `--board 60` and caught a merge tombstone still rendering as
occupied; this regeneration does not re-run that probe (a docs-only
session re-confirms claims against the code that would produce them,
not by re-executing another session's own live run) — fresh read this
session confirms the fix still stands: `occupied?`'s own docstring and
body still exclude `:status :merged` from bed occupancy, unchanged
since ADR-0067 landed it.

**NIST supply-chain posture, HELD unchanged (alignment fixes 4,
ADR-0053).** `bin/verify-nist-lock` still wired as `make test`'s own
third line (confirmed live, `Makefile` line 41). The licensing inquiry
itself stays open (see "Live work," above) — untouched this arc.

**`workspace.edn`, HELD unchanged (alignment fixes 1, ADR-0050
AR-F1-4).** 33 lines (confirmed live), untouched this arc.

**The player arc's own pointer.** `notes/adr/0066-player-fold.md`
(the fold, total over the emitter's real trigger set, self-anchored in
absolute epoch millis) and `notes/adr/0067-player-board.md`
(the whiteboard itself, plus the fresh-clone-green rider) — full
account in each; this arc's own close record is `notes/adr/
0068-player-arc-close.md`.

## Design-channel contract (how sessions run)

Two channels: design (chat) plans/verifies vs public clone; Code
executes pasted prompts, self-archives, R30 ceremony. Evidence over
ruling — live probes have overturned survey rows, channel claims, and
the author's own summaries repeatedly across every arc now: the
alignment arc's own EXP-SBOM citation drift and three ADR-0054
precision gaps; the UX arc's own A-2 grammar-vs-path verification gap
and 38-vs-36 token-count method drift — the standing rule those two
same-arc instances generalized into `.agents/rulings.md`: **audit
evidence uses the mechanism it recommends**, not a cheaper proxy for
it. This arc adds a phrasing instance, not just a method one: the UX
epilogue's own AR-EP-5 found this very file's `emit_hl7.clj` claim
imprecisely WORDED, correct in substance — the standing rule now
explicitly covers claim phrasing, not only verification method (see
"Component graph," above). Continuity: new design sessions read THIS
file + roadmap + latest ADR execution record — structural claims in any
continuity prompt carry citations or an explicit `[unverified]` tag
`[A, AR-B-5]`.
