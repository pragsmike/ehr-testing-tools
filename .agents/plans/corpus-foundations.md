# Plan: Corpus Foundations

The phase sequence from bootstrap to a working generation capability and
its first executed experiment. One line per phase: what it delivers,
where it stands, and the prompt that drove it (once archived).

| Phase | Deliverables | Status | Prompt |
|---|---|---|---|
| P0 | Workflow housekeeping: prompts-dir semantics, `make pack-push`, pack-ritual wording | Done (2026-07-23) | `.agents/prompts/archive/2026-07-23-p0-p2-landing.md` |
| P1 | ADR-0004 (capability-oriented structure, `ehr` CLI contract), ADR-0005 (artifact lockfile) | Done (2026-07-23) | `.agents/prompts/archive/2026-07-23-p0-p2-landing.md` |
| P2 | `docs/components.md` + license F-rows, `docs/experiments/EXP-A4.md` protocol, positioning open-decision closed | Done (2026-07-23) | `.agents/prompts/archive/2026-07-23-p0-p2-landing.md` |
| P3 | Artifact registry, result/invocation/locator/canonical schemas, `corpus.generate`, `ehr` CLI, EXP-A4 executed (manifest v1) | Done (2026-07-24) | `.agents/prompts/archive/2026-07-24-p3-generation-exp-a4.md` |
| P4 | JVM as a lockfile artifact; manifest v1.1; resource-equation notation trial (`docs/notation.md`, `docs/pipeline.edn`/`.md`); EXP-B2 executed (parse→serialize round-trip fidelity); mutation capability (`corpus.operators`, FHIR locator grammar, `ehr-testing-tools.lineage`, `corpus.mutate`, `ehr corpus mutate`) — FHIR only | Done (2026-07-24) | `.agents/prompts/archive/2026-07-24-p4-mutation-exp-b2.md` |
| P5 | Corpus intake (foreign corpora, catalog + intake records); EXP-C5 executed (FHIR validator offline behavior, 15 categories classified); `gate.fhir` (two-step, EXP-C5-derived verdict mapping, IG machinery built/unpinned) and `gate.v2` (base-structural, HAPI) landed; `gate.report` (aggregation, diffing) + `ehr gate fhir\|v2` CLI; contract pairing (5 defect operators vs `gate.fhir`, `^:integration` suite against the real validator) + the Gate kind law tested against the real engine; pipeline as-built (Intake/Gate/Report `:built`); pattern nursery #13 trial evidence (second real diagram-caught gap); README/positioning/gate-calibration updated | Done (2026-07-24) | `.agents/prompts/archive/2026-07-24-p5-gates.md` |
| P6 | The Check capability (`ehr-testing-tools.check`): v1 assertion vocabulary (`:matches-expected`, `:present`/`:absent`, `:value`, `:count`, `:schema`), golden equivalence via a declared ordered canonicalizer list, `ehr check` CLI; `ehr-testing-tools.diff` promoted from a private test helper (a second real consumer, `check.clj`, earned it). Notation gains union resources and external stages (`docs/notation.md`); `docs/pipeline.edn`'s `datum` union closes the P5 disconnected-wire gap; pattern nursery #13 promoted tentative → validated. Baseline-relative gating (`ehr gate fhir\|v2 DIR --baseline report.edn`, `gate.report/baseline-relative-report`), proven end to end against the real FHIR validator (`test-integration/baseline_gating_test.clj`). `docs/use-cases.edn`/`.md`: 14 equation-anchored use cases, generated via a new `ehr-testing-tools.usecases` renderer and `make use-cases`. Tier-1 pipeline lint (`ehr-testing-tools.lint`, `make lint-pipeline`): every catalytic resource in `docs/pipeline.edn` and `docs/use-cases.edn` resolves to one of the four targets, seeded-violation-tested, not yet CI-wired. | Done (2026-07-25) | `.agents/prompts/archive/2026-07-25-p6-check-usecases.md` |
| R | Judge/gate factorization (Phases 0–1 of `.agents/plans/judge-gate-refactor.md`): `docs/palgebra-design.md` + ADR-0009 landed; `gate.*` namespaces renamed to `judge.*`; finding `:policy` renamed `:disposition`; `docs/pipeline.edn`'s `:gate` kind renamed `:judge` (route-by-verdict documented as derived); `docs/gate-calibration.md` renamed `docs/judge-calibration.md`; full suite + coverage + integration suite (real validator/HAPI) green throughout. No new capability; Phase 2 (palgebra claim sweep) and Phase 3 (the verdict split) are later sessions. | Done (2026-07-25) | `.agents/prompts/archive/2026-07-25-r1-judge-gate-renames.md` |
| R2 | Palgebra claim sweep (Phase 2 of `.agents/plans/judge-gate-refactor.md`, D9): a top-level `palgebra/` tree claims the diagram renderer + cyberneutics example equation sets (`palgebra/tools`, `palgebra/examples`; `SKILL.md` updated in place), the generic signature-loading machinery (`palgebra.signature`, parameterized by a caller's own stage-kind set -- this repo's five kinds now load from `docs/signature.edn`, D13), and the catalytic-lint mechanism (`palgebra.lint`, parameterized by a caller's own taxonomy -- the four concrete EHR targets stay in `ehr-testing-tools.lint`, delegating). `palgebra.deps-lint` + `make lint-deps` enforce the dependency direction (`palgebra.* ↛ ehr-testing-tools.*`, tests included); a toy signature (two sorts, three stages, no EHR vocabulary) in `palgebra/test` exercises the loader and lint mechanism directly. Claiming, not improving: no rewrite toward the specified language (sorts, `⨟`, lower/erase/emit stay design-only, Phase 4). Byte-identical regeneration of `docs/pipeline.md`/`docs/use-cases.md` verified; full suite + coverage + both lints green; integration suite (real validator/HAPI) green as a courtesy pass. Phase 3 (the verdict split) is a later session. | Done (2026-07-25) | `.agents/prompts/archive/2026-07-25-r2-palgebra-claim.md` |
| R3 | The verdict split (Phase 3 of `.agents/plans/judge-gate-refactor.md`, D10): verdict gains a fourth arm, `no-verdict` paired with a `:cause` keyword in a sibling field (Malli-enforced total pairing, `judge.finding/VerdictOutcome`/`valid-cause-pairing?`); `:indeterminate` is now reserved, no producer anywhere in this repo. `judge.fhir`'s terminology-suppressed classification emits `no-verdict(:terminology-suppressed)` (O2 resolved as designed); `verdict-mapping-version` bumped v1→v2; `judge.v2`'s docstring states it produces neither. `judge.report`'s schema/aggregation/diff carry the fourth arm; baseline-relative mode reads pre-split (three-valued) baselines forward, unmigrated (a frozen fixture proves it). CLI gains `ehr gate ... --treat-no-verdict-as pass\|rejected` plus a distinct default exit code (3) for an unhandled no-verdict aggregate -- the policy-totality law (D10) made explicit at the act layer. **`worst-of`'s ranking was revised mid-session**: the plan's own recommendation (rank `no-verdict` above `rejected`) made every real, US-Core-profiled Synthea file's aggregate verdict `:no-verdict` regardless of an actual injected defect (confirmed: one mutant bundle carried 3062 terminology-suppressed issues alongside its own defect), which the real-validator integration run (`make integration`) caught directly -- overriding rather than losing to the contract-pairing/baseline-gating polarity regression. Revised, author-decided: `:pass < :indeterminate < :no-verdict < :rejected` (a confirmed violation still dominates the aggregate); ADR-0010 and `docs/palgebra-design.md`'s D10/O1/O2 register amended in place with the reasoning. Both integration suites (real validator/HAPI) pass with zero edits after the revision, as the plan requires. Full suite + coverage + both lints + golden regeneration green throughout. | Done (2026-07-25) | `.agents/prompts/archive/2026-07-25-r3-verdict-split.md` |
| P7 | v2 mutation over the fixture corpus: done. v2 locator grammar extended to segment/segment-repeat/field/field-repeat/component/subcomponent (MSH-1/MSH-2 off-by-one convention documented in `locator.clj`, resolved in `corpus.er7`); `corpus.er7` substrate (delimiter split/join, byte-identical round-trip over all five fixtures incl. the two adversarial ones); v2 seed catalog in `corpus.operators` (5 operators: `blank-required-field`, `corrupt-encoding-characters`, `malformed-datetime-value`, `truncate-segment-fields`, `corrupt-segment-name`), each empirically verified to convict under `judge.v2`'s base-structural tier before registration — several plausible candidates (dropping PID entirely, corrupting PID's own name, blanking a non-header field) were probed and found NOT to convict at this tier and are recorded as dropped in `corpus.operators`'s own docstring, not shipped unconvictable; `corpus.mutate` + `ehr corpus mutate` format dispatch (FHIR path unchanged, verified by the full suite staying green); v2 contract-pairing suite (`test/ehr_testing_tools/v2_contract_pairing_test.clj`, in-process/unit speed — `judge.v2`'s twin of the FHIR integration suite, not `test-integration/`, since no real artifact or subprocess is needed). Foreign-corpus sample: **partially satisfied (2026-07-26)**, no longer simply awaited — `.agents/prompts/archive/2026-07-26-simhospital-corpus-adoption.md` vendored Google SimHospital's 1,013-message HL7 v2.3 corpus into `test/fixtures/v2/simhospital/` under ADR-0011 (Apache-2.0, synthetic by construction, with `PROVENANCE.md` and F23–F25), and proved both substrates against it: `corpus.er7` round-trips all 1,013 messages byte-identically (F26) and `corpus.intake` catalogs the whole corpus with no `src/` change, recovering its 403 distinct patients through the catalog (F27). Two things this does *not* close. (1) **The relationship purpose is untouched**: a sample from the motivating partner team — their real message shapes, their local quirks, their profile — is what tells this repo whether its substrate matches a corpus anyone actually runs; a synthetic corpus from a third party cannot answer that, and the same is true of the IG-pinning blocker below. (2) **Event coverage is narrow**: this particular SimHospital run is 610 `ORU^R01` + 400 `ADT^A01` + 2 `MDM^T02` + 1 `ADT^A34`, with **no `ORM^O01`, `ADT^A02`, or `ADT^A03`** — so order/result correlation and patient movement remain unexercised. Filling that is Phase B of `docs/research/HL7v2-sanitized-corpus-research.md` (a pinned SimHospital run against a custom pathway), a named follow-on session, not a matter of finding another sample. IG pinning (awaiting the motivating team's target profile artifact) and EXP-D3 (awaiting the NIST license inquiry, F1/F15) remain externally blocked, untouched by this session, by design. EXP-A3 (Synthea `PatientExporter` v2 spike) stays demoted to backlog, unchanged from P5's framing. Enforcement-wave candidates still include wiring `make lint-pipeline` into CI (P6 shipped the lint itself, not its CI enforcement) alongside the pre-existing items below. **CAL-1 (2026-07-25) calibrated this session's dropped-candidate finding**: it now has a facts-register row (F22, HAPI source + a fresh reproduction) and a `docs/judge-calibration.md` v2 section, and the EXP-D3 row names the three dropped operators as that experiment's adoption case. | Done (2026-07-25) | `.agents/prompts/archive/2026-07-25-p7-v2-mutation.md` |
| Publication | Editorial coherence pass (README, docs/ reading order, pipeline/notation/components/experiments/positioning rewrites); minimal CI (`.github/workflows/ci.yml`, hermeticity verified); ADR-0008 (go-public gate walked and met; repo published) | Done (2026-07-24) | `.agents/prompts/2026-07-24-publication-wave.md` |
| Enforcement wave (ENF-1) | Every check named below already existed and was already green locally; this session only wires each into a hook or CI so it can't silently rot (ADR-0006 staged enforcement). **Step 0**: corrected the First-release row's false "coverage-threshold gating landed" claim (it hadn't — no fail-threshold flag existed anywhere before this session). **Step 1**: `.githooks/pre-push` — WSL check (pre-commit's precedent) + `make test` + `lint-pipeline` + `lint-deps`, refusing the push on failure with `git push --no-verify` as the documented escape hatch; opt-in via the existing `core.hooksPath` mechanism, no new automation. **Step 2**: `ci.yml`'s per-push job gains `lint-pipeline` and `lint-deps` steps, a generated-doc freshness step (`make pipeline && make use-cases && git diff --exit-code docs/pipeline.md docs/use-cases.md` — python3 confirmed preinstalled on the ubuntu-latest runner image, no setup step needed), and a coverage floor: `deps.edn`'s `:coverage` alias now carries cloverage's own `--fail-threshold 85` (forms), one source of truth for `make coverage` and CI alike, a margin below the current 89.43% forms / 93.20% lines. The palgebra namespace-direction lint (`lint-deps`, D9) **landed in CI** in this same step, alongside `lint-pipeline`. **Step 3**: new `.github/workflows/integration.yml` — the nightly-integration proposal this row used to list as unshipped now **resolves into a shipped workflow**: `schedule` (07:00 UTC) + `workflow_dispatch`, `make integration` against an artifact cache primed via `ehr artifact fetch` and cached with `actions/cache` keyed on `artifacts.lock.edn`'s hash; never in the per-push path, reports without blocking any merge. Verified locally, green throughout: `make test` (410 tests/1008 assertions), both lints, the freshness pair (byte-identical regeneration), `make coverage` (89.43%/93.20%, above the 85% floor), `make integration` (8 tests/24 assertions). **First real `workflow_dispatch` run of `integration.yml`, post-push, cold artifact cache**: green, 10m29s end to end (run [30175880198](https://github.com/pragsmike/ehr-testing-tools/actions/runs/30175880198)) — confirms the cache-priming step's three `ehr artifact fetch` calls and the real validator/HAPI integration suite both work unmodified on the actual GitHub Actions runner, not just locally. | Done (2026-07-25) | `.agents/prompts/archive/2026-07-25-enf1-enforcement-wave.md` |
| First release | Gates capability landed (`gate.fhir`/`gate.v2`, done P5); the resource-equation notation trial (pattern nursery #13) concluded — promoted to validated, P6. Coverage-threshold gating — **corrected 2026-07-25 (ENF-1): this row previously read "landed," which was false** — no `:coverage` alias or Makefile invocation ever carried a cloverage fail-threshold flag before ENF-1 (confirmed by `git log -p -- Makefile`); `ci.yml`'s own header comment had it right ("No coverage threshold gating yet -- that's the enforcement wave"). Gating lands in the Enforcement-wave row (ENF-1, this same date), not here; this row now only tracks the remaining First-release items. Version tag, published coordinates (Clojars vs. Maven Central, `docs/positioning.md` open decision), guide-repo cross-references begin. | Not started — the milestone after publication (ADR-0008) | — |

**v2 mutation deferral, updated (P5).** P4 scoped mutation to FHIR
only; EXP-B2 already characterized v2 ER7 round-trip fidelity via HAPI
HL7v2's `PipeParser` (faithful for realistically-populated messages;
confirmed trailing-delimiter canonicalization on a message crafted to
trigger it), so the representation question v2 mutation will need is
already answered — only the operator catalog and locator grammar for
v2 remain to be built (P6). What changed in P5: v2 mutation's
dependency used to be framed as EXP-A3 alone (the Synthea
`PatientExporter` plugin spike, the only route to v2 *generation* this
repo had). `corpus.intake` (P5) opens a second route — a foreign v2
corpus, brought by whoever is gating their own pipeline's output,
already exists as v2-mutation input without waiting on generation.
EXP-A3 is demoted to backlog accordingly, not abandoned: it still
matters for the guide's own Experiment 3 (which needs *this repo's*
generated v2 output specifically, not a foreign corpus), but it is no
longer this repo's own v2-mutation blocker. `docs/experiments.md`'s
EXP-A3 row is updated with this rationale.

**Closed (P7, 2026-07-25).** The operator catalog and locator grammar
this note named as the only remaining work landed in P7: see the P7
row above. This note's own dependency chain is now fully resolved for
the fixture corpus (EXP-B2's representation answer, applied; P5's
intake route, unused this session in favor of the committed fixtures
directly; the grammar and catalog, built). What remains open is
exactly what this note never promised to resolve — a real *foreign* v2
corpus sample, partially satisfied 2026-07-26 (a vendored synthetic
one, ADR-0011; a partner-team sample and broader event coverage still
open), tracked on the P7 row above, not here.
