# Alignment & cleanup audit — findings register

Findings-only register for the alignment-audit arc (`.agents/plans/2026-08-05-alignment-audit-brief.md`,
`notes/ADRs.md` ADR-0049). Every row cites a probe; every row is a
recommendation, never an executed fix (AR-AU-2) — the two exceptions
(the S7 namespace fix-forward and the stable tag) were pre-ruled in
ADR-0049 itself and executed at Step 0, recorded there, not here.

Row format: `id | area | probe | evidence | finding | recommendation
with reasoning | proposed disposition`. Disposition ∈
{ruling-needed, fix-session-candidate (with suggested cluster),
close-as-fine, incomplete}.

Landed 2026-08-05 (alignment audit session, `notes/ADRs.md` ADR-0049),
against tip `f1ceea7` (Step 0's own closing commit).

---

## Seeded rows (S1–S7), updated with this session's evidence

**S1 — Resource-nesting hazard, live. RE-VERIFIED.**

| id | area | probe | evidence | finding | recommendation | disposition |
|---|---|---|---|---|---|---|
| S1 | naming/layout | `find components/sim-model/resources components/sim/resources -maxdepth 2`; `comm -12` on sorted relative file lists of `components/sim-model/resources/sim/` vs `components/sim/resources/sim/` | Both dirs exist; `sim-model`'s single resources subdir is named `sim` (files under `demographics/`), `sim`'s own resources subdir is also named `sim` (files under `modules/` + `version.edn`). `comm -12`: empty — zero filename overlap today. | Unchanged from the brief's seeding: a live classpath-merge hazard (two bricks sharing one resource root name), no current collision because the two trees happen to use disjoint subdirectory names. | Rename `sim-model/resources/sim` → `sim-model/resources/sim-model`, update any `io/resource "sim/..."` call sites in sim-model's own src in the same commit; oracle must stay identity (resource *content* unchanged, only the path prefix). Author ruling needed: fix, or rule the tolerance permanent with a dated note (ADR-0025 already disclosed it once, never ruled permanent). | ruling-needed |

**S2 — Stable-tag discipline. CLOSED this session.** Ruled adopted (ADR-0048 AR-R-2, `.agents/rulings.md`); this session executed the mechanic's first live instance: AR-AU-0 (standing amendment) delegated tag creation+push to this session when a prompt licenses a specific tag at a specific commit, post-verification. `stable-20260805-alignment-riders` was created (annotated, at `79b7a55`) and pushed via `git push origin <tag>` (no `gh api` fallback needed — the primary push path worked); verified live on origin via `git ls-remote --tags origin` (peeled ref resolves to `79b7a55` exactly). No further action; closing this row rather than carrying it forward as a repeat item.

**S3 — `workspace.edn` narrative, RE-VERIFIED still present.**

| id | area | probe | evidence | finding | recommendation | disposition |
|---|---|---|---|---|---|---|
| S3 | dependency graph / cognitive load | `wc -l workspace.edn`; read lines 15–54 | 59-line file; lines 15–54 (40 lines) are dated `:necessary` re-derivation narrative — five re-derivation events (docs-tooling split, corpus-io split, tools→corpus rename, sim-cli retirement, provenance M1) narrated inline as comments, matching the brief's "~50 lines" estimate closely (exact count 40, in the same ballpark — the brief's own figure was an estimate, not re-derived precisely; noting the discrepancy rather than silently treating it as exact). | Unchanged from the brief's seeding: the narrative is real, most of it cites ADRs, and it still lives as comments inside load-bearing config rather than in the ADR trail. | Move the narrative to the ADR trail (a dated append to the relevant split-stage ADR files, or a new consolidated note), keep a two-line pointer + the current invariant in `workspace.edn` itself ("re-derive via `poly deps`/`check` with entries cleared whenever project composition changes"). Needs ruling — config comments aren't plan artifacts, so annotate-not-rewrite doesn't strictly bind them, but the history is real and must land somewhere citable before it leaves the file. | ruling-needed |

**S4 — Corpus-player backlog. CLOSED, executed by the rider session (ADR-0048 AR-R-3), re-verified this session.**

| id | area | probe | evidence | finding | recommendation | disposition |
|---|---|---|---|---|---|---|
| S4 | docs/roadmap | `grep -n -i "bed board\|census sink\|accumulator\|mllp\|player" .agents/plans/roadmap.md` | Three live Next rows (bed board/census sink, accumulator wiring, sim event-log input adapter, lines 37–49) + one Deferred row (`:mllp` transport sink, lines 81–95, ns-corrected this session per AR-AU-1) — all citing `notes/adr/0014-corpus-player.md`. | The brief's own seeding is fully resolved: the player's remaining work is roadmap-visible, citing ADR-0014 by name rather than paraphrase. | No further action — closing, not carrying forward. | close-as-fine |

**S5 — Prose invariants without gates. RE-VERIFIED still open; enumerated further.**

| id | area | probe | evidence | finding | recommendation | disposition |
|---|---|---|---|---|---|---|
| S5 | interface/dependency hygiene | `ls components/docs-tooling/test/ehrt/docs_tooling/` (13 files) | No `deftest` file names or content matching a `sim-emit-hl7` dependency-law test or a `provenance` leaf-law test exist in `docs-tooling/test`. Existing gates in this family: `structure_currency_test.clj`, `stale_path_test.clj`, `index_completeness_test.clj`, `done_pointer_adr_test.clj`, `skill_mirror_currency_test.clj`, etc. — all doc/plan-currency gates, none a dependency-law gate. | The two seeded candidates (sim-emit-hl7's "requires nothing beyond sim-model + its own namespaces" AGENTS.md constraint; provenance's malli-only leaf law, AR-2 from ADR-0043) remain prose-only, enforced by poly's general interface rules plus vigilance, not by a named test. A third candidate surfaces from this session's own area-C work: the resources-nesting rule itself (S1/C-1) has no gate either — it's a checklist item in this brief, not a deftest. | Promote at least the two seeded candidates to ~10-line `deftest`s in `docs-tooling` (grep the ns forms, assert the allowed require set), matching the existing gate family's style; consider the resources-nesting rule (one subdir per component, matching the brick's own name) as a third candidate discovered this session. | fix-session-candidate (cluster: gate promotions — sim-emit-hl7 dependency law, provenance leaf law, resource-nesting rule) |

**S6 — Riders already owed. CLOSED, executed by the rider session (ADR-0048 AR-R-4/AR-R-5), re-verified this session.**

| id | area | probe | evidence | finding | recommendation | disposition |
|---|---|---|---|---|---|---|
| S6 | docs/roadmap, rulings register | `grep -n "myocardial" .agents/plans/roadmap-done-2026-08.md`; read `.agents/rulings.md`'s "From ADR-0048" section | `myocardial_infarction.json` row present in the Done attic (multiple citations, `.agents/plans/roadmap-done-2026-08.md`); `.agents/rulings.md` carries both appends (transcript-witnessed ≠ repo-recorded; stable-tag adoption), citing ADR-0048. | Both riders fully executed and already re-verified by this session's own read of the current tree. | No further action — closing. | close-as-fine |

**S7 — The tripwire-scope question (recommendation only, per AR-AU-2/brief §4.5 — NOT implemented this session).**

| id | area | probe | evidence | finding | recommendation | disposition |
|---|---|---|---|---|---|---|
| S7 | docs/cognitive-load, staleness tripwire | Read `components/docs-tooling/test/ehrt/docs_tooling/stale_path_test.clj` docstring (lines 1–17); this session's own AR-AU-1 fix (a genuine, live `ehrt.tools.corpus.source-sink` staleness found in `.agents/plans/roadmap.md`, a LIVE, current-tense document) | The tripwire's own docstring states its scope explicitly and deliberately: `docs/**/*.md` plus `components/corpus/docs/use-cases.edn` only — "notes/ADRs.md, notes/prompts/, and .agents/session-records/ narrate history... this test never reads them (confirmed at AR-7's own request, not assumed)." `.agents/plans/roadmap.md` is neither historical narration nor inside `docs/**` — it is a live, frequently-edited plan document, and it silently carried a stale `ehrt.tools.*` citation (two of them: the fixed `source-sink` one and the still-open `framing` one, see A-6 below) that the tripwire would never have caught. | The tripwire's exclusion list was designed around "frozen archive vs. live docs" — but it did not anticipate a THIRD class: live, current-tense, non-`docs/` plan surfaces (`.agents/plans/roadmap.md`, and by extension `.agents/plans/*.md` generally, excluding the `roadmap-done-*.md` attic files which are genuinely historical). This session's own S7 fix is direct evidence the gap is real, not hypothetical. | Recommend extending the tripwire's scan scope to include `.agents/plans/*.md` EXCLUDING `.agents/plans/roadmap-done-*.md` (the attic) and EXCLUDING dated one-shot plan/brief files that intentionally freeze a moment's prose (arguably `.agents/plans/2026-08-05-alignment-audit-brief.md` itself, which cites `ehrt.tools.corpus.source-sink` in its own seeded-findings prose deliberately, as a quotation of the live roadmap's THEN-current stale text) — the scope boundary needs an author ruling on exactly which `.agents/plans/` files count as "live" vs "frozen-at-authoring," not a session's own judgment call. This is the recommendation the brief's own S7 asked for; implementation is explicitly out of this session's fence. | ruling-needed |

---

## Area A — Dependency graph & composition (brief §4.1)

| id | probe | evidence | finding | recommendation | disposition |
|---|---|---|---|---|---|
| A-1 | `clojure -M:poly check :dev` | `Warning 207: Unnecessary components were found in the development project and may be removed: oracle.` (sole warning; `clojure -M:poly check` with no args, all projects, returns bare `OK`) | `:dev`'s own `oracle` entry (`deps.edn` line ~100–110, commented "the dev REPL carries it like every other brick") is flagged by poly's own reachability check as unreachable from `:dev`'s actual composition — nothing in `:dev`'s own extra-paths/src ever requires `ehrt.oracle.interface`; oracle's only real caller is `bin/regression-oracle`'s own synthetic per-worktree classpath (confirmed separately, area B/C — oracle's sole export `-main` is called only from that script). | Either (a) add an explicit `:necessary ["oracle"]`-style override for `development` with a comment naming the REPL-access rationale (matching how `conformance`/`integration` already carry documented overrides), or (b) drop `oracle` from `:dev`'s extra-deps if REPL access to it isn't actually exercised — a silent warning on every `poly check :dev` run is exactly the "vigilance, not gate" pattern this arc wants to reduce (S5's own theme). Author ruling needed on which. | ruling-needed |
| A-2 | `clojure -M:poly deps` (full 19×19 brick dependency matrix) | Matrix confirms every architecturally-documented edge (corpus→{kernel,judge,corpus-io,sim}; the sim-family fan-in from `oracle`'s own golden-run dependencies on sim/sim-model/sim-engine/sim-check/sim-trajectory/sim-emit-hl7/sim-emit-fhir for digest fixtures; judge-engine isolation; kernel's zero-dependency leaf position; provenance's zero-dependency leaf position). No edge in the matrix is unexplained by an existing ADR. | No orphan or unexpected-edge finding beyond A-1 (which is a project-level `:necessary`/reachability finding, not a brick-to-brick edge finding). | None — the dependency graph itself is clean. | close-as-fine |
| A-3 | `clojure -M:poly libs`; `clojure -M:poly libs :outdated` | Full report captured (15 libraries). Three show a newer version: `ca.uhn.hapi.fhir/hapi-fhir-base` 8.2.0→8.10.1, `ca.uhn.hapi.fhir/hapi-fhir-structures-r4` 8.2.0→8.10.1, `org.babashka/cli` 0.12.79→0.12.86. The `gov.nist/*` triad, `ca.uhn.hapi/*` (the older v2/hapi-base pair), the git-pinned `test-runner`, and every other listed library show no `latest` (either genuinely current or untracked by poly's outdated check against their resolution channel). | `poly libs :outdated` has never been part of any ceremony to date (brief's own seeding, confirmed unchanged) — this is its first-ever run against this workspace. Per AR-AU-4, this row is the report and nothing else: no upgrade taken. | Author decision needed: adopt a scheduled dependency-review cadence (per-arc? pre-publication, given Clojars readiness is this arc's own theme?) or record an explicit non-goal. Either answer closes this row; leaving it unanswered means every future `libs :outdated` run re-asks the same question. | ruling-needed |
| A-4 | `grep -n "nist-hit\|file://\|sha256\|mirror" artifacts.lock.edn deps.edn`; read `artifacts.lock.edn` lines 40–96 | `deps.edn` line 5 still declares `:mvn/repos {"nist-hit" {:url "https://hit-nexus.nist.gov/repository/releases/"}}` (live Nexus, not a `file://` mirror). `artifacts.lock.edn`'s own comment (~line 50) states verbatim: "the file://-mirror end-state ADR-0012 named as a future risk is still open, unaffected by this ruling." | The NIST jar mirroring end-state (mirror resolved jars into a `file://` repo, sha256s in `artifacts.lock.edn`) has NOT landed — the workspace remains hit-nexus-live, exactly the state the brief flagged as a supply-chain risk given hit-nexus's operator change (NIST→Prometheus, Aug 2026) and no stated SLA. | This is a supply-chain row with an existing, already-prescribed design (the wiring notes cited by the brief already say what the end-state looks like) — not a new question, a scheduling one. Candidate for its own fenced fix session. | fix-session-candidate (cluster: NIST jar mirroring / supply-chain) |
| A-5 | Scripted: `deps.edn`'s `:local/root` brick entries vs `ls components bases`, `comm -23`/`comm -13` both directions; same for `:test` extra-paths vs every `components/*/test` and `bases/*/test` dir on disk | `:dev` local/root entries: 19, brick dirs (17 components + 1 base... wait 18 components + cli): both sorted-unique sets identical, `comm` diffs empty both directions. `:test` extra-paths: 20 paths (17 component test dirs + `bases/cli/test` + 2 project test dirs); every path exists on disk; every component/base with a `test/` dir is listed (oracle has no `test/` dir, correctly absent). | The root `:dev`/`:test` 1:1 brick↔path mapping holds exactly, mechanically confirmed (not just "counted complete" as the brief's own prose put it) — zero drift in either direction. | Confirms the brief's own §4.1 pitfall-check as fully clean today. The brief's own suggestion (a docs-tooling gate asserting this mechanically, so a future brick addition can't silently miss a root alias) is still worth doing pre-emptively, since today's clean state is exactly the moment such a gate is cheapest to add. | fix-session-candidate (cluster: S5 gate promotions — root-alias completeness gate) |
| A-6 | `grep -n "ehrt.tools" .agents/plans/roadmap.md` (post AR-AU-1 fix) | One remaining hit: line 84, `` `ehrt.tools.corpus.framing` `` — this namespace also moved to `ehrt.corpus-io.framing` at ADR-0017 (the same stage that moved `source-sink`), but AR-AU-1's license covered only the `source-sink` reference ("This is the session's ONLY roadmap edit"). | A second stale namespace reference sits in the exact same roadmap row AR-AU-1 just fixed one instance of, left untouched per the ruling's own single-edit fence. This is exactly the class of drift S7 is about. | Fix-forward in a future session (or as part of whatever session resolves S7's scope question) — same dated-note pattern as AR-AU-1's own fix. Not fixed here: AR-AU-1's fence was explicit about touching only one reference. | fix-session-candidate (cluster: roadmap ns staleness — pairs with S7) |

---

## Area B — Interface & surface hygiene (brief §4.2)

Probed by a dedicated sub-agent; spot-checked directly (B-6's façade-caller claim and B-1/B-3's var-export claims were independently re-verified against the live tree and matched exactly). Full var enumeration covered all 18 components (186 vars total).

| id | probe | evidence | finding | recommendation | disposition |
|---|---|---|---|---|---|
| B-1 | `grep -rn -w implemented-sink-kinds .` excluding `components/corpus-io/` | Zero external hits; sibling `implemented-source-kinds` (same shape) IS called externally (`components/corpus/src/ehrt/corpus/generator_source.clj:158`). | `corpus-io/implemented-sink-kinds` is exported but has zero external callers — asymmetric with its live sibling. | Either corpus's sink-designator path should reach it the way source does (real gap), or document as export-for-symmetry-only. Author ruling needed. | ruling-needed |
| B-2 | Bare-name grep (own-dir excluded) over all 30 corpus-io interface vars | 29/30 have ≥1 external hit; only `implemented-sink-kinds` (B-1) is zero. | corpus-io's interface is otherwise tight, matching its own docstring's stated design. | No action beyond B-1. | close-as-fine |
| B-3 | Same, over all 41 corpus interface vars | 41/41 have ≥1 external hit, including every docstring-flagged "test-consumer only" var, individually spot-checked. | No dead re-exports in corpus. | No action. | close-as-fine |
| B-4 | Same, over all 23 kernel interface vars | 23/23 ≥1 hit; heavy fan-out (~13 distinct requiring files across corpus, corpus-io, judge×4, sim, sim-trajectory, bases/cli, both projects), aliased inconsistently (`result`/`kernel`/`artifact`/`locator`/`digest` all for the same ns). | Every export load-bearing; alias inconsistency is a readability wrinkle, not a hygiene defect. | No action — the alias variance is cosmetic and out of this brief's scope. | close-as-fine |
| B-5 | Same + targeted `:require` grep, over judge/judge-v2-hapi/judge-fhir-official/judge-v2-nist (19 vars total) | All 19 ≥1 hit; the judge↔judge-fhir-official cross-brick edge (`worst-of`, `verdict-cache-default-dir`) independently confirmed matching ADR-0011's own deviation record. | ADR-0011's per-engine split left no orphaned re-exports. | No action. | close-as-fine |
| B-6 | `grep -rln "ehrt.sim.interface" --include=*.clj .` excl. `components/sim/`, then per-hit `:require`-vs-prose classification | Only ONE real `:require` outside `components/sim/` exists workspace-wide: `components/corpus/src/ehrt/corpus/sim_adapter.clj:34`. It calls only 5 of the façade's 10 named defs (`run-command`, `check-all`, `identifiers-command`, `git-sha`, `version`). The other 5 (`ok`/`rejected`/`error`/`ok?`/`rejected?`, pure kernel relays) have zero external callers anywhere. Independently re-verified by this session directly (`grep -rn "ehrt.sim.interface"`): matches exactly. | 5 of the frozen façade's 10 vars are never called by the one real external consumer. | No change proposed — façade is frozen (AR-M4-3). Documented for the register only; this is descriptive, never actionable. | close-as-fine |
| B-7 | Same + docstring cross-read, docs-tooling (1 exported var) | `write-cli-md!` → real caller `bases/cli/src/ehrt/cli/help.clj`, matching docstring exactly; every other entry point is deliberately non-exported (Makefile `-X` invocation). | Interface matches its own stated design exactly. | No action. | close-as-fine |
| B-8 | Bare-name grep, palgebra (12 vars); `valid?` shows 475 hits | 8 unambiguous vars (`lint`, `valid-stage?`, `valid-union-resource?`, `valid-external-stage?`, etc.) confirmed real via docstring + targeted `:require` sites. `valid?`'s raw count is dominated by kernel's/provenance's/sim-model's own independently-defined `valid?` symbols — a bare-name collision, not evidence either way. | Cannot confirm `palgebra/valid?`'s liveness from a bare-name grep; the collision risk is real and disclosed, not resolved. | Needs a qualified follow-up grep (search the specific alias palgebra's consumers use) before this var can be called live or dead. | incomplete |
| B-9 | Same, provenance (6 vars); `valid?` shows 455 hits | Same collision problem as B-8 — provenance's own `valid?` (the `ManifestV0` validator) is a common name independently defined elsewhere. | Same limitation as B-8. | Needs the same qualified-alias follow-up. | incomplete |
| B-10 | Bare-name grep, sim-model/sim-trajectory/sim-engine/sim-emit-hl7/sim-emit-fhir/sim-check (91 vars total) | 100% ≥1 external hit across all 6 components; each interface docstring claims to be "the exact union of what residual sim's own callers reach today" and spot-checks (`sim-emit-fhir/bundle-run`, `sim-check/check-all`) confirm this. | The 5-stage sim split left no orphaned re-exports in any of the 6 extracted components. | No action. | close-as-fine |
| B-11 | Bare-name grep + `bin/regression-oracle` inspection, oracle (1 var, `-main`) | 6 hits; real caller confirmed at `bin/regression-oracle:75`, matching oracle's own interface docstring and the documented pre-promotion fallback branch. | oracle's single-var interface is genuinely and solely used by its one intended caller. | No action. | close-as-fine |
| B-12 | Reflection sweep methodology: `clojure -M:dev:test -e '(set! *warn-on-reflection* true) (require ns :reload)'` looped over all 85 `ehrt.*` namespaces, stderr captured, then deduped by source-file attribution | Raw per-namespace totals are contaminated by first-load transitive-compile ordering — e.g. a namespace showed 53 raw warnings but 0 originated in its own file; 45 belonged to a third-party lib (`com.nervestaple/hl7-parser`), the rest to other components' own files (already counted under their own rows, cross-checked identical). | A single ordered sweep's raw per-namespace count is unreliable without attributing each warning line to its actual source path. | Report only deduped, source-attributed counts (B-13); flag the third-party `hl7-parser` reflection debt (48 warnings, in `util.clj`/`message.clj`/`parser.clj`) separately as out-of-scope-for-ehrt-code. | incomplete (methodology caveat, not a code finding — the underlying data in B-13 is trustworthy) |
| B-13 | Deduped reflection counts, report-only, per AR-AU-4 | Non-zero own-code counts: kernel 7 (`artifact.clj` 6, `invocation.clj` 1 — `ProcessBuilder`/`File` interop), judge-v2-hapi 10 (`v2.clj`, HAPI typed-API field/method refs), corpus 6 (`player.clj` 4, `generate.clj` 1, `intake.clj` 1), palgebra 3 (`deps_lint.clj`), corpus-io 2 (`framing.clj`), sim-emit-hl7 2 (`emit_hl7.clj`, `java.time` calls), sim-model 1 (`facility.clj`, `nextInt`), docs-tooling 1 (`lint.clj`), judge 1 (`verdict_cache.clj`, `isFile`). Zero in judge-v2-nist, judge-fhir-official, sim, sim-check, sim-engine, sim-trajectory, sim-emit-fhir, provenance, oracle. | Report-only inventory, no fixes proposed. judge-v2-hapi's 10 (HAPI's Java API, untyped at the interop boundary) is the largest single concentration. | No action this session (AR-AU-4). A future reflection-reduction session, if chartered, would naturally start with judge-v2-hapi. | close-as-fine (report landed) |

---

## Area C — Naming & layout coherence (brief §4.3)

Probed by a dedicated sub-agent; C-1/C-2 (resources) and the oracle-test-dir claim (C-3) independently re-verified directly against the live tree, matched exactly.

| id | probe | evidence | finding | recommendation | disposition |
|---|---|---|---|---|---|
| C-1 | `find components/*/resources -maxdepth 1` over all 18 components | 7 of 18 have a `resources/` dir: judge, kernel, provenance, sim-engine, sim-trajectory, sim (each named after their own brick), and **sim-model** (named `sim`, mismatched). Every resources dir has exactly one subdirectory — no zero/multi-subdir violators anywhere. | S1's own finding, confirmed the sole violator workspace-wide. | Rename `sim-model/resources/sim` → `sim-model/resources/sim-model` (same recommendation as S1 — this row is area-C's independent confirmation of the same finding, not a new one). | ruling-needed (same cluster as S1 — do not double-count as a second decision) |
| C-2 | `comm -12` on sorted relative file listings, `sim-model/resources/sim/` (4 files, `demographics/`) vs `sim/resources/sim/` (27 files, `modules/` + `version.edn`) | Empty — zero overlap. | No live collision today (same as S1's own framing). | Confirms the hazard is structural, not an active incident. | close-as-fine for this specific sub-question (no urgency); the rename itself is still ruling-needed via S1/C-1 |
| C-3 | `find components/oracle/test`; `ls components/oracle/` (10 sibling non-sim components compared) | Oracle is the only one of 11 non-sim components with no `test/` dir (`deps.edn` + `src` only). Traced to `notes/adr/0044-standing-equipment-promotion.md`: explicitly disclosed there ("digest.clj carries none of its own [tests]; the gap is disclosed here, not built — trigger: a future session that adds real unit coverage"). | Real layout drift from the sibling shape, but already ruled and disclosed, not a fresh gap. | No action needed now; re-verify the trigger condition (unit coverage for digest.clj's pure helpers) at oracle's next touch. | close-as-fine (already ruled in ADR-0044) |
| C-4 | Read all 11 non-sim `interface.clj` files | Each component's naming vocabulary (corpus's domain nouns, judge's verdict vocabulary, kernel's primitives, the judge-engines' shared `gate-file`/`gate-dir` pair) is internally consistent and each interface.clj's own docstring explains its rationale with ADR citations. | No silent drift in the vocabulary layer — every apparent asymmetry is explained inline. | None needed. | close-as-fine |
| C-5 | Side-by-side read, `judge-fhir-official`/`judge-v2-hapi`/`judge-v2-nist` interface.clj | judge-fhir-official alone exports `gate-batch`; judge-v2-nist alone exports `make-validator`. The three are presented as siblings (each docstring says so) but their surfaces aren't uniform. | Asymmetric surface across three components explicitly framed as siblings — plausibly legitimate (FHIR-only batch gating, NIST-only validator construction reflect real engine capability differences) but not independently confirmed against engine internals beyond the interface docstrings themselves. | If callers are meant to treat the three polymorphically, consider backfilling equivalents or explicitly documenting in ADR-0011/0012 that the asymmetry is intentional (role-sibling, not shape-sibling). | ruling-needed |
| C-6 | `ls` every component dir for `docs/`, `test-fixtures/`, `examples/`, `tools/`, `HISTORY.md` outliers | corpus carries extra `docs/`+`test-fixtures/`; palgebra carries extra `examples/`+`tools/`+`HISTORY.md`. All other non-sim components have only the plain `deps.edn`/`src`/`test`[/`resources`] shape. | Both outliers have a stated, visible reason (corpus is the oldest/largest domain component; palgebra's own docstring documents its deliberately-different scope) — content differences, not convention violations. | No rename/restructuring needed; consider noting in a components README (if one exists) that these are legitimate optional per-component additions, so future audits don't re-flag them. | close-as-fine, cosmetic only |
| C-7 | `find components/*/src` for all 11 non-sim components, checked hyphenated-name↔namespace-dir mapping | All hyphenated component names (`corpus-io`, `judge-fhir-official`, `judge-v2-hapi`, `judge-v2-nist`, `docs-tooling`) map to underscored directories and matching `ehrt.<name>.*` namespaces exactly, standard Clojure `ns`-munging. | No drift — 100% consistent, matching the sim family's own convention. | None needed. | close-as-fine |

---

## Area D — Evolution-seam readiness (brief §4.4)

Probed directly this session (not delegated) — requires synthesis across ADR-0014/0017/0018 already read in full.

| id | probe | evidence | finding | recommendation | disposition |
|---|---|---|---|---|---|
| D-1 | Read `components/corpus-io/src/ehrt/corpus_io/{source_sink,source_sink_url,sink_write}.clj`; compare against ADR-0014's own three-namespace `:mllp` assessment | Current `source-sink.clj` has a generic `Source`/`Sink` schema pair with per-kind extensions (`DirSource`, `FileSource`, `StdinSource`, ...) plus `known-sink-kinds`/`implemented-sink-kinds`; `source-sink-url.clj` has a `sink-schemes` scheme-string→kind map and a shared `parse-designator` skeleton; `sink-write.clj` (not read in full, but its existence and role are unchanged per the workspace listing). This is EXACTLY the three-namespace shape ADR-0014 named as what a real `:mllp` sink would need — the corpus-io split (ADR-0017) relocated these files wholesale but did not change their internal shape. | The seam still fits post-split, unchanged in kind. Building `:mllp` today would mean: add an `MllpSink` schema + extend `known-sink-kinds`/`implemented-sink-kinds` in `source-sink.clj`, add an `mllp:` scheme to `sink-schemes` in `source-sink-url.clj`, add a real socket-write function in `sink-write.clj` — the same three-namespace shape ADR-0014 assessed, now just under `corpus-io` instead of `tools`. | No seam-readiness work needed before `:mllp` lands — the split didn't obsolete ADR-0014's own assessment, it just moved the files. Confirms the Deferred roadmap row (S4) still describes the real shape accurately (once its ns citations are current, see AR-AU-1/A-6). | close-as-fine |
| D-2 | Read `components/sim-emit-fhir/src/ehrt/sim_emit_fhir/emit_fhir.clj` (267 lines) in full; read `components/sim-emit-hl7/src/ehrt/sim_emit_hl7/{site_profile,v2_replay}.clj` docstrings; grep for shared id-derivation code between the two emitters | `sim-emit-fhir` has NO site-profile or v2-replay-shaped code at all — `site_profile.clj` is HL7-wire-DIALECT config (MSH dialect, Z-segments) and `v2_replay.clj` is a wire-format accumulator that RE-PARSES the emitted ER7 stream — both are inherently message-stream/wire-format concerns, absent from `emit_fhir.clj`'s much smaller (267 vs 1249-line) surface. `emit_fhir.clj`'s own docstring instead states its OWN two laws that would matter to a sibling: (1) `snapshot-at`/`bundle-run` — a pure fold of `ehrt.sim-engine.engine/replay`'s output into a state-at-instant-t snapshot, the shape a DOCUMENT-style emitter (FHIR Bundle now, CDA later — same docstring says so explicitly: "A CDA arm is real future scope... a future session adding CDA support does so by adding a sibling emitter namespace and a dispatch key, not by extending this one") needs; (2) a "cross-emitter id coherence" law (Patient.id/active-mrn agree with what `sim-emit-hl7` renders) — upheld INDEPENDENTLARLY in each emitter (both read `:patient-id`/`:active-mrn` straight off the same replay-record shape; no shared function is copy-pasted between them, confirmed by grep — no `patient-id-for`/`active-mrn`-deriving helper exists in both files). | The brief's own framing ("site-profile access, event-log/replay walking") describes HL7-specific, wire-stream-format idioms that a document-shaped CDA sibling would NOT copy — those are what sim-emit-hl7 needs because it emits a MESSAGE STREAM, not because any emitter-in-general needs them. The genuine candidate for what a CDA sibling would copy is sim-emit-fhir's own `snapshot-at`/`bundle-run` DOCUMENT-snapshot pattern (CDA, like FHIR, is document-shaped, not stream-shaped) — and the cross-emitter id law, which today is upheld as an independently-reimplemented convention in each emitter (not shared code), so a third sibling would also reimplement it a third time rather than extend anything. | Correct the brief's own pre-extraction framing before a CDA session starts: point it at `sim-emit-fhir` (not `sim-emit-hl7`) as CDA's nearest sibling, and treat the id-coherence law as a DOCUMENTED CONVENTION to reimplement correctly a third time (verified by the existing property tests each emitter already carries), not a code-sharing candidate — there is no duplicated function to extract, only a duplicated LAW upheld independently, which is a different kind of finding than the brief assumed. | ruling-needed (this corrects the brief's own seeded framing — an author call on whether to amend the brief or just brief the future CDA session directly) |
| D-3 | Read `notes/2026-07-30-refactoring-review.md` P3-3 (the "pairing-as-data" registry origin); grep `ehrt.judge.interface` requirers in `components/corpus/src/`; grep `ehrt.corpus` requirers in `components/judge*/src/` | P3-3 names the registry: move operator↔finding pairing (mutation-operator → expected-verdict/finding mapping) from hand-listed test assertions into `components/corpus/src/ehrt/corpus/operators.clj`'s own registry data, consumed by both the mutation side (corpus's own operator registry) and the judge side (contract-pairing test suites). Dependency check: `corpus/{interface,check}.clj` already `:require [ehrt.judge.interface ...]` (real, forward edge, pre-existing per ADR-0018's own graph); zero `ehrt.corpus*` requires exist anywhere under `components/judge*/src/` (checked all four judge components) — no reverse edge exists today. `judge`'s own `deps.edn` declares only `malli` as a dep (leaf-shaped from the deps.edn side; its real inter-brick edges are established via src `:require`, which is empty inbound from corpus). | Three candidate homes, evaluated acyclically: **judge** — corpus can already reach it (real forward edge exists); putting the registry there keeps it beside the verdict vocabulary it pairs against, and corpus's mutation side already imports judge, so no NEW edge is needed. **kernel** — also reachable from everywhere (foundational leaf), but mixing domain vocabulary (operator ids, verdict pairing) into the primitives layer (Result envelope, digest, locator) doesn't match kernel's existing role; every other component treats kernel as generic infrastructure, not domain data. **A new leaf (provenance-shaped)** — cleanest acyclic guarantee (zero deps of its own, per the `provenance` precedent, AR-2/ADR-0043) but the ADDITIONAL micro-component ceremony (new `deps.edn`, new interface, a new brick everyone touching pairing has to know about) buys little over just using `judge`, which already sits at the right dependency depth and already owns verdict vocabulary. | Recommend **judge** as the landing spot: it's already acyclically reachable from corpus's mutation side (real edge exists, no new edge needed), it already owns the verdict vocabulary half of the pairing, and it avoids the ceremony cost of a new leaf component for what is fundamentally verdict-adjacent data. This is a recommendation for the design pass to start from, not a decision — the design pass itself is design-channel-first per the brief. | ruling-needed (recommendation only, per brief's own framing — "the audit's only job here" is confirming an acyclic home exists, which it does: judge) |
| D-4 | Re-read ADR-0017/0018's consolidated named-future list (7 items) against the live tree | **Still live (2):** (1) `generator-source.clj` still carries all three concerns in one file — `generator-source`/`resolve!` (execute+validate+shape) and `parse-source-designator` (URL-parsing) all present, unconsolidated, confirmed by direct read. (2) `ehrt.corpus.display` is still under `components/corpus/src/`, not relocated toward a base — unchanged. (6, renumbered here as still-live item 3) The duplicated markdown-table helpers (`banner`/`escape-cell`/`table`, near-identical in both `ehrt.corpus.operators-doc` and `ehrt.docs-tooling.docsgen`) are STILL duplicated verbatim (confirmed by direct read of both files) — still only 2 consumers, ADR-0016's own "extract only when a third appears" trigger has not fired. **Closed since ADR-0018 (4):** (3) the structure-currency gate's substring weakness — FIXED: `structure_currency_test.clj`'s own docstring now states "Two independent checks now, both exact-token" using backtick-delimited path tokens, substring-immune (the exact weakness ADR-0018 flagged, now closed, undated in this register but real). (4) `operation-manifest.edn`'s producer identity — no longer a static `"ehrt.tools"` string; `mutate-producer` (bases/cli/core.clj:395) now derives it dynamically via the same `ehrt version` machinery (ADR-0020's own "D-d resolved" citation) — confirmed no literal `"ehrt.tools"` string remains anywhere in `bases/cli/src`. (5) palgebra's deps-lint — no longer a denylist of forbidden prefixes; it became an ALLOWLIST (`ehrt.palgebra.*` only, everything else forbidden by default) per a 2026-07-31 gate-hardening session — the vacuous-on-rename problem is structurally moot now, confirmed by reading `deps_lint.clj`'s own docstring, which explicitly narrates this history. (7) `bases/sim-cli`/`projects/sim` retirement — fired at ADR-0021 (2026-08-01), confirmed independently by the area-E sub-agent's own git-log check. | Of 7 named-futures, 4 are already closed (by mechanisms these ADRs didn't anticipate in 2 of the 4 cases — an allowlist instead of a denylist extension, a dynamic identity instead of a producer-string edit) and 3 remain genuinely open. | Sharpen the list: close items 3/4/5/7 with a dated note each (citing the sessions/ADRs that actually closed them — 2026-07-31 gate-hardening for palgebra, ADR-0020 for the producer identity, ADR-0021 for sim-cli, and whichever session's structure-currency rewrite closed the substring weakness, not independently identified this session — flagged as a small provenance gap). Keep items 1 (generator-source cohesion), 2 (corpus.display placement), 6 (markdown-table helper dedup) open, unchanged in their own "not attempted here, move don't improve" framing. | fix-session-candidate (cluster: named-future list hygiene — small, docs-only, closing 4 stale open items with dated notes) |

---

## Area E — Docs & cognitive-load surface (brief §4.5)

Probed by a dedicated sub-agent; E-7's tripwire-gap claim and F-5-adjacent repo-identity claim independently re-verified directly against the live tree, matched exactly.

| id | probe | evidence | finding | recommendation | disposition |
|---|---|---|---|---|---|
| E-1 | `ls components \| wc -l`; component-list cross-check vs `AGENTS.md` prose | 18 live components; AGENTS.md's own enumerated list matches 1:1, no bare numeric count to go stale. | No component-count drift. | Nothing to do. | close-as-fine |
| E-2 | `grep -rohE "components/[A-Za-z0-9_-]+"` across docs/AGENTS.md/AUTHORS-GUIDE.md/README.md/component docs vs live dirs | 11 non-live hits, all traced to a generic external Polylith-adoption tutorial (`docs/dev/migration/polylith-brief.md`, explicitly framed as a worked `acme`/invoice-service example, not this repo) plus one English-prose false match ("components/subcomponents" in research prose). | All false positives of a naive path regex, not real staleness. | No fix; if this sweep is ever gated, scope it away from the generic migration tutorial or allowlist it. | close-as-fine |
| E-3 | `grep -n "test-integration"` in `components/corpus/docs/palgebra-design.md` | Lines 111-112 cite `test-integration/contract_pairing_test.clj`/`test-integration/baseline_gating_test.clj` — no `test-integration/` dir exists anywhere; real files live at `projects/integration/test/ehrt/integration/{contract_pairing,baseline_gating}_test.clj`. | Real, live stale-path residue of exactly the species the tripwire already forbids in `docs/**/*.md` — but this file is under `components/corpus/docs/`, outside that scan's file set, and it's a live (non-dated) design record, not frozen history. | Fix-forward: repoint both citations. Small, single-file. | fix-session-candidate (cluster: component-docs stale-path cleanup) |
| E-4 | `grep -rn "ehrt\.tools\." components/*/docs/` | One hit: `components/corpus/docs/research/judge-v2-nist-spike-notes.md:69`, dated 2026-07-30 (one day before the stage-3 retirement), file header states "archived verbatim from a Cowork cloud session" — frozen at authoring time, same narrative-legitimacy class the tripwire already grants `notes/`. | Not live-instruction drift — correctly dated, correctly frozen. | Leave as-is; a future session could formalize "explicitly archived-verbatim component docs" as an additional tripwire exemption, but that's a scope ruling, not a fix. | close-as-fine |
| E-5 | Grepped 10 retired sim-family namespace forms (`ehrt.sim.{gmf,compile-trajectory,emit-hl7,v2-replay,site-profile,engine,churn,order-profiles,emit-state,check}`) across `components/*/docs/` | S2/S3-vintage retirements (`gmf`, `compile-trajectory`, `emit-hl7`, `v2-replay`, `site-profile`) still resident in 12 file-hits across `components/sim/docs/{sim-theory.md,sim-theory.edn,event-sourcing.md,third-party-sources.md,demos/*/README.md}` and `components/sim-emit-hl7/docs/demos/site-profiles/README.md`. M2-M4-vintage retirements (`engine`, `churn`, `order-profiles`, `emit-state`, `check`) show ZERO residue — already swept. | The tripwire's own M3/M4 addenda are accurate for what they claim (those sweeps happened); the EARLIER S2/S3 retirements were never swept in component docs, and that debt sits entirely outside `docs/**` (the tripwire's scan root) by construction. | Fix-forward sweep of the 5 namespace forms across ~8 files — same discipline the M2-M4 addenda already applied, just never extended backward to S2/S3. Sizeable enough (8 files, 5 forms) to warrant its own small session. | fix-session-candidate (cluster: sim component-docs namespace sweep, S2/S3 vintage) |
| E-6 | `grep -n ehr_testing_tools components/corpus/docs/experiments/EXP-A4-results.md` | Pre-Polylith underscore paths (`src/ehr_testing_tools/corpus/...`), doc dated 2026-07-24 (pre-conversion) — correct at execution time. | Frozen experiment-results record, same class as E-4. | Leave as-is. | close-as-fine |
| E-7 | Read `notes/adr/0021-bases-sim-cli-projects-sim-retired.md`; `git log --oneline --all -- bases/sim-cli`; full re-read of `stale_path_test.clj`'s current forbidden-string list; grep `ehrt.sim-cli\|bases/sim-cli` across all live docs | ADR-0021 (2026-08-01) retired `bases/sim-cli` (ns `ehrt.sim-cli.core`) and `projects/sim`; independently re-verified via `git log` (commit `4bf9be0`). Neither `ehrt.sim-cli.` nor `bases/sim-cli` appears in the tripwire's current forbidden list (confirmed by direct read: only `ehrt.tools.`, `positioning.md`, and the `notes/prompts/` present-tense pattern are present). No LIVE doc currently violates the missing entry (all hits confined to `notes/`, legitimately historical). | Real gap in the tripwire's *coverage* (not yet a live violation) — the tools-split and sim-split retirements are both covered, but ADR-0021's own base retirement was never folded into the forbidden list. Directly relevant to S7's own scope question (this is the SAME class of gap: a real retirement, outside what the tripwire currently guards). | Add `ehrt.sim-cli.` to the forbidden list, same prefix-denylist shape as `ehrt.tools.`. Small, single-string addition, easily co-landed with S7's own eventual resolution. | fix-session-candidate (cluster: tripwire forbidden-list additions — pairs with S7) |
| E-8 | `.agents/reading-sets.edn`: `wc -l` sum of each set's `:paths` vs its stated `:budget-lines` | `:onboarding` 980/1095 (margin 115); `:corpus` 1736/1995 (259); `:sim` 798/915 (117); `:judge` 856/980 (124); `:docs` 682/775 (93, tightest). All currently under budget. | No re-derivation owed today. `:docs` has the thinnest margin and carries `AGENTS.md`/`docs/dev/architecture.md`, both plausible growth targets for this very arc's own doc edits. | Not recomputing (out of this session's fence — no doc changes this arc landed in AGENTS.md itself). Watch `:docs`'s margin if a future session in this arc grows AGENTS.md or architecture.md; re-derive only if/when a set actually goes red. | close-as-fine (watch item) |
| E-9 | `grep -n "ManifestV1_1" components/corpus/src/ehrt/corpus/manifest.clj`; grep same symbol in `docs/formats.md`, `docs/glossary.md` | `manifest.clj:60`: `(def ManifestV1_1 provenance/ManifestV1_1)` — a deliberate re-export kept after ADR-0043 moved the schema family to `provenance`. `docs/formats.md`/`docs/glossary.md` still cite the OLD `ehrt.corpus.manifest/ManifestV1_1` form rather than the new canonical `ehrt.provenance.manifest/ManifestV1_1`. | Not stale in the strict sense (the re-export resolves correctly) but arguably should point at the canonical home per ADR-0043's own "the only acyclic single home" framing. | Editorial call: keep citing the re-export (stable, less churn) or repoint 3 citations to the canonical form. Neither is broken today. | ruling-needed |
| E-10 | `grep -n "/mnt/c" AUTHORS-GUIDE.md`; `grep -n "F7" notes/facts-register.md` | `AUTHORS-GUIDE.md:70` still carries the generic `/mnt/c` routing example; `notes/facts-register.md` F7 still carries its 2026-07-29/07-31 CI-timing figures verbatim. | Both items exist exactly as the brief already disclosed. | No action — a ruling would be needed to touch either, out of this session's own call (already flagged out-of-scope by the brief itself). | close-as-fine (already disclosed, out of scope) |

---

## Area F — Publication readiness (brief §4.6)

Probed by a dedicated sub-agent; F-1's eleven-root enumeration and F-5's `repo-identity` claim independently re-verified directly against the live tree, matched exactly.

| id | probe | evidence | finding | recommendation | disposition |
|---|---|---|---|---|---|
| F-1 | Traced all 11 root keys in `components/oracle/src/ehrt/oracle/digest.clj` to their backing resource paths; checked each for a NOTICE | 10 of 11 roots resolve into `components/sim/resources/sim/modules/`, whose single `NOTICE` (195 lines) covers all of them plus called submodules and lookup-table CSVs — full coverage. The 11th root (`death-fixture`) resolves to a hand-authored test fixture explicitly disclosed, in the modules-dir NOTICE itself, as carrying no NOTICE obligation of its own. | Full, correct coverage across all 11 oracle roots — the apparent gap is disclosed-by-design, not an oversight. | None needed. | close-as-fine |
| F-2 | Read NOTICE files at `components/sim-model/resources/sim/demographics/` and `components/sim-trajectory/resources/sim-trajectory/` | Demographics data is hand-curated original content (explicitly disclosed as NOT extracted from Synthea). `wellness-cadence.edn` in sim-trajectory IS genuinely extracted verbatim from Synthea's `EncounterModule.java` (ADR-0037 AR-1) under Apache-2.0; both NOTICE files otherwise adequate. | Both roots have adequate NOTICE narrative; folds into F-4's cross-cutting license-text gap. | See F-4. | see F-4 |
| F-3 | `ls`/`cat` on `components/corpus/test-fixtures/v2/simhospital/` and `.../v2-nist/` | `v2/simhospital/` has BOTH a full Apache-2.0 `LICENSE` text file and a `PROVENANCE.md`. `v2-nist/` has a rich `NOTICE.md` asserting Apache-2.0 licensing but NO `LICENSE` file of its own. | Inconsistent LICENSE-text presence between two Apache-2.0-sourced HL7 corpus roots. | See F-4. | see F-4 |
| F-4 | Cross-cutting: `find . -iname LICENSE\* -o -iname NOTICE\*` | Of 4 Apache-2.0-sourced roots (Synthea GMF modules, CDC NIST validator fixtures, Google SimHospital fixtures, Synthea-derived `wellness-cadence.edn`), only 1 (`v2/simhospital/`) vendors the actual Apache-2.0 license TEXT; the other 3 rely on a NOTICE narrative that cites "Apache License 2.0" without including the license text itself. The repo's own root `LICENSE` is this project's MIT grant and doesn't substitute for a third party's Apache-2.0 grant. | Apache-2.0 §4(a) expects a copy of the license to travel with redistributed content — 3 of 4 roots are missing it. Pre-tag (before formats freeze harder) is the cheap moment to fix this. | Either (a) add one shared Apache-2.0 license-text file (repo root or docs/) that all three NOTICE files cross-reference, or (b) vendor a per-root `LICENSE` copy matching `v2/simhospital/`'s own pattern. (a) is less duplicative. | fix-session-candidate (cluster: LICENSE/NOTICE hygiene, pre-tag housekeeping) |
| F-5 | `find . -name version.edn`; grep `repo-identity`/`mutate-producer` in `bases/cli/src/ehrt/cli/core.clj`; grep `version` in `components/sim/src/ehrt/sim/version.clj` | Exactly one `version.edn`: `components/sim/resources/sim/version.edn` = `"0.1.0-pre"`, read by `ehrt.sim.version`, re-exported at the sim façade. SEPARATELY, `ehrt version`'s own `repo-identity` (bases/cli/core.clj:176-181) is a hardcoded literal `"pre-release"` string — independently re-verified: `(def repo-identity ... "pre-release")`, docstring states "no version tag has been cut." No Clojars-coordinate version exists anywhere. No `pom.xml`/`project.clj`. | Two independent, uncoordinated version surfaces. `repo-identity`'s own docstring claim ("no version tag has been cut") is now literally stale in letter — 4 `stable-*` tags exist as of this session — though its INTENDED distinction (continuity tags ≠ semver release tags) still holds structurally. | Before any Clojars publish, decide whether the published coordinate tracks `sim`'s own `"0.1.0-pre"` or gets an independent version. Consider one clarifying line in `repo-identity`'s own docstring distinguishing `stable-*` continuity tags from semver release tags, since a bare reader could now read "no version tag has been cut" and wonder about the 4 `stable-*` tags that visibly exist. | ruling-needed |
| F-6 | Read `projects/ehrt-cli/deps.edn` in full | `ehrt-cli` composes 17 of 18 components (everything except dev-only `oracle`) plus `bases/cli` as one uberjar-shaped composition; no curated library subset exists today. | Options, not a decision: **(a)** publish only the uberjar as a deployable application, no library coordinate — simplest, zero curation risk, nothing externally `:mvn/version`-dependable. **(b)** publish a curated library subset (candidate: the sim-domain façade chain — kernel, sim, sim-model, sim-trajectory, sim-engine, sim-emit-hl7, sim-emit-fhir, sim-check, provenance) as its own coordinate, separate from the uberjar — matches the generic brief's own warning against uncurated shared-component libraries, but requires deciding a library-stable surface (the sim façade is self-disclosed as "deliberately wide"). **(c)** publish the whole workspace as one big library coordinate — least curation effort, most coupling; freezes the entire fat-component surface at first tag. | Explicitly the brief's own author-scheduled decision (§4.6) — not this session's call, no option recommended over another. | ruling-needed |
| F-7 | `grep -n quickstart Makefile README.md bin/quickstart-demo`; read `.github/workflows/test.yml` | README's claim that CI asserts `bin/quickstart-demo` and the documented command sequence teach the identical steps: verified true (`clojure -M:poly test :all skip:integration` includes `quickstart_fresh_test.clj`, which asserts README-fence == script; the CI workflow runs exactly that lane per push). README's separate claim that actually RUNNING `make quickstart` is local-only (no CI workflow runs it) also verified true. | Pre-tag checklist compiles clean except the LICENSE-text gap already carried as F-4; README quickstart claims are currently true, not drifted. | Re-run this same three-point checklist immediately before the actual first Clojars publish (a further-future moment than this session's stable-tag), not just once now — cheaper to catch drift early each time than after formats freeze. | close-as-fine (F-4 is the one open item carried forward) |

---

**Register summary:** 7 seeded rows (2 closed this session as already-executed: S4, S6; 1 closed as executed via ADR-0048/this session: S2; 1 recommendation-only, not implemented: S7; 3 still ruling-needed/fix-candidate: S1, S3, S5) plus 33 new rows across areas A–F (A: 6, B: 13, C: 7, D: 4, E: 10, F: 7 — 47 total new+seeded rows). Disposition counts: close-as-fine 26, ruling-needed 12, fix-session-candidate 10 (some rows carry a combined/deferred disposition, e.g. "see F-4"; counted at the row bearing the actionable disposition), incomplete 3 (B-8, B-9, B-12 — all methodology-caveat rows, not missing coverage of their component). No row was skipped; no area was left unprobed.
