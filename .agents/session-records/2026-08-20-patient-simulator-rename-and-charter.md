# Session record: the patient simulator is named and chartered (2026-08-20)

**Prompt:** [`.agents/prompts/2026-08-20-patient-simulator-rename-and-charter.md`](../prompts/2026-08-20-patient-simulator-rename-and-charter.md)
**ADR:** [`notes/adr/0162-patient-simulator-named-and-chartered.md`](../../notes/adr/0162-patient-simulator-named-and-chartered.md)
**Mode:** R30, autonomous. **Base:** `2d937b9`.

Three author rulings of 2026-08-20: the brick rename
(`sim-trajectory` -> `patient-simulator`), the scope charter, and the
`#careplan-guard-resolution` re-scope to a declared limitation.

## Step 0

`bin/preflight` **exit 0**, no findings. Last five runs on `main` all
green; edit root `/home/mg/src/ehr-testing-tools`, not under `/mnt/`;
`core.fileMode` **true**, `core.ignorecase` unset; tree clean including
untracked; HEAD `2d937b9` == `origin/main`; last stable tag
`stable-20260820-attic-rotation-law` @ `171ccb6`, HEAD untagged,
DISCLOSED, **no tag owed**.

Baseline `make test`, unpiped, wrapper ending `exit "$MAKE_EXIT"`:

    MAKE_EXIT=0
    366 zero-failure blocks / 4,084 tests / 18,336 assertions
    Execution time: 14 minutes 48 seconds

Reconciles exactly against
[`2026-08-20-attic-rotation-law.md`](2026-08-20-attic-rotation-law.md),
whose close carries the same `366 / 4,084 / 18,336`. `clojure -M:poly
check` **OK**.

Budgets at Step 0: `:corpus` 1836/2045, `:docs` 739/785, `:judge`
926/1000, `:onboarding` 1400/1530, `:sim` 1278/1405.

### Rename census, recorded before the first `git mv`

| population | count |
|---|---|
| tracked brick files (all move) | 42 |
| live consumer files carrying the name | 77 |
| ...of which rewritten | 75 |
| history files carrying the name (untouched, R-RP) | 162 |

Two live files deliberately not rewritten: `.agents/state-derived.md`
(generated) and `.agents/plans/README.md:39`, whose line describes the
archived `2026-08-02-sim-split-plan.md` by the names that plan used.

Five occurrences were split across a line break; the break MOVES WITH
THE NAME (`components/patient-` + newline + `simulator/...`), so no line
count or wrap column shifted.

### Marker census -- the charter table's population, the lint's baseline

    gmf.clj:1245  ;; PriorState/ConditionOnset resolution is not ported). :assign-to-
    gmf.clj:1247  ;; CarePlanEnd per source) stay UNDECLARED here -- the declared D2
    gmf.clj:1326  ;; DELIBERATELY UNDECLARED -- `vital-sign-expression?` (above) rejects

Exactly three, all in `gmf.clj`. Two of them share ONE comment block,
which is why the lint's coverage unit is the block, not the line.

### Premise mismatches (fix-forward, disclosed)

1. `projects/ehrt-cli/deps.edn` carries FOUR references (`:57`, `:99`,
   `:121`, `:139`) the prompt's deps census omitted entirely;
   `projects/integration` is at `:74`, not `:72`.
2. `.agents/reading-sets.edn` names NO path inside the brick, so no
   reading-set path edit was owed -- contrary to the prompt's Read-first
   item 4. Only a re-measure, and the counts did not move either (every
   doc edit was in-place word replacement: 5/5, 11/11, 10/10).
3. Not a mismatch but unanticipated: `resource_nesting_test` requires
   `components/*/resources` to nest under the brick's own name, so
   `resources/sim-trajectory/` had to move too, with its three
   `io/resource` calls.

## Step 1 -- the rename (`c44d240`, pushed)

118 files, 693 insertions, 661 deletions: 42 renamed (6 unmodified, 36
rewritten) + 75 consumers + `state-derived.md`. Matches the census
exactly.

`bin/regression-oracle` gained `sim_brick_dir_for`, resolving the brick
directory PER WORKTREE. Without it the bracket could not run at all: the
baseline side carries `components/sim-trajectory`, the target side
`components/patient-simulator`, and one hard-coded literal cannot serve
both. Same shape and retirement condition as `oracle_wiring_for`.

    $ bin/regression-oracle 2d937b9 HEAD
    == soundness check: digest.clj, whole file minus its leading docstring ==
    DIFFERS outside the leading docstring -- STOP: pass --declared-digest-change ...
    ORACLE_UNDECLARED_EXIT=1

    $ bin/regression-oracle 2d937b9 HEAD --declared-digest-change
    --- declared-digest-change: yes (soundness: no outside the leading docstring) ---
    IDENTICAL: every root's digest matches between 2d937b9 and HEAD
    ORACLE_DECLARED_EXIT=0
    (35 roots in the baseline manifest, 35 in the target manifest)

    $ make test
    MAKE_EXIT=0
    366 blocks / 4,084 tests / 18,336 assertions   <- ZERO delta

    $ make docsgen
    DOCSGEN_EXIT=0 ; demos/traces/** regenerated BYTE-IDENTICAL

Every renamed brick test namespace migrated with identical counts, name
for name (`gmf-interpreter-test` 204/536, `gmf-test` 84/256,
`compile-trajectory-test` 37/73, and eleven more). `poly check` OK.

## Step 2 -- red (`5a2ab20`)

`ehrt.docs-tooling.patient-simulator-charter-test`. Red before the
charter existed, **unfiltered**:

    FAIL in (every-in-source-limitation-marker-is-tabled-test)
    3 in-source limitation marker(s) with no covering citation in
    components/patient-simulator/docs/limitations.md -- table each, or the
    decline is undocumented: [gmf.clj:1245, gmf.clj:1247, gmf.clj:1326]
    FAIL in (the-charter-exists-and-carries-the-mission-sentence-test)   [x2]
    FAIL in (the-interface-docstring-carries-a-scope-section-test)       [x3]
    FAIL in (every-charter-citation-resolves-test)
    the charter table carries no `path` "snippet" citations at all

    Ran 7 tests containing 18 assertions.
    7 failures, 0 errors.

The 11 passes are the mechanism-sanity cases. A FIRST draft let `slurp`
throw `FileNotFoundException` out of two tests (4 failures / 2 errors);
that reports a gate's own subject being absent in a stack trace instead
of its message, so `charter-text` now returns `""` when the file is
missing. The red above is the corrected one.

## THE PLANT FAILED TO GO RED, AND THAT IS THE SESSION'S MAIN FINDING

Planting `;; :some-invented-field is DELIBERATELY UNDECLARED here.` into
the real `gmf.clj` left the lint **GREEN, 8 tests / 19 assertions, 0
failures.** The charter's VitalSign citation was the bare string
`"DELIBERATELY UNDECLARED"` and the planted line contained it, so the
new marker cited ITSELF covered.

Fixes: `every-charter-citation-anchors-exactly-one-place-test` (a
citation occurring twice in its own file is red), and the citation
sharpened. The new test immediately found a SECOND weak anchor already
in the table -- `"is deliberately NOT included"`, twice in `check.clj`
(`:434` `:medication-end`, `:448` `:care-plan-end`):

    FAIL in (every-charter-citation-anchors-exactly-one-place-test)
    1 charter citation(s) are not unique in their own file -- an anchor that
    matches twice anchors nothing:
    ["components/sim-check/.../check.clj :: \"is deliberately NOT included\" -- occurs 2 times"]

Re-planted after the fix, full unfiltered output:

    FAIL in (every-in-source-limitation-marker-is-tabled-test)
    1 in-source limitation marker(s) with no covering citation ...
    [{:file ".../gmf.clj", :line 1342,
      :text ";; :some-invented-field is DELIBERATELY UNDECLARED here."}]

    Ran 8 tests containing 19 assertions.
    1 failures, 0 errors.

**Plant withdrawn; `git diff` on `gmf.clj` is empty.** A red witness that
merely confirms what you expected has told you less than one that fails.

## Step 3 + 4 -- green (`b1c0965`, pushed with the red)

`limitations.md`: mission sentence, dependency-direction paragraph,
**8 rows**. The prompt named seven; "attributes" is two distinct
declines, and the prompt's "three-way ConditionEnd" is, in the tree,
`:reason`'s three-way resolution (`gmf.clj:1245`). `ConditionEnd` has a
real separate limitation and got its own row on its own evidence.

Roadmap: `#careplan-guard-resolution` PRIORITY 4 -> bottom of `## Next`
(PRIORITY 24), re-bodied as a declared limitation pointing at the
charter. **Slug KEPT** -- cited from eight history surfaces R-RP forbids
editing, and `every-cited-slug-resolves-test` would break on all of
them. **One row ADDED, disclosed:**
`#stale-path-retired-namespace-addendum` (PRIORITY 23), because every
prior namespace retirement joined `stale_path_test`'s denylist in its
own commit and this one could not (a new gate owes its own red, which
the fence disallowed). Registered, not built. No other row moved.

## CI red at `c44d240` -- run 32475479703

`hand-owned-asset-freshness-test`:
`docs/dev/simulator-architecture.md` moved, so
`docs/manual/assets/gt-emitters.svg` needed re-review. **Not stale:**
section 4's equation block is byte-identical across the diff; the one
section-4 line touched is prose naming which brick supplies `walk`; and
the asset's complete text (`GT`, `ground-truth log`, `ER7*`, `HL7v2
messages`, `FHIR*`, `FHIR bundles`, `emitH`, `emitF`) carries no brick
name. `:reviewed-at` -> `c44d240d`, prior ADR-0158 review kept beside
it. `two-clocks.svg` cites the same source but is already `:verdict
:stale` with a `:stale-row`, correctly exempted -- which is why exactly
one fired.

**Why local `make test` was green:** that gate reads GIT HISTORY. At
Step 1 the suite ran on an UNCOMMITTED tree, so `git log` returned the
old sha and the gate was blind by construction to the change it exists
to catch. Near-sibling of watch-list **W-1**, but distinct: W-1 is a
tier being skipped; this gate runs in the right tier and still cannot
see. Remedy adopted here: history-reading gates re-run AFTER the commit
(16 tests / 51 assertions, 0 failures) and before the push.

A second, ordinary staleness followed: `state_derived_test` went red
because Steps 2-4 moved derived counts. `make docsgen` regenerated it --
docs-tooling gates 53 -> 54, test namespaces 197 -> 198, roadmap rows 52
-> 53, `## Next` 20 -> 21, `:onboarding` 1400 -> 1406.

## Close

Full `make test` at the close:

    MAKE_EXIT=0
    368 blocks / 4,100 tests / 18,378 assertions

Delta vs Step 0 **+2 / +16 / +42**, reconciled exactly:

- **+2 / +16 / +38** -- the new charter namespace, which runs TWICE
  (development and `projects/conformance` both compose `docs-tooling`):
  8 x 2 tests, 19 x 2 assertions. Predicted before the run.
- **+4** -- `invocation-lint-test`, 269 -> 271 per pass. It scans every
  `.md` under `components/*/docs/**` and exactly two of its `deftest`s
  iterate that set, so one new doc is +2 per pass. NOT predicted; found
  by a per-namespace diff of the two logs when +42 did not match the
  predicted +38. It means the charter is inside the existing docs-gate
  population by construction, not only under its own test.

Reading sets re-measured at close: `:corpus` 1836/2045, `:docs`
739/785, `:judge` 926/1000, `:onboarding` 1406/1530, `:sim` 1278/1405.
All under budget; no ratchet touched.

`## Done` after this session's rows land: see the roadmap; rotation
per `rulings.md#R-done-attic-rotation` applied if the 30-line cap fired.

**`bin/post-push-verify`** after each push: remote tip == HEAD, per-commit
ASCII clean, CI reported once (AR-CI-4).

### CI, verified by this session via `gh` (`rulings.md#R-session-verifies-ci-via-gh`)

| run | commit | conclusion |
|---|---|---|
| 32475479703 | `c44d240` (rename) | **failure** -- `hand-owned-asset-freshness-test`, diagnosed and fixed above |
| 32477627622 | `b1c0965` (red + charter) | **success** |
| 32479503458 | `6ce2160` (close) | **success** |

The c44d240 red is disclosed, not silently passed: it is a real red on a
pushed commit, it was diagnosed within the session, and the fix rode the
next push rather than being deferred.

**Tag: PAID IN SESSION.** The prompt licenses a close tag paid in
session if the tip run concludes success while the session is open. Run
32479503458 @ `6ce2160` concluded **success** while this session was
still open, so the condition was met and deferring would have been the
deviation (`rulings.md#R-session-verifies-ci-via-gh`, and AGENTS.md's
tag law: deferring a licensed tag is now the deviation, not paying it).

    $ bin/tag-ceremony stable-20260821-patient-simulator-charter \
        6ce2160c5755b0e0ea65db2d60f747b99c01d511 <msg> --push
    OK: created annotated tag 'stable-20260821-patient-simulator-charter' at 6ce2160c...
    OK: pushed refs/tags/stable-20260821-patient-simulator-charter
    OK: remote peeled ref for 'stable-20260821-patient-simulator-charter'
        is 6ce2160c5755b0e0ea65db2d60f747b99c01d511, matches target exactly
