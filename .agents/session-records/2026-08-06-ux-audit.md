# 2026-08-06 — UX audit: every stranger-facing surface surveyed, nothing moved

## Scope

Session prompt naming AR-UA-0 through AR-UA-7. Prior: the tag law
landed and was design-channel-verified (`1d93e6c`,
`notes/adr/0057-tag-law.md`). This is the UX arc's audit session
(`.agents/plans/2026-08-06-ux-arc-brief.md` §6 step 1): survey every
user-facing surface — invocation docs, `help.clj`'s own spec data,
every exit-2 error path in `bases/cli`/`ehrt.sim.run`, and the
first-contact experience — and land a findings register, findings-only,
absolute (AR-UA-1). Executed Step 0's own standing-ceremony tag
(`stable-20260806-tag-law` at `1d93e6c`, the first tag this workspace
has run purely under AR-T-1's rule (ii), no specific per-tag license),
then probed all four areas (one delegated to a sub-agent for Area A's
full doc-tree sweep, the rest run directly with live CLI transcripts),
and landed `.agents/plans/2026-08-06-ux-audit-findings.md` — 21 rows
carrying a disposition plus a complete 100-string classified appendix
of every user-facing string in `help.clj`. Full account, rulings
verbatim, and every finding's reasoning: `notes/ADRs.md` ADR-0058.

## Red→green evidence highlights

No gates written or changed this session (findings-only, AR-UA-1) — a
docs-and-tag-only session's own proof is the suite staying green and
untouched throughout, not a red→green cycle. Full suite: 218 `Test
results:` lines / 0 failures / 0 errors at Step 0's own baseline
(`1d93e6c`), identical shape after the register landed (`41c1bdb`).
`clojure -M:poly check`: OK at both checkpoints. The deliberate
error-triggering commands this session ran (Area C: a missing
`--config` file, a malformed EDN config, a missing `--seed`, an unknown
flag, an unknown top-level command, a missing `--lockfile`) are live
CLI transcripts, not test-suite assertions — they are evidence for the
register's own rows, not gate changes; none wrote outside the session
scratchpad except one gitignored, immediately-removed `out/` directory
(Area D's quickstart probe).

## Judgment calls and their ratification status

- **Area A delegated to a sub-agent, not probed directly.** The
  brief's own scope (every command fence across `README.md`,
  `AUTHORS-GUIDE.md`, `docs/**/*.md`, and every `components/*/docs/**`
  tree — 106 candidate files) is large, mechanical grep-and-classify
  work well suited to delegation; the sub-agent's own findings were
  cross-checked against the CLI's real grammar (`docs/cli.md`, the
  generated ground truth) before landing in the register. Not
  separately ratified; disclosed here as a methodology choice, not
  assumed silently correct — the same disclosure discipline the
  alignment audit's own Area B/C/E/F sub-agent delegations used.
- **The Quickstart's network-dependent remainder was not run (D-4).**
  Running the full README Quickstart (artifact fetches, Synthea
  generation, mutate/gate/check) would have meant multi-minute
  network-dependent operations for a UX-signal question the first two,
  non-network commands (`bin/ehrt help`, bare `bin/ehrt corpus
  generate`) already answered clearly (help-screen noise vs.
  run-output terseness). Marked `incomplete` in the register per
  AR-UA-7's own honest-partial license, disclosed rather than silently
  skipped — not separately ratified, a direct reading of AR-UA-7's own
  budget clause.
- **U2/U3 given no independent disposition of their own** — U2 folds
  entirely into Area B's own rows (B-1..B-6) and the APPENDIX; U3
  resolves into C-1. Both point elsewhere in the register rather than
  duplicating a disposition, to avoid the register's own summary
  double-counting one finding under two headings (the same discipline
  the alignment register's own summary paragraph used for its "see
  F-4"-style rows). Disclosed in the register's own closing paragraph.
- **B-5 and D-1 (bare `bin/ehrt`'s exit code) counted once, not
  twice**, since they are the identical live finding surfaced by two
  different areas' own probes (a help-voice probe and a first-contact
  probe landing on the same transcript). Same for B-6/D-3. Disclosed
  in both rows and in the register's own summary arithmetic.

## Findings and HEAD landed

The register's own headline findings (full detail: ADR-0058, and the
register itself): U1 expanded from 8 to 11 stale-invocation file:line
groups plus one independent flag-pairing defect; C-1 (the brief's own
open `[audit]` item) resolved as worse than framed — a raw, uncaught
exception at the wrong exit code, the sole file-path-consuming code
path in the CLI that never entered the `Result` vocabulary; the
complete 100-string help-text appendix (24 ADR citations, 14 milestone
tokens, 11 over-long strings, confirmed live in the `bin/ehrt help`
transcript); a live exit-code inconsistency between bare `bin/ehrt`
and `bin/ehrt help` despite byte-identical output; a new, unseeded
finding (C-4) that unrecognized flags are silently accepted with no
diagnostic at all, assessed as the single most user-hostile gap
surveyed; and a positive finding (D-4) that the CLI's own run output,
once past the help screen, is already close to ideal — the UX arc's
real work concentrates in help text and error text, not general
command output.

Two commits this session: `41c1bdb` (Step 1, the register + index
entry) and this record's own closing commit (Step 2). One tag: `stable-
20260806-tag-law` at `1d93e6c` (Step 0, a ref, not a commit).
