# 2026-08-06 — Tag law: sessions tag, every surface says so, and the retired phrase can never return

## Scope

Session prompt naming AR-T-1 through AR-T-4. Prior: ux riders landed
and was design-channel-verified (`56a2c21`, `notes/adr/0056-ux-riders.md`),
having deferred its own two licensed tags citing the `build-session`
skill's rule #8 — correct behavior at the time, since the tag law
genuinely still read author-only on every surface that skill rule
pointed at. This session found and fixed the actual cause: the law
lived on five live surfaces (`AGENTS.md`, `AUTHORS-GUIDE.md`, the
`build-session` skill plus its `.claude/skills/` mirror,
`.agents/state.md`, and `.agents/rulings.md`'s own AR-R-2), and
ADR-0051's own 2026-08-05 reconciliation had amended exactly one. This
session restated the law once canonically (AR-T-1), swept all five
surfaces in one commit behind a red→green gate (AR-T-2/3), then
executed the three tags now licensed under the fixed law (AR-T-4). Full
account, rulings verbatim, the surface table, the red transcript, and
all three tag verifications: `notes/ADRs.md` ADR-0057.

## Red→green evidence highlights

`ehrt.docs-tooling.tag-law-test` (new): captured red against the
unswept tree first — 3 failures, exactly the three surfaces
(`AGENTS.md`, `.agents/skills/build-session/SKILL.md`,
`.claude/skills/build-session/SKILL.md`) that carried the literal
retired string `"stay author-only in every mode"` at that instant, out
of the six surfaces scanned. Green after the sweep, same run: 0
failures. Full suite: 216 `Test results:` lines / 0 failures at Step 0
baseline (`56a2c21`) → 218 / 0 failures after the sweep (+2, the new
namespace tested across two project classpaths — expected, not a
regression). `ehrt.docs-tooling.skill-mirror-currency-test` held green
throughout (216 assertions each run) — the canonical skill edit and its
mirror copy landed byte-identical.

## Judgment calls and their ratification status

- **The rulings.md edit shape** — AR-R-2's own sentence was left
  unedited in place (a pointer-note appended beside it) rather than
  rewritten, reading AR-T-2's "appended per fix-forward discipline, not
  edited in place" as applying to the whole entry, not just requiring a
  separate new section. Not separately ratified; a direct reading of
  the prompt's own instruction, disclosed here rather than assumed
  silently correct.
- **The propagation-lesson third-instance note landed inside the sweep
  commit**, not as its own follow-up append, since AR-T-2 already
  required a `.agents/rulings.md` edit in that same commit and splitting
  one file's edit across two commits would have violated staging
  hygiene for no benefit. Disclosed in ADR-0057 directly.
- **`.agents/state.md`'s tag-mechanic paragraph was corrected in place,
  not fully regenerated** — this file's own regeneration contract
  (AR-C-1) reserves whole-file regeneration for arc-close sessions; this
  was a targeted fix-forward correction of one stale claim (the tag
  count and the fixes-5/alignment-close disposition), the same
  discipline every non-arc-close session uses when it finds one claim
  wrong. Consistent with precedent, not separately ratified.

## Findings and HEAD landed

No findings outside this session's own named scope. Two commits this
session: `263e36b` (Step 1, the sweep + gate) and this record's own
closing commit (Step 3). The three tags (AR-T-4) are refs, not commits
— no commit associated with Step 2.
