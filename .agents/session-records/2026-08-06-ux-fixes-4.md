# 2026-08-06 — UX fixes 4: the help speaks to operators — the approved rewrite lands, gated

## Scope

Session prompt naming AR-U4-0 through AR-U4-5. Prior: ux fixes 3
landed and was design-channel-verified (`7665baa`,
`notes/adr/0061-ux-fixes-3.md`). The design channel's help-spec voice
rewrite draft
(`.agents/plans/2026-08-06-help-rewrite-draft.md`, register rows
B-1/B-2/B-3/B-4(a)) arrived author-approved in full. This session
lands the draft, applies its §3 replacement text to `cli-spec`
verbatim, co-lands the §5 voice gate, and regenerates `docs/cli.md`.
Full account, rulings verbatim, the token-relocation table, and the
red/before-after transcripts: `notes/ADRs.md` ADR-0062.

## A mid-session STOP-AND-REPORT

§3's own code fence for `exit-codes` showed the def with no docstring
— just the relocation comment where a real docstring (citing
ADR-0004/ADR-0010) currently lives. Applying that literally would
delete the docstring, but the same draft's §4 and the session's own
Fences both name "def-level docstrings" as out of scope — a genuine
contradiction inside the ruled artifact, not ambiguity resolvable by
guessing. Flagged via `AskUserQuestion` before touching `help.clj`;
ruled: keep the docstring byte-identical, add the comment beside it.
No other `def` in `cli-spec` carried this conflict.

## A disclosed count divergence (AR-U4-2's own escape hatch, not a stop)

The draft claimed "natural red today: 38 tokens," derived from a raw
`grep` over `help.clj` source lines 14–178. The actual §5 gate walks
the realized `cli-spec` DATA, not source text, and found **36** real
hits on the unrewritten tree — witnessed live (help.clj temporarily
reverted, gate run red, then the rewrite reapplied). Traced in full in
ADR-0062: the draft's raw-text count missed 2 real tokens sitting one
line past its own stated range (`play`'s `--sink` flag, source line
179) and 2 more from `gate-common-flags`' `--treat-no-verdict-as`
being reused via `into` across three verbs in the actual data (cited
once in source, walked three times), while over-counting 4 tokens
that live only in maintainer docstrings (never rendered) and 2 more
already sitting in a pre-existing relocation comment
(`;; D10 (ADR-0019)`, present before this session). Net: 38 − 6 + 2 +
2 = 36, matching the gate exactly. All 36 are addressed by §3; the
gate is green with 0 matches after the rewrite.

## Red→green evidence

`cli-spec-rendered-strings-carry-no-agent-speak-test`
(`bases/cli/test/ehrt/cli/help_voice_test.clj`), against the
unrewritten tree: `Ran 2 tests containing 164 assertions. 22 failures,
0 errors.` — 22 failing strings account for all 36 token hits (several
strings carry more than one token). After the rewrite: `0 failures, 0
errors`, same 164 assertions. The mechanism-sanity pairing test (each
token class caught standalone; `MSH-7`/`msg-%03d`/`PID-3`/plain dates
survive) was green both before and after, since it exercises the
pattern function directly, not `cli-spec`.

`clojure -M:poly check`: OK, both before and after. `clojure -M:poly
test :all skip:integration`: 511 tests, 0 failures, 0 errors, after
the rewrite. `bin/verify-nist-lock`: OK, 6/6 coordinates. `bases/cli`'s
own two test namespaces (`help-voice-test` + the pre-existing
`help-test` coverage suite): 11 tests, 229 assertions, 0 failures, 0
errors.

## Judgment calls and their ratification status

- **The exit-codes docstring exception is the one deviation from "§3
  verbatim," and it was author-ruled mid-session, not assumed.** See
  above — `AskUserQuestion` before any `help.clj` edit landed.
- **The 36-vs-38 divergence is disclosed, not silently absorbed or
  treated as a blocker.** AR-U4-2 explicitly names this path
  ("the delta is disclosed with each divergent token named") as an
  alternative to strict reconciliation — taken here, full trace in
  ADR-0062's token-relocation table.
- **`doctor`'s own D13 citation shares `version`'s adjacent comment,
  rather than getting a second dedicated one.** The draft places one
  `;; Honest pre-release identity ruling: D13.` comment immediately
  above `version` and doesn't repeat it above `doctor`, one group
  later — read as intentional given how scrupulous the draft is about
  comment placement everywhere else, not a gap.
- **`gate-common-flags`' single relocation comment clears all three
  reuse sites at once.** `--treat-no-verdict-as`'s `ADR-0010` citation
  lives once in source (the shared def) but renders three times (`gate
  v2`/`fhir`/`v2-nist`, each via `into` or direct reference) — one
  comment above the shared def covers all three, since the gate walks
  data, not source lines.
- **Oracle bracket ends at this session's own fix commit (`2f88d48`),
  not this record's own closing commit** — following
  ADR-0059/ADR-0060/ADR-0061's own precedent, since a docs-only
  ADR/record commit carries no digest risk to check.

## Findings and HEAD landed

`bases/cli/src/ehrt/cli/help.clj`'s `cli-spec` now speaks operator
language throughout: every rendered `:doc`/`:meaning`/`:positional-doc`/
`:default` string is free of `ADR-NNNN`/`D-number`/`SS-number`/`ruling
N` shorthand, with the reasoning relocated to an adjacent `;;` comment
(or, for `exit-codes`, kept in its own untouched docstring plus a
comment beside it). `docs/cli.md` regenerated in full (`make cli-doc`)
per its own wholly-generated convention. The new
`ehrt.cli.help-voice-test` gate holds the line going forward. The
oracle bracket (`7665baa` → `2f88d48`) confirms all eleven
vendored-root golden digests byte-identical — this session touched
only rendered help text, no emitted byte moved.

Two commits this session: `677eca6` (Step 1, the draft lands with its
index entry) and `2f88d48` (Step 2, red captured before the fix, the
rewrite + gate + `docs/cli.md` regeneration) — plus this record's own
closing commit (Step 3). One tag: `stable-20260806-ux-fixes-3` at
`7665baa` (Step 0, a ref, not a commit).
