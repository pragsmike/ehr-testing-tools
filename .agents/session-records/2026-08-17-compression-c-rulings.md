# 2026-08-17 -- compression arc session C: the standing rulings register, the build-session skill split, the reading-sets header

Decision record: [`notes/adr/0145-rulings-standing-register.md`](../../notes/adr/0145-rulings-standing-register.md).
Prompt: [`.agents/prompts/2026-08-17-compression-c-rulings.md`](../prompts/2026-08-17-compression-c-rulings.md).
Ceremony mode: **R30** (commit and push at each checkpoint, unattended), taken
from the prompt's own first line, not assumed.

## Step 0 -- preflight and baseline

`bin/preflight`, every line disclosed:

| check | result |
|---|---|
| last five CI runs on `main` | one **PENDING** (`e0cd0755`, this session's predecessor), four green. Reported, not awaited (AR-CI-4) |
| edit-root | OK -- `/home/mg/src/ehr-testing-tools`, not under `/mnt/` |
| tree-clean (untracked included) | OK |
| HEAD vs remote | OK -- `e0cd07553e057b7ba831c21495147873c07def66` |
| last `stable-*` tag / HEAD tagged? | `stable-20260816-adr-index-generated`; **HEAD not tagged** |

Baseline gates, unpiped with exit codes captured:

- `make test > <log> 2>&1; MAKE_EXIT=$?` -> **MAKE_EXIT=0**, **336 project runs
  / 3,802 deftest blocks / 17,278 assertions**, 0 failures, 0 errors.
  Reconciles **exactly** with ADR-0144's recorded 336 / 17,278.
- `clojure -M:poly check` -> **exit 0, OK**.

### The tag licence was NOT paid, and the run id is reported

The prompt licensed `stable-20260817-roadmap-row-contract` at `e0cd075` "ONLY
on relayed CI success for run **32023934757**; else STOP with the run id." **No
relay reached this session's prompt context, so the tag is not paid.**

Disclosed in full, because the session knows more than that and the knowing is
exactly what the licence forbids acting on: `bin/preflight` found the run
`in_progress`, and a later `gh run view 32023934757` in this same session
returned `conclusion: success`. `rulings.md#R-unrelayed-tag-condition-stops`
exists because ADR-0134's R0 hit this shape, stopped, and was ruled "Pay it,
message verbatim" **by the author**. Same course here. The distinction from
ADR-0143's conditional tag -- which the session *could* satisfy by re-checking,
because its condition was the run's state -- is that this condition is a relay,
and a session cannot relay to itself.

STOP taken on the tag item only; the rest of the charter proceeded.

### Owed items (1)-(4)

Only (4) changed the tree. `git ls-remote --tags origin` shows
`stable-20260815-review-3-fixes` present as an ANNOTATED tag peeling to
`b96c246430038b4d38aa60a391de5e376e61cd24`, which is what
`roadmap.md#review-3-tag-unpushed` said was owed. The row closed: six lines
moved verbatim to the attic (**read-back: 6 of 6 lines byte-identical against
`HEAD`**), one `## Done` pointer tokened with the sha. Landed as `11a2eff`.

(1) the `intake-staging-dir` trigger is **still owed** -- the prompt's slot was
blank. (2) the five mis-sectioned rows stay put, listed again in ADR-0145.
(3) PRIORITY: the ruling's own named source, the compression-arc row's text,
records only `A -> B -> C -> D`, so nothing moves -- ADR-0144 F-9 confirmed
rather than re-litigated.

### A charter premise corrected before it was relied on

The prompt says four sets sit above baseline by formula. Measured: **five**.
`:onboarding`'s formula is 1,670 against a 1,665 baseline, because ADR-0144's
own itemisation of that set sums to 1,393 against its stated 1,446 and records
`build-session/SKILL.md` at 258 lines when it was already 309. Raised as an
erratum in ADR-0145 rather than silently corrected.

Also disclosed: the prompt's context says 46 blocks and "hundreds of bullets".
The census found **55 blocks and 181 bullets**.

## Step 1 -- census (`57a27f5`)

Generated by the co-landed `bin/rulings-migrate-0145 --markdown`, from the same
tables the migration executes. 55 blocks plus the file's own header, 181
bullets, 186 classification entries: **91 STANDING, 89 ARC-LOCAL, 6
SUPERSEDED**, each with the sentence that decides it. Full tables in
`.agents/plans/2026-08-17-rulings-census.md`; six findings for the author in the
ADR, including F-3 (a question ADR-0119 asked 26 ADRs ago that nothing answered).

## Step 2 -- red (`c4b4ee6`, committed, push held per R-RP)

| assertion | red |
|---|---|
| every row matches the row contract | **170** of 170 top-level bullets |
| slugs unique | 0 -- vacuous |
| every cited ADR resolves | 0 -- vacuous |
| three lines a row | **156** of 170 rows |
| no `## From` block headings remain | **55** |
| a SUPERSEDED row names an existing successor | 0 -- vacuous |
| `reading-sets.edn` comment cap | **480** against 20 |

Three assertions are vacuous at red because their subject is the row and no row
exists yet. That is ADR-0144's F-11 class, anticipated rather than
rediscovered: the non-vacuity is carried by mechanism-sanity cases on synthetic
strings, all passing, and each one exists because of the `\b`-terminator bug
that session found.

**Runner finding, disclosed:** `clojure -M:poly test :project:docs-tooling`
aborted at `rulings-lint-test` and never reached `reading-set-budget-test`, so
the 480/20 red came from running that namespace alone on a minimal classpath.
Two runs, said so.

## Step 3 -- green

`bin/rulings-migrate-0145` (once, committed):

```
blocks moved     : 56          blocks found VERBATIM : 56
lines moved      : 1702        blocks missing        : 0
.agents/rulings.md : 1758 -> 238 lines (237 by wc)
```

`bin/verify-skill-history-0145`:

```
blocks checked : 19    blocks found VERBATIM : 19
blocks missing : 0     moved lines total     : 227
mirror byte-equal : yes
```

`diff -r .agents/skills .claude/skills` -> **no output**.

**No numstat identity is claimed.** ADR-0144 established that a diffstat cannot
express a move of this shape as an identity and recommended the read-back be
the instrument named in the fence; this session takes that recommendation.

### Reading sets, measured after every edit landed

| set | before | after | budget | baseline |
|---|---:|---:|---:|---:|
| `:onboarding` | 1,449 | **1,501** | 1,665 -> **1,665 (held)** | 1,665 (held) |
| `:corpus` | 1,961 | **1,774** | 2,245 -> **2,045** | -> **2,045** |
| `:sim` | 1,407 | **1,220** | 1,610 -> **1,405** | -> **1,405** |
| `:judge` | 1,055 | **868** | 1,205 -> **1,000** | -> **1,000** |
| `:docs` | 868 | **681** | 990 -> **785** | -> **785** |

**A fence trip, reported not absorbed.** The prompt's Step 3.4 says "every
budget and baseline re-set DOWN to the standing formula at the new actuals".
Four do. `:onboarding` does not: its actual RISES 1,449 -> 1,501, because the
same session that removed 192 lines of skill from the set added the 237-line
register to it by Q5, and the formula therefore points UP (1,730) where the
ratchet forbids up. Its budget holds at 1,665.

What the fences actually require is met -- no budget increased, every budget is
at or below its baseline, no set is over budget -- so the stated exit condition
holds and no STOP fires. What is not met is the method. The honest summary is
that the ratchet bit without falling: `:onboarding`'s headroom went from the
15% the formula grants to **11%**. Session D is chartered at what is left,
`.agents/session-records/README.md` (222) and `.agents/prompts/README.md` (170),
392 lines that grow by one every session and are the only `:onboarding` paths
this arc has not reached.

## Gates at close

- `make test` unpiped, `out/` cleared first -> **MAKE_EXIT=0**, **338 project
  runs / 3,830 deftest blocks / 17,354 assertions**, 0 failures, 0 errors.
  Reconciles against the Step-0 baseline of 336 / 3,802 / 17,278 exactly: +2
  project runs (the new `rulings-lint-test` namespace, counted once in
  `development` and once in `docs-tooling`), +28 blocks (12 new deftests in
  that namespace plus 2 in `reading-set-budget-test`, x2), +76 assertions
  (38 x 2).
- `make docsgen` -> exit 0, **no drift**: `git status` shows no change under
  `docs/` or to `notes/ADRs.md`.
- `clojure -M:poly check` -- exit 0, OK.
- `gitleaks git --staged -v` before every commit -- no leaks found.
- **No regression-oracle claim is made or owed**: the only files under `src`
  or `test` this session touched are
  `components/docs-tooling/test/ehrt/docs_tooling/rulings_lint_test.clj` (new)
  and `reading_set_budget_test.clj`. No `src` file anywhere changed.
- `bin/rulings-migrate-0145` was staged `100644` after `chmod +x` because
  `core.fileMode=false` hides the mismatch locally; fixed with `git
  update-index --chmod=+x` **before** the commit, which is ADR-0144's own
  `executable-bits-test` incident anticipated rather than repeated.

## Deviations, collected

1. **The licensed tag was not paid** -- the licence's relay condition was never
   met. Run id 32023934757 reported. `R-tag-law` calls deferral the deviation;
   the disclosed reason is that the condition itself did not hold.
2. **`:onboarding`'s budget did not fall**, against a prompt that said every
   budget would. Numbers above; D chartered.
3. **The red commit was held from the push** and went out with its green
   successor (R-RP), as instructed.
4. **Red evidence came from two runs**, not one, because the Polylith runner
   aborts a project at its first failing namespace.
5. **Two `make test` runs overlapped and their shared log was discarded.**
   A first authoritative run was started before the ADR-0144 erratum was
   appended; killing it and starting a second one against the same log file
   interleaved both, and the combined log reported 571 project runs / 30,410
   assertions -- roughly double, and meaningless. Recorded because the numbers
   were briefly on screen and could have been reported: the figures above come
   from a single clean run, over the final tree, to a fresh log, with no other
   test process alive (`pgrep` checked). The same class as the pipe-swallows-
   the-exit-code rule, arriving from a direction that rule does not name.
6. **The skill's history moved WHOLE, not sentence-by-sentence.** The prompt
   says "history sentences moved verbatim"; the replacement steps are authored,
   so the history has no clean line boundary to subtract, and each block moved
   entire instead. A step's imperative therefore appears in both files. Same
   call ADR-0144 made for roadmap rows, and made verifiable the same way.
