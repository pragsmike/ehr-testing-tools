# 2026-08-18 -- repo review 4: the assessment, hybrid shape, register and plan landed, nothing fixed

**Ceremony mode:** R30 (commit and push at each checkpoint,
unattended), taken from the prompt.
**ADR:** `notes/ADRs.md` ADR-0154 -- the reasoning of record; this file
is the ceremony log only (`rulings.md#R-session-narrative-hierarchy`).
**Row:** `roadmap.md#repo-review-4`, **STAYS OPEN** -- this session
lands the assessment, not the fixes.
**Skill:** `.agents/skills/repo-review/SKILL.md`, steps 1-5 only.

## Step 0

Fresh clone of the edit root, `git checkout 4d6ff78`. `bin/preflight`
(main), exit 0, every finding disclosed:

- last five CI runs on `main` all green -- `4d6ff78`
  (`2026-08-18T23:18:25Z`), `5563f71`, `c1a40d0`, `c509e46`, `1e261f5`
- edit root `/home/mg/src/ehr-testing-tools`, not under `/mnt/`
- working tree clean, untracked included
- local HEAD `4d6ff7836c4632010bedee47bf6663439e86a2f1` == `origin/main`
- last stable tag `stable-20260818-surge-policy-self-check-202` @
  `5563f71f`; **DISCLOSED: HEAD not tagged stable-*** -- no tag owed at
  Step 0, per the prompt
- **sequencing gate confirmed: no arc mid-flight** (ADR-0153 closed,
  its tag paid in session)

Baseline `make test`, redirect not pipe, full log, `MAKE_EXIT` captured
in its own file: **`MAKE_EXIT=0`**, **348** zero-failure blocks /
**3,960** tests / **17,758** assertions, `grep -cE '^(FAIL|ERROR) in'`
= **0**. Reconciles exactly against the figures the prompt cites for
ADR-0153. Wall **1,273 s**, disclosed as **not comparable** to
ADR-0149's 14m52s: three sub-agents shared the machine, one running two
full 35-root oracle digests.

`clojure -M:poly check` and `bin/verify-nist-lock` green as part of the
target. Reading sets, from the generated `state-derived.md`: `:corpus`
1801/2045, `:docs` 708/785, `:judge` 895/1000, `:onboarding` 1398/1530,
`:sim` 1247/1405.

**Oracle pre-digest**, this run's own artifact: deps block copied from
`bin/regression-oracle`'s own `run_one`, `-m ehrt.oracle.digest`, exit
0, **35 `.edn` files**, 35-line `sha256sum` manifest. Used to re-derive
L-1's headline claims rather than accept them.

**No Step-0 mutation of any kind.** Review 3 landed a rubric amendment
at its own Step 0; this session amends nothing.

## Sub-agents

Three dispatched in parallel with the coordinator's own battery, each
in its own fresh clone of `4d6ff78`, no probe cap, each returning rows
in the register's format plus the commands that produced them:

| line | subject | rows | wall |
|---|---|---:|---|
| L-1 | oracle coverage | 9 | ~22 min |
| L-2 | exit-code / harness truthfulness | 12 | ~16 min |
| L-3 | generated-surface completeness | 16 | ~37 min |

**Provenance discipline, per the prompt:** no sub-agent row entered the
register until the coordinator re-derived at least one cited artifact
per finding in its own clone. Final tally, mechanically extracted:
**20 fully re-derived**, **17 re-derived in part**, **0 recorded as
"sub-agent claim, coordinator could not reproduce"**.

## Probe budget

12 per dimension, 96 cap. **No dimension exhausted it:** D1 5, D2 6,
D3 4, D4 4, D5 4, D6 3, D7 5, D8 4 -- 35 of 96 used. Probes not run are
named per dimension in the plan's Part 4, not silently skipped.

## What landed

| file | what |
|---|---|
| `.agents/plans/2026-08-18-repo-review-findings.md` | the register, 72 rows, new |
| `.agents/plans/2026-08-18-repo-review-4-plan.md` | the plan, 8 sessions / 10 rulings, new |
| `.agents/prompts/2026-08-18-repo-review-4.md` | prompt archive, new (self-archived first) |
| `.agents/session-records/2026-08-18-repo-review-4.md` | this file, new |
| `.agents/plans/README.md` | two index entries for the two new plans |
| `.agents/plans/roadmap.md` | ONE line on `#repo-review-4`; row stays OPEN, now at its 6-line cap |
| `.agents/state-derived.md` | regenerated by `make state-derived` (not a fence breach, per the prompt) |
| `notes/adr/0154-repo-review-4.md` | the assessment ADR |
| `notes/ADRs.md` | regenerated by `make adr-index` |

**Nothing in `src/`, nothing in any test, no skill amendment, no rulings
amendment, no roadmap row closed, no finding fixed.**

## Gates run before the close

The doc gates this session's own edits touch were run mid-session rather
than left to the close suite, and **caught two real omissions**:
`index_completeness_test` (the two new plans were missing from
`.agents/plans/README.md`) and `prompt_record_pairing_test` (the prompt
archive had no paired session record yet). Both fixed before the close
suite -- which is the pairing gate doing exactly its job.

## Close

Full `make test` re-run at the close, redirect not pipe, `MAKE_EXIT`
captured: **`MAKE_EXIT=0`**, **348** zero-failure blocks / **3,960**
tests / **17,758** assertions, zero `FAIL`/`ERROR` lines.

**Delta vs Step 0: ZERO, in every figure** (348 / 3,960 / 17,758 at both
ends) -- as predicted, since this session adds no test and touches no
`src`. Per-namespace reconciliation is therefore trivially satisfied:
the two runs are the same suite.

**A timing measurement worth keeping, taken by accident.** Step 0 ran
under three-sub-agent contention at 1,273 s; the close ran on a quiet
machine at **944 s (15m44s)**, against ADR-0149's recorded **892 s**
(14m52s). About **+6% across five ADRs** that added test namespaces --
growth, not regression. This settles the suite half of the prompt's
timing probe. The `docsgen` per-push tier question ADR-0149 left open is
NOT settled: `make test` does not run `docsgen`, and the only figure
this review obtained is L-3's 2:48 under contention against ADR-0149's
119.40 s.

`bin/post-push-verify` output and the CI run id and conclusion are
recorded below, after the push.
