# 2026-08-07 — Repo review 1: the first assessment

## Scope

Session prompt naming AR-RR-0 through AR-RR-4, running the
`repo-review` skill's own steps 1–4 (history scan, probe battery,
dated register, first-assessment scoreboard) — NOT steps 5–7 (the
mitigation plan, ruled fix sessions, arc close), which follow only
after the author rules on the register this session lands. Full
narrative and every ruling: `notes/adr/0077-repo-review-1.md`.

This session's own preflight: working directory confirmed the ext4
clone; tip `89c0d24` exactly, matching the prompt's own stated HEAD —
no premise mismatch this time (unlike ADR-0076's own predecessor
session). `clojure -M:poly check`: OK. Full suite baseline: green,
261 namespaces, 511 assertions, 0 failures/0 errors. Last-five CI
conclusions on main disclosed: `89c0d24`/`9a34409`/`9cc3563`/`d0129b9`
all success, `74ebc6b` failure (the already-disclosed, already-fixed-
forward off-ceremony landing ADR-0076 recorded — re-surfaced here only
because it's still inside the five-run window, not a fresh finding).
Oracle pre-digest (`bin/regression-oracle 89c0d24 89c0d24`): all
twenty-seven roots IDENTICAL. `stable-20260807-quality-riders` tagged
at `89c0d24` and pushed (AR-RR-0).

## The probe battery — how it actually ran

Six of the eight rubric dimensions (D2 guard coverage, D3 environment
independence, D4 error honesty, D6 sampling adequacy, D7 continuity
integrity, D8 operator experience) were delegated to independent,
parallel, read-only sub-agents, each briefed with the dimension's own
named probes from the skill and the session prompt's seeded watch-
list, and each instructed under the same standing discipline every
audit arc has used: evidence by re-derivation, re-hash, or re-run,
never by re-reading a claim as its own verification. Dimension 1
(claim–reality coherence, mostly `ls`/`wc`/`grep` one-liners) and
dimension 5 (mirror/derivation drift, a single `make docsgen` +
`git diff` check) ran directly in the landing session — both single-
command-shaped, with no benefit to delegating.

D8's own sub-agent had live execution latitude (it built and ran the
real `bin/ehrt` against ~28 live documentation fence groups, plus a
full Quickstart run and `make integration`'s real FHIR-validator
subprocess) — scratch output was confined to a fresh `out/` directory
the whole time and fully deleted afterward, confirmed by `git status
--porcelain` empty both before and after that agent's own run. No
other sub-agent wrote to the tracked tree at all.

## Headline findings

- **D4-1, the register's highest-severity row:** a nil-vs-empty-array
  conflation in `.listFiles()` calls — the exact pattern ADR-0076
  fixed at ONE call site this arc — survives at 9+ other production
  call sites, one of which (`bases/cli/core.clj`'s `mutate-command`
  path) is DEMONSTRATED to produce a clean, successful, wrong
  `{:count 0}` result on a real I/O failure, with zero error surfaced.
  Two siblings of the same root cause surfaced independently in D3-4
  (`kernel/artifact.clj`'s unchecked `.renameTo`) and D8-2/D8-3
  (`corpus mutate`'s unwrapped file-read) — three different
  dimensions' probes converging on one fix-session-shaped defect.
- **D3-2:** ADR-0076's own newly-found `sim-engine` property-test
  flake (unpinned generator seed) generalizes to ALL 71 `defspec`
  forms in the repo — none pins a seed. Not a special case; the
  repo-wide norm.
- **D6-1:** the census's `:closure-file-count` JSON-only undercount
  (first disclosed ADR-0074) is confirmed STILL LIVE in the code,
  unfixed, now a three-times-repeated cost across the vendoring arc.
- **D7-4/D7-5:** pairing-as-data's "four closes old" carry (ADR-0050
  D-3 through ADR-0074) independently re-derived and confirmed EXACT.
- **D2:** of ~24 standing rulings in `.agents/rulings.md`, 6 have a
  confirmed enforcing test, ~10 are correctly gateless by nature
  (process/judgment disciplines, not testable invariants), and 4 have
  a real, plausibly-fixable gap — including the Deferred-section
  in-place-closure contract (AR-A-5), which has ALREADY caused one
  real incident (`myocardial_infarction.json`) with no gate to catch
  a repeat.
- **The overwhelmingly clean dimensions (D1, D5, D7, D8's own bulk):**
  every derived doc regenerated byte-identical (D5), every state.md/
  NOTICE/reading-set claim re-derived exact (D1), ~26 of ~28 live
  documentation fences ran exactly as taught including several
  matching the doc's own claimed numeric output precisely (D8).

## Judgment calls and their ratification status

- **Delegating six of eight dimensions to parallel sub-agents** rather
  than running every probe directly — a scope call by this session,
  not separately author-ruled. Justified by the register's own
  evidence discipline being explicitly stated to and followed by every
  sub-agent, and by this session independently re-deriving two
  dimensions itself (D1, D5) plus spot-checking sub-agent claims
  against directly-read source before compiling the register (e.g.
  the oracle-root count, the reading-sets.edn budgets).
- **Severity-over-volume scoring for D4** (one finding, RED) versus
  D2/D3/D6 (several findings each, YELLOW) — a channel judgment call
  within the rubric's own "green/yellow/red by finding count and
  severity" instruction, reasoned explicitly in the register's own
  summary and restated in ADR-0077. Not separately author-ruled;
  the author's own rulings on the register itself are the next act.
- **Naming the D3-4/D4-1/D8-2/D8-3 cross-dimension pattern** in the
  register's own summary, rather than treating each as an independent
  row — a synthesis this session made explicitly, flagged as a
  candidate for one combined fix session rather than four, for the
  author's own ruling to accept or reject.

## Verification

- `clojure -M:poly check`: OK, Step 0 and again at Step 3.
- Full suite: green at Step 0 baseline (261/511/0/0). Re-run at Step 3
  after landing ADR-0077's own index/README/roadmap edits — caught a
  real gap (below), fixed forward, green again after.
- `gitleaks git --staged -v`: clean, both commits this session; also
  clean on every push (pre-push hook).
- Post-push message verification, both commits: checked directly.
- `bin/regression-oracle 89c0d24 <this session's own closing tip>`:
  all twenty-seven roots IDENTICAL, soundness "yes outside ns form."
- Tag verification: `stable-20260807-quality-riders` peeled ref
  resolves to `89c0d24` exactly.
- CI, this session's own two pushes, watched directly (not assumed):
  `ac6ef5f` failure (run `31230302344`, the index-completeness gap
  above), `075db9b` success (run `31230768905`, watched to
  conclusion).

## Deviations, disclosed

No premise mismatch — the prompt's own stated tip (`89c0d24`) matched
the live tree exactly at session start, unlike ADR-0076's own
predecessor session. **Two real, disclosed gate catches, same root
cause, caught at two different points:**

1. Step 2's own commit (`ac6ef5f`, the register landing) omitted its
   own file's entry in `.agents/plans/README.md` — this session did
   not re-run the full suite locally before pushing that commit (the
   push gate itself only runs `gitleaks`+`poly check`, per this
   repo's own standing design, `AGENTS.md`/ADR-0003 — tests are CI's
   job, not the push gate's), so the gap surfaced only in CI, which
   went red for roughly ten minutes (run `31230302344`) until Step
   3's own commit fixed it forward as a side effect of fixing the
   next catch.
2. Step 3's own first draft (ADR-0077, this record, the prompt
   archive) made the identical mistake for its own three new files
   across all three index READMEs — this time caught LOCALLY, because
   this session did re-run the full suite before Step 3's own push
   (3 failures, `index-completeness-test`, exactly the three missing
   entries named). Fixed forward in the same step; full suite green
   again after.

Both are disclosed here as the gate doing its own job, not silently
corrected — and as a process lesson for this session's own remaining
steps and any future session: running the full suite before EVERY
push, not just the closing one, would have caught #1 locally too.
Every fence AR-RR-4 named otherwise held: no src, no test, no doc-
content fix, no gate change landed this session beyond the register
itself, this ADR and its indexes, the roadmap Done pointer, and this
record + the prompt archive.
