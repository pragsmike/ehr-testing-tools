# 2026-08-06 — UX riders: the arc opens — brief lands, tags licensed, the compaction pointers come home

## Scope

Session prompt naming AR-U0-1 through AR-U0-4, opening the UX arc.
Prior: the alignment arc closed and was design-channel-verified
(`12d3aa3`, `notes/adr/0055-alignment-arc-close.md`). Docs and tags
only — no `src/` touched at any point. Full account, rulings, the
Step 0 probe table, and both tag disclosures: `notes/ADRs.md`
ADR-0056.

Step 0 (preflight) confirmed the working directory is the ext4 clone
(`~/src/ehr-testing-tools`, not `/mnt/c`), tip `12d3aa3`, the brief
present at `.agents/plans/2026-08-06-ux-arc-brief.md` (114 lines,
non-empty). AR-U0-1's three probeable claims (no `:cli` alias, the
demo READMEs' stale invocation form, `bin/ehrt`'s cd-to-root
behavior) were re-probed fresh against the live tree — all three
HELD, no correction owed to the brief. Baseline: `clojure -M:poly
check` OK; full suite green (216 `Test results:` lines, 0
failures/errors); `gitleaks detect -v` clean (667 commits); oracle
pre-digest (`bin/regression-oracle 12d3aa3 12d3aa3`) all eleven roots
IDENTICAL.

**AR-U0-2's two tags — licensed, not executed.** Neither
`stable-20260805-alignment-fixes-5` nor `stable-20260805-alignment-close`
exists locally or on origin — both fully pending, the fixes-2
verify-and-disclose shortcut does not apply. This workspace's standing
ceremony law reserves tags to the author in every ceremony mode
(`AGENTS.md`; `AUTHORS-GUIDE.md` §1; ADR-0055's own AR-AC-0
precedent) — this session prepares the exact commands and licenses
them, does not run `git tag`/`git push` itself. Full commands in
ADR-0056.

Step 1 (`2991a70`, AR-U0-1/AR-U0-3) landed the brief with its
`.agents/plans/README.md` index entry, and rotated the three
scaffolding-compaction Done pointers (ADR-0045/0046/0047) from the
live roadmap's own Done section to `.agents/plans/roadmap-done-2026-08.md`,
under a new dated header placed immediately before the existing
alignment-arc attic section so the attic reads chronologically. One
commit — both are `.agents/plans/` hygiene. Staging reviewed before
commit (`git diff --cached --stat`): exactly the four files in scope;
the unrelated pre-existing untracked `config/busy-weekday.md` was
confirmed left out. `gitleaks git --staged -v` clean. Full suite
re-confirmed green post-edit, pre-push (216 `Test results:` lines, 0
failures/errors, ANSI-code-tolerant recount this time — the first
pass's `tail`-only capture had already shown the same shape but
didn't preserve the raw log for an exact grep). Pushed; post-push
verification: one delta against the message file, the known harmless
trailing-newline artifact.

Step 2 (this record) authored `notes/adr/0056-ux-riders.md` directly,
appended its own index line to `notes/ADRs.md`, corrected
`notes/adr/README.md`'s own stale file count (53→54, the same
staleness class every prior ADR landing has fixed forward), added the
Done pointer (`- 2026-08-06 — ux-riders — ADR-0056`) in the same
commit as the index line (so `ehrt.docs-tooling.done-pointer-adr-test`
never sees a dangling reference), ran the oracle bracket (below),
archived this prompt, and recorded this session.

## Deviations, disclosed

- **AR-U0-2's tags stayed licensed, not executed — the standing
  ceremony law, not a premise mismatch.** The session prompt's own
  Step 0 sequencing describes the tag acts as something this session
  performs ("Then AR-U0-2's two tag acts, verified"); this workspace's
  standing rule (tags are AUTHOR ACTION in every ceremony mode) takes
  precedence, exactly as it did for ADR-0055's own AR-AC-0. Both
  tags' exact commands are recorded in ADR-0056 for the author to run.
- **Step 0's oracle pre-digest and Step 1's post-edit full-suite
  rerun captured the raw log rather than trusting a `tail`-only
  capture.** The first full-suite run was piped through `tail -40`
  for a quick look; a second run captured the complete log so the
  "216 `Test results:` lines, 0 FAIL/ERROR/Exception" claim in
  ADR-0056 names an actual count, not an inference from the tail. Not
  a premise failure — belt-and-suspenders for a claim this session's
  own verification discipline treats as load-bearing.

## Findings (disclosed, not fixed — out of this session's own fence)

- AR-U0-1's probe of demo invocation surfaces found two `config.edn`
  files (`components/sim/docs/demos/order-result/config.edn`,
  `.../module-mix/config.edn`) that also carry the stale `clojure
  -M:cli` form in a header comment — outside the brief's own README
  scope, so not a correction to the brief, but worth folding into the
  audit session's own U1 row when it surveys every invocation
  surface.

## Verification

- `bin/regression-oracle 12d3aa3 2991a70` (baseline: this session's
  own pre-session tip; target: Step 1's own commit, the tip
  immediately before this record's own closing commit — no `src/`
  touched at any point this session): `IDENTICAL: every root's digest
  matches between 12d3aa3 and 2991a70` — all eleven vendored-root
  batches byte-identical, exactly as expected for a docs-and-tags-only
  session. No `--declared-digest-change` licensed or needed.
- Full suite (`clojure -M:poly test :all skip:integration`): green at
  Step 0 baseline (216/0/0) and again post-Step-1-edit before push
  (216/0/0, identical shape).
- `clojure -M:poly check`: OK, every step.
- `gitleaks git --staged -v`: clean, every commit this session;
  `gitleaks detect -v` (full history): clean at Step 0 baseline (667
  commits).
- Post-push message verification (Step 1): one delta against the
  message file, the known harmless trailing-newline artifact.
- AUTHOR ACTION items (both tags, AR-U0-2) named in ADR-0056 with
  exact commands, licensed, not executed by this session.

Commits, in order: `2991a70` (Step 1, brief + rotation) and this
session's own closing records commit (Step 2).
