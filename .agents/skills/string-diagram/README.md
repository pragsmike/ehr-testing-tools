# string-diagram

Converts resource equations (`A × B → C` style) into Mermaid string
diagrams, or extends an existing set of resource equations with both
the equations and the diagram. See [`SKILL.md`](SKILL.md) for the full
trigger conditions and workflow.
