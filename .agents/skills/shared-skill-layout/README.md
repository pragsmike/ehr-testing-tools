# shared-skill-layout

Diagnoses and standardizes where custom skills should live across
Windows-native and WSL agent environments — use when a skill is visible
on one side but not the other, or when deciding whether a skill belongs
in `.agents` or `.codex`. See [`SKILL.md`](SKILL.md) for the full
trigger conditions and workflow.
