# extraction-stage

Characterize → extract → verify → records: the split-stage discipline
behind the judge-engine extractions and the three-stage `tools` split,
proving zero behavior change byte-for-byte rather than asserting it.
See [`SKILL.md`](SKILL.md) for the full procedure.
