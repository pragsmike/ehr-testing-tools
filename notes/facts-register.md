# Facts Register — ehr-testing (workspace)

<!-- Tracks load-bearing, externally verifiable assertions made anywhere in
     this workspace (docs, deps.edn, memory files) about tools, licenses,
     and ecosystem capabilities -- evidence and a last-verified date, so a
     claim can be checked instead of trusted. See AUTHORS-GUIDE.md §4 for
     the assert -> register -> date discipline this file exists to
     support. Instantiated 2026-07-28 (discipline-parity session, R25);
     see notes/discipline-parity-audit.md row M5/M5a for why this file
     didn't exist before this session and why the Index sub-table below
     is tools' own addition, adopted on top of sim's base F-row shape. -->

Fresh sequence, starting at F1 — **not** a continuation of
`notes/sim/facts-register.md`'s or `notes/tools/facts-register.md`'s own
numbering (those are frozen provenance, moved intact, F-rows not
migrated forward automatically per each file's own header). A
workspace-level claim that repeats one of theirs needs its own fresh row
here, re-verified — unless explicitly carried forward with an origin
citation, as F1 below does.

## Index

Hand-maintained, not enforced by a lint or CI check (same choice as
`notes/ADRs.md`'s own lack of a table of contents check). Update this
table in the same commit as any new F-row — one line per row: an
extractive digest (a clause already present in the full row below, not a
new characterization), the last-verified date, and the status column
verbatim.

| # | Claim digest | Last verified | Status |
|---|---|---|---|
| F1 | Sim's manifest generator identity is `"ehrt.sim"` (not `"ehr-testing-sim"`) since ADR-0001's rename; a stale test asserting the old identity went unexecuted until ADR-0005 first ran the path for real | 2026-07-28 | verified (carried forward, origin `notes/ADRs.md` ADR-0005) |
| F2 | `bases/sim-cli` (and its composing `projects/sim`) are DEPRECATED as of 2026-07-29 (R33, ADR-0009): kept working, own tests keep running, but the user path never mentions them and `AGENTS.md` marks the dev path deprecated. Retirement trigger (dated, not scheduled): retire when a future review finds no use of either outside their own test suites | 2026-07-29 | verified (this session's own ruling; re-check the trigger condition, not this claim, at that future review) |
| F3 | `SETUP.md`'s on-ramp (fresh clone → generated + judged corpus) re-validated 2026-07-29 by a fresh-context agent walk: no prior repo knowledge, started from the rendered root README alone. Reached a generated corpus, gated it (`gate v2` 5/5 pass, `gate fhir` correctly rejected a mutant), and checked it (7/7 pass) in 15 commands, one recovery step included. Four friction points found and fixed in `SETUP.md`'s Troubleshooting section the same session | `SETUP.md` §4 | This session's own fresh-agent walk (transcript: this session's own record). Bar cited: `notes/tools/prompts/2026-07-24-onboarding-wave.md`'s Step 4 self-audit acceptance test — the walk's own verbatim success-report text is not preserved anywhere in git history (tools-era sessions predate this workspace's own session-record discipline, `notes/discipline-parity-audit.md` row M13); cited from provenance instead, per that prompt's own acceptance criteria and the commits it produced (`dd6ba2d`, `09eb094`, `f4e5c76`) | 2026-07-29 | verified (this session's own walk; the *old* report's exact text is a named gap, not silently assumed) |
| F4 | The CDC wrapper's last tagged release (1.4.0) builds and validates fully offline after one dependency fetch, **without** the SSL-verification-disabling Maven flags its own `README.md` documents — `hit-nexus.nist.gov`'s certificate validates under a stock JVM/Maven trust store today. 16/16 harness runs (both shipped sample profiles, all mutants) were byte-identical between an online run and a `unshare -r -n` network-isolated re-run | `docs/experiments/EXP-D3-results.md` Round 1 | This session's own build (`mvn package` against unmodified `pom.xml` at pinned commit `e059e58`) and offline re-run; supersedes the SSL-bypass framing in the pre-EXP-D3 research doc's own D3 evidence line (`hit-v2-nist-validator` §D3), which was accurate for the README's own build instructions but not for present-day `hit-nexus.nist.gov` TLS behavior | 2026-07-29 | verified |
| F5 | The six NIST-origin coordinates the CDC wrapper depends on (tools/F9) resolve cleanly from `hit-nexus.nist.gov`, cross-confirmed by two independent HTTP/TLS clients (Maven's own resolver and a plain `curl`, default verification, no `-k`) — both agree on every sha256 | `docs/experiments/EXP-D3-results.md` Jar Inventory table | Direct fetch + sha256 of all six coordinates via both mechanisms, this session | 2026-07-29 | verified |
| F6 | CDC's own `ProfileManager.filterAndConvert` keeps only 4 of the underlying NIST engine's 8 classification strings (`Error`/`Warning`/`Alert`/`Informational`) — `Affirmative` and `Validation Notes` are silently dropped from both `NistReport`'s entry lists and its summary counts (`High Alert`/`Specification Error` are dropped too, by source reading, but were not observed in this session's corpus). Against the wrapper's own shipped baseline message, this discarded 2,014 of 2,663 raw findings (75.6%) | `docs/experiments/EXP-D3-results.md` Round 3 | Source read of `gov.cdc.ProfileManager`/`NistReport` (wrapper 1.4.0) against `reference.conf` inside `hl7-v2-validation-1.7.3.jar`, plus a direct empirical count via reflection on `ProfileManager`'s own `validator` field, this session | 2026-07-29 | verified |
| F7 | CI's per-push lane (`.github/workflows/test.yml`, `poly test :all skip:integration`) genuinely executes the full non-integration suite: a fresh green run (id `30501195915`, sha `bdffdbc5`, 2026-07-29) logs exactly 177 `Testing ehrt.*` namespace announcements, matching this workspace's own local full-suite count; 0 failures/0 errors throughout | CI run `30501195915` (`gh run view --log`) | Direct log fetch and line count, this session, cross-checked against the local author-measured clone-location numbers this session's own SETUP.md entry cites (`SETUP.md` §4) | 2026-07-29 | verified |
| F8 | NIST's hosted IGAMT 2 registration page carries a disclaimer stating IGAMT is public-domain U.S. government software (17 USC §105), hosted on a NIST server visible only to NIST staff and the registrant, redistributable/modifiable with a derived-from/modified notice, with no NIST endorsement or reliability guarantee implied | NIST IGAMT 2 registration page (hosted) | Author observation, 2026-07-29, transcribed verbatim | 2026-07-29 | verified (transcription; scope notes qualify what it does and doesn't cover — see full row) |

## Register

| # | Claim | Where asserted | Evidence | Last verified | Status |
|---|---|---|---|---|---|
| F1 | `components/sim/src/ehrt/sim/manifest.clj`'s generator identity has reported `"ehrt.sim"` since ADR-0001's own mechanical rename (2026-07-28) — not `"ehr-testing-sim"`, sim's pre-rename self-identity. This is a worked example for why this register exists: `projects/conformance/test/ehrt/tools/sim_manifest_contract_test.clj` asserted the *old* string, but that test path had never actually executed end to end before ADR-0005's in-process `ehr sim` mount — always skipped, local and CI both, for lack of a sibling checkout at the moments it previously ran. The mount is what first let it run for real, and it caught its own staleness on the first real run. Fixed (test corrected to `"ehrt.sim"`, not left red — AUTHORS-GUIDE.md's two-failure-modes discipline: the check misencoded its own invariant, so the check was the thing to fix, not reality). | `components/sim/src/ehrt/sim/manifest.clj:77`, `projects/conformance/test/ehrt/tools/sim_manifest_contract_test.clj` | `notes/ADRs.md` ADR-0005's own deviation record ("A genuine, previously-latent finding, surfaced by the mount, not caused by it") | 2026-07-28 | verified (carried forward from ADR-0005 at register instantiation, discipline-parity session) |
| F2 | `bases/sim-cli`/`projects/sim` deprecation (R33, ADR-0009, 2026-07-29): the CLI rename session's own author ruling marked sim's standalone CLI deprecated rather than removed, on the reasoning that `bin/ehrt sim run` (the in-process mount, ADR-0005) is now the presented surface for every user-facing doc, while `bases/sim-cli` stays buildable and tested for whoever still depends on the standalone artifact directly. Retirement trigger, named here so it isn't lost to prose: **retire when a review finds no use outside its own tests.** Not scheduled — this row is the pointer a future review checks, not a countdown. | `AGENTS.md` ("Landed so far"), `notes/ADRs.md` ADR-0009 | This session's own ruling (`notes/prompts/2026-07-29-ehr-testing-development-resumption.md`, R33) | 2026-07-29 | verified (a ruling, not an external fact — "verified" means the ruling is accurately transcribed here, not that the trigger has fired) |
| F3 | **The SETUP on-ramp claim: a fresh clone with no prior knowledge of this repo, starting only from the rendered root `README.md`, can reach a generated and judged corpus.** Re-validated 2026-07-29 (pre-takeover storefront-polish session, R40/R41): a fresh-context agent (no repo knowledge, no session memory, handed only the rendered `README.md` text and the instruction "get to a generated, judged corpus on this machine") worked the Quickstart end to end — `corpus generate` (real Synthea run), `corpus mutate`, `gate v2` (5/5 pass), `gate fhir` (correctly rejected the mutant, exit 1), `check` (7/7 pass) — in 15 commands including one self-recovered failure (a stale `target/corpus/synthea-s1-p5` directory from prior dev-machine activity, not a fresh-clone condition). R40's bar: a prior documented success report from a domain expert (a Python developer), against `ehr-testing-tools` pre-merge. That report's own verbatim text could not be located in git history — this workspace's `.agents/session-records/` discipline (`notes/discipline-parity-audit.md` M13) is sim-only in origin; tools-era sessions (including `notes/tools/prompts/2026-07-24-onboarding-wave.md`, the session that first built `SETUP.md` for exactly this trial audience) never committed a session report, only the prompt and the resulting commits. Cited from that provenance instead of the report itself, per AGENTS.md's "record it and ask" discipline (autonomous session: recorded, not blocked on). That prompt's own Step 4 acceptance test (five questions, each answerable with a file+section citation from a fresh clone) is the closest live equivalent of R40's bar, and this session's own walk clears a stricter version of it (real command execution, not a citation exercise). Four friction points the walk surfaced were fixed the same session: `corpus generate`'s `:out-dir-exists` rejection was undocumented; a harmless `run!` namespace-shadow warning leaks onto stdout; `gate ... --report` still dumps the full result to stdout alongside writing the file; `poly test :all`'s multi-minute runtime was unstated. All four are now `SETUP.md` §4 Troubleshooting entries. | `SETUP.md` §4, `README.md`'s Quickstart (the `corpus generate` rejection-behavior comment) | This session's own fresh-context agent walk (full transcript in this session's own record); `notes/tools/prompts/2026-07-24-onboarding-wave.md` Step 4 (bar cited from provenance); commits `dd6ba2d`/`09eb094`/`f4e5c76` (what that prompt actually produced) | 2026-07-29 | verified (this session's own walk is directly verified; the *prior* domain-expert report is a named provenance gap, not assumed to exist) |
| F4 | **EXP-D3 (2026-07-29): the CDC wrapper's last tagged release, `1.4.0` (commit `e059e58ac5592baff57f04ee744398357d3258f3`), builds and validates fully offline after one initial dependency fetch — and does so *without* the SSL-verification-disabling Maven flags (`-Dmaven.wagon.http.ssl.insecure=true` et al.) its own `README.md` at that commit still documents.** `mvn dependency:resolve`/`mvn package` against the unmodified `pom.xml` (which declares a live `hit-nexus` repository) succeeded outright under Maven's default, verified TLS trust — `hit-nexus.nist.gov`'s certificate validates today under a stock JVM/Maven trust store. 16/16 harness runs (both of the wrapper's shipped sample profiles, all mutants) were byte-identical between an online run and a re-run inside `unshare -r -n` (network genuinely severed — a direct `curl` to `hit-nexus.nist.gov` from inside the isolated namespace returned no response, exit code `000`). This refines, rather than contradicts, tools/F9: tools/F9's "not a live network Nexus" finding was accurate for `main` post-2026-04-03 (which vendors the six jars into a `file://` local repo, added *after* the 1.4.0 tag); the 1.4.0 release itself, predating that vendoring commit, still resolves live against `hit-nexus.nist.gov` — and does so cleanly, with no SSL bypass needed. | `docs/experiments/EXP-D3.md`, `docs/experiments/EXP-D3-results.md` Round 1 | This session's own build, at the pinned tag, both online and network-isolated | 2026-07-29 | verified |
| F5 | **EXP-D3: all six NIST-origin coordinates the CDC wrapper depends on (tools/F9's own list) resolve cleanly from `hit-nexus.nist.gov`, cross-confirmed by two independent HTTP/TLS clients** — Maven's own Aether resolver (isolated `-Dmaven.repo.local`) and a plain `curl` (default TLS verification, no `-k`) — agreeing on every sha256. Full coordinate/URL/sha256 table in the results file. This re-verifies tools/F14 (`hit-nexus.nist.gov` liveness) against the exact six coordinates, not just the repository root, and supplies the jar inventory a future `artifacts.lock.edn` entry set needs. | `docs/experiments/EXP-D3-results.md` Jar Inventory table | Direct fetch + sha256 of all six coordinates via both mechanisms, this session, 2026-07-29 | 2026-07-29 | verified |
| F6 | **EXP-D3: CDC's own `ProfileManager.filterAndConvert` (wrapper 1.4.0) keeps only 4 of the underlying NIST engine's 8 classification strings** (`Error`/`Warning`/`Alert`/`Informational` kept; `Affirmative`/`Validation Notes` confirmed dropped by direct execution; `High Alert`/`Specification Error` dropped by source reading, not observed in this session's corpus) — silently, from both `NistReport`'s entry lists and its summary counts. Against the wrapper's own shipped `TEST_PROF` baseline message, this discarded 2,014 of 2,663 raw findings (75.6%), all of them `Affirmative` or `Validation Notes`. Consequence: a consumer reading only `ProfileManager.validate()`'s public output never sees the specific classification (`Affirmative`) the engine uses to say "this optional/RE field or group is empty, and that's fine" — which is the entire reason two of `judge-v2`'s three named-customer mutations (`docs/judge-calibration.md` CAL-1) don't convict against the two profiles this session tested (Round 5) | `docs/experiments/EXP-D3-results.md` Round 3 | Source read of `gov.cdc.ProfileManager`/`NistReport` against `reference.conf` inside `hl7-v2-validation-1.7.3.jar`, cross-checked by reflection on `ProfileManager`'s private `validator` field to obtain the raw, unfiltered `Report` for the same input, this session | 2026-07-29 | verified |
| F7 | **CI reference-timing verification (2026-07-29, WSL clone-location session).** CI's per-push lane genuinely runs the full non-integration suite, not a subset: fetched the log of the most recent green push run (`30501195915`, sha `bdffdbc5`, "Add ER7 encoding reference.", `conclusion: success`) and counted 177 `Testing ehrt.*` namespace announcements — the exact count this workspace's own extraction session already used as its local full-suite reference. `0 failures, 0 errors` throughout; the `poly test :all skip:integration` step itself ran in 1m46s (23:58:39Z→00:00:25Z UTC), the full job (checkout + toolchain setup + `poly check` + tests) in 2m11s (23:58:16Z→00:00:27Z UTC). This is the CI-side number `SETUP.md` §4's WSL clone-location entry cites; local reference numbers for the same full suite, author-measured the same day on this session's own machine (no hostname/path beyond the generic `/mnt/c/...` form): **~20 min** run against a `9p`/`drvfs`-mounted repo (`/mnt/c/...`), **3:15** against a WSL ext4 clone, **~2.5 min** on CI's own Ubuntu runner (this row), and **8 min** for `make integration` on the ext4 clone. | `SETUP.md` §4 (Troubleshooting) | `gh run view 30501195915 --log`, this session, 2026-07-29; local numbers are this session's own `time` measurements, not independently re-verified here | 2026-07-29 | verified |
| F8 | **IGAMT registration disclaimer, captured verbatim (2026-07-29, author-observed while registering on NIST's hosted IGAMT 2):** "Application for the use of the IGAMT does not guarantee that the applicant will be granted approval for use by the National Institute of Standards and Technology (NIST). The IGAMT software is hosted on a NIST server, and your information will not be visible to users other than NIST staff and yourselves. Information provided in the IGAMT does not imply NIST endorsement of any particular product, service, organization, company, information provider, or content. This software was developed at the NIST by employees of the Federal Government in the course of their official duties. Pursuant to title 17 Section 105 of the United States Code, this software is not subject to copyright protection and is in the public domain. NIST assumes no responsibility whatsoever for its use by other parties, and makes no guarantees, expressed or implied, about its quality, reliability, or any other characteristic. We would appreciate acknowledgment if the software is used. The outcome of the use of IGAMT can be redistributed and/or modified freely provided that any derivative works bear some notice that they are derived from it, and any modified versions bear some notice that they have been modified." **Three scope notes, load-bearing for the pending NIST licensing inquiry and future profile-artifact work:** **(i)** the §105 public-domain statement is conclusive for IGAMT itself, but only *supportive* — not conclusive — for the six NIST HL7v2-validator jar coordinates (`artifacts.lock.edn`, `:license-status :use-permitted--unstated--confirmation-pending`); those are a separate codebase from IGAMT, and the pending inquiry (`tools/NIST-reply`, `notes/ADRs.md`) stays open regardless of this disclaimer. **(ii)** the redistribute/modify clause means an IGAMT-exported profile bundle (`PROFILE.xml` + siblings) is redistributable *with* a derived-from notice, and a modified notice if edited — this is the license verification a future vendored `:profile` artifact in `artifacts.lock.edn` will need, and that notice obligation must be carried into that artifact's own `:license-note` when it lands, not re-derived from scratch. **(iii)** profiles authored on NIST's *hosted* IGAMT are visible to NIST staff per the disclaimer's own second sentence — anyone who needs to treat a profile as proprietary during authoring should use self-hosted IGAMT (`usnistgov/hl7-igamt`) instead. | NIST IGAMT 2 registration page (hosted instance), observed by the author 2026-07-29 during registration | Direct author observation and verbatim transcription, 2026-07-29; cross-referenced against `notes/ADRs.md`'s existing `tools/NIST-reply` open-hole entry and `artifacts.lock.edn`'s six NIST-origin entries | 2026-07-29 | verified (transcription verified against the source page at observation time; the page's own content is not independently re-checked here and may change) |
