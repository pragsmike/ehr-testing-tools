# Facts Register

<!-- Tracks load-bearing, externally verifiable assertions made anywhere in
     this repo (docs, README, experiment backlog) about tools, licenses,
     and ecosystem capabilities — evidence and a last-verified date, so a
     claim can be checked instead of trusted. This is the F-table half of
     ehr-testing-guide's notes/claims-register.md (see that repo's
     ADR-0013); there is no C-table here because there is no manuscript
     tracking drafting coverage. See notes/ADRs.md ADR-0002 and
     AUTHORS-GUIDE.md section 4 for the assert -> register -> date
     discipline this file exists to support. -->

| # | Claim | Where asserted | Evidence | Last verified | Status |
|---|---|---|---|---|---|
| F1 | NIST v2-validation (usnistgov/v2-validation) ships no license artifact anywhere in the repo: no LICENSE/COPYING/NOTICE at root or in any of its 3 sbt modules, no license block in `build.sbt` or bundled dependency POMs, no license text in a systematic 10-file source-header sample, no README statement, GitHub's own detector 404s. Classified `license-unstated`. NIST's general open-source policy (nist.gov/open/license) is suggestive-not-sufficient — it doesn't name this repo, though a tool-portfolio page (`nist.gov/itl/.../hl7-v2-conformance-testing-tools`) states NIST offers the HL7 v2 testing-tool *category* — which includes this repo — as "public domain resources," without naming it verbatim. Public-domain status remains plausible but unverified. | `docs/positioning.md` (go-public gate), `docs/experiments/EXP-SBOM-results.md`, `docs/research/License Status of NIST HL7 v2 Validation Software  Evidence-Based Classification.md` | https://github.com/usnistgov/v2-validation (229-entry recursive tree, `truncated:false`); https://api.github.com/repos/usnistgov/v2-validation/license (404); https://www.nist.gov/itl/health-it-testing-infrastructure/testing-tools/hl7-v2-conformance-testing-tools | 2026-07-24 | inventory (EXP-SBOM) + deep research executed — still license-unstated at repo level; official public distribution channel identified (hit-nexus.nist.gov, [F14](../notes/facts-register.md)), enabling a fetch-at-build (Mode 2) adoption path whose use-rights basis is NIST's general software statement ([F15](../notes/facts-register.md)); residual narrowed to two questions — foreign-copyright redistribution confirmation (F15) and `hl7-tools` org provenance ([F16](../notes/facts-register.md), a distinct question from this row's usnistgov §105 argument) — inquiry narrowed to 4 questions with a named recipient (Robert Snelick, NIST Systems Interoperability Group); inquiry draft maintained privately by the author; this row mirrors ehr-testing-guide claims-register F3 — that repo's F3 update happens after the author reviews these results |
| F2 | Synthea ships under Apache License 2.0 (GitHub repository-license API, SPDX `Apache-2.0`); current release is v4.0.0, published 2026-03-05 (GitHub's "latest release" endpoint instead returns a non-semver rolling tag, `master-branch-latest`, dated 2026-07-22 — v4.0.0 is the current versioned release). | `docs/components.md` (Synthea) | https://api.github.com/repos/synthetichealth/synthea/license ; https://api.github.com/repos/synthetichealth/synthea/releases | 2026-07-23 | verified |
| F3 | HAPI HL7v2 is dual-licensed MPL/GPL at the licensee's election, per the project's `pom.xml` (no canonical top-level LICENSE file recognized by GitHub's detector); current release is v2.6.0, published 2025-02-05. Mirrors ehr-testing-guide claims-register F2 (license only; that row does not cover the release version). | `docs/components.md` (HAPI HL7v2) | https://raw.githubusercontent.com/hapifhir/hapi-hl7v2/master/pom.xml ; https://api.github.com/repos/hapifhir/hapi-hl7v2/releases | 2026-07-23 | verified |
| F4 | HAPI FHIR ships under Apache License 2.0; its license page is Smile-Digital-Health-branded and points to Smile CDR for commercial support, supporting Smile Digital Health as steward. | `docs/components.md` (HAPI FHIR) | https://api.github.com/repos/hapifhir/hapi-fhir/license ; https://hapifhir.io/hapi-fhir/license.html | 2026-07-23 | verified |
| F5 | The official FHIR validator (hapifhir/org.hl7.fhir.core) ships under Apache License 2.0; its README describes it as HL7-maintained core FHIR tooling (also underlying HL7's IG publisher). | `docs/components.md` (Official FHIR validator) | https://api.github.com/repos/hapifhir/org.hl7.fhir.core/license ; https://raw.githubusercontent.com/hapifhir/org.hl7.fhir.core/master/README.md | 2026-07-23 | verified |
| F6 | CDC's `lib-hl7v2-nist-validator` LICENSE file is standard Apache License 2.0 boilerplate (`Copyright 2024 CDC.gov`), though GitHub's automatic license detector misclassifies the repository as `NOASSERTION`/"Other" — verified by reading the raw file rather than trusting the detector. | `docs/components.md` (CDC lib-hl7v2-nist-validator) | https://raw.githubusercontent.com/CDCgov/lib-hl7v2-nist-validator/main/LICENSE | 2026-07-23 | verified |
| F7 | SUSHI (FHIR/sushi) ships under Apache License 2.0 and, per its FHIR Foundation Project Statement, is maintained by the HL7 community. | `docs/components.md` (FSH/SUSHI) | https://api.github.com/repos/FHIR/sushi/license ; https://raw.githubusercontent.com/FHIR/sushi/master/README.md | 2026-07-23 | verified |
| F8 | NIST IGAMT (usnistgov/hl7-igamt) has no repo-level LICENSE/COPYING/NOTICE and no `<licenses>` block in its root `pom.xml`, but 2 of 12 systematically-sampled source files (`ConformanceProfile.java`, `Valueset.java`, both in domain-model packages) carry a first-party Javadoc header citing 17 U.S.C. §105 and declaring public-domain status — evidentially distinct from v2-validation (F1), which has zero such evidence anywhere. The other 10 sampled files (5 backend modules, both Angular frontends) carry no header. Repo-level classification remains `license-unstated`; the two-file finding does not license the whole repository. | `docs/components.md` (IGAMT), `docs/experiments/EXP-SBOM-results.md` | https://github.com/usnistgov/hl7-igamt (5,709-entry recursive tree, `truncated:false`); https://raw.githubusercontent.com/usnistgov/hl7-igamt/master/conformance-profile/... (header sample) | 2026-07-23 | inventory executed (EXP-SBOM) — license-unstated at repo level; direct NIST confirmation pending (inquiry drafted alongside F1's) |
| F9 | CDC `lib-hl7v2-nist-validator`'s build vendors NIST's HL7 v2 engine as 6 jar+pom pairs checked into git under `lib/**` and resolved via a local `file://` Maven repository declared in `pom.xml` (`<repository><id>local-nist</id><url>file://${project.basedir}/lib</url></repository>`) — it does **not** pull from a live network Nexus at build time, and no SSL/TLS-verification-disabling configuration exists anywhere in the build (`pom.xml`, `.github/workflows/mvn-settings.xml`, `.github/workflows/deploy-to-maven.yml` all checked; no `.mvn/` or other `settings.xml` in the repo). This corrects `docs/components.md`'s prior CDC-wrapper claim ("SSL verification disabled in upstream build config"), which had no F-row and does not match current primary evidence. `hit-nexus.nist.gov` does exist, but only as inert publish-target metadata inside the vendored NIST POMs themselves, never referenced by CDC's own build. All 6 vendored NIST-origin coordinates (`gov.nist:hl7-v2-parser:1.7.3`, `gov.nist:hl7-v2-profile:1.7.3`, `gov.nist:hl7-v2-validation:1.7.3`, `gov.nist:xml-util:2.1.0`, `gov.nist.hit:hl7-v2-schemas:1.7.2`, `com.github.hl7-tools:validation-report:1.2.0`) carry no license metadata in their own POMs or in the one jar opened, and none exist on Maven Central under any coordinate tried (0 results by group and by artifact ID; direct `repo1.maven.org` path probes 404). Of the remaining 20 dependency coordinates in the closure (all Maven-Central-sourced), 19 carry permissive or weak-copyleft-consumed-unmodified licenses classified `license-verified-compatible` against ADR-0001 (9 Apache-2.0, 3 MIT/MIT-style, 1 MIT-by-convention, 5 EPL-1.0/2.0-by-analogy-to-ADR-0001's-MPL-carve-out, 1 BSD-3-Clause). | `docs/components.md` (CDC wrapper), `docs/experiments/EXP-SBOM-results.md` | https://raw.githubusercontent.com/CDCgov/lib-hl7v2-nist-validator/main/pom.xml ; repo tree + vendored POM/jar contents (see results file for full per-coordinate evidence) | 2026-07-23 | verified — supersedes the unregistered SSL/Nexus claim this row now backs |
| F10 | CDC `lib-hl7v2-nist-validator`'s dependency closure includes exactly one copyleft license at runtime (not test) scope: `xom:xom:1.3.7` (GNU LGPL 2.1, per its Maven Central POM `<licenses>` block), pulled transitively via the NIST-origin `hl7-v2-profile` and `xml-util` coordinates (both themselves `license-unstated`, F9). ADR-0001 blesses "MPL/GPL dual-licensed components consumed as unmodified dependencies under their MPL election" but does not name LGPL; `xom` is single-licensed with no alternative election, so a strict reading of ADR-0001's blocker clause ("copyleft-only dependencies... are adoption blockers") would apply. Not classified compatible or incompatible here — flagged for author ADR review; practically secondary to the primary NIST-license blocker in F9. | `docs/components.md` (CDC wrapper), `docs/experiments/EXP-SBOM-results.md` | https://repo1.maven.org/maven2/xom/xom/1.3.7/xom-1.3.7.pom | 2026-07-23 | flagged — not adjudicated (out of EXP-SBOM's scope; see results file "xom / LGPL-2.1" discussion) |
| F11 | Synthea v4.0.0's CLI has two independent RNG seeds, not one: `-s`/`--seed` (patient-generation seed) and `-cs`/`--clinicianSeed` (a genuinely separate stream driving practitioner assignment — name, gender, email, practitioner ID), the latter defaulting to `System.currentTimeMillis()` when omitted (confirmed directly: unpinned EXP-A4 runs' own `metadata/*.json` reported `clinicianSeed` values matching the run's wall-clock time). `-p` is `populationSize`, not a parallelism control; the actual parallelism control is the `generate.thread_pool_size` property (default `-1`, meaning "match `Runtime.getRuntime().availableProcessors()`"), settable per-run via `--generate.thread_pool_size=<n>`, and EXP-A4 verified it has no effect on output determinism at thread counts 1 vs. 4 (identical canonicalized tree hash both ways) once both seeds are pinned. | `docs/components.md` (Synthea), `docs/experiments/EXP-A4.md` (Amendments), `docs/experiments/EXP-A4-results.md` | Synthea v4.0.0 jar's own `--help` usage text; `synthea.properties` extracted from the jar; `docs/experiments/EXP-A4-results.md` "Round: Baseline, initial" and "Round: Parallelism" | 2026-07-24 | verified |
| F12 | Eclipse Temurin JDK 17.0.19+10 (Linux x64, HotSpot, `jdk` image type) ships under the GNU General Public License v2 with the Classpath Exception — confirmed by reading the actual `LICENSE` and `ADDITIONAL_LICENSE_INFO` files bundled inside the distribution itself (`legal/java.base/LICENSE`, `legal/java.base/ADDITIONAL_LICENSE_INFO`), not a third-party description of it. The Classpath Exception is what makes a GPLv2-licensed *runtime* compatible with ADR-0001's Apache-2.0 target for code that merely runs on it (as opposed to statically incorporating GPL source) — this repo consumes the JDK only as an unmodified runtime artifact, never links against or redistributes its source. The archive's sha256 was computed independently by re-downloading it from the Adoptium-published GitHub release asset and hashing the bytes directly (not trusting a published checksum file). | `artifacts.lock.edn` (kind `:runtime`), `docs/components.md` | `https://github.com/adoptium/temurin17-binaries/releases/download/jdk-17.0.19%2B10/OpenJDK17U-jdk_x64_linux_hotspot_17.0.19_10.tar.gz`; `~/.local/jdk/jdk-17.0.19+10/legal/java.base/LICENSE` and `ADDITIONAL_LICENSE_INFO` (read directly, P4 session); sha256 `d8afc263758141a66e0e3aafc321e783f7016696f4eaea067d340a269037d331` computed by independent re-download + `sha256sum` | 2026-07-24 | verified |
| F13 | HAPI FHIR's current stable release on Maven Central is 8.2.0 (`ca.uhn.hapi.fhir:hapi-fhir-base`, `ca.uhn.hapi.fhir:hapi-fhir-structures-r4`); HAPI HL7v2's current stable release on Maven Central is 2.6.0 (`ca.uhn.hapi:hapi-base`, `ca.uhn.hapi:hapi-structures-v24`) -- confirming the v2.6.0 already recorded in F3. The two projects have genuinely distinct Maven group IDs (`ca.uhn.hapi.fhir` vs `ca.uhn.hapi`), not a shared one with different artifact suffixes. | `deps.edn` (EXP-B2) | https://search.maven.org/solrsearch/select?q=g:ca.uhn.hapi.fhir+AND+a:hapi-fhir-structures-r4&core=gav ; https://search.maven.org/solrsearch/select?q=g:ca.uhn.hapi+AND+a:hapi-structures-v24&core=gav | 2026-07-24 | verified |
| F14 | `hit-nexus.nist.gov` is a live, publicly reachable, unauthenticated NIST-operated Maven repository that currently serves all six NIST-origin coordinates the CDC wrapper vendors (F9): `gov.nist:hl7-v2-validation:1.7.3`, `gov.nist:hl7-v2-parser:1.7.3`, `gov.nist:hl7-v2-profile:1.7.3`, `gov.nist:xml-util:2.1.0`, `gov.nist.hit:hl7-v2-schemas:1.7.2`, `com.github.hl7-tools:validation-report:1.2.0` (`.pom` and `.jar` for each). A byte-comparison of the published `hl7-v2-validation-1.7.3.jar` against the CDC-vendored copy confirms they are identical artifacts, including an identical license-silent `META-INF/MANIFEST.MF`. `robots.txt` disallows crawler indexing of `/repository/` and `/service/` only, not access. The CDC wrapper's own Maven-Central-published POM independently lists this host as a `<repository>` source, corroborating it as the CDC team's real build-time dependency source (not merely inert metadata). **Nature: volatile** — a hosted service, not a version-controlled artifact; the IGAMT/GVT Prometheus-transition notices (see F1, F8) make its longevity uncertain, so this row's Last-verified date is load-bearing and should be rechecked before any session relies on Mode-2 (fetch-at-build) adoption. | `docs/components.md` (NIST v2-validation, CDC wrapper), `docs/research/License Status of NIST HL7 v2 Validation Software  Evidence-Based Classification.md` | `https://hit-nexus.nist.gov/repository/releases/` (HTTP 200 for all six coordinates' `.pom`+`.jar`, per the report); Maven Central record for `gov.cdc:lib-hl7v2-nist-validator` listing `hit-nexus.nist.gov` as a `<repository>` | 2026-07-24 | verified — volatile, re-verify before relying on it |
| F15 | NIST's canonical software-licensing statement (`nist.gov/open/license`, matching the `usnistgov/opensource-repo` `LICENSE.md` template) permits use, copying, modification, and distribution of NIST-authored software provided the notice is kept intact, and notes the software "may be subject to foreign copyright (as per 17 U.S.C. §105)." Unlike NIST's parallel statement for NIST *data*, which contains an affirmative worldwide grant covering any foreign copyright NIST might hold, the software statement is silent on redistribution rights outside jurisdictions where §105 domestically applies — a materially weaker posture for a worldwide MIT-distributed repo (ADR-0007) than the data statement's explicit grant. This is the basis of the "use rights vs. redistribution rights" distinction the research report's Mode 1/Mode 2 adoption analysis turns on. | `docs/components.md` (NIST v2-validation), `docs/experiments/EXP-SBOM-inquiry-draft.md`, `docs/research/License Status of NIST HL7 v2 Validation Software  Evidence-Based Classification.md` (section B1/B2) | https://www.nist.gov/open/license ; https://github.com/usnistgov/opensource-repo/blob/main/LICENSE.md ; https://www.nist.gov/copyrights-disclaimers (parallel data-statement contrast, per the report) | 2026-07-24 | verified (statement text); the redistribution-rights gap is the residual question put to NIST (inquiry draft, question b) |
| F16 | `com.github.hl7-tools:validation-report` — one of the six NIST-origin coordinates the CDC wrapper vendors (F9) — has a live GitHub upstream at `github.com/hl7-tools/validation-report` (README retrieved HTTP 200 on `master`), but no `LICENSE` file on either `master` or `main` (both HTTP 404), independently reprobed 2026-07-24. `hl7-tools` is a GitHub Organization (org id 12650686); who publishes or controls it is unknown — this is a distinct provenance question from the `usnistgov` 17 U.S.C. §105 argument (F1, F15), since `hl7-tools` is not a `usnistgov`-owned org and no evidence connects it to NIST beyond this one vendored artifact. | `docs/components.md` (CDC wrapper) | https://raw.githubusercontent.com/hl7-tools/validation-report/master/README.md (200); https://raw.githubusercontent.com/hl7-tools/validation-report/master/LICENSE (404); https://raw.githubusercontent.com/hl7-tools/validation-report/main/LICENSE (404); https://api.github.com/repos/hl7-tools/validation-report (`"type": "Organization"`) | 2026-07-24 | verified (absence); org provenance unresolved |
| F17 | HAPI FHIR's Bundle-entry `id`-vs-`fullUrl` behavior on parse is a documented, configurable parser setting, not unconditional: `ParserOptions.overrideResourceIdWithBundleEntryFullUrl` (default `true`) — when a `Bundle.entry.fullUrl` is present, the parsed resource's `id` is overridden from that `fullUrl`; set to `false`, the original resource id survives parsing unmodified. This setting only explains an *override to an equivalent value* for `urn:uuid:`-style fullUrls (the id and the uuid are the same value), not EXP-B2's actual observation. EXP-B2 (P4, `docs/experiments/EXP-B2-results.md`) found `Bundle.entry[].resource.id` dropped **entirely** (no `id` key at all, not merely re-set) on 14/14 sampled files, all using Synthea's `urn:uuid:` fullUrls — this matches a distinct, open, unresolved upstream defect, GitHub issue `hapifhir/hapi-fhir#5645` ("Id of resource entry isn't serialized when full url of entry is a urn:uuid:", reported against HAPI FHIR 5.7.9/6.10.3, no maintainer resolution found as of this check). Conclusion: the general fullUrl-overrides-id behavior is a documented, configurable default; the specific total-drop EXP-B2 measured is a known bug tied to `urn:uuid:` fullUrls specifically, not the documented override working as designed. | `docs/components.md` (HAPI FHIR), `docs/experiments/EXP-B2-results.md` (backs that file's finding retroactively) | https://hapifhir.io/hapi-fhir/apidocs/hapi-fhir-base/ca/uhn/fhir/context/ParserOptions.html (`isOverrideResourceIdWithBundleEntryFullUrl`/`setOverrideResourceIdWithBundleEntryFullUrl`, default `true`, parse-time); https://github.com/hapifhir/hapi-fhir/issues/5645 | 2026-07-24 | verified |
| F18 | The official FHIR validator's (`hapifhir/org.hl7.fhir.core`) current stable GitHub release, as of 2026-07-24, is `6.9.12` (published 2026-07-15); its `validator_cli.jar` release asset is 187,081,756 bytes with sha256 `0e53ab1d1a6f1e35f505255c0b8ce10a35fcf27e6e96b503640f784cd07e5ad6`, computed by downloading the asset directly and hashing the bytes (not trusting a published checksum file — same discipline as F12's JDK hash). | `artifacts.lock.edn` (kind `:engine`, name `fhir-validator-cli`), `docs/experiments/EXP-C5.md` | `https://api.github.com/repos/hapifhir/org.hl7.fhir.core/releases/latest` (`tag_name: "6.9.12"`, asset `validator_cli.jar`); direct download + `sha256sum` | 2026-07-24 | verified |
| F19 | `slf4j-nop`'s current stable release on Maven Central is 2.0.17 (published under the `org.slf4j` group, parented by `slf4j-parent:2.0.17`); SLF4J (all modules, including its provider jars like `slf4j-nop`) ships under the MIT License per the project's own licensing page. Added to this repo's `:test`/`:coverage` aliases only (not the base `:deps`) to silence the "No SLF4J providers were found" warning HAPI FHIR's transitive `slf4j-api` dependency emits during test runs -- a library consumer of this repo chooses its own binding, so the base deps stay silent on logging implementation. | `deps.edn` (`:test` and `:coverage` aliases) | https://search.maven.org/solrsearch/select?q=g:org.slf4j+AND+a:slf4j-nop&core=gav (latest `2.0.17`); https://www.slf4j.org/license.html ("SLF4J source code and binaries are distributed under the MIT license") | 2026-07-25 | verified |
| F20 | `cognitect.test-runner.api/test` (the exec-fn `deps.edn`'s `:test` alias actually invokes) does not derive its test directories from the invoking alias's `:extra-paths`; it reads only its own `:dirs` key, and when that key is empty or absent it falls through to the base `cognitect.test-runner/test` function, whose `:dir` option defaults to `#{"test"}` -- confirmed by reading the vendored source directly: `cognitect/test_runner.clj`'s `test` fn, `(let [dirs (or (:dir options) #{"test"})] ...)`; `cognitect/test_runner/api.clj`'s `do-test` maps the caller's `:dirs` to `:dir` only via `(when (seq dirs) (set dirs))`, so an empty/omitted `:dirs` passes `nil` through rather than the caller's classpath. Reproduced directly, not just read: temporarily narrowing `deps.edn`'s `:test` alias `:dirs` to `["test"]` (dropping `"palgebra/test"`, `:extra-paths` left unchanged) drops the suite from 375 tests/873 assertions to 361 tests/847 assertions -- the 14 `palgebra/test` tests silently stop running even though `palgebra/test` remains on the classpath via `:extra-paths`; restored immediately after (`:dirs ["test" "palgebra/test"]`, 375/873 again). This is the mechanism `deps.edn`'s own `:test` alias comment already names (discovered R2, `.agents/prompts/archive/2026-07-25-r2-palgebra-claim.md`) -- this row is its first F-row record, reproduced fresh for P7. | `deps.edn` (`:test` alias comment, "`:dirs` is explicit... or its tests silently never run") | `~/.gitlibs/libs/io.github.cognitect-labs/test-runner/dfb30dd.../src/cognitect/test_runner.clj` and `.../test_runner/api.clj` (read directly); local reproduction (`clojure -X:test`: 375/873 -> 361/847 with `:dirs ["test"]` -> 375/873 restored) | 2026-07-25 | verified |
| F21 | The pinned `io.github.cognitect-labs/test-runner` git dependency (tag `v0.5.1`, sha `dfb30dd`) exposes two entry points with two distinct, non-interchangeable exclusion-option keys, confirmed by reading both source files directly: `cognitect.test-runner/-main` (the CLI entry point, `-m cognitect.test-runner`) takes `:exclude` (singular, its `-e`/`--exclude` cli option, `cognitect/test_runner.clj`'s `cli-options`); `cognitect.test-runner.api/test` (the exec-fn `deps.edn`'s `:test`/`:integration` aliases actually invoke) takes `:excludes` (plural, destructured directly in `do-test`, `cognitect/test_runner/api.clj`, then adapted to the base fn's singular `:exclude` internally). This reconfirms, as a first F-row, a fact this repo's own `deps.edn` history once stated inline via a comment naming exactly this plural/singular split -- removed in the 2026-07-25 CI-hotfix commit (`12e5a1e`) when hermeticity moved from a tag-filter to a path-split and the comment's claim became moot for this repo's own `:test` alias (nothing on that alias's path carries the `:integration` tag any more, so `:excludes` there is now belt-and-braces, not load-bearing) -- but the underlying two-entry-point fact was never wrong, and this row gives it a register trace instead of leaving it in a removed comment. Reconstruction check: the author's recollection of "a session spent on a plural-vs-singular exclusion key" matches this exactly; no second, distinct incident (e.g. a cloverage exclusion flag) was found in `.agents/prompts/archive/` or `git log -p -- deps.edn Makefile` for the coverage/test aliases -- the recollection was accurate as stated, not a conflation of two incidents. | `deps.edn` (`:test`/`:integration` aliases, `:exec-fn cognitect.test-runner.api/test`) | `~/.gitlibs/libs/io.github.cognitect-labs/test-runner/dfb30dd.../src/cognitect/test_runner.clj` (`-e`/`--exclude`, singular) and `.../test_runner/api.clj`'s `do-test` (`:excludes`, plural, destructured); `git show 12e5a1e -- deps.edn` (the removed comment this row reconfirms) | 2026-07-25 | verified |
| F22 | HAPI HL7v2 v2.6.0's `ValidationContextFactory.defaultValidation()` (`ca.uhn.hl7v2.validation.impl.ValidationContextFactory`) -- the context `judge.v2` wires onto its `DefaultHapiContext` (`judge/v2.clj`'s `new-context`) -- resolves to a `ValidationContextImpl` built from `DefaultValidationBuilder`, which extends `DefaultValidationWithoutTNBuilder` (adding only a TN/US-phone-number primitive rule) and registers, for all HL7 v2 versions: two message-level rules, `choiceElementsRespected()` and `onlyAllowableSegmentsInSuperStructure()`, plus primitive-type/lexical rules for FT, ST, TX, ID/IS, SI, NM, DT, TM, NULLDT, and (version-dependent) TSComponentOne/DTM. **No rule anywhere in this chain checks segment presence/requiredness or field cardinality** -- this is the mechanism behind P7's empirical probe finding (`corpus/operators.clj`'s module docstring, commit `1c1d634`): three candidates -- dropping the PID segment entirely, corrupting PID's own segment-name, blanking a non-header field like PID-7 -- each probed `:pass`, not `:rejected`. Reproduced fresh for this session (2026-07-25, `judge.v2/execute` invoked directly against `test/fixtures/v2/adt-a01-admit.hl7` mutated the same three ways): all three return `:validation-exceptions []` -- the **empty vector**, not a collected-but-policy-downgraded finding. This corrects a plausible but wrong inference: `onlyAllowableSegmentsInSuperStructure` is a real, registered rule that in principle could flag a structural defect, and it is applied strictly post-parse (`AbstractValidator.testMessageRules`, called from `validate(Message, ValidationExceptionHandler)`, collecting into the same handler shape `judge.v2`'s `collecting-handler` supplies -- confirmed by reading `AbstractValidator.java` directly) -- so `judge.v2`'s own author-ruling policy of never escalating a collected exception to `:rejected` (`judge/v2.clj` docstring, point 2) was a plausible explanation for the corrupt-segment-name candidate's `:pass`. The empirical re-run rules that out for this case: the rule simply never fires for an unrecognized non-header segment name in this fixture, so there is nothing for the policy to downgrade. Whether `onlyAllowableSegmentsInSuperStructure` fires for some other structural shape (e.g. a recognized segment repeated or out of position within its group) is not established by this evidence and is left open, not claimed. The blank-PID-7 case has an independent, source-confirmed explanation: every lexical-format rule this builder registers that isn't a bare length/trim rule wraps its check in `emptyOr(...)` (`DT`: `emptyOr(date())`; `TM`: `emptyOr(time())`; `SI`: `emptyOr(nonNegativeInteger())`; `NM`: `emptyOr(number())`; `TSComponentOne`/`DTM`: "empty or datetime format", the pre-2.5 branch applying to this fixture's declared v2.4) -- an empty value trivially satisfies every one of them, unlike a malformed non-empty value (which `malformed-datetime-value` supplies, and which does convict via a parse-time `DataTypeException` per the pairing suite). | `docs/judge-calibration.md` (v2 tier calibration section), `src/ehr_testing_tools/corpus/operators.clj` (module docstring, cross-reference only) | P7 probe: commit `1c1d634aab74212cdd34423961496adc4262e66c` (`corpus.operators: v2 seed catalog...`), `operators.clj` module docstring; HAPI HL7v2 source at tag `v2.6.0` (matches the release pinned in `deps.edn`, F3/F13): `https://raw.githubusercontent.com/hapifhir/hapi-hl7v2/v2.6.0/hapi-base/src/main/java/ca/uhn/hl7v2/validation/impl/ValidationContextFactory.java` ; `https://raw.githubusercontent.com/hapifhir/hapi-hl7v2/v2.6.0/hapi-base/src/main/java/ca/uhn/hl7v2/validation/builder/support/DefaultValidationBuilder.java` ; `https://raw.githubusercontent.com/hapifhir/hapi-hl7v2/v2.6.0/hapi-base/src/main/java/ca/uhn/hl7v2/validation/builder/support/DefaultValidationWithoutTNBuilder.java` ; `https://raw.githubusercontent.com/hapifhir/hapi-hl7v2/v2.6.0/hapi-base/src/main/java/ca/uhn/hl7v2/validation/AbstractValidator.java` (`validate`/`testMessageRules` method bodies); fresh reproduction, this session (`judge.v2/execute` against the three mutated fixtures, WSL/JDK 17, `clojure -M -e`, ad hoc scratch script, not committed -- three `:validation-exceptions []` results) | 2026-07-25 | verified |