# Facts Register — ehr-testing (workspace)

<!-- Tracks load-bearing, externally verifiable assertions made anywhere in
     this workspace (docs, deps.edn, memory files) about tools, licenses,
     and ecosystem capabilities -- evidence and a last-verified date, so a
     claim can be checked instead of trusted. See AUTHORS-GUIDE.md §4 for
     the assert -> register -> date discipline this file exists to
     support. Instantiated 2026-07-28 (discipline-parity session, R25);
     see notes/discipline-parity-audit.md row M5/M5a for why this file
     didn't exist before this session and why the Index sub-table below
     is tools' own addition, adopted on top of sim's base F-row shape. -->

Fresh sequence, starting at F1 — **not** a continuation of
`notes/sim/facts-register.md`'s or `notes/tools/facts-register.md`'s own
numbering (those are frozen provenance, moved intact, F-rows not
migrated forward automatically per each file's own header). A
workspace-level claim that repeats one of theirs needs its own fresh row
here, re-verified — unless explicitly carried forward with an origin
citation, as F1 below does.

## Index

Hand-maintained, not enforced by a lint or CI check (same choice as
`notes/ADRs.md`'s own lack of a table of contents check). Update this
table in the same commit as any new F-row — one line per row: an
extractive digest (a clause already present in the full row below, not a
new characterization), the last-verified date, and the status column
verbatim.

| # | Claim digest | Last verified | Status |
|---|---|---|---|
| F1 | Sim's manifest generator identity is `"ehrt.sim"` (not `"ehr-testing-sim"`) since ADR-0001's rename; a stale test asserting the old identity went unexecuted until ADR-0005 first ran the path for real | 2026-07-28 | verified (carried forward, origin `notes/ADRs.md` ADR-0005) |
| F2 | `bases/sim-cli`/`projects/sim` retirement trigger (R33, ADR-0009, 2026-07-29) FIRED and executed 2026-08-01 (P3-6): parity gap found first (`check`/`identifiers`/`version` verbs, `run`'s `--arrival-gap`/`--at`/`--format` flags had no `ehrt` equivalent -- `identifiers` was actively doc-taught, contradicting the trigger's own "no use outside its own tests" precondition), wired into `ehrt sim`, THEN both bricks deleted -- not a silent capability loss | 2026-08-01 | fired and executed (see `notes/ADRs.md`'s 2026-08-01 sim-cli-retirement amendment for the parity map and deletion evidence) |
| F3 | `SETUP.md`'s on-ramp (fresh clone → generated + judged corpus) re-validated 2026-07-29 by a fresh-context agent walk: no prior repo knowledge, started from the rendered root README alone. Reached a generated corpus, gated it (`gate v2` 5/5 pass, `gate fhir` correctly rejected a mutant), and checked it (7/7 pass) in 15 commands, one recovery step included. Four friction points found and fixed in `SETUP.md`'s Troubleshooting section the same session. **Superseded-pending-rewalk, 2026-07-31 (review catch-up session):** this walk predates `out/`, `ehrt show`, `ehrt play`, and the `corpus generate` cold-start sim default (ADR-0013/0014/0015-amendment) — its 15-command shape is no longer the current on-ramp. Not rewalked this session by design: this row's own bar (R40, a fresh-context agent with no prior repo knowledge or session memory) is exactly what a session already holding this review's own history cannot supply | `SETUP.md` §4 | This session's own fresh-agent walk (transcript: this session's own record). Bar cited: `notes/tools/prompts/2026-07-24-onboarding-wave.md`'s Step 4 self-audit acceptance test — the walk's own verbatim success-report text is not preserved anywhere in git history (tools-era sessions predate this workspace's own session-record discipline, `notes/discipline-parity-audit.md` row M13); cited from provenance instead, per that prompt's own acceptance criteria and the commits it produced (`dd6ba2d`, `09eb094`, `f4e5c76`) | 2026-07-31 | superseded-pending-rewalk (the 2026-07-29 walk itself is unchanged and still accurately transcribed; only its currency against today's CLI surface is what's flagged) |
| F4 | The CDC wrapper's last tagged release (1.4.0) builds and validates fully offline after one dependency fetch, **without** the SSL-verification-disabling Maven flags its own `README.md` documents — `hit-nexus.nist.gov`'s certificate validates under a stock JVM/Maven trust store today. 16/16 harness runs (both shipped sample profiles, all mutants) were byte-identical between an online run and a `unshare -r -n` network-isolated re-run | `components/tools/docs/experiments/EXP-D3-results.md` Round 1 | This session's own build (`mvn package` against unmodified `pom.xml` at pinned commit `e059e58`) and offline re-run; supersedes the SSL-bypass framing in the pre-EXP-D3 research doc's own D3 evidence line (`hit-v2-nist-validator` §D3), which was accurate for the README's own build instructions but not for present-day `hit-nexus.nist.gov` TLS behavior | 2026-07-29 | verified |
| F5 | The six NIST-origin coordinates the CDC wrapper depends on (tools/F9) resolve cleanly from `hit-nexus.nist.gov`, cross-confirmed by two independent HTTP/TLS clients (Maven's own resolver and a plain `curl`, default verification, no `-k`) — both agree on every sha256 | `components/tools/docs/experiments/EXP-D3-results.md` Jar Inventory table | Direct fetch + sha256 of all six coordinates via both mechanisms, this session | 2026-07-29 | verified |
| F6 | CDC's own `ProfileManager.filterAndConvert` keeps only 4 of the underlying NIST engine's 8 classification strings (`Error`/`Warning`/`Alert`/`Informational`) — `Affirmative` and `Validation Notes` are silently dropped from both `NistReport`'s entry lists and its summary counts (`High Alert`/`Specification Error` are dropped too, by source reading, but were not observed in this session's corpus). Against the wrapper's own shipped baseline message, this discarded 2,014 of 2,663 raw findings (75.6%) | `components/tools/docs/experiments/EXP-D3-results.md` Round 3 | Source read of `gov.cdc.ProfileManager`/`NistReport` (wrapper 1.4.0) against `reference.conf` inside `hl7-v2-validation-1.7.3.jar`, plus a direct empirical count via reflection on `ProfileManager`'s own `validator` field, this session | 2026-07-29 | verified |
| F7 | CI's per-push lane (`.github/workflows/test.yml`, `poly test :all skip:integration`) genuinely executes the full non-integration suite: a fresh green run (id `30501195915`, sha `bdffdbc5`, 2026-07-29) logged exactly 177 `Testing ehrt.*` namespace announcements at that commit; **refreshed 2026-07-31** (review catch-up session): this session's own local `poly test :all skip:integration` logs 190 at this session's HEAD (three new per-push doc-hygiene tests landed this session — structure-currency, stale-path, CLAUDE.md-presence — plus growth since 2026-07-29), 0 failures/0 errors throughout; a local count, not re-pulled from a fresh CI run, so cite the run that produced it | CI run `30501195915` (`gh run view --log`, 2026-07-29 figure); this session's own local run (2026-07-31 figure) | Direct log fetch and line count, 2026-07-29; this session's own `clojure -M:poly test :all skip:integration color-mode:none > log 2>&1; grep -c '^Testing' log`, 2026-07-31 | 2026-07-31 | verified |
| F8 | NIST's hosted IGAMT 2 registration page carries a disclaimer stating IGAMT is public-domain U.S. government software (17 USC §105), hosted on a NIST server visible only to NIST staff and the registrant, redistributable/modifiable with a derived-from/modified notice, with no NIST endorsement or reliability guarantee implied | NIST IGAMT 2 registration page (hosted) | Author observation, 2026-07-29, transcribed verbatim | 2026-07-29 | verified (transcription; scope notes qualify what it does and doesn't cover — see full row) |
| F9 | `tools.deps` honors `:mvn/repos` only against the top `deps.edn` file actually being resolved for a given invocation, not a workspace root merged in from elsewhere; under Polylith, each `projects/*/deps.edn` is its own resolution root for `poly test :all`, so root `deps.edn`'s own `:mvn/repos` does not propagate — every project consuming a non-Central coordinate needs its own entry | root `deps.edn`, `projects/{conformance,integration,ehrt-cli}/deps.edn` `nist-hit` entries | 2026-07-30 | verified (judge-v2-nist landing finding) |
| F10 | `ehrt gate v2-nist` against the use-case-15 fixture (`components/tools/test-fixtures/v2-nist/covidELR/231HL7TestFilewithHHSData.txt`, profile `components/tools/test-fixtures/v2-nist/COVID19_ELR-v2.3.1`) deterministically returns exit 3, `:no-verdict`/`:cause :profile-spec-error`, 473 findings, `:by-code` `{"value-set/VS Not Found" 28, "structure/O-Usage" 109, "structure/Usage" 103, "structure/Length Spec Error" 221, "structure/Dynamic Mapping Match" 8, "content/Constraint Success" 4}` | `docs/use-cases.md` "Profile-tier HL7v2 conformance gating (NIST)" | Originally probed `notes/2026-07-30-refactoring-review.md` §6 D (2026-07-30); independently re-run this session, byte-for-byte matching totals/by-code | 2026-07-31 | verified |
| F11 | ADR-0013's TTY/pipe output contract: every envelope-emitting command (`gate`/`generate`/`mutate`/`intake`, kin) prints an unchanged EDN envelope on stdout when piped/redirected (the default this session independently re-confirmed); `--pretty` forces the human-summary form even into a pipe, overriding that default | `docs/formats.md` "Every other command also senses a terminal now"; `notes/ADRs.md` ADR-0013 | This session's own runs: `ehrt gate v2 ... > file` (piped) produced the unchanged EDN envelope, field-for-field matching `docs/formats.md`'s own captured example; `ehrt gate v2 ... --pretty > file` (same piped context) produced the human-summary form instead. The real-terminal default itself was not independently re-run this session (no TTY in this shell) — piped default and the `--pretty` override are what's re-verified here | 2026-07-31 | verified (piped default + `--pretty` override only; real-terminal default not independently re-run this session) |
| F12 | ADR-0014's byte-identity claim: `ehrt play PATH --sink file:...` writes bytes byte-identical to an unpaced batch write through the same designator, regardless of `--rate` — pacing changes when bytes move, never which bytes | `docs/formats.md` "Display is not wire format"; `notes/ADRs.md` ADR-0014 | This session's own run: the same fixture played through `--sink file:...` at `--rate 100000000` and again at `--rate 1000000` produced `cmp`-confirmed byte-identical output | 2026-07-31 | verified |
| F13 | ADR-0015's own self-amendment (2026-07-30): bare `ehrt corpus generate` (no subcommand) now generates via the sim engine, not Synthea, and succeeds on a cold artifact cache with no `ehrt artifact fetch` step first | `notes/ADRs.md` ADR-0015 (self-amendment); `README.md` Quickstart | This session's own run: bare `ehrt corpus generate --seed 99 --out-dir ...` exited 0 with `manifest.edn`'s `:stage :simulated`, no artifact-fetch step run beforehand | 2026-07-31 | verified |
| F14 | ADR-0016's docs-tooling split (2026-07-31, tools-split stage 1): generated-doc byte-identity re-verified after the move — `docs/dev/pipeline.md`/`docs/use-cases.md` byte-identical to their pre-move sha256 (`sha256sum -c`); `docs/operators.md`/`docs/cli.md` differ by exactly one line each, the renderer's own "edit this file instead" banner citing its new true path, the deliberate consequence of the move itself. Per-push lane: 191 `Testing ehrt.*` namespaces before, 193 after — the +2 fully explained by the deliberate `docsgen` split (one namespace became two, each running in the same two composing projects everything else already ran in) — 0 failures/0 errors both times. **Follow-up, same day:** `bin/quickstart-demo`'s pre-existing lost executable bit (found during Step 1's baseline, unrelated to the split itself) was restored (`chmod +x`, WSL ext4 clone — `git status` showed zero change, confirming the index was correct all along); a full `make quickstart` re-run afterward passed every taught command with its expected exit code (exit 2 only on the script's own tree-clean postcondition, tripped solely by this stage's own not-yet-committed files, confirmed byte-identical to the pre-run tree state, not by anything the run wrote) | `notes/ADRs.md` ADR-0016 | This session's own before/after `make docsgen` + `sha256sum -c` pair, and before/after `clojure -M:poly test :all skip:integration color-mode:none > log 2>&1; grep -c '^Testing' log` pair; the same-day `chmod +x` + full `make quickstart` re-run | 2026-07-31 | verified |
| F15 | ADR-0017's corpus-io split (2026-07-31, tools-split stage 2): per-push lane namespace COUNT unchanged at 193 before/after (not merely equal by coincidence — the full sorted `Testing ehrt.*` occurrence list diffs to exactly the nine moved namespaces renamed `ehrt.tools.corpus.* → ehrt.corpus-io.*`, nothing added or dropped), 0 failures/0 errors both runs; all four generated docs sha256-identical to the pre-move baseline (docsgen untouched by this stage); five CLI baseline commands (`corpus generate sim`, `corpus intake dir:`, `corpus operators --format v2`, `gate v2`, `corpus mutate`) re-run into a fresh scratch dir, every output file byte-identical to the pre-move baseline except the two expected differences (the embedded scratch-dir path itself, and `operation-manifest.edn`'s `:git` field gaining `-dirty` — this session's own uncommitted tree, not a regression) | `notes/ADRs.md` ADR-0017 | This session's own before/after `clojure -M:poly test :all skip:integration color-mode:none > log 2>&1; grep '^Testing' log \| sort` diff; before/after `sha256sum -c` on the four generated docs; before/after CLI command re-run with per-file diff | 2026-07-31 | verified |

| F16 | ADR-0018 (2026-07-31, tools-split stage 3): the tools component is retired -- renamed components/corpus, interface redesigned from live consumers (38 defs kept of 64: 25 kernel/judge/engine relays dissolved to their owning interfaces, 9 kept defs renamed to registry-prefixed corpus vocabulary, Assertion deleted on grep evidence of zero live consumers). Per-push lane occurrence count unchanged at 193 before/after, 0 failures/0 errors both runs; the sorted occurrence-list diff is exactly 52-out/52-in renames (20 corpus brick test namespaces x2 projects, incl. sim-test -> sim-adapter-test; 12 conformance project-test namespaces renamed ehrt.conformance.*), zero deletions. Five CLI seam commands byte-identical modulo scratch-dir name; operation-manifest.edn differs only by :git gaining -dirty (uncommitted tree, F15 precedent); its :producer :name stays the literal "ehrt.tools" by AR-6 byte-identity (named-future). Generated docs differ only by rename-attributable lines (banners, fixture/doc paths, corrected ehrt.judge.report/diff-reports citation). poly check OK, zero warnings, :necessary re-derived a fourth time (conformance [docs-tooling judge-fhir-official judge-v2-nist]; integration [judge-fhir-official], judge-v2-nist dropped, judge-v2-hapi retained after its trial drop turned the integration lane red -- the corpus brick own contract-pairing test requires it). Integration lane green after that fix. The structure-currency gate never went red for the corpus rename: presence-by-substring passes vacuously (corpus is a substring of corpus-io) -- recorded as a named-future, the doc updates relied on the sweep | `notes/ADRs.md` ADR-0018 | This session own before/after per-push occurrence-list diff, seam-command per-file diffs, generated-doc diff attribution scan, poly check/deps captures, and the red->green integration lane re-run | 2026-07-31 | verified |

| F17 | Gate-hardening session (2026-07-31), closing ADR-0018's post-split named-futures 3 and 5: `ehrt.palgebra.deps-lint`'s denylist (`ehrt.tools.*`/`ehrt.sim.*`) went half-vacuous once `ehrt.tools.*` stopped naming anything real (ADR-0018 stage 3) -- rewritten as an allowlist (`ehrt.palgebra.*` only), derived empirically from palgebra's real require set (grep over every src/test `ns` form found no requires outside `ehrt.palgebra.*`, `clojure.*`, `malli.*` -- self-containment held, no escalation needed). `ehrt.docs-tooling.structure-currency-test` checked presence only, by raw substring (`corpus` a substring of `corpus-io`, AR-6's own "never went red" finding) -- rewritten with exact backtick-delimited path tokens for presence and a line-anchored first-cell table-row extraction for absence (catches a retired brick's row with no denylist to maintain, and is immune to a row's own prose mentioning a retired brick as history). Red->green evidence for all three failure modes this session predicted: (a) a temporary `ehrt.kernel.interface` require seeded into `components/palgebra/src/ehrt/palgebra/lint.clj` failed `the-real-palgebra-tree-passes-deps-lint-test`, reverted, re-confirmed green; (b) removing the `` `components/corpus` `` token from AGENTS.md while `` `components/corpus-io` `` stayed failed the presence test citing `` `components/corpus` `` by name, reverted, re-confirmed green; (c) adding a `` `components/tools` `` row to architecture.md's bricks table failed the absence test citing that exact row, reverted, re-confirmed green. Full per-push lane (`poly check` + `poly test :all skip:integration`) green before and after, 0 failures/0 errors throughout | `notes/ADRs.md` ADR-0002 (2026-07-31 amendment); `components/palgebra/src/ehrt/palgebra/deps_lint.clj`; `components/docs-tooling/test/ehrt/docs_tooling/structure_currency_test.clj` | This session's own grep of palgebra's real require set; three seed-then-revert red->green runs (`clojure -M:poly test :brick:palgebra` / `:brick:docs-tooling`); full `make test` run before and after | 2026-07-31 | verified |

| F18 | Storefront + ruled literals session (2026-08-01). **README "What you get" block**: replaced a fabricated example output block with real output, captured by actually generating a Synthea patient this session and running the taught `corpus mutate` + `gate fhir` commands against it -- real result `rejected out/demo-mutants/patient.json (2560 findings)`. Discovery made along the way, corrected in the prose rather than hidden: base FHIR does not require `Patient.gender` (0..1 cardinality), so `remove-required-element` at that locator earns zero new findings -- the identical Synthea patient gates `rejected` with the identical 2560-finding count both *before* and *after* the mutation, confirmed by running `gate fhir` on the unmutated file directly. This exact caveat was already recorded in `docs/judge-calibration.md`'s own defect/locator table (pre-existing, not a new finding), just not reflected in the README's prose or the matching inline comment in the Quickstart fence / `bin/quickstart-demo` -- both corrected to state plainly that the rejection is real but not caused by this mutation, citing `docs/judge-calibration.md`. **CI claim corrected**: README previously claimed the Quickstart's commands are "asserted by CI on every push" -- actually true only of `make quickstart-fresh` (the fence<->script line-for-line structural check, which runs every push via `docs-tooling`'s inclusion in the `conformance` project's `:necessary` set); the commands' real execution (`make quickstart`) is not wired into any CI workflow today, contradicting `ehrt.docs-tooling.quickstart-fresh`'s own docstring claim that the "slow half" is integration-tier -- `integration.yml`'s real content has no such step. README corrected to the true claim; the docstring/integration.yml drift is left as a named-future, not fixed (out of this session's own fence). **A genuine, previously-latent bug found and fixed**: the README's prior "Update README" commit had added a second ` ```sh ` fence (the "What you get" teaser box) before the real Quickstart fence; `ehrt.docs-tooling.quickstart-fresh`'s own extractor is anchored to "the one ` ```sh ` fence in README.md" and silently started reading the wrong block, failing `make quickstart-fresh` outright once actually run (never caught before this session, since nothing in this repo's own history had run it against that commit until now). Fixed non-invasively by retagging the teaser box ` ```bash ` (still shell-highlighted, no longer string-equal to the extractor's anchor) rather than touching the extractor or the real Quickstart fence -- `make quickstart-fresh` confirmed green after. A full `make quickstart` run afterward passed every taught command at its expected exit code; the only postcondition failure was the tree-clean check tripping on this session's own not-yet-committed files (F14 precedent). **README register tripwire** (AR-3): `ehrt.docs-tooling.stale-path-test` extended with a second, independent check scoped to `README.md` only, forbidding internal provenance codes (`ADR-\d+`, `EXP-[A-Z]?\d+`, `DOC-\d+`, bare `D\d+`) in body prose, with markdown link destinations (`](...)`) and HTML comments stripped before matching so the Maturity table's own `EXP-A4-results.md`/`EXP-B2-results.md` hrefs don't trip it. Red->green proved twice: as permanent fixture-based tests (`each-register-code-pattern-is-actually-caught-test`, `link-destinations-and-html-comments-are-exempt-test`), and as a real manual seed-then-revert run against the actually-tracked `README.md` (a literal `ADR-0012` sentence inserted into real prose failed `readme-body-carries-no-internal-provenance-codes-test` citing exactly `["ADR-0012"]`, reverted, re-confirmed green). **Producer literal** (AR-4): see `notes/ADRs.md`'s 2026-08-01 amendment for the ruling; this row's own evidence is the red->green pinning-test run (red against `"ehrt.tools"`, green against `"ehrt"`) plus the `bases/cli`, `corpus-io`, and `docs-tooling` brick test runs confirming no other consumer broke. **Test-tree naming bless** (AR-5): recorded in the same ADR amendment, no code changes. | `README.md`; `bin/quickstart-demo`; `docs/judge-calibration.md`; `components/docs-tooling/test/ehrt/docs_tooling/stale_path_test.clj`; `bases/cli/src/ehrt/cli/core.clj`; `bases/cli/test/ehrt/cli/core_test.clj`; `docs/formats.md`; `components/corpus-io/test/ehrt/corpus_io/operation_manifest_test.clj`; `notes/ADRs.md` (2026-08-01 amendment) | This session's own real `corpus generate synthea` + `corpus mutate` + `gate fhir` runs (both pre- and post-mutation, same file, both 2560 findings); `make quickstart-fresh` red (wrong-fence bug) -> fixed -> green; a full `make quickstart` run, every command at its taught exit code; the seed-then-revert `ADR-0012` run against real `README.md`; the pinning-test red (`"ehrt.tools"`) -> green (`"ehrt"`) run; `bases/cli`/`corpus-io`/`docs-tooling` brick test runs, 0 failures/0 errors throughout | 2026-08-01 | verified |
| F19 | **Sim adopts `ehrt.kernel.result` (P3-5, ruled 2026-08-01, `notes/ADRs.md` ADR-0022): every emitted byte and every error path is unchanged.** `ehrt.sim.result` (identical malli schema and seven-function surface to `ehrt.kernel.result`) deleted; twelve sim namespaces repointed to `ehrt.kernel.interface` (not `ehrt.kernel.result` directly -- poly's brick-boundary check refused the bare namespace with `Error 101` the first time, fixed by requiring the interface, which already re-exports the same seven functions verbatim). A before/after byte capture across two seeds' full run output (`--emit hl7`/`--emit fhir`/`--format er7`/`--format ground-truth`), the `run \| check` pipe, `identifiers`, `version`, `corpus generate sim`, and six representative error paths (missing seed, bad seed value, `:out-dir-exists`, malformed/empty stdin to `check`, missing seed on `identifiers`) was byte-identical everywhere except this session's own before/after directory-tag naming (confirmed identical once substituted) and one inherently random JVM temp-report filename in a babashka.cli error message (message text itself unchanged). `poly check` OK; per-push lane 168 `Testing ehrt.*` namespaces both before and after (git-stash-verified against the same HEAD `4bf9be0`), 0 failures/0 errors both times, identical final tallies. | `notes/ADRs.md` ADR-0022 | This session's own `git stash`/`stash pop` round-trip to get a true pre-edit test-count baseline against the post-edit tree; `poly check` runs before and after; the full before/after CLI byte-capture pair (two seeds, all format/emit combinations, the run\|check pipe, six error paths), diffed | 2026-08-01 | verified |
| F20 | Sim-origin facts topology: `notes/sim/facts-register.md` is frozen provenance (F1–F22, its own numbering, never migrated into this sequence); a live claim restating one of its rows cites it origin-qualified (`sim/FN`), same convention `notes/ADRs.md`'s own preamble already states for ADRs | `notes/sim/facts-register.md` header; this row | 2026-08-02 | verified (citation-stubs pass, migration session 3) |
| F21 | The vital-sign reference table's three LOINC codes (`8480-6`/`8462-4`/`59408-5`) are cross-checked against HL7 FHIR's own public Vital Signs profile examples (`loinc.org` returned 403 to automated fetch); reference ranges are author-curated, unverified | `components/sim-trajectory/resources/sim-trajectory/vital-signs.edn` | 2026-08-02 | verified (LOINC codes only) |
| F22 | GMF coverage Wave D stage D3's own seven new vital-sign-table LOINC codes (Glucose `2345-7`, Urea Nitrogen `3094-0`, Calcium `17861-6`, Sodium `2951-2`, Potassium `2823-3`, Chloride `2075-0`, Carbon Dioxide `2028-9`) are verified active against a live public FHIR terminology server's own `$lookup` operation (`r4.ontoserver.csiro.au`, CSIRO), `loinc.org` again returning 403 to automated fetch; reference ranges are author-curated, unverified, the same bar F21 already sets | `components/sim-trajectory/resources/sim-trajectory/vital-signs.edn`; `components/sim-trajectory/docs/gmf-interpreter.md` section 14 (D3c finding 4) | 2026-08-02 | verified (LOINC codes only) |
| F23 | GMF coverage Wave G's own wellness-cadence table (`EncounterModule.recommendedTimeBetweenWellnessVisits`, lines 176-201) is transcribed VERBATIM from a live WebFetch of the pinned Synthea source, not hand-curated (unlike F21/F22's own vital-sign ranges) -- the chronic-medications annual cap (same method, lines 209-211) is EXCLUDED by ruling, deferred to the calibration register | `components/sim-trajectory/resources/sim-trajectory/wellness-cadence.edn`, its own NOTICE addendum | 2026-08-03 | verified (transcription verified against the pinned raw source at fetch time) |

## Register

| # | Claim | Where asserted | Evidence | Last verified | Status |
|---|---|---|---|---|---|
| F1 | `components/sim/src/ehrt/sim/manifest.clj`'s generator identity has reported `"ehrt.sim"` since ADR-0001's own mechanical rename (2026-07-28) — not `"ehr-testing-sim"`, sim's pre-rename self-identity. This is a worked example for why this register exists: `projects/conformance/test/ehrt/tools/sim_manifest_contract_test.clj` asserted the *old* string, but that test path had never actually executed end to end before ADR-0005's in-process `ehr sim` mount — always skipped, local and CI both, for lack of a sibling checkout at the moments it previously ran. The mount is what first let it run for real, and it caught its own staleness on the first real run. Fixed (test corrected to `"ehrt.sim"`, not left red — AUTHORS-GUIDE.md's two-failure-modes discipline: the check misencoded its own invariant, so the check was the thing to fix, not reality). | `components/sim/src/ehrt/sim/manifest.clj:77`, `projects/conformance/test/ehrt/tools/sim_manifest_contract_test.clj` | `notes/ADRs.md` ADR-0005's own deviation record ("A genuine, previously-latent finding, surfaced by the mount, not caused by it") | 2026-07-28 | verified (carried forward from ADR-0005 at register instantiation, discipline-parity session) |
| F2 | `bases/sim-cli`/`projects/sim` deprecation (R33, ADR-0009, 2026-07-29): the CLI rename session's own author ruling marked sim's standalone CLI deprecated rather than removed, on the reasoning that `bin/ehrt sim run` (the in-process mount, ADR-0005) is now the presented surface for every user-facing doc, while `bases/sim-cli` stays buildable and tested for whoever still depends on the standalone artifact directly. Retirement trigger, named here so it isn't lost to prose: **retire when a review finds no use outside its own tests.** Not scheduled — this row is the pointer a future review checks, not a countdown. **UPDATE 2026-08-01 (P3-6, F2 fired and executed):** the retirement review (`notes/prompts/2026-08-01-ehr-testing-retire-sim-cli.md`) found the trigger's own precondition NOT literally met -- `docs/simulate-your-facility.md` actively taught the standalone `sim identifiers` invocation as the answer to a real user FAQ (privacy/data-removal), and `sim check`/`sim version` plus several `run` flags (`--arrival-gap`/`--at`/`--utc-offset`/`--format`) had no `ehrt` equivalent at all -- escalated per the review's own AR-1 rather than accepting silent capability loss. Author ruling: wire the gaps, then fire. `ehrt sim check`/`identifiers`/`version` mounted (`ehrt.corpus.sim-adapter`'s new `check!`/`identifiers!`/`version!`, same dependency-direction invariant as `run!`); `run`'s `--arrival-gap`/`--at` gained the `:coerce :long` entries they'd always needed (silently broken before, uncoerced strings reaching arithmetic); `--format er7`/`ground-truth` bare-stdout modes mounted via a new `:bare-text` result-metadata convention in `main!` (preserves the real exit code, unlike `:category :display-text`); `docs/simulate-your-facility.md`/`docs/glossary.md` repointed to the `ehrt` spelling; `docs/cli.md` regenerated. Real subprocess smoke-tested (`bin/ehrt sim check`/`identifiers`/`version`, the `run --format ground-truth \| sim check` pipe) before deletion. THEN `bases/sim-cli` and `projects/sim` deleted for real: root `deps.edn`'s `poly/sim-cli` entry and test path gone, `workspace.edn`'s `"sim"` project entry gone (`:necessary` re-derived a fifth time, unchanged), `docs/dev/architecture.md`'s mermaid node/edge and both bricks' table rows removed, `AGENTS.md`/`SETUP.md`/`docs/dev/README.md` swept to past tense, `projects/conformance/test/.../sim_cli_real_invocation_test.clj` renamed to `ehrt_sim_run_real_invocation_test.clj` (it never subprocessed sim-cli -- always `bin/ehrt sim run` -- so the old name was doubly stale). Coverage accounting: `components/sim`'s own 21 test namespaces ran 4x workspace-wide before (`conformance`/`ehrt-cli`/`sim`/`integration`, each composing `poly/sim` independently) and run 3x after (same namespaces, one fewer duplicate pass, zero coverage loss) -- `ehrt.sim-cli.core-test` (sim-cli's own suite) retired with the code it tested, not a loss. `ehrt.docs-tooling.structure-currency-test`'s absence gate (ADR-0018's own P3-6-adjacent hardening) caught the stale `bases/sim-cli` architecture-table row on its own, unprompted, before the doc sweep landed -- captured as red evidence, then green after the fix. `poly check` OK throughout; per-push lane (`conformance`+`ehrt-cli`, `sim` project gone) and the integration lane both 0 failures/0 errors before and after. | `AGENTS.md` ("Landed so far"), `notes/ADRs.md` ADR-0009 and its 2026-08-01 amendment | This session's own ruling (`notes/prompts/2026-07-29-ehr-testing-development-resumption.md`, R33); the 2026-08-01 retirement session's own parity map, red→green structure-currency capture, coverage-accounting before/after diff, real subprocess smoke tests, and full per-push + integration lane runs | 2026-08-01 | fired and executed |
| F3 | **The SETUP on-ramp claim: a fresh clone with no prior knowledge of this repo, starting only from the rendered root `README.md`, can reach a generated and judged corpus.** Re-validated 2026-07-29 (pre-takeover storefront-polish session, R40/R41): a fresh-context agent (no repo knowledge, no session memory, handed only the rendered `README.md` text and the instruction "get to a generated, judged corpus on this machine") worked the Quickstart end to end — `corpus generate` (real Synthea run), `corpus mutate`, `gate v2` (5/5 pass), `gate fhir` (correctly rejected the mutant, exit 1), `check` (7/7 pass) — in 15 commands including one self-recovered failure (a stale `target/corpus/synthea-s1-p5` directory from prior dev-machine activity, not a fresh-clone condition). R40's bar: a prior documented success report from a domain expert (a Python developer), against `ehr-testing-tools` pre-merge. That report's own verbatim text could not be located in git history — this workspace's `.agents/session-records/` discipline (`notes/discipline-parity-audit.md` M13) is sim-only in origin; tools-era sessions (including `notes/tools/prompts/2026-07-24-onboarding-wave.md`, the session that first built `SETUP.md` for exactly this trial audience) never committed a session report, only the prompt and the resulting commits. Cited from that provenance instead of the report itself, per AGENTS.md's "record it and ask" discipline (autonomous session: recorded, not blocked on). That prompt's own Step 4 acceptance test (five questions, each answerable with a file+section citation from a fresh clone) is the closest live equivalent of R40's bar, and this session's own walk clears a stricter version of it (real command execution, not a citation exercise). Four friction points the walk surfaced were fixed the same session: `corpus generate`'s `:out-dir-exists` rejection was undocumented; a harmless `run!` namespace-shadow warning leaks onto stdout; `gate ... --report` still dumps the full result to stdout alongside writing the file; `poly test :all`'s multi-minute runtime was unstated. All four are now `SETUP.md` §4 Troubleshooting entries. **Superseded-pending-rewalk, 2026-07-31 (review catch-up session):** this walk's own 15-command shape predates `out/`, `ehrt show`, `ehrt play`, and the `corpus generate` cold-start sim default (ADR-0013/0014/0015-amendment) — the claim above is frozen as of 2026-07-29 and is no longer a live description of the on-ramp; not rewalked this session, since this row's own bar (R40, a fresh-context agent with no prior repo knowledge or session memory) is exactly what a session already holding this review's own history cannot supply — recorded as a standing errand, not executed here. | `SETUP.md` §4, `README.md`'s Quickstart (the `corpus generate` rejection-behavior comment) | This session's own fresh-context agent walk (full transcript in this session's own record); `notes/tools/prompts/2026-07-24-onboarding-wave.md` Step 4 (bar cited from provenance); commits `dd6ba2d`/`09eb094`/`f4e5c76` (what that prompt actually produced) | 2026-07-31 | superseded-pending-rewalk (the 2026-07-29 walk itself is unchanged and still accurately transcribed; only its currency against today's CLI surface is what's flagged — see claim) |
| F4 | **EXP-D3 (2026-07-29): the CDC wrapper's last tagged release, `1.4.0` (commit `e059e58ac5592baff57f04ee744398357d3258f3`), builds and validates fully offline after one initial dependency fetch — and does so *without* the SSL-verification-disabling Maven flags (`-Dmaven.wagon.http.ssl.insecure=true` et al.) its own `README.md` at that commit still documents.** `mvn dependency:resolve`/`mvn package` against the unmodified `pom.xml` (which declares a live `hit-nexus` repository) succeeded outright under Maven's default, verified TLS trust — `hit-nexus.nist.gov`'s certificate validates today under a stock JVM/Maven trust store. 16/16 harness runs (both of the wrapper's shipped sample profiles, all mutants) were byte-identical between an online run and a re-run inside `unshare -r -n` (network genuinely severed — a direct `curl` to `hit-nexus.nist.gov` from inside the isolated namespace returned no response, exit code `000`). This refines, rather than contradicts, tools/F9: tools/F9's "not a live network Nexus" finding was accurate for `main` post-2026-04-03 (which vendors the six jars into a `file://` local repo, added *after* the 1.4.0 tag); the 1.4.0 release itself, predating that vendoring commit, still resolves live against `hit-nexus.nist.gov` — and does so cleanly, with no SSL bypass needed. | `components/tools/docs/experiments/EXP-D3.md`, `components/tools/docs/experiments/EXP-D3-results.md` Round 1 | This session's own build, at the pinned tag, both online and network-isolated | 2026-07-29 | verified |
| F5 | **EXP-D3: all six NIST-origin coordinates the CDC wrapper depends on (tools/F9's own list) resolve cleanly from `hit-nexus.nist.gov`, cross-confirmed by two independent HTTP/TLS clients** — Maven's own Aether resolver (isolated `-Dmaven.repo.local`) and a plain `curl` (default TLS verification, no `-k`) — agreeing on every sha256. Full coordinate/URL/sha256 table in the results file. This re-verifies tools/F14 (`hit-nexus.nist.gov` liveness) against the exact six coordinates, not just the repository root, and supplies the jar inventory a future `artifacts.lock.edn` entry set needs. | `components/tools/docs/experiments/EXP-D3-results.md` Jar Inventory table | Direct fetch + sha256 of all six coordinates via both mechanisms, this session, 2026-07-29 | 2026-07-29 | verified |
| F6 | **EXP-D3: CDC's own `ProfileManager.filterAndConvert` (wrapper 1.4.0) keeps only 4 of the underlying NIST engine's 8 classification strings** (`Error`/`Warning`/`Alert`/`Informational` kept; `Affirmative`/`Validation Notes` confirmed dropped by direct execution; `High Alert`/`Specification Error` dropped by source reading, not observed in this session's corpus) — silently, from both `NistReport`'s entry lists and its summary counts. Against the wrapper's own shipped `TEST_PROF` baseline message, this discarded 2,014 of 2,663 raw findings (75.6%), all of them `Affirmative` or `Validation Notes`. Consequence: a consumer reading only `ProfileManager.validate()`'s public output never sees the specific classification (`Affirmative`) the engine uses to say "this optional/RE field or group is empty, and that's fine" — which is the entire reason two of `judge-v2`'s three named-customer mutations (`docs/judge-calibration.md` CAL-1) don't convict against the two profiles this session tested (Round 5) | `components/tools/docs/experiments/EXP-D3-results.md` Round 3 | Source read of `gov.cdc.ProfileManager`/`NistReport` against `reference.conf` inside `hl7-v2-validation-1.7.3.jar`, cross-checked by reflection on `ProfileManager`'s private `validator` field to obtain the raw, unfiltered `Report` for the same input, this session | 2026-07-29 | verified |
| F7 | **CI reference-timing verification (2026-07-29, WSL clone-location session).** CI's per-push lane genuinely runs the full non-integration suite, not a subset: fetched the log of the most recent green push run (`30501195915`, sha `bdffdbc5`, "Add ER7 encoding reference.", `conclusion: success`) and counted 177 `Testing ehrt.*` namespace announcements — the exact count this workspace's own extraction session already used as its local full-suite reference. `0 failures, 0 errors` throughout; the `poly test :all skip:integration` step itself ran in 1m46s (23:58:39Z→00:00:25Z UTC), the full job (checkout + toolchain setup + `poly check` + tests) in 2m11s (23:58:16Z→00:00:27Z UTC). This is the CI-side number `SETUP.md` §4's WSL clone-location entry cites; local reference numbers for the same full suite, author-measured the same day on this session's own machine (no hostname/path beyond the generic `/mnt/c/...` form): **~20 min** run against a `9p`/`drvfs`-mounted repo (`/mnt/c/...`), **3:15** against a WSL ext4 clone, **~2.5 min** on CI's own Ubuntu runner (this row), and **8 min** for `make integration` on the ext4 clone. **Refreshed 2026-07-31 (review catch-up session):** the 177 figure is stale — the review session (2026-07-30) had already measured 185 (`notes/2026-07-30-refactoring-review.md` §6 A); this session's own local run measures 190 at this session's own HEAD, after landing three new per-push doc-hygiene tests (structure-currency, stale-path, CLAUDE.md-presence). This is a local count, not a fresh CI log — cite the run that produced each figure, don't average or reconcile them. | `SETUP.md` §4 (Troubleshooting) | `gh run view 30501195915 --log`, 2026-07-29 (177); `notes/2026-07-30-refactoring-review.md` §6 A (185); this session's own `clojure -M:poly test :all skip:integration color-mode:none > log 2>&1; grep -c '^Testing' log`, 2026-07-31 (190) | 2026-07-31 | verified |
| F8 | **IGAMT registration disclaimer, captured verbatim (2026-07-29, author-observed while registering on NIST's hosted IGAMT 2):** "Application for the use of the IGAMT does not guarantee that the applicant will be granted approval for use by the National Institute of Standards and Technology (NIST). The IGAMT software is hosted on a NIST server, and your information will not be visible to users other than NIST staff and yourselves. Information provided in the IGAMT does not imply NIST endorsement of any particular product, service, organization, company, information provider, or content. This software was developed at the NIST by employees of the Federal Government in the course of their official duties. Pursuant to title 17 Section 105 of the United States Code, this software is not subject to copyright protection and is in the public domain. NIST assumes no responsibility whatsoever for its use by other parties, and makes no guarantees, expressed or implied, about its quality, reliability, or any other characteristic. We would appreciate acknowledgment if the software is used. The outcome of the use of IGAMT can be redistributed and/or modified freely provided that any derivative works bear some notice that they are derived from it, and any modified versions bear some notice that they have been modified." **Three scope notes, load-bearing for the pending NIST licensing inquiry and future profile-artifact work:** **(i)** the §105 public-domain statement is conclusive for IGAMT itself, but only *supportive* — not conclusive — for the six NIST HL7v2-validator jar coordinates (`artifacts.lock.edn`, `:license-status :use-permitted--unstated--confirmation-pending`); those are a separate codebase from IGAMT, and the pending inquiry (`tools/NIST-reply`, `notes/ADRs.md`) stays open regardless of this disclaimer. **(ii)** the redistribute/modify clause means an IGAMT-exported profile bundle (`PROFILE.xml` + siblings) is redistributable *with* a derived-from notice, and a modified notice if edited — this is the license verification a future vendored `:profile` artifact in `artifacts.lock.edn` will need, and that notice obligation must be carried into that artifact's own `:license-note` when it lands, not re-derived from scratch. **(iii)** profiles authored on NIST's *hosted* IGAMT are visible to NIST staff per the disclaimer's own second sentence — anyone who needs to treat a profile as proprietary during authoring should use self-hosted IGAMT (`usnistgov/hl7-igamt`) instead. | NIST IGAMT 2 registration page (hosted instance), observed by the author 2026-07-29 during registration | Direct author observation and verbatim transcription, 2026-07-29; cross-referenced against `notes/ADRs.md`'s existing `tools/NIST-reply` open-hole entry and `artifacts.lock.edn`'s six NIST-origin entries | 2026-07-29 | verified (transcription verified against the source page at observation time; the page's own content is not independently re-checked here and may change) |
| F9 | **`tools.deps` resolves `:mvn/repos` only against the top `deps.edn` file actually in play for a given invocation — it does not merge a value declared in some other, workspace-root `deps.edn` into a separately-resolved sibling file.** Under Polylith, `poly test :all` resolves each of `projects/conformance`, `projects/integration`, and `projects/ehrt-cli`'s own `deps.edn` independently; none of them inherit root `deps.edn`'s own `:mvn/repos` map merely by belonging to the same workspace. Consequence: any component pulling in a non-Maven-Central coordinate (directly or transitively) needs the resolving repository repeated in every project `deps.edn` that ends up depending on it, not just the workspace root — and `poly check` alone does not catch a missing entry (it does not resolve artifacts); only a real `poly test :all` run does, on an artifact-resolution failure. | root `deps.edn`, `projects/{conformance,integration,ehrt-cli}/deps.edn` `nist-hit` entries | Clojure's own `deps.edn` reference documentation states, under `:mvn/repos`: *"Repositories declared in dependency poms and deps.edn are not used - all necessary repositories must be defined in the top deps.edn file being used"* (`https://clojure.org/reference/deps_edn`, `:mvn/repos` section) — supportive of, but not phrased in Polylith's own per-project-invocation terms; the 2026-07-30 judge-v2-nist landing session (`notes/prompts/2026-07-30-ehr-testing-judge-v2-nist-landing.md` deviation record; `notes/ADRs.md` ADR-0012 deviation record) is the primary evidence for the Polylith-specific consequence — `poly check` passed with the tools-interface wiring in place, but `poly test :all` failed on artifact resolution until the same `:mvn/repos` entry was repeated in all three project `deps.edn` files, matching `poly/judge-v2-nist`'s own pre-existing local-root convention at that layer (ADR-0011's flat, project-level `deps.edn` discipline). The three `nist-hit` entries confirmed present, byte-identical, at `6f2ea56`. | 2026-07-30 | verified |
| F10 | **P2-1, 2026-07-31 review catch-up: `ehrt gate v2-nist` against the use-case-15 fixture is a stable, reproducible pin, independently re-run this session.** `bin/ehrt gate v2-nist components/tools/test-fixtures/v2-nist/covidELR/231HL7TestFilewithHHSData.txt --profile components/tools/test-fixtures/v2-nist/COVID19_ELR-v2.3.1` (the exact strip `docs/use-cases.md`'s "Profile-tier HL7v2 conformance gating (NIST)" section teaches) exits 3, `:no-verdict`, `:cause :profile-spec-error`, `:finding-count 473`, `:by-code {"value-set/VS Not Found" 28, "structure/O-Usage" 109, "structure/Usage" 103, "structure/Length Spec Error" 221, "structure/Dynamic Mapping Match" 8, "content/Constraint Success" 4}` — byte-for-byte matching the totals and by-code the 2026-07-30 review session's own probe recorded (`notes/2026-07-30-refactoring-review.md` §6 D). | `docs/use-cases.md` "Profile-tier HL7v2 conformance gating (NIST)"; `components/tools/docs/use-cases.edn` | `notes/2026-07-30-refactoring-review.md` §6 D (original probe, 2026-07-30); this session's own independent re-run of the identical command, 2026-07-31, matching totals/by-code exactly | 2026-07-31 | verified |
| F11 | **P2-1, 2026-07-31 review catch-up: ADR-0013's TTY/pipe output contract, the piped half and the `--pretty` override, independently re-confirmed this session.** Every envelope-emitting command prints the unchanged EDN result envelope on stdout when piped/redirected — this session ran `ehrt gate v2 components/tools/test-fixtures/v2 > file` (redirected) and got back the full `{:status :rejected, :category ..., :payload {...}}` envelope, field-for-field matching `docs/formats.md`'s own captured example shape. `--pretty` forces the human-summary form even in that same piped context instead — `ehrt gate v2 components/tools/test-fixtures/v2 --pretty > file` produced a plain `pass  <path>` / `totals: ...` summary, not the envelope. The real-terminal default itself (no `--pretty`, stdout genuinely a TTY) was **not** independently re-run this session — no TTY available in this non-interactive shell — so this row verifies the piped default and the flag override only, not the terminal-detection branch itself. | `docs/formats.md` "Every other command also senses a terminal now (ADR-0013)"; `notes/ADRs.md` ADR-0013 | This session's own two runs (piped-default, `--pretty`-override), 2026-07-31 | 2026-07-31 | verified (piped default + `--pretty` override only — see scope note in the claim) |
| F12 | **P2-1, 2026-07-31 review catch-up: ADR-0014's player byte-identity claim, independently re-confirmed this session.** `ehrt play <fixture> --sink file:<path>` at `--rate 100000000` and, separately, at `--rate 1000000` against the same input file produced `cmp`-confirmed byte-identical output files — pacing changed only *when* the write happened, never *which* bytes it wrote, exactly as `docs/formats.md`'s "Display is not wire format" section claims. | `docs/formats.md` "Display is not wire format"; `notes/ADRs.md` ADR-0014 | This session's own run: two `ehrt play` invocations at different `--rate` values through the same `file:` sink designator, `cmp` byte-identical, 2026-07-31 | 2026-07-31 | verified |
| F13 | **P2-1, 2026-07-31 review catch-up: the ADR-0015 self-amendment's cold-start default, independently re-confirmed this session.** Bare `ehrt corpus generate --seed 99 --out-dir <scratch>` (no `generate sim`/`generate synthea` subcommand) exited 0 and wrote a `manifest.edn` with `:stage :simulated` — the sim engine, not Synthea — with no `ehrt artifact fetch` step run first and no artifact-cache dependency encountered. | `notes/ADRs.md` ADR-0015 (self-amendment, "bare `corpus generate` now means `generate sim`"); `README.md` Quickstart | This session's own run against a scratch `--out-dir`, 2026-07-31 | 2026-07-31 | verified |
| F20 | **Sim-origin facts topology (migration session 3, item 3(a), 2026-08-02).** `notes/sim/facts-register.md` (F1–F22, its own numbering) is frozen provenance, moved intact at the merge, never migrated into this live sequence and never re-numbered. A live claim that restates one of its rows cites it origin-qualified (`sim/FN`) rather than copying the frozen text — the same convention `notes/ADRs.md`'s own preamble already states for its `sim/ADR-NNNN` citations, extended here so this register self-describes its own two-file topology instead of leaving it implicit. This session's own citation-stubs pass (survey of `docs/`, `AGENTS.md`, `README.md`, `notes/ADRs.md`, and `components/sim/src` docstrings against all 22 frozen F-rows and all 16 frozen sim ADRs) is the worked example: uncited restatements found for `sim/F2`, `sim/F3`, `sim/F4`, `sim/F6`, `sim/F7`, `sim/F8`, `sim/F9`, `sim/F14`, and nine of the sixteen sim ADRs, now cited at their live restatement site; full accounting in `.agents/session-records/2026-08-02-migration-session-3.md`. | `notes/sim/facts-register.md` header; `notes/ADRs.md` preamble (the analogous ADR-citation rule) | This session's own survey and citation pass, 2026-08-02 | 2026-08-02 | verified |
| F21 | **GMF coverage Wave D stage D1 vital-sign reference table (2026-08-02): the three LOINC codes `components/sim-trajectory/resources/sim-trajectory/vital-signs.edn` cites (`8480-6` systolic BP, `8462-4` diastolic BP, `59408-5` oxygen saturation by pulse oximetry) were cross-checked against HL7 FHIR's own public Vital Signs profile examples, since `loinc.org` itself returned HTTP 403 to automated fetch at verification time.** `hl7.org/fhir/bp.html` confirms `8480-6`/`8462-4` as the Blood Pressure profile's own fixed component codes (mm[Hg]); `hl7.org/fhir/observation-example-satO2.json.html` confirms `59408-5` ("Oxygen saturation in Arterial blood by Pulse oximetry") as its own worked example's code. `:units`/`:reference-range` in the same table are typical-adult, author-curated values, NOT independently source-verified (the same "representative clinical ranges" disclosure `order-profiles.edn`'s own header already makes for its lab-analyte ranges, sim/F7's own precedent). | `components/sim-trajectory/resources/sim-trajectory/vital-signs.edn`, its own NOTICE | This session's own two WebFetch calls against `hl7.org/fhir`, 2026-08-02 (`loinc.org/8480-6`/`8462-4`/`59408-5` all returned 403 first) | 2026-08-02 | verified (LOINC codes only — reference ranges are author-curated, unverified, per the claim) |
| F23 | **GMF coverage Wave G wellness-cadence table (2026-08-03, `notes/ADRs.md` ADR-0037 AR-1): `components/sim-trajectory/resources/sim-trajectory/wellness-cadence.edn` transcribes Synthea's own `EncounterModule.recommendedTimeBetweenWellnessVisits` VERBATIM from the pinned source, not hand-curated content the way F21/F22's own vital-sign ranges are.** Fetched live via WebFetch against `raw.githubusercontent.com/synthetichealth/synthea/7e08387c68a7f0e21d13076609a159fd473fc902/src/main/java/org/mitre/synthea/modules/EncounterModule.java`, this session — the age-banded interval table (lines 176-201: months-tier for age <= 3 years, years-tier otherwise) transcribed field-for-field into the resource's own two-vector shape. The same method's own chronic-medications annual cap (lines 209-211) is EXCLUDED by ruling (AR-1), not omitted by oversight — deferred to the calibration register as a named item ("wellness cadence chronic-meds cap"), disclosed in both the resource's own header comment and its NOTICE addendum. | `components/sim-trajectory/resources/sim-trajectory/wellness-cadence.edn`, its own NOTICE addendum | This session's own WebFetch of the pinned raw source, 2026-08-03 | 2026-08-03 | verified (transcription verified against the pinned raw source at fetch time; the chronic-meds cap is a disclosed exclusion, not part of this claim) |
