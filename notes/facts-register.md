# Facts Register

<!-- Tracks load-bearing, externally verifiable assertions made anywhere in
     this repo (docs, deps.edn, memory files) about tools, licenses, and
     ecosystem capabilities — evidence and a last-verified date, so a claim
     can be checked instead of trusted. This is the F-table half of
     ehr-testing-tools' notes/facts-register.md pattern (see that repo's
     AUTHORS-GUIDE.md section 4); there is no C-table here because there is
     no manuscript tracking drafting coverage. See notes/ADRs.md ADR-0003
     and AUTHORS-GUIDE.md section 4 for the assert -> register -> date
     discipline this file exists to support. -->

| # | Claim | Where asserted | Evidence | Last verified | Status |
|---|---|---|---|---|---|
| F1 | The HL7v2 ER7 parse/emit library's Maven coordinates are `org.clojars.cmiles74/clojure-hl7-parser`, version `3.5.1` — the Maven **group** is `org.clojars.cmiles74`, not the GitHub repo name (`cmiles74/clojure-hl7-parser`), a mismatch worth flagging since it's easy to get wrong when adding the dependency by hand. | `deps.edn` (`:deps` entry, with an inline comment noting the coordinates were verified), `.agents/memory/architecture.md` ("HL7v2 in Clojure") | The upstream project's own `project.clj` (`github.com/cmiles74/clojure-hl7-parser`), read directly per `.agents/memory/architecture.md`'s original verification session; not re-fetched this session | 2026-07-26 | verified (inherited from architecture.md; not re-verified this session) |
| F2 | Synthea ships 85 disease modules as plain JSON under `src/main/resources/modules/`, each a probabilistic state machine (Generic Module Framework) with real codes embedded inline — SNOMED CT (conditions), LOINC (labs), RxNorm (medications) — under the Apache-2.0 license (redistributable). Verified example: the asthma module has 52 states. | `.agents/memory/architecture.md` ("Synthea"), `docs/problem-statement.md` (Provenance, §"Glass-box auditability" — asthma module cited by name) | `github.com/synthetichealth/synthea`, `src/main/resources/modules/` (module count), `src/main/resources/modules/asthma.json` (state count) — read directly per architecture.md's original verification session; not re-fetched this session | 2026-07-26 | verified (inherited from architecture.md; not re-verified this session) |
| F3 | Google Simulated Hospital (`github.com/google/simhospital`) is licensed Apache-2.0 and is archived (no longer receiving source changes). Its `pkg/pathway` step vocabulary (~30 scripted step types including ADT churn — Transfer, BedSwap, TransferInError, Cancel\*, Pending\*, Merge, DeleteVisit) is the documented lineage of this repo's pathway IR step vocabulary. | `.agents/memory/architecture.md` ("Google Simulated Hospital"), `notes/ADRs.md` ADR-0002 (Context) | `github.com/google/simhospital` (license + archived status), `pkg/pathway` (step-type listing) — read directly per architecture.md's original verification session; not re-fetched this session | 2026-07-26 | verified (inherited from architecture.md; not re-verified this session) |
| F4 | CPT codes are AMA-licensed and excluded from this repo by constraint (never emitted); SNOMED CT and LOINC are free to use for US-context work — SNOMED CT via NLM/UMLS US-member access, LOINC under its own free license — alongside RxNorm, ICD-10-CM, and CVX. | `docs/problem-statement.md` (§3 "Standard terminology"), `AGENTS.md` (Constraints), `.agents/memory/architecture.md` (Terminology decisions) | AMA CPT licensing terms (`ama-assn.org`); NLM/UMLS SNOMED CT US Edition membership terms (`nlm.nih.gov`); LOINC's own free-use license (`loinc.org`) — per architecture.md's original verification session; not re-fetched this session | 2026-07-26 | verified (inherited from architecture.md; not re-verified this session) |
| F5 | The SNOMED CT to ICD-10-CM map is published by NLM as part of UMLS — the official U.S. mapping (the current map ships in the US Edition of SNOMED CT), built by dual independent mapping with third-expert adjudication of discordant mappings. Access requires a UMLS/UTS license (free for SNOMED CT in the US and free for ICD-10-CM in the US) — this is the pinned data artifact `docs/sim-theory.edn` names as `snomed-icd10-map`, the one sanctioned code translation (DG1). | `docs/third-party-sources.md` (Tier 1), `docs/sim-theory.edn` (`:emit-hl7` catalytic, `snomed-icd10-map`) | `nlm.nih.gov/research/umls/mapping_projects/snomedct_to_icd10cm.html`, fetched directly this session | 2026-07-26 | verified this session |
| F6 | Two independent-of-this-repo validation tool families exist for HL7v2 conformance, matching problem-statement.md claim #1's proof strategy: (a) NIST's HL7 v2 Conformance Testing portfolio (General Validation Tool, IGAMT, TCAMT) at `hl7v2tools.nist.gov`, offered as public-domain resources; (b) HAPI HL7v2 (`hapifhir/hapi-hl7v2` on GitHub), an open-source Java library for HL7v2 parsing. Neither is wired into this repo yet — both are named as Tier 2 validation/calibration anchors, future CI integration. | `docs/third-party-sources.md` (Tier 2), `docs/problem-statement.md` (Validation claim #1) | `nist.gov/itl/health-it-testing-infrastructure/testing-tools/hl7-v2-conformance-testing-tools`, `hl7v2tools.nist.gov`; `github.com/hapifhir/hapi-hl7v2` — fetched directly this session (HAPI's specific license terms were not confirmed by the fetch and are not asserted here) | 2026-07-26 | verified this session (HAPI license unconfirmed) |
| F7 | `resources/order-profiles.edn`'s CBC and BMP panel LOINC codes are real, active LOINC codes with the exact component structure encoded: CBC panel `58410-2` (component analytes used here: `6690-2` Leukocytes, `789-8` Erythrocytes, `718-7` Hemoglobin, `4544-3` Hematocrit, `777-3` Platelets — all `R`/required in the panel); BMP panel `51990-0` (component analytes used here: `2339-0` Glucose, `6299-2` Urea nitrogen, `38483-4` Creatinine, `49765-1` Calcium, `2947-0` Sodium, `6298-4` Potassium, `2069-3` Chloride, `20565-8` Carbon dioxide total — all `R`/required). LOINC is free to use (per F4); no CPT codes appear anywhere in the profile. | `resources/order-profiles.edn`, `docs/sim-theory.edn` (`order-profiles` catalytic) | `loinc.org/58410-2` and `loinc.org/51990-0` — each panel's own "Details for each LOINC in Panel" table, fetched directly this session via browser | 2026-07-26 | verified this session |
| F8 | The US Social Security Administration never assigns SSNs with area number (first three digits) `000`, `666`, or in the range `900`–`999` — these are permanently excluded from issuance (the `900`–`999` block being reserved for ITIN, not SSN, use). `ehr-testing-sim.persona/persona`'s synthetic SSN-shaped identifier uses a fixed area number of `900` for exactly this reason — obviously-synthetic by construction, the same design move ADR-0007 already made for provider NPIs (a real, documented never-issued range, not an arbitrary sentinel format), and stated as `persona.clj`'s own design-choice docstring. | `src/ehr_testing_sim/persona.clj` (`ssn-area-prefix` docstring) | `en.wikipedia.org/wiki/Social_Security_number` ("Numbers with 666 or 900–999 ... are never allocated"; "excluding area numbers 000, 666, and 900–999" from the 2011 randomization change) — fetched via browser this session; SSA's own `ssa.gov` pages returned HTTP 403 to this session's fetch tool and could not be checked directly, so this claim rests on a secondary source, not SSA's own site | 2026-07-26 | verified this session (secondary source only — SSA.gov itself unreachable this session) |
| F9 | `org.clojars.cmiles74/clojure-hl7-parser` 3.5.1 implements NO ER7 escape-sequence handling in either direction. WRITE side: `parser/pr-field`/`pr-content` concatenate field content into the wire string with no encoding step at all — a literal `\|^~&` character embedded in content is emitted raw. READ side: `parser/read-text`'s and `read-subcomponents`' escape-handling branches (which would call the library's own `read-escaped-text` function) are commented-out dead code in the library's own source, and `parser/delimiter?` doesn't even exempt the escape character (`\`) from ending a token early — so a literal delimiter character embedded in unescaped content corrupts field/component boundaries on parse, and even a value this repo escapes on write comes back from `message/get-field-first-value` STILL escaped (undecoded), never as the original. `ehr-testing-sim.emit-hl7/escape-er7`/`unescape-er7` are this repo's own documented workaround (encode on write, decode on read) — a Milestone M4 property test caught a related self-inflicted bug in the workaround's OWN first `unescape-er7` implementation (naive five-pass sequential replace let two adjacent encoded tokens spuriously spell a THIRD token at their boundary; fixed to a single regex pass). | `src/ehr_testing_sim/emit_hl7.clj` (the Task 4 escaping section, `escape-er7`/`unescape-er7` docstrings) | Read directly from the unpacked jar, `org/clojars/cmiles74/clojure-hl7-parser/3.5.1/clojure-hl7-parser-3.5.1.jar`'s own `com/nervestaple/hl7_parser/parser.clj` — `read-text` (comment block above its `delimiter?` cond-branch), `read-subcomponents` (same pattern), and `delimiter?` itself (the commented-out `:escape` check) — this session, not a secondary source | 2026-07-26 | verified this session (primary source: the dependency's own shipped .clj source) |
| F10 | SNOMED CT `36971009` is "Sinusitis (disorder)"; `185345009` is "Encounter for symptom"; `315618009` is "FESS - Functional endoscopic sinus surgery - sphenoethmoidectomy" (a procedure concept). Used in `test/ehr_testing_sim/fixtures/fixture-clinic.json` (M5a's hand-written GMF interpreter fixture, ADR-0013 point 6 — this project's own authored data, not vendored Synthea content, so no NOTICE obligation) for `ConditionOnset`/`Encounter`/`Procedure` state concept triplets. | `test/ehr_testing_sim/fixtures/fixture-clinic.json` | NCBO BioPortal SNOMED CT US Edition concept pages (`bioportal.bioontology.org/ontologies/SNOMEDCT?p=classes&conceptid=<id>`), confirmed by web search this session (no direct browser fetch — `browser.ihtsdotools.org` and `clinicaltables.nlm.nih.gov` both returned 403/unreachable to this session's fetch tools) | 2026-07-27 | verified this session (search-engine-mediated, not a direct primary-source fetch — see this row's own evidence note) |
| F11 | LOINC `8310-5` is "Body temperature" (component "Body temperature", `LP65487-8`). Used in `test/ehr_testing_sim/fixtures/fixture-clinic.json` for its `Observation` state's concept triplet. | `test/ehr_testing_sim/fixtures/fixture-clinic.json` | `loinc.org/8310-5` (title confirmed via web search this session; direct fetch returned HTTP 403) | 2026-07-27 | verified this session (search-engine-mediated) |
| F12 | RxNorm RXCUI `308191` is "Amoxicillin 500 MG Oral Capsule". Used in `test/ehr_testing_sim/fixtures/fixture-clinic.json` for its `MedicationOrder` state's concept triplet. | `test/ehr_testing_sim/fixtures/fixture-clinic.json` | RxNorm/NDC crosswalk data, confirmed via web search this session (direct fetch to `rxnav.nlm.nih.gov` timed out — DNS/network unreachable to this session's fetch tools) | 2026-07-27 | verified this session (search-engine-mediated, not a direct RxNav API fetch — see this row's own evidence note) |
| F14 | The standard FHIR/HL7 test-data security label is HL7 v3-ActReason code `HTEST`, display "test health data", CodeSystem `http://terminology.hl7.org/CodeSystem/v3-ActReason` -- the label `ehr-testing-sim.emit-state` stamps into `meta.security` on every resource it renders (post-M6, ADR-0014), so a real system that ever received this data could find and purge it by a standard security-label query. | `src/ehr_testing_sim/emit_state.clj` (`htest-security`) | `hl7.org/fhir/testing.html`'s own worked XML example (`<security><system value=".../v3-ActReason"/><code value="HTEST"/><display value="test health data"/></security>`), fetched directly this session | 2026-07-27 | verified this session |
| F18 | **`docs/simulate-your-facility.md`'s FAQ example config had real schema drift, corrected this session** (its own maintenance comment requires this check whenever schemas change; go-public's Task 2 is the first session to actually run it). Six mismatches against the live malli schemas: (1) `:facility`/ward `:id` were strings, schema wants `:keyword`; (2) a ward (`TELE`) omitted the required `:surge-format` field; (3) `:providers[].wards` referenced ward names as strings, schema wants a `[:vector :keyword]` of ward ids; (4) `:payers[].id` were keywords, schema wants `:string`; (5) `:pathways[].pathway` were bare name strings — **there is no named-pathway registry**; `ehr-testing-sim.run` validates `:pathways` directly against `ehr-testing-sim.pathway/PathwaysConfig`, so `:pathway` must be a full `{:name :steps}` pathway definition, the same shape `docs/demos/order-result/config.edn` already uses (`:modules`, by contrast, genuinely IS a vector of name strings — `ehr-testing-sim.run/resolve-modules` resolves those against `resources/modules/`, a real asymmetry now called out in the FAQ's own inline comment); (6) `:site-profile`'s `:code-tables` entries were bare strings, schema wants `{:code ... :coding-system?}` maps, and `:z-segments[].on`/raw-vector `:fields` should have been `:trigger` (a `:set`, not a vector) and `:fields` as `{:path ...}`/`{:literal ...}` maps — both confirmed against `docs/demos/site-profiles/config-aldric.edn`'s own real usage. The corrected example was then round-tripped through the actual live schemas (`ehr-testing-sim.config/valid-facility?`/`valid-provider-template?`, `ehr-testing-sim.persona/Payer`, `ehr-testing-sim.pathway/valid-pathways-config?`, `ehr-testing-sim.churn/valid-churn-profile?`, `ehr-testing-sim.site-profile/valid-profile?`) and confirmed valid, not just manually reasoned about. | `docs/simulate-your-facility.md` (the FAQ's own example config) | `src/ehr_testing_sim/config.clj`, `pathway.clj`, `persona.clj`, `churn.clj`, `site_profile.clj` (the schemas themselves); `src/ehr_testing_sim/run.clj` (`resolve-modules` docstring, confirming the pathway/module asymmetry); `docs/demos/order-result/config.edn` and `docs/demos/site-profiles/config-aldric.edn` (real usage examples the correction was checked against); a standalone script requiring the real namespaces and calling each schema's own `valid-*?`/`m/validate` function against the corrected config, run this session and removed after (not committed) | 2026-07-27 | verified this session |
| F19 | **Cold-clone smoke test, go-public Task 4.** A fresh `git clone` of this repo's own committed HEAD (`03e9178`, working tree clean, no sibling checkout needed) into a scratch directory, followed by `clojure -X:test`, passes end to end: **403 tests / 1058 assertions, 0 failures/0 errors, ~35s wall time** — a stranger's first `git clone` + `clojure -X:test` works with no setup beyond the Clojure CLI itself. Cache-isolation attempts, honestly reported: `HOME`-override isolation did not take effect for `.m2`/`.gitlibs` in this environment (this install's `clojure` script resolves `:config-dir` from `$HOME` but resolves the Maven/gitlibs cache elsewhere regardless — confirmed by `~/.m2`'s unchanged mtime after the run); `GITLIBS=<scratch>` DID isolate correctly (384K freshly populated, proving the one git-coordinate dependency — `io.github.cognitect-labs/test-runner` — resolves cleanly from public GitHub with zero pre-existing cache); `-J-Dmaven.repo.local=<scratch>` did NOT isolate the Maven-coordinate deps (the scratch dir stayed empty; resolution still hit the real `~/.m2`) — this session did not find a working full-isolation flag for the Maven side in the time available, so the Maven-coordinate portion of this claim rests on `deps.edn` inspection, not a forced from-scratch download: every dependency (`org.clojure/clojure`, `metosin/malli`, `org.babashka/cli`, `org.clojure/data.json`, `org.clojars.cmiles74/clojure-hl7-parser`, plus test-only `org.clojure/test.check`/`org.slf4j/slf4j-nop`/`cloverage`) resolves from Clojars or Maven Central, the Clojure CLI's own default `:mvn/repos`, both public and unauthenticated — no private registry, no credential, no sibling-repo dependency anywhere in `deps.edn`. | `deps.edn`; go-public session's own Task 4 | `git clone` of local HEAD into a WSL scratch dir; `clojure -X:test` run there (both a `HOME`-override attempt and a `GITLIBS`/`-Dmaven.repo.local`-override attempt); `du`/`find`/mtime checks against the real `~/.m2`/`~/.gitlibs` to confirm which isolation attempt actually took effect; `deps.edn` read directly for every dependency's coordinate/repo | 2026-07-27 | verified this session (partial isolation — see this row's own caveat) |
| F17 | **The 400/1052-Windows-vs-403/1058-WSL test-count discrepancy (recorded in the post-M6 session's own state) does not reproduce.** Re-run fresh, on the same HEAD commit (`03e9178`), `clojure -X:test` reports **403 tests / 1058 assertions, 0 failures/0 errors, identically on both Windows-side and WSL-side Clojure.** An independent enumeration (walking `test/` via `clojure.tools.namespace.find/find-namespaces-in-dir`, listing every var carrying `:test` metadata rather than trusting the runner's own summary line) produced the exact same 403-entry, alphabetically sorted list of fully-qualified test-var names in both environments — `diff --strip-trailing-cr` between the two runs' output is empty; the two environments' listings differ only in CRLF-vs-LF line endings, not content. A grep of `src/` and `test/` for environment-conditional test gating (`System/getenv`, `System/getProperty`, file-existence guards, `skip-when-absent` patterns) found none — the one place this repo ever had such a pattern (`blaze_integration_test.clj`'s skip-when-Blaze-absent guard) was already deleted by ADR-0014, before the discrepancy was even recorded. **Conclusion: the discrepancy was a session-timing snapshot artifact, not an environment-dependent test surface** — the Windows figure was almost certainly captured earlier in that session than the WSL pre-push-hook figure, before that session's final file writes landed, not evidence of three tests that exist in one environment and not the other. **Canonical count for docs/README, per the standing WSL-git-authority convention (AUTHORS-GUIDE.md section 1): 403 tests / 1058 assertions** — both environments now agree on this number, so the WSL-vs-Windows tiebreak this row was written to resolve turned out not to be needed. | `notes/facts-register.md` (this row); go-public session's own addendum item, folded into Task 2's counts pass | `clojure -X:test`, run independently on Windows-side and WSL-side Clojure against HEAD `03e9178`; a standalone `clojure.tools.namespace.find`-based test-var enumeration script, run in both environments and diffed (`diff --strip-trailing-cr`, exit 0 — no content difference); `grep` sweep of `src/`,`test/` for environment-conditional gating (none found) | 2026-07-27 | verified this session |
| F16 | LOINC's Copyright Notice and License (fetched live 2026-07-27) requires, for any product incorporating portions of the Licensed Materials (Group 1 Artifacts: LOINC codes/table), Section 10's exact notice text — reproduced verbatim in `NOTICE` (repo root) — plus pairing every extracted LOINC identifier with its own display name (this repo's `:display` field on every concept triplet already satisfies this). Bulk redistribution of the LOINC Table or other Group 1/2/3 Artifacts is not covered by that same permission and this repo does not attempt it; only individually-verified codes (F7, F11) are embedded. LOINC® and RELMA® are registered US trademarks of Regenstrief Institute, Inc.; Group 1 (codes/table) is copyright Regenstrief Institute, Inc. and the LOINC Committee. | `NOTICE` (repo root, LOINC section) | `https://loinc.org/license/`, fetched directly via browser this session (WebFetch returned HTTP 403 to this session's fetch tool; the in-app browser tool succeeded) — full "Copyright Notice and License" and "Notice of Third-Party Content and Copyright Terms" page text read in full | 2026-07-27 | verified this session |
| F15 | **Go-public Task 0 secrets/PII audit, pre-visibility-flip.** `gitleaks` v8.30.1 (binary from its GitHub release, SHA-256-verified against the published checksums file before use) found zero leaks scanning this repo's full `git log --all` history (24 commits) and zero leaks scanning its on-disk working tree (`--no-git` mode, ~14.7 MB incl. caches). The same binary found zero leaks across the full reachable history of the public `pragsmike/packs` repo (24 commits — all four sibling pack files: `ehr-testing-sim-pack.txt`/`-skills-pack.txt`, `ehr-testing-tools-pack.txt`/`-skills-pack.txt`), fetched via a fresh anonymous `git clone`. A supplementary `git log -p --all` / `git grep` regex sweep (personal emails, the local Windows username, `C:\Users\...` and non-`/mnt/c` absolute paths, private hostnames, secret-shaped keywords, credential-shaped filenames) over both repos' full history surfaced nothing actionable: one legitimate NIST public point-of-contact email (`robert.snelick@nist.gov`) appears in `ehr-testing-tools`' own pack content — already reviewed and deliberately retained (not redacted) by that repo's own 2026-07-24 history-reset/redaction session, which this scan's own commit-log read confirmed is the true root of `packs`' currently-reachable history; out of scope for this repo's own audit. This repo's own commit/pack content contains no personal email, no local username, no private hostname, and no credential anywhere in its reachable history. | `notes/facts-register.md` (this row); the go-public session's own Task 0 STOP-AND-REPORT gate | `gitleaks version` → `8.30.1`; `gitleaks detect` (git-history and `--no-git` modes) against this repo and against a fresh clone of `github.com/pragsmike/packs`; supplementary `git log -p --all`/`git grep` regex sweep over both — commands and raw JSON/log output retained in this session's own scratch directory, not committed to either repo | 2026-07-27 | verified this session |
| F13 | **SUPERSEDED** — test removed per ADR-0014; row retained for history, per this register's own append-only discipline. This repo's own development/test JVM (as invoked by `clojure -X:test` in the M6 session's own environment) is JDK 8 (`1.8.0_311`) — `java.net.http.HttpClient` (JDK 11+) is therefore unavailable; `test/ehr_testing_sim/blaze_integration_test.clj`'s own HTTP calls used `java.net.HttpURLConnection` (JDK 1.1+) instead, for exactly this reason. This was an observation about ONE session's own runtime, not a repo-wide pinned minimum-JDK claim (`deps.edn` states no `:min-jdk` and this repo has no CI yet to enforce one) — recorded so a future session doesn't reintroduce `java.net.http` without re-checking, should this repo ever grow another JDK-version-sensitive HTTP call site. | `test/ehr_testing_sim/blaze_integration_test.clj` (deleted; its own header docstring, while it existed) | `(System/getProperty "java.version")`, run directly the M6 session | 2026-07-27 | superseded 2026-07-27 (test removed, ADR-0014) |
| F20 | **Public-polish session test count, superseding F17's own 403/1058 for current-count purposes (F17 itself is untouched, append-only — it remains the correct historical record for HEAD `03e9178`).** After this session's Task 1 (`--format edn\|json\|er7`) and Task 2 (version identity: `ehr-testing-sim.version`, `sim version`/`--version`, manifest honesty), `clojure -X:test` reports **426 tests / 1101 assertions, 0 failures/0 errors** on Windows-side Clojure (this session's own development environment) — 23 new tests / 43 new assertions over F17's baseline, all in `cli_test.clj`, `manifest_test.clj`, and the new `version_test.clj`. Coverage 97.15%/98.70% forms/lines (up from the M6 baseline 96.39%/98.34% cited at go-public). The pinned regression fixture is untouched (confirmed via `git status`/`git diff` over `test/ehr_testing_sim/fixtures/` — this session's entire diff is `cli.clj`/`manifest.clj`/new `version.clj` plus tests and docs, no engine/RNG-path file touched). `SETUP.md`'s own maintenance comment requires citing the current count and re-running its verification ladder whenever a session materially edits it; this is that update. | `SETUP.md` (verification ladder), `README.md` (badge area, implicitly) | `clojure -X:test`, run directly this session on the working tree described above | 2026-07-27 | verified this session |
| F21 | **Follow-up session (same day): `--format ground-truth`, closing the `sim run \| sim check` gap F20's own session had only worked around in docs.** `ehr-testing-sim.cli` gained a third bare format (alongside `er7`): `ground-truth` renders the run's own `:ground-truth` vector, bare EDN, to stdout — exactly the shape `check-command` requires on stdin — gated the same pre-dispatch way `er7` requires `--emit hl7` (here: requires the `run` verb; any other action is a structured `:rejected`, never a silent no-op). `clojure -X:test` now reports **432 tests / 1116 assertions, 0 failures/0 errors** (6 new tests / 15 new assertions over F20's own count, all in `cli_test.clj`, including a CLI-to-CLI pipe regression test — `run-then-check-cli-pipe-round-trips`/`-for-any-seed` — that chains two real `main!` invocations via `with-in-str`, not just the two capability functions called directly). Coverage 97.36%/98.99% (up from F20's 97.15%/98.70%). The pinned regression fixture remains untouched (this change touches only `cli.clj` and its own tests/docs). README.md's and SETUP.md's Quick Start lines updated to the real, now-working `sim run --format ground-truth \| sim check` pipe, replacing F20's own workaround (`check < docs/demos/persona-enriched/ground-truth.edn`, which still works and is still documented as an alternative for checking a log from anywhere). | `README.md`, `SETUP.md` (Quick Start), `src/ehr_testing_sim/cli.clj` | `clojure -X:test` and `clojure -M:coverage`, run directly this session; the documented pipe re-run live via the real standalone CLI (two subprocesses, not in-process) | 2026-07-27 | verified this session |
