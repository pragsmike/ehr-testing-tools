2026-07-30 — judge-v2-nist landing: NIST profile-tier v2 judge from the Cowork skeleton (direct engine, no CDC wrapper)

Repo: `github.com/pragsmike/ehr-testing-tools`, main at latest (probe first). Autonomous session per R30: commit AND push at each checkpoint; hooks gate; tags are the author's alone. Ask nothing mid-session — decision procedures below; skip-and-report anything they don't cover. Do not name the author's machines, usernames, or local filesystem paths outside generic forms (`~`, `/mnt/c/...`) in anything committed.

Context

A cloud Cowork session (2026-07-30) spiked the NIST v2-validation engine 1.7.3 directly from Clojure — execution-verified, not read-verified — and produced a component skeleton. Deliverables sit in `~/Downloads`: `judge-v2-nist-skeleton_tar.gz` (component: `deps.edn`, `src/ehrt/judge_v2_nist/{interface,v2}.clj`, `test/ehrt/judge_v2_nist/v2_test.clj`, plus `spike.clj` and `NOTES.md`, which are session artifacts, NOT component files) and a standalone copy of `NOTES.md`. The spike's headline: `hl7.v2.validation.SyncHL7Validator` is a Java-friendly synchronous API — `ValidationContextBuilder` accepts the Π bundle piecewise, `.check(message, msgId)` returns `gov.nist.validation.report.Report`, and the only Scala surface crossed is one `CollectionConverters/asJava` call at validator construction. This flips the platform research doc's §D.5 recommendation (CDC wrapper as integration point): the wrapper's ERROR/WARNING filtering discards the Affirmative/Informational/Spec-Error signal the verdict policy and the mutate↔judge loop need. The skeleton has NO `gov.cdc` dependency — CDC appears only as the Π fixture source (their Apache-2.0 test resources), the `VALUSETBINDINGS.xml` spelling shim, and a pattern citation. The design channel verified this by reading every file (2026-07-30).

Standing facts this session builds on, all already in the tree: the six NIST jar sha256s are in `artifacts.lock.edn` (EXP-D3, acquired 2026-07-29, `:use-permitted--unstated--confirmation-pending`); ADR-0005's 2026-07-24 amendment rules this repo redistributes NONE of them (user-initiated fetch from hit-nexus); the IGAMT registration disclaimer is in the facts register. Nothing in this session changes that licensing posture.

Rulings

* [A] Direct engine adoption: the three `gov.nist` coordinates (`hl7-v2-validation`, `hl7-v2-parser`, `hl7-v2-profile`, all 1.7.3) resolve from hit-nexus. No `gov.cdc` dependency, ever; the CDC wrapper remains a worked example cited in docs only.
* [A] NIST-originated code is treated as public domain for now; the pending inquiry (EXP-SBOM-inquiry-draft) stays open and this session neither cites it as resolved nor waits on it. No jars are vendored into the repo (ADR-0005 amendment stands). Jars live in the local `~/.m2` cache; a mirror/fork plan is deferred to next month — do not block on it, do not build it. Small things ARE mirrored: the Π fixture files are committed (they are CDC's Apache-2.0 test resources, not NIST jars — the amendment does not cover them and Apache-2.0 permits redistribution with attribution).
* [A] msg-id contract: no implicit default when a profile declares more than one message id. Single-id profile → that id, no `:msg-id` needed. Plural ids and no explicit `:msg-id` → refuse. Sorting as a default-picker was considered and rejected by the author. Mechanism per decision procedure below.
* [A] Grow `ehrt.judge.finding/Cause` to `[:enum :terminology-suppressed :profile-spec-error]`, co-landed with this judge (the enum change and its first consumer in one change, per co-landing discipline). `judge-v2-nist` then returns `:cause :profile-spec-error` directly for Specification Error captures; the `:proposed-cause` rider in the skeleton is deleted, and its test updated to assert the real cause.
* [A] CI/network: assume hit-nexus is reachable this session. If it is not, stop at the last green checkpoint and report — no workarounds, no vendoring.
* [A] Fixture provenance: the committed Π fixture is CDC's `COVID19_ELR-v2.3.1` bundle plus one companion ER7 test message, taken from the author's local clone under `~/Documents/NIST/lib-hl7v2-nist-validator/src/test/resources/`. It lands with a `NOTICE.md` in the fixture directory recording: source repo URL, the clone's HEAD commit sha, Apache-2.0, per-file sha256s, and the sentence that this is a stand-in until a project-authored IGAMT export replaces it (the IGAMT disclaimer's derived-from notice obligation is the eventual vendored-`:profile` requirement — cite the facts-register row).

Read first

`~/Downloads/NOTES.md` (the spike record — its "Wiring into the workspace" section is this session's backbone; where it and these rulings differ, the rulings win); the unpacked tarball, every file; `AGENTS.md`; `notes/ADRs.md` — ADR-0005 and its 2026-07-24 amendment, ADR-0010 (verdict ranking), ADR-0011 (per-engine judge split); `components/judge/src/ehrt/judge/finding.clj` (Cause enum, Finding/verdict schemas); `components/judge-v2-hapi/` in full (interface docstring conventions, `v2.clj` invocation-record discipline, `deps.edn` shape, test conventions incl. fixture paths relative to workspace root) — this is the sibling the new component must be indistinguishable from in discipline; `components/tools/src/ehrt/tools/interface.clj` (the ADR-0011 re-export layer and its qualification comments); `artifacts.lock.edn` (the six NIST entries — read the `:license-note` strings); `components/tools/docs/experiments/EXP-D3-results.md` and `experiments.md` (results-are-classification-only rule); `components/tools/docs/research/NIST-HL7v2-dev-test-platform.md` §D.5; `docs/engine-onboarding.md` if present (the skeleton's docstring cites it — if absent, note in the deviation record and drop the citation from the docstring).

Steps

1. Probe and baseline. Fresh state on the WSL ext4 clone (not `/mnt/c` — SETUP.md Troubleshooting), `git status` clean, `clojure -M:poly check` and the full test suite green BEFORE any change; record the baseline counts. Unpack the tarball to a temp dir. No commit.
2. Archive the session artifacts (provenance before code). `NOTES.md` → `components/tools/docs/research/judge-v2-nist-spike-notes.md`, verbatim plus a dated one-line header naming its origin (Cowork cloud session, 2026-07-30) and this prompt as the landing vehicle. `spike.clj` → same directory as `judge-v2-nist-spike.clj`, with a header comment: archived as evidence, contains cloud-session absolute paths, not runnable here. If the research dir's index/README enumerates files, add both. Commit: `docs: archive judge-v2-nist spike record and script (Cowork 2026-07-30, pre-landing provenance)`
3. Grow the Cause enum. `ehrt.judge.finding/Cause` → `[:enum :terminology-suppressed :profile-spec-error]`; update the docstring's specimen note (second live specimen: NIST Specification Error — Π itself defective, criterion not fully applicable). If `judge` component tests enumerate Cause values, extend them. Do not commit yet — co-lands with step 4.
4. Land the component. `components/judge-v2-nist/` from the tarball: `deps.edn`, `src`, `test` only (no `spike.clj`, no `NOTES.md`). Edits to the skeleton before first commit: a. Delete the `:proposed-cause` machinery: `interpret`'s Specification Error branch returns `{:verdict :no-verdict :cause :profile-spec-error}`; update `spec-error-is-no-verdict-proposing-new-cause` to `spec-error-is-no-verdict-with-profile-spec-error-cause` asserting `:cause`. b. msg-id contract per the ruling: `execute` with no `:msg-id` and `(> (count msg-ids) 1)` refuses per the decision procedure below; add a test for the plural-refusal path and one for the single-id default path (synthetic validator-state maps suffice — `execute` destructures plain keys). c. Docstring hygiene: the component docstring's "ADR-open: adoption decision" note now cites the ADR written in step 8; fix the `docs/engine-onboarding.md` citation per Read-first. Root `deps.edn`: add `:mvn/repos {"nist-hit" {:url "https://hit-nexus.nist.gov/repository/releases/"}}` at the top level (root-only — tools.deps ignores component-level `:mvn/repos`); `poly/judge-v2-nist {:local/root "components/judge-v2-nist"}` in `:dev` AND `:ehrt`, matching the sibling entries' placement and comment style; `"components/judge-v2-nist/test"` in `:test` `:extra-paths`. Gate: `clojure -M:poly check` clean; component unit tests green (pure `interpret` — no engine needed on classpath for these, but resolution will occur; that is fine per the CI ruling). Commit: `feat: judge-v2-nist -- NIST profile-tier v2 judge, direct engine (ADR-0011 third sibling; Cause grows :profile-spec-error, co-landed)`
5. Verify jar resolution against the lockfile (verification, not recording — the sha256s are already there from EXP-D3). Locate the six resolved jars in `~/.m2` (three direct `gov.nist` 1.7.3 + transitive `gov.nist:xml-util:2.1.0`, `gov.nist.hit:hl7-v2-schemas`, `com.github.hl7-tools:validation-report` at the lockfile's versions); sha256 each; compare against `artifacts.lock.edn`. All match → append a dated verification line to each entry's `:license-note` or (if the lockfile has a cleaner convention for re-verification) follow it. Any mismatch → STOP THE SESSION at the previous checkpoint and report jar-by-jar: this is either supply-chain drift or a lockfile error, and per AUTHORS-GUIDE §7 distinguishing which is the author's call, not yours. Commit: `chore: artifacts.lock.edn -- re-verify six NIST jar sha256s at judge-v2-nist landing (resolution path: hit-nexus)`
6. Commit the Π fixture and the engine-in-the-loop test. Copy `COVID19_ELR-v2.3.1/` (all profile-bundle XML files as shipped, including `VALUESETS-disabled.xml` — do not rename or "fix" it; the suppressed-VS verdict path depends on it) and ONE companion ER7 message from the CDC clone's test resources into `components/tools/test-fixtures/v2-nist/`, with `NOTICE.md` per the ruling. Then a new test namespace (`v2_engine_test.clj` or the sibling-consistent name) that: builds the validator from the fixture dir via `make-validator`, asserts the bundle-sha256s map covers exactly the files present, gates the message, and asserts verdict, cause, and per-area finding counts. Expected from the spike: 473 findings (structure 441, value-set 28, content 4) and `:no-verdict / :terminology-suppressed` — but the spike wired only PROFILE+CONSTRAINTS; `make-validator` wires everything present, so MEASURE FIRST, then pin the measured numbers in the test with a provenance comment (engine 1.7.3, fixture sha256s, this session's date). The numbers are stable because both engine and fixture are hash-pinned; if a number differs from the spike's, record the delta and the wiring difference in the deviation record — that is a finding about the spike's partial wiring, not a defect. Commit: `test: judge-v2-nist engine-in-the-loop -- CDC COVID19_ELR Pi fixture (Apache-2.0, NOTICE'd), measured verdict pinned`
7. Tools-layer re-exports. `ehrt.tools.interface`: require `ehrt.judge-v2-nist.interface`, re-export as `v2-nist-make-validator`, `v2-nist-gate-file`, `v2-nist-gate-dir`, in the ADR-0011 comment block's style, with one comment line noting the signature difference from `v2-gate-file` (these take validator-state; Π is an input at this tier and context construction dominates cost — build once per bundle, reuse). If the CLI/help layer (`ehr help`, `docs/cli.md`) enumerates gates, add the entry in matching style; if that expansion balloons (new subcommand plumbing), skip it, note it as follow-on work in the deviation record, and keep the interface re-export only. Commit: `feat: tools interface -- v2-nist gate re-exports (profile tier; validator-state signature noted)`
8. ADR. Next free number, chronological placement per the ADRs file's own rules: "judge-v2-nist adopts the NIST engine directly." Decisions recorded: direct `SyncHL7Validator` over the CDC wrapper and why (report filtering discards verdict-policy signal; version-drift smell; hit-nexus makes coordinates resolvable — supersedes the platform research doc's §D.5 recommendation); msg-id explicit-when-plural (sorting-as-default rejected); Cause grows `:profile-spec-error` (second specimen); fixture stand-in status and its NOTICE obligations; no-vendor posture reaffirmed (ADR-0005 amendment), mirror/fork deferred, dated. Interpretation lives here, not in any results file. Commit: `docs: ADR-00XX -- judge-v2-nist direct-engine adoption (msg-id contract, Cause growth, fixture provenance, no-vendor reaffirmed)`
9. Research-doc erratum, fix-forward. `components/tools/docs/research/NIST-HL7v2-dev-test-platform.md`: dated erratum block (matching the file's existing erratum style if one exists, else a clearly-marked dated addendum at the section) at §D.5 noting the recommendation is superseded — direct engine via `SyncHL7Validator`, per the archived spike notes and the new ADR. No body rewrite. Commit: `docs: research erratum -- D.5 CDC-wrapper recommendation superseded by direct-engine spike (dated, fix-forward)`
10. Gate and close. `clojure -M:poly check`; FULL test suite (src changed); `make integration` if the pre-push discipline expects it for src changes; push. Self-archive this prompt to `notes/prompts/2026-07-30-ehr-testing-judge-v2-nist-landing.md` with a deviation-record appendix capturing: every decision-procedure branch taken, the measured step-6 numbers vs the spike's, the msg-id mechanism chosen and its precedent, anything skipped. Commit: `docs: archive judge-v2-nist landing prompt with deviation record`

Decision procedures

* msg-id refusal mechanism (step 4b): read how `judge-v2-hapi` and `judge-fhir-official` treat caller-contract violations (as opposed to engine/data failures, which are values per result-not-throw). Follow that precedent. If no precedent exists: throw `ex-info` from `execute` with `{:type :ambiguous-msg-id :msg-ids [...]}` — a caller error is a programming defect, not an engine verdict, and must not masquerade as `:rejected` or `:no-verdict` — and record the choice as needing author ratification in the deviation record.
* Fixture absent at `~/Documents/NIST/lib-hl7v2-nist-validator/...`: clone `github.com/CDCgov/lib-hl7v2-nist-validator` fresh to a temp dir, pin and record the HEAD sha in `NOTICE.md`, proceed. If the fixture layout differs from the spike's description (file names, disabled-VS file), take what is actually there and record the difference.
* hit-nexus unreachable at any resolution point: stop at the last green checkpoint, report which step, push what is committed. Do not vendor, do not mirror, do not retry with altered TLS settings.
* `VALUSETBINDINGS.xml` (one E) present in the fixture: the component already accepts both spellings — commit the fixture as shipped, do not rename.
* Research-dir index conventions differ from step 2's assumed filenames: follow the index, record the actual paths.
* Any unexpected `git status` dirt at any point: STOP that step, report the file list.
* Cause enum consumers beyond `finding.clj` and the fixed tests (grep for `:terminology-suppressed` and `Cause` workspace-wide before step 3): if a consumer hard-codes the enum's arity or values (schema round-trip tests, docs tables), update it in step 3's commit; if updating it would mean touching frozen `notes/tools/**`, cite-don't-edit and note it.

---

## Deviation record

**Every decision-procedure branch, evaluated:**

1. **msg-id refusal mechanism.** No precedent found: `ehrt.judge-v2-hapi` and `ehrt.judge-fhir-official` are both throw-free (`grep -rn ex-info` against both component directories returned zero hits) — neither has a concept requiring caller disambiguation of this shape. `judge-fhir-official.fhir/gate-batch` returns a `kernel/error :batch-attribution-missing` *value* for a validator-contract violation, but that is the engine's own contract (the subprocess's Bundle output), not a caller-supplied-ambiguous-argument case, so it wasn't treated as the same precedent. Followed the decision procedure's own fallback: `execute` throws `ex-info` with `{:type :ambiguous-msg-id :msg-ids [...]}` when the profile declares more than one msg-id and no explicit `:msg-id` is given. Flagged in ADR-0012 for author ratification, per the procedure's own instruction — this is a genuinely new caller-contract pattern for this workspace's judge engines, not a settled convention.
2. **Fixture-absent fallback.** NOT triggered. The fixture existed exactly where the ruling assumed
   (a `~/Documents` path, which on this machine is a symlink to the Windows-side Documents folder,
   containing a real, already-cloned `CDCgov/lib-hl7v2-nist-validator` checkout at HEAD
   `eeac90c5f88dca3018992005232acdf3da644d88`, remote confirmed `https://github.com/CDCgov/lib-hl7v2-nist-validator.git`). No fresh clone was needed.
3. **hit-nexus reachability.** Reachable throughout — the three direct `gov.nist` coordinates and their three transitives (`xml-util`, `hl7-v2-schemas`, `validation-report`) all resolved cleanly on first attempt, twice more on later `poly check`/`poly test` runs, and a fourth time during the jar re-verification step. No STOP condition fired.
4. **`VALUSETBINDINGS.xml` spelling variant.** Not present in this fixture at all — the vendored `COVID19_ELR-v2.3.1` bundle ships only `PROFILE.xml`, `CONSTRAINTS.xml`, and `VALUESETS-disabled.xml`. The component's dual-spelling acceptance (`VALUESETBINDINGS.xml` / `VALUSETBINDINGS.xml`) remains untested by this fixture — noted, not fixed, since fabricating a bindings file just to exercise that branch would be inventing fixture content this ruling didn't ask for.
5. **Research-dir index conventions.** No index or README exists under `components/tools/docs/research/` (confirmed by `find`) — step 2's assumed filenames were used as-is, nothing to reconcile.
6. **Unexpected `git status` dirt.** One genuine hiccup, self-caused and self-caught, not a repo-state surprise: this session initially edited `deps.edn` and `components/judge/src/ehrt/judge/finding.clj` via the native-Windows (`/mnt/c/...`) mount instead of the WSL ext4 clone (`~/src/ehr-testing-tools`) all git operations and builds actually ran against. Caught immediately (a `grep` on the ext4 clone came back empty), the edits were copied into the correct clone, and the stray `/mnt/c` copies were reverted with `git checkout --` (safe, since the content was already duplicated correctly in the clone of record and the `/mnt/c` tree carries no independent git operations of its own). No data was lost; no commit was made from the wrong clone. Recorded here as a genuine session-management lesson, not a decision-procedure branch this prompt named — worth a standing note for any future session splitting file edits (fast, native-path Reads/Writes) from git/build operations (WSL-only) across two separate clones of the same repo.
7. **Cause enum consumers beyond `finding.clj`.** Grepped for `:terminology-suppressed` and `Cause` workspace-wide before step 3: 8 files matched (`judge-fhir-official/{fhir,test/fhir_test}.clj`, `bases/cli/test/.../core_test.clj`, `judge/test/{verdict_cache,report,finding}_test.clj`, `judge/src/report.clj`). Every hit uses `:terminology-suppressed` as a specimen value or type-checks `[:cause {:optional true} finding/Cause]` — none hard-codes the enum's arity or full value set. Nothing needed updating; none fall under frozen `notes/tools/**`.

**Measured step-6 numbers vs. the spike's own `NOTES.md`.** Finding counts matched exactly: 473 total (structure 441, value-set 28, content 4) — same wiring as the spike (only `PROFILE.xml`+`CONSTRAINTS.xml` get wired; `VALUESETS-disabled.xml`'s name deliberately doesn't match `bundle-files`' `:value-sets` spelling, so it's never picked up — confirmed by inspecting the measured `bundle-sha256s` map, which carries exactly `{:profile ... :constraints ...}`, two keys). The verdict/cause did NOT match the spike's own prediction: spike notes said `:no-verdict/:terminology-suppressed`; measured is `:no-verdict/:profile-spec-error`. This is not a wiring difference — it is a direct, expected consequence of this same session's own Cause-growth work (step 3/ADR-0012): the spike's own code returned `:terminology-suppressed` for Specification-Error captures only as a placeholder, because `:profile-spec-error` didn't exist in the shared enum yet at spike time (it carried a `:proposed-cause` rider alongside for exactly this reason). `interpret` now returns the real cause directly. Recorded in the pinned test's own docstring and in ADR-0012's deviation record as well, so this isn't only findable here.

**Anything skipped, and why:**

- **CLI/help.clj gate-verb expansion** (step 7's own permitted skip): a real `gate v2-nist` verb needs a profile-bundle-dir flag this tier doesn't share with `gate v2`/`gate fhir` (both take a bare PATH) and validator-state reuse across one CLI invocation — real design work, not a re-export. Skipped; named as follow-on in ADR-0012.
- **`make integration`** (step 10): NOT run. `notes/ADRs.md` ADR-0003 is explicit doctrine that tests — including the integration lane — are deliberately excluded from the pre-push gate (irreversibility-only: WSL provenance, `gitleaks detect`, `poly check`); ADR-0004/R18-19 (ENF-1) keeps the integration lane scheduled/`workflow_dispatch`-only, never per-push, specifically because it depends on artifact-fetch machinery (synthea, temurin-jdk, fhir-validator-cli) unrelated to anything this session touched. Nothing this session added lives in `projects/integration`'s own test tree. Ran the actual per-push-equivalent gate instead: `clojure -M:poly check` (green) and `clojure -M:poly test :all skip:integration` (green, 181 namespaces, 0 failures/0 errors, exit code captured directly to a file per this workspace's own tail-masks-exit-code lesson — never piped).
- **Mirroring the six jars into a `file://` repo** (CDC's own pattern, named in the spike's own notes): explicitly deferred per this prompt's own ruling ("deferred to next month — do not block on it, do not build it"). Not built, not scheduled.
- **A separate `.agents/session-records/` entry**: this session's own step 10 named only the prompt-archival-with-deviation-record as the close-phase deliverable (unlike the judge-engine-extraction sibling session, which also wrote one) — not added, to keep this session's own deliverables literal to what was asked rather than assumed from a sibling's precedent.

**Everything else in the numbered steps executed exactly as written**, each gated by a green `clojure -M:poly check` (and, where a build step touched dependencies, a green component/full test run) before its own commit, and pushed immediately after each commit per the R30 standing mode.
