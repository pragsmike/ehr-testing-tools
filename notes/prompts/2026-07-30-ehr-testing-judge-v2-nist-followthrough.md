# 2026-07-30 — judge-v2-nist follow-through: msg-id ex-info ratified, `:mvn/repos` fact registered, ADR citation-space disambiguation (docs-only)

Repo: `github.com/pragsmike/ehr-testing-tools`, main at `6f2ea56` or later (probe first). Autonomous session per R30: commit AND push at each checkpoint; hooks gate; tags are the author's alone. Ask nothing mid-session — decision procedures below; skip-and-report anything they don't cover. Do not name the author's machines, usernames, or local filesystem paths outside generic forms (`~`, `/mnt/c/...`) in anything committed. Docs-only session: no `src/` changes, no test changes; per ADR-0003 the gate is `clojure -M:poly check` plus the push hooks — the full suite is not required unless a step surprises you into touching a brick.

## Context

Three riders from the 2026-07-30 judge-v2-nist landing (`6f2ea56`), all author-ruled in the design channel after independent verification of that session's output.

First, the landing flagged the msg-id `ex-info` mechanism for author ratification (ADR-0012, deviation record item 1) because no sibling judge engine had a caller-contract-violation precedent. The channel's verification found the precedent one level up: `AGENTS.md`'s own Result-not-throw bullet already ends "Exceptions are for programmer error only." An ambiguous `:msg-id` — the caller failing to disambiguate a plural-id profile — is a programming defect in the call, not an engine or data outcome, so the mechanism was doctrine-consistent all along; the sibling grep just looked one layer too low. The author ratifies.

Second, the landing discovered that root `deps.edn`'s `:mvn/repos` does not propagate to `projects/*/deps.edn` under `poly test :all` — each project is its own tools.deps resolution root, so all three projects needed their own `nist-hit` entry. That is a load-bearing, externally verifiable fact about the build that will bite again the first time any other non-Central coordinate lands; it belongs in the workspace facts register, not only in a deviation record.

Third, workspace ADR-0012 now collides in citation space with the frozen tools-era ADR-0012 (`notes/tools/ADRs.md`, the `ehr sim` mount design), which workspace ADR-0005 cites unqualified throughout its body ("ADR-0012's five properties, exercised", etc.) — unambiguous when written, ambiguous since the landing. The author considered renumbering and rejected it: numbers are load-bearing in immutable places (commit messages, archived prompts, the landing's own `v2.clj` docstrings), and the register's own rule — no ADR number is ever reassigned once used — exists for exactly this reason. The fix is the citation-qualification rule the 2026-07-29 hygiene session already established for frozen facts-register rows, promoted to a standing rule for ADRs and applied fix-forward to the stragglers.

## Rulings

* [A] The msg-id `ex-info` mechanism is RATIFIED, on the doctrinal basis of `AGENTS.md`'s Result-not-throw carve-out ("Exceptions are for programmer error only"). The general boundary, stated once so future engines never re-litigate it: engine and data outcomes are values (findings, verdicts, `:check-exception` captures, `kernel/error` results); caller-contract violations are programmer error and fail fast via `ex-info` with descriptive data. ADR-0012 carries the generalization; `AGENTS.md` gets at most one appended clause citing ADR-0012 as the worked example.
* [A] Renumbering is REJECTED (recorded, so the question is settled, not merely unasked). Standing citation rule instead: a bare `ADR-00XX` in any workspace-register document means the workspace `notes/ADRs.md` record; frozen-era ADRs are always cited origin-qualified (`notes/tools/ADRs.md ADR-00XX`, `notes/sim/... ADR-00XX`). The rule lands in `notes/ADRs.md`'s own header/preamble; existing unqualified frozen-era references are qualified fix-forward, dated — no body rewrites, no renumbering, and frozen files under `notes/tools/**` and `notes/sim/**` are never edited.
* [A] The `:mvn/repos` discovery enters the workspace facts register as a new F-row (next free number), with the Index table updated in the same commit per the register's own header rule.

## Steps (as executed)

1. Probe and baseline. Fresh state on the WSL ext4 clone, `git status` clean, `clojure -M:poly check` green. No commit.
2. Ratification. ADR-0012's Status line updated to record author ratification (`[A]`, ADR-0007 provenance tag), dated 2026-07-30; a dated ratification note appended immediately after the flagged msg-id paragraph, stating the doctrinal basis and the general boundary verbatim from the ruling. `AGENTS.md`'s Result-not-throw bullet got one appended clause (em-dash continuation, not a new bullet) naming caller-contract violations as the programmer-error case and citing ADR-0012. Commit `418001b`.
3. Facts-register row F9 (next free number — F8 was the last existing row). Index sub-table and full Register both updated in the same commit, evidence citing the three `nist-hit` `deps.edn` entries at `6f2ea56`, the landing session's own deviation record, and Clojure's own `deps.edn` reference doc (`https://clojure.org/reference/deps_edn`, `:mvn/repos` section: *"Repositories declared in dependency poms and deps.edn are not used - all necessary repositories must be defined in the top deps.edn file being used"*) as supportive documented evidence, cited alongside the session's own observed behavior as primary evidence for the Polylith-specific consequence (the doc doesn't speak in per-project-invocation terms itself). Commit `94c07af`.
4. Citation-space disambiguation, three edits plus two found-in-passing fixes: (a) `notes/ADRs.md` preamble — added the standing citation rule, motivated by the ADR-0012 collision and the renumbering rejection, dated and citing this session; (b) ADR-0005's body — every unqualified tools-era `ADR-0012`/`ADR-0013` reference origin-qualified to `` `notes/tools/ADRs.md` ADR-00XX ``, matching line 1051's own pre-existing style (see deviation record for the two references left deliberately inside verbatim quotations, handled via a bracketed editorial note plus the file's own already-qualified next sentence); (c) workspace ADR-0012's head — one dated note recording the number collision and pointing at the preamble rule. Found in passing while reading `projects/conformance/deps.edn` for the F9 row's evidence: two more unqualified tools-era `ADR-0013` references in its own comments, qualified in the same commit per the decision procedure's own instruction to fix such stragglers when found. Commit `8155fa5`.
5. Gate and close. `clojure -M:poly check` green throughout, `git status` clean at every checkpoint. Push after every commit. This file.

## Deviation record

**Every decision-procedure branch, evaluated:**

1. **Genuinely ambiguous ADR-0005 reference (STOP condition).** Not triggered inside ADR-0005 itself — every unqualified `ADR-0012`/`ADR-0013` reference in ADR-0005's body reads unambiguously as tools-era once the surrounding sentence is read (all of ADR-0005 is about the CLI mount design, topically distinct from judge-v2-nist), so all were qualified directly rather than flagged. A genuinely ambiguous instance was found *elsewhere*, not in ADR-0005 — see item 4 below.
2. **Other workspace documents citing frozen-era ADR numbers unqualified, found during this session's reads.** `projects/conformance/deps.edn` carried two: `;; poly/sim: ADR-0013's subprocess-only rule is superseded` and `;; ... the ADR-0013-era reasons ...`. Both are unambiguously the frozen `notes/tools/ADRs.md` ADR-0013 (same subprocess-only rule ADR-0005 discusses at length) — qualified in step 4's commit per the decision procedure's own instruction. `projects/integration/deps.edn` and `projects/ehrt-cli/deps.edn` were also grepped; `integration`'s carries no ADR-0012/0013 references at all, and `ehrt-cli`'s carries only workspace-native bare `ADR-0012` citations about `judge-v2-nist` itself (correct by the new rule, left untouched) — except one genuinely ambiguous line, item 4 below.
3. **Facts-register next-free-number / Index drift.** No drift found: F8 was the last row and the last Index entry, in agreement. F9 assigned cleanly, Index and Register updated in the same commit.
4. **A genuinely ambiguous reference found outside ADR-0005 (flagged, not edited, per the "when unsure which register a citation means, flag, don't edit" decision procedure).** `projects/ehrt-cli/deps.edn` line 25: `` ;; poly/sim: the `ehrt sim` mount (ADR-0004, ADR-0012 fulfilled) ``. This phrase ("ADR-0012 fulfilled") is a near-verbatim echo of ADR-0005's own pre-edit title ("The `ehr sim` mount: ADR-0012 fulfilled...") and almost certainly means the frozen tools-era ADR-0012 (the mount's original design commitment, fulfilled by ADR-0005's landing) — but this exact file also carries two workspace-native bare `ADR-0012` citations about `judge-v2-nist` two lines above it (lines 1, 18), so a reader hitting line 25 cold, in a file otherwise about `judge-v2-nist`, could plausibly misread it as the workspace record. Left unedited, flagged here for the author or a future session to qualify with actual certainty rather than this session's own best guess.
5. **AGENTS.md bullet-edit conflict with enforcement.** Not triggered — no doc-lint or hook greps the Result-not-throw bullet's exact text; the one-sentence clause landed cleanly.
6. **Unexpected `git status` dirt.** None at any checkpoint — clean before step 1, clean immediately after each commit's push.

**Out of scope, named, not touched.** `notes/ADRs.md` ADR-0002's own body carries three more unqualified tools-era references (`` `tools/ADR-0013` `` already qualified at one spot, but `` ADR-0013's own subprocess-only rule `` and `` R15/ADR-0013 `` unqualified nearby, lines ~355-435 in the current file). These are outside step 4's literal scope (named as "ADR-0005's body" only) and were not edited — named here as a stale-citation candidate for whichever future session next touches ADR-0002's own citations, same fix-forward treatment this session gave ADR-0005's and `projects/conformance/deps.edn`'s.

**Everything else in the numbered steps executed exactly as written**, each gated by a green `clojure -M:poly check` before its own commit, pushed immediately after per the R30 standing mode.
