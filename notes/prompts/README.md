# notes/prompts/ (historical archive)

Frozen as of 2026-08-01: session prompts through the agent-UX capture
session stay here, unmoved. **Every session prompt from 2026-08-01
onward archives to [`.agents/prompts/`](../../.agents/prompts/README.md)
instead** — see that directory's own README for why (`notes/ADRs.md`
ADR-0023). No migration of these existing files is planned by this
session; that's named, fenced, future work.

**Index convention (item 10, migration session 3, 2026-08-02):** every
file here except this README follows the dated pattern
`YYYY-MM-DD-<slug>.md` — not restated as a per-file list (there are 28
of them; a list would break this file's own one-screen budget). The
exact frozen set was pinned per-push by `notes-prompts-frozen-test`
until `e189418` deleted it, and is a CONVENTION now;
`ehrt.docs-tooling.index-completeness-test` still enforces the naming
convention itself rather than a literal list for this one directory.
