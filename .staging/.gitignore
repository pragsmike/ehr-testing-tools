# Clojure / clj tooling
.cpcache/
target/
.nrepl-port
.lsp/.cache/
.clj-kondo/.cache/
build/*.jar

# Emacs
*.elc

# Python (skills with helper scripts, e.g. string-diagram)
__pycache__/
*.pyc

# OS cruft
.DS_Store
Thumbs.db

# Local Claude Code tool-permission state (per-clone, not shared config).
.claude/settings.local.json

# Editor droppings
*.swp
*~

# Pack output (regenerated by `make pack` / `make pack-skills`)
/ehr-testing-tools-pack.txt
/ehr-testing-tools-skills-pack.txt

# Generated corpora (ADR-0005: manifests + hashes are the committed
# record, not the generated bytes themselves)
/out/
