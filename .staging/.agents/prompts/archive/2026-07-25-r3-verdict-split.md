# R3 — The verdict split: no-verdict(cause), policy totality, CLI surface

You are working in `ehr-testing-tools` (public). This session executes Phase 3 of `.agents/plans/judge-gate-refactor.md` — the refactor batch's one semantic change: the verdict type gains a fourth arm, `no-verdict` with a cause, distinguishing "the judge could not reach a verdict" from "the criterion doesn't decide this subject" (`docs/palgebra-design.md` D10, §II.5). Everything else in this session exists to make that change total: `worst-of`, the report schema and diff, baseline-relative reading, and the CLI's explicit policy surface. Test-first per ADR-0006 — this is the session where red→green evidence matters most; the resulting spec/match failures are the policy-totality law enforcing itself, so drive each site from a failing test.
Read first: `AGENTS.md`, `AUTHORS-GUIDE.md`, `notes/ADRs.md` (ADR-0009), `docs/palgebra-design.md` (D10, O1, O2, §II.5, §II.7), `.agents/plans/judge-gate-refactor.md` (Phase 3), `src/ehr_testing_tools/judge/finding.clj` (Severity, `worst-of`), `src/ehr_testing_tools/judge/fhir.clj` (the verdict-mapping table, `verdict-mapping-version`, the terminology-suppressed classification), `src/ehr_testing_tools/judge/v2.clj` (its never-indeterminate claim), `src/ehr_testing_tools/judge/report.clj` (report schema, diff, baseline-relative), `src/ehr_testing_tools/cli.clj` (exit-code mapping, `--baseline`), `docs/judge-calibration.md`, EXP-C5 results, both `test-integration/` suites, README's gate section. Ritual: commit → `git push origin`. Save this prompt to `.agents/prompts/2026-07-XX-r3-verdict-split.md`; final commit archives it.
Author rulings in effect: O2 resolved as designed — terminology-suppressed is `no-verdict`, cause `:terminology-suppressed` (the judge failed to fully apply the criterion; the criterion didn't fail to decide). Representation — verdicts remain keywords (`:pass`, `:rejected`, `:indeterminate`, `:no-verdict`); a `:cause` keyword accompanies `:no-verdict` and only `:no-verdict`, Malli-enforced. `:indeterminate` keeps its name in v1 (same conservatism as O1: old baseline reports serialize it); renaming to `:ambiguous` is deferred to signature-format v2. After this migration nothing produces `:indeterminate` — it becomes a reserved value with no producer, stated plainly where the verdict set is defined. `worst-of` ranks `:no-verdict` above `:rejected` — a corpus that couldn't be fully judged is not a corpus that passed — with a comment recording that rank-ordering is policy-flavored and pre-registered as an O3-adjacent exhibit (design §II.5). CLI — one new flag, `--treat-no-verdict-as pass|rejected`; the default is a distinct exit code for no-verdict outcomes, so no workflow silently inherits a handling default (the policy-totality law, D10). `--fail-on` is NOT built — recorded as waiting on a real need. Old reports read forward — baseline-relative mode must accept pre-split baseline EDN (three-valued) without migration; `:no-verdict` is strictly new.
Step 0 — ADR-0010
Append to `notes/ADRs.md`: ADR-0010 — Verdict partiality is explicit: the no-verdict arm. Context: `:indeterminate` conflated "the criterion doesn't decide this subject" with "the judge could not reach a verdict" (operational partiality — in practice, gate.fhir's terminology-suppressed classification, its only producer). Decision: the four-value verdict per the rulings above; abstractly the judge is total and partiality is introduced by lowering (design §II.5, §III.2) — `no-verdict` is the cost layer's one declared leak, given a typed channel and a totality law. Consequence: every consumer of verdicts must handle the fourth arm; the CLI's distinct default exit code is that law surfacing at the act layer. Rejected: folding causes into findings (policies would spelunk); renaming `:indeterminate` now (serialization stability, joins O1 at v2). Cites D10, O1, O2.
Commit: `ADR-0010: explicit verdict partiality (no-verdict arm)`.
Step 1 — The verdict type and `worst-of` (red first)

1. Failing tests first in `judge/finding_test.clj`: `worst-of` over seqs containing `:no-verdict` (alone, with `:rejected`, with all four); the Malli schema accepting `:no-verdict`+`:cause` and rejecting `:cause` on any other verdict; empty-seq still `:pass`.
2. Implement in `judge/finding.clj`: the verdict set gains `:no-verdict`; `verdict-rank` gains the fourth case ranked above `:rejected` (with the O3-exhibit comment); the schema per the representation ruling; docstrings state the reserved-value status of `:indeterminate`.

Commit: `Verdict: no-verdict(cause) arm; worst-of total over four values (ADR-0010)`.
Step 2 — judge.fhir produces it; judge.v2's claim updates

1. Failing test: a terminology-suppressed issue classifies as `:no-verdict` with `:cause :terminology-suppressed` (use/extend the existing EXP-C5-derived fixtures).
2. `judge/fhir.clj`: the suppressed branch emits the new form; the verdict-mapping table's version bumps (`verdict-mapping-version` → next value, cited note appended — the mapping's output vocabulary changed even though the EXP-C5 classification logic did not).
3. `judge/v2.clj` docstring: its claim becomes "never produces `:indeterminate` or `:no-verdict`" with the one-line reason (no terminology, no profile at its base-structural tier).

Commit: `judge.fhir: terminology suppression is no-verdict (:terminology-suppressed); mapping version bumped (O2)`.
Step 3 — Reports, diff, baseline compatibility

1. Failing tests: a report containing `:no-verdict` round-trips; `diff-reports` surfaces verdict changes to/from `:no-verdict`; baseline-relative mode reads a pre-split baseline fixture (three-valued EDN, checked into test resources as a frozen artifact) against a current report containing `:no-verdict`.
2. `judge/report.clj`: schema and aggregation handle the fourth arm; the old comment about preserving `:indeterminate` (near line 119 pre-R1) resolves — replace it with the current story. If the report carries a format-version key, bump it; if not, do NOT introduce one this session (that's signature-format territory) — note the gap in the plan's Phase 4 list instead.

Commit: `Reports: no-verdict flows through aggregation, diff, and baseline-relative reads (old baselines read forward)`.
Step 4 — The CLI's policy surface

1. Failing tests in `cli_test.clj`: default behavior — any `:no-verdict` in the aggregate yields the new distinct exit code (pick the next free code after the existing 0/rejected/error assignments; document the full mapping in one place); `--treat-no-verdict-as pass` and `--treat-no-verdict-as rejected` remap it; flag validation rejects other values.
2. Implement in `cli.clj`; README's gate section gains the exit-code table and the flag, framed per ADR-0009's vocabulary (the CLI is the repo's one gate; this flag is its policy made explicit).
3. `docs/judge-calibration.md`: one paragraph on what no-verdict means operationally and when `--treat-no-verdict-as` is appropriate (echoing the escalate-vs-retry distinction, design §II.5).

Commit: `ehr gate: --treat-no-verdict-as + distinct default exit code (policy totality at the act layer)`.
Step 5 — Integration proof
Run `make integration` (artifacts cached per R1/R2). Required outcomes: contract-pairing suite passes untouched — the polarity regression; `:rejected` still reads as success with zero edits to that suite — and baseline-gating passes (edits only if it asserts on the report schema shape, not on verdicts). If either needs more than mechanical accommodation, stop and reassess against the plan before proceeding — that would mean the split disturbed the judge/policy boundary, which is exactly what it must not do.
Commit (if any edits): `Integration: accommodate report schema (no verdict-semantics changes)`.
Step 6 — Close out
Full suite + coverage + both lints + golden regeneration check green. `docs/palgebra-design.md`: decision-register statuses update — D10 implemented, O2 resolved (record the ruling), O1 unchanged-open with the `:ambiguous` rename noted alongside it for v2. Plan Phase 3 → Done; `corpus-foundations.md` R row summary extended. Grep: every `match`/`case`/`cond` over verdict keywords handles all four or has a deliberate total default — list the sites checked in the commit message. Archive this prompt.
Commit: `R3 complete: verdict split landed, polarity regression intact (archives prompt)`.
