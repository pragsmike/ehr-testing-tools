2026-07-27 — UX-1 (build): the CLI surface changes once — zero-flag generate, one flag vocabulary, sniffing gate, three conveniences
Context
The UX-1 capture (ADR-0019, `docs/source-sink-design.md` Part IX, D8–D14) is now fully ruled: the author has decided the sequencing — UX-1 runs as its own build session before SS-1, and SS-1 rebases onto its result — and resolved all three OPEN items: OPEN-1 → error default (mixed-format directory under bare `gate` is an error naming the override, not a silent per-file split), OPEN-2 → population 5, OPEN-3 → `ehr doctor` ships in the first release. This session implements D9–D13 against `cli-spec`, re-verifies every user-facing command strip, and records the four resolutions in the registers. D10's renames are the breaking half; SS-1's URL surface is additive on top of this session's vocabulary — that is why this ordering preserves D14's one-surface-change intent with no users yet to migrate.
This session touches `corpus/generate.clj` and the gate dispatch seam, so per the ADR-0016 trigger list it owes T2, not just T0+T1.
Read first

* `docs/source-sink-design.md` Part IX + Decision Register D8–D14, OPEN-1..3; `notes/ADRs.md` ADR-0019, ADR-0016 (tiers), ADR-0012 and `notes/ehr-testing-sim-mounting-note.md` (the mounting contract — see ruling 6's probe)
* `src/ehr_testing_tools/cli/help.clj` (`cli-spec`), `cli.clj` or the dispatch namespace, `docsgen.clj`
* `src/ehr_testing_tools/corpus/generate.clj` (default-locale/ default-timezone precedent; manifest fields), `corpus/intake.clj` (`sniff-format`), `corpus/operators.clj` (registry shape), `artifact.clj` (lockfile iteration), `judge/fhir.clj` + `judge/v2*` (gate entry points)
* `docs/use-cases.md` (the ten strips), `SETUP.md` (doctor's checklist source + its own examples), `Makefile`, `.github/workflows/` (old-spelling ripple sites)

Author rulings

1. The four decisions are final for this session: UX-1-then-SS-1; OPEN-1 error default; OPEN-2 `--population 5`; OPEN-3 `doctor` in first release. Step 1 records them: the Decision Register's OPEN rows get a dated status change to Resolved (fix-forward — amend the status and add the resolution, do not rewrite the row's framing); the plan's coordination paragraph and UX-1 row status update to match. No ADR amendment needed — ADR-0019 already delegated these to build time.
2. Test-first, with the acceptance property made precise by probe. D9's property is "zero-flag `ehr corpus generate` is byte-reproducible." Probe what actually varies between two identical runs on this machine: if any corpus payload file differs, STOP AND REPORT — that is a determinism finding, not something to paper over. If only the manifest carries wall-clock record-keeping fields (generated-at and kin), the committed property test asserts payload byte-identity plus manifest identity modulo an explicitly named field list, and the design doc's Part IX gains one sentence naming those fields as D8-exempt record-keeping (same reasoning as `--received`). The hermetic half of the test (injected fakes: argument assembly shows the pinned defaults) lives in `test/`; the two-real-runs half lives in `test-integration/` at whatever size keeps it honest.
3. D10 renames are total within live surfaces, frozen in records. `--lockfile` / `--out` / `--out-dir` / positional-`PATH`-with- `--path` land in `cli-spec`; then sweep the repo for old spellings (`--lockfile-path`, `--output-dir`, `--input`, `--source-dir`) and update every LIVE site: Makefile, SETUP.md, README.md, docs/*, `.github/workflows/`, test code and fixtures. Frozen provenance (ADRs, facts rows, `.agents/prompts/archive/`, HISTORY) stays verbatim. `docs/cli.md` regenerates via `make cli-doc`; the freshness gate must pass.
4. D11 mechanics: bare `ehr gate PATH` sniffs via `corpus.intake/sniff-format` — no second sniffing mechanism. Single file → dispatch. Directory → sniff all files: homogeneous → dispatch; mixed → exit 2 (operational/usage, not a verdict) with a message naming both files-by-format counts and the explicit remedy (`ehr gate v2 PATH` / `ehr gate fhir PATH`). A file the sniffer cannot classify is the same exit-2 shape, naming the file. The mixed-directory error test precedes the implementation.
5. D13 specifics: `ehr version` prints an honestly-pre-release identity — the string "pre-release" plus the git describe/sha when available, plus each lockfile artifact's name@version; it must not invent a semver. `ehr artifact fetch --all` iterates every lockfile entry through the existing fetch path (per-artifact Result values; one failure does not abort the rest; exit worst-of). `ehr doctor` runs SETUP.md's verification checklist as checks with per-check pass/fail lines (java resolution via the registry, artifact cache presence per lockfile entry, hooksPath wiring, WSL detection where the platform makes it meaningful), exit 0 all-pass / 1 any-fail / 2 cannot-even-check; hermetic tests via injected fakes per the `artifact_test.clj` pattern. Add a maintenance note at doctor's checklist source stating the SETUP.md freshness obligation (hand-maintained, same-commit rule — the NAV-1 index pattern).
6. The mounting contract is probed before anything renames. Read ADR-0012 and the mounting note FIRST: if the properties sim mounts against name any flag this session renames or any default it changes, STOP AND REPORT before implementing — the contract update may need a coordinated sim-side session, and that coordination is the author's call, not this session's. If the contract is untouched by D9–D13 (it may concern properties, not flags), say so explicitly in the report and proceed.
7. The shipped default properties file is a pinned artifact of this repo. `resources/` gains the minimal Synthea properties D9 names, with a header comment stating it is the zero-flag default, part of the reproducibility pin, and change-controlled (edits are behavior changes to the zero-flag run). Its content: the minimum that makes the zero-flag corpus useful to the quickstart and the FHIR gate — probe Synthea's defaults rather than assuming which exporters are on.
8. Every user-visible strip re-verifies. All ten `docs/use-cases.md` strips re-run end-to-end with the new surface (artifacts fetched); `make quickstart-fresh` re-verified with zero-flag `generate` as its first command (amend the quickstart content accordingly — that IS this session's demo). Structural enforcement stays green.
9. Tier obligation: T0 per commit; T1 and T2 before the final commit (generate.clj and the gate seam are both on the ADR-0016 trigger list). Report wall times; the verdict cache should make T2 materially cheaper than its cold baseline — say whether it did.

Steps
Step 1 — Record the four resolutions
Per ruling 1. Commit: `docs: OPEN-1..3 resolved (error default; population 5; doctor in first release); UX-1-then-SS-1 sequencing recorded`
Step 2 — Mounting-contract probe
Per ruling 6. If clean, one sentence in the report; if not, STOP. (No commit — probe only, unless the note itself needs a dated clarification, then: `notes: mounting-note clarified against UX-1 surface changes`)
Step 3 — Determinism probe + acceptance property tests (red)
Per ruling 2: run the probe, then author the failing hermetic test (pinned-defaults argument assembly) and the integration property test. Commit: `test: UX-1 acceptance properties (red) — zero-flag defaults assembly; byte-reproducibility with named record-keeping exemptions`
Step 4 — D9 + D7-file: defaults and the shipped properties
Implement the six defaults in `cli-spec`/`generate.clj`; author `resources/` properties per ruling 7; green the Step 3 tests. Commit: `feat: zero-flag corpus generate — pinned deterministic defaults (D9), shipped default properties`
Step 5 — D10: the vocabulary, everywhere live
Per ruling 3, including regeneration and sweep. Commit: `feat!: one flag vocabulary — --lockfile/--out/--out-dir/positional PATH; old spellings removed (D10, ADR-0019)`
Step 6 — D11: sniffing gate
Per ruling 4, test-first. Commit: `feat: ehr gate PATH — sniff dispatch; mixed or unclassifiable input is exit-2 naming the override (D11, OPEN-1)`
Step 7 — D12: mutate defaults
Derived `--out-dir`; `:default-locator` registry support (declaring none for any operator — that is calibration work, per D12). Commit: `feat: corpus mutate derived out-dir; operators may declare :default-locator (D12)`
Step 8 — D13: version, fetch --all, doctor
Per ruling 5, test-first with fakes. Commit: `feat: ehr version, artifact fetch --all, ehr doctor (D13, OPEN-3)`
Step 9 — Re-verification wave
Per ruling 8: use-cases strips, quickstart amendment, `make cli-doc`, all freshness gates. Commit: `docs: use-cases and quickstart re-verified against the new surface; cli.md regenerated`
Step 10 — Tiers, archive, push
T0 + T1 + T2 per ruling 9, wall times recorded. Archive this prompt; push. Commit: `prompts: archive 2026-07-27 ux-1 build session`
Final report
Mounting-contract probe outcome (ruling 6); determinism probe outcome and the exact record-keeping exemption list (ruling 2); the old-spelling sweep site count (live updated vs frozen left); doctor's check list as shipped; T0/T1/T2 wall times with cache effect; red→green evidence per test-first; deviations.
