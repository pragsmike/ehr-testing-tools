;; The signature data ehrt.docs-tooling.pipeline loads and passes to
;; palgebra.signature (design D13, docs/palgebra-design.md; claimed
;; R2, .agents/plans/judge-gate-refactor.md Phase 2): exactly the five
;; stage kinds this repo's pipeline uses. Their one-line laws stay in
;; prose at docs/notation.md's "The five stage kinds" table -- law-as-
;; data is design O4, not this session.
{:kinds #{:transform :normalize :enrich :judge :feedback}}
