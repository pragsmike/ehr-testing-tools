ehrt.patient-simulator -- resources/patient-simulator/ provenance notice
====================================================================

GMF coverage Wave D stage D1 (`notes/ADRs.md` ADR-0029, D1a schema
RULING, Q2+Q3): `vital-signs.edn` is HAND-CURATED content, not
extracted or copied from any Synthea checkout -- the same disclosure
`components/sim-model/resources/sim/demographics/NOTICE` already makes
for this project's own demographics tables, for the same reason (no
sibling Synthea checkout was consulted to extract from; AGENTS.md's
standing "do not invent facts about upstream sources" rule applies to
the codes, not the ranges -- see below).

What IS independently verified: the three LOINC codes
(`8480-6`/`8462-4`/`59408-5`) were cross-checked against HL7 FHIR's own
public Vital Signs profile examples (`hl7.org/fhir/bp.html` for the two
blood-pressure components, `hl7.org/fhir/observation-example-
satO2.json.html` for oxygen saturation) -- `loinc.org` itself returned
HTTP 403 to automated fetch at verification time (2026-08-02), the same
"real codes, verified against a citable public source" discipline
`components/sim/resources/sim/order-profiles.edn`'s own header already
establishes for this project's lab-analyte codes (`notes/facts-
register.md` F7's own sim/F7 citation for that precedent; this table's
own citation is `notes/facts-register.md` F21).

What is NOT independently source-verified: `:units`/`:reference-range`
are typical adult reference values, the same "representative clinical
ranges, not tied to one lab's exact methodology... plausibility, not
diagnostic precision" goal `order-profiles.edn`'s own header states for
its own analyte ranges -- author-curated, not extracted from a
methodology-specific source.

Table keys are the exact vital-sign NAME STRINGS `sepsis.json`'s own
`vital_sign` field spells them (`components/patient-simulator/docs/gmf-
interpreter.md` section 11, D1a-3) -- a closed, curated vocabulary this
table itself defines and grows by evidence (a future candidate module
needing an unlisted name is a real, visible interpreter rejection, never
a silent wrong answer, `vital-signs.edn`'s own header comment).

Recorded: 2026-08-02. All content is synthetic reference data; no PHI,
no real-person data, by construction (this repo's standing constraint,
`docs/problem-statement.md` / `AGENTS.md`).

**GMF coverage Wave D stage D3 addendum (2026-08-02, ADR-0029, D3c
finding 4).** Seven new rows (`Glucose`/`Urea Nitrogen`/`Calcium`/
`Sodium`/`Potassium`/`Chloride`/`Carbon Dioxide`) -- `uti/ed_bundle.json`'s
own real need, a repeated Basic/Comprehensive Metabolic Panel
`DiagnosticReport`. The seven LOINC codes (`2345-7`/`3094-0`/`17861-6`/
`2951-2`/`2823-3`/`2075-0`/`2028-9`) were verified ACTIVE this session
against a live public FHIR terminology server's own `$lookup` operation
(`r4.ontoserver.csiro.au`, CSIRO) -- `loinc.org` again returning HTTP
403 to automated fetch, the same discipline the original three rows
already establish; this table's own citation is `notes/facts-
register.md` F22. `:units`/`:reference-range` are author-curated
typical-adult values, NOT independently source-verified beyond the
LOINC code itself, the same bar the original three rows already set.

**GMF coverage Wave G addendum (2026-08-03, ADR-0037 AR-1):
`wellness-cadence.edn` is GENUINELY EXTRACTED upstream content, unlike
every table above this addendum.** Transcribed verbatim from
Synthea's own `EncounterModule.recommendedTimeBetweenWellnessVisits`
(`src/main/java/org/mitre/synthea/modules/EncounterModule.java`, lines
176-201) at the pin `7e08387c68a7f0e21d13076609a159fd473fc902` -- no
sibling Synthea checkout was consulted (this project keeps none, per
this same NOTICE's own opening disclosure); verification was a live
WebFetch of the pinned raw source
(`raw.githubusercontent.com/synthetichealth/synthea/7e08387c68a7f0e21d13076609a159fd473fc902/src/main/java/org/mitre/synthea/modules/EncounterModule.java`),
this session, 2026-08-03. The method's own chronic-medications annual
cap (same method, lines 209-211) is EXCLUDED by ruling, not omitted by
oversight -- deferred to the calibration register as a named item
("wellness cadence chronic-meds cap"); the table's own header comment
carries the same disclosure. This table's own citation is
`notes/facts-register.md` F23.

**License text (2026-08-05, ADR-0054, register F-4).** This addendum's
own opening paragraph, above, establishes that `wellness-cadence.edn`
is genuinely extracted, verbatim, from Synthea's own source
(`EncounterModule.java`) under ADR-0037 AR-1 -- unlike every other
table in this file, which is hand-curated and carries no such
obligation. Synthea is Apache-2.0-licensed (the same license terms
`components/sim/resources/sim/modules/NOTICE` already establishes for
every other Synthea-derived source this project vendors), so this
extraction is under Apache-2.0. The complete Apache License 2.0 text
is vendored at LICENSES/Apache-2.0.txt, added 2026-08-05 per ADR-0054
(register F-4).
