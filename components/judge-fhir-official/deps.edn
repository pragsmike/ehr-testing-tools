{:paths ["src"]

 :deps {org.clojure/data.json {:mvn/version "2.5.2"}
        ;; HAPI FHIR (EXP-B2): parse-validation aid and round-trip
        ;; comparison baseline for FHIR JSON. Moved from
        ;; components/judge/deps.edn at the per-engine extraction
        ;; (ADR-0011) -- carrying forward ADR-0008's own disclosure that
        ;; nothing in this workspace `:import`s its classes directly yet
        ;; (still true; not resolved by this session, see ADR-0011).
        ca.uhn.hapi.fhir/hapi-fhir-base {:mvn/version "8.2.0"}
        ca.uhn.hapi.fhir/hapi-fhir-structures-r4 {:mvn/version "8.2.0"}}

 :aliases {:test {:extra-paths ["test"]
                  :extra-deps {org.clojure/test.check {:mvn/version "1.1.3"}
                               org.slf4j/slf4j-nop {:mvn/version "2.0.17"}}}}}
