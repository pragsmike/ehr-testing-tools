{:paths ["src" "resources"]
 ;; Neither metosin/malli nor org.babashka/cli has any real require left
 ;; in residual sim's src or test (sim split B, M4, 2026-08-04,
 ;; .agents/plans/2026-08-04-sim-split-b-plan.md AR-M4-5e) -- malli's
 ;; last user, manifest.clj, lost it during M1's own thinning
 ;; (ehrt.provenance.interface now carries the schema family); neither
 ;; run.clj, identifiers.clj, nor version.clj ever required babashka.cli.
 ;; Dropped rather than left declared-but-unused; `poly check` plus the
 ;; full suite (both lanes) is the proof nothing else relied on this
 ;; component's own declaration.
 :deps {}
 :aliases {:test {:extra-paths ["test"]
                  :extra-deps {org.clojure/test.check {:mvn/version "1.1.3"}
                               org.slf4j/slf4j-nop {:mvn/version "2.0.17"}}}}}
