# Test fixtures

Moved to [`test-fixtures/`](../../../test-fixtures/) at the repository
root — the fixture tree is a shared, cross-component resource (a demos
front door for the same reason `notes/adr/0073-demos-front-door.md`
moved the sim demos), not a maintainer-only home for `components/corpus`
alone (fixture relocation, 2026-08-09, `notes/adr/0099-fixture-
relocation.md`). This file exists only so the conventional pointer
resolves; the actual fixtures live there.
